# 数字化应急预案自动生成系统 — 完整测试报告

- 测试日期：2026-09-08
- 测试范围：前端（React WebApp + 移动端 WebApp）、后端（FastAPI）、数据库（PostgreSQL）
- 测试方式：API 自动化脚本 + 自动化测试套件（pytest / vitest / Playwright E2E）+ 真实浏览器 UI 实测（多分辨率）
- 代码改动：**未修改任何代码**（测试脚本均存放于临时目录，容器内仅安装测试用浏览器环境）

---

## 1. 测试环境

| 项 | 值 |
|---|---|
| 生产前端 | http://localhost:8082（shuzihuayuan，nginx） |
| 开发前端 | http://localhost:5173（vite dev） |
| 后端 API | http://localhost:8000（uvicorn，单 worker，dev override） |
| 数据库 | PostgreSQL 16 @ localhost:5438（volumn shuzihuayuan_pgdata） |
| 测试账号 | qa_e2e_test@test.com（user 角色，普通用户） |
| 浏览器 | Chromium headless / headed（Edge 同内核可推及） |

---

## 2. 测试统计总览

| 维度 | 结果 |
|---|---|
| API 手工用例 | 60+ 用例，核心通过 |
| 后端 pytest | ≈1398 通过 / 3 失败（均为既有环境类问题）/ 1 集合错误（缺 scrapling 依赖）/ 收尾挂起 |
| 前端 vitest | 36 文件，**220 全绿** |
| Playwright E2E | 18 通过 / 19 失败（多为测试基建问题，见 8.3） |
| 浏览器 UI 实测 | 8 大类，7 项通过，1 项严重缺陷 |
| 性能/稳定性 | 60s 1650 请求 0 失败，单接口并发见 5.2 |
| 缺陷数 | 严重 3、高 3、中 4、低 6 |

---

## 3. 功能测试

### 3.1 认证模块（API + UI）
| 用例 | 结果 |
|---|---|
| 正确账号登录 | ✅ 200，返回 access/refresh token |
| 错误密码 | ✅ 401「邮箱或密码错误」，与不存在邮箱提示一致（无用户枚举） |
| 注册新用户 | ✅ 200（需 `password_confirm` 字段，前后端字段名需对齐）|
| 重复邮箱注册 | ✅ 400「该邮箱已被注册」 |
| 弱密码（<6 位）/非法邮箱 | ✅ 422 校验拦截 |
| 刷新令牌 / 伪造 refresh | ✅ 正常换发 / 401 拒绝 |
| 修改密码（旧密码错 / 改后旧密码失效）| ✅ 正确拦截与生效 |
| 忘记密码（不存在邮箱）| ✅ 统一提示，不泄露是否存在 |
| 登录页 UI 校验文案（空提交/乱邮箱/错密码）| ✅ 中文友好提示 |
| 移动端登录 | ❌ **严重缺陷**：输入即抛 TypeError，无法登录（见缺陷 #1）|

### 3.2 企业模块
| 用例 | 结果 |
|---|---|
| 创建 / 列表 / 搜索 / 详情 / 修改 / 删除 | ✅ 全链路通过 |
| 分页与边界（page/page_size 0、-1、超大、越界）| ✅ 精确校验（422/空集）|
| 关键词搜索（含 SQL 注入串）| ✅ 按字面处理，不报 500 |
| 新企业驾驶舱汇总 | ✅ risk_counts/hazard_counts/completion 等字段结构正确 |
| 企业详情页 UI（11 个 tab）| ✅ 基本信息/组织架构/风险分级管控/应急资源/周边环境等全部渲染 |
| 越权访问他人企业（详情/修改/删除/化学品/风险源/资源）| ✅ 一律 404「企业不存在」，无数据泄露 |

### 3.3 预案模块
| 用例 | 结果 |
|---|---|
| 创建 / 详情 / 修改 / 复制 / 删除 / 列表 / 按企业过滤 | ✅ 全通过 |
| 章节列表 / 版本历史 | ✅ 正常返回 |
| 空预案导出校验 | ✅ 返回具体缺失章节清单（友好文案）|
| UI：预案列表空态、新建按钮、驾驶舱入口 | ✅ 正常 |

### 3.4 其他功能点
- 个人资料修改、风格偏好读写、聊天会话列表、我的菜单：✅
- 公开页（风险告知卡/风险公示/隐患公示/隐患上报）：无效 token 均返回 404 中文提示「链接已失效」✅
- 化学品库（2997 条）/ 法规库（123 条）分页：✅
- 系统配置 `/configs`、数据字典、AI 配置、用户管理、角色菜单的权限拦截：普通用户 → 403 ✅
- 403 无权限页 / 404 页面（UI）：✅ 均含「返回工作台」引导

---

## 4. 兼容性测试

| 视口 | 结果 |
|---|---|
| 1920×1080（企业、预案）| ✅ 无横向滚动 |
| 1366×768 | ✅ 正常 |
| 1024×768 | ✅ 侧边栏 200px，可用 |
| 375×812（移动端）| ✅ 自动切换 `/m/*` 移动版渲染正常（登录输入缺陷除外）|
| 浏览器 | ⚠️ 仅 Chromium 实测；Edge（同内核）可推及；Firefox/Safari 建议补充 |
| 移动端 WebApp | ⚠️ 无离线安装清单，依赖 networkMode=offlineFirst + 断线横幅（未实机断网验证）|

---

## 5. 性能测试

### 5.1 稳定性
- 60 秒持续混合调用（登录/企业/预案/工作台/化学品库）：**1650 请求，0 失败**（≈27 RPS）

### 5.2 并发（单 worker 环境，dev override）
| 接口 | 并发 | 平均 | p95 | p99 |
|---|---|---|---|---|
| 化学品库（2997 行）| 100×10 | 59ms | 101ms | 105ms |
| 登录（bcrypt）| 30×10 | 2321ms | 4471ms | 4476ms |
| 企业列表 | 60×15 | 1369ms | 2093ms | — |
| 企业列表 | 120×30 | 2789ms | 4732ms | — |
| 工作台统计 | 100×10 | 221ms | 380ms | 395ms |

- 结论：**企业列表接口并发放大最明显**（60→120 并发时延迟翻倍）；登录 bcrypt 计算偏重（单次 ~0.3s），高并发 ~2.3s；生产为 4 worker 配置，预计缓解，建议上线前用 4 worker 复测。

### 5.3 稳定性风险观察项
- 测试期间后端容器内存由 **1.54GB 上升至 2.90GB**（负载与 E2E 期间），需监控是否存在缓存/连接积累。
- 数据库持续存在 2 条 `idle in transaction` 连接，其中一条挂起 **11 分钟**（疑似后台生成任务事务未提交/连接未归还），建议排查 LLM 生成/重试链路的会话与事务管理。

### 5.4 压力
- 100 并发错误登录全量 401，无 503/崩溃，但**无任何限流**（见缺陷 #6）。

---

## 6. 安全测试

### 6.1 越权
- 跨用户企业/预案/化学品/风险源访问全部 404 隐藏 ✅；越权列表为空 ✅
- 普通用户访问 admin 接口（用户管理/角色/AI 配置/三方配置/数据字典）→ 403 ✅
- HMAC 外部接口未配置时正确返回 503「External API not configured」✅

### 6.2 注入与 XSS
- SQL 注入：登录/搜索注入串全部按字面处理，无 500 ✅
- 存储型 XSS：企业名可原样存 `<script>`（后端不转义）；前端 React/antd 默认转义缓解；**但上传接口可上传 .html 并以 text/html 静态访问（高危，见缺陷 #2）**

### 6.3 敏感信息
- `/users/me`、用户管理响应仅含 id/email/name/role/created_at，无密码哈希 ✅
- `/configs` 普通用户可读全部业务配置（无密钥）⚠️
- `/prompts` 普通用户可读 73 条提示词全文 ⚠️

### 6.4 认证与密钥
- JWT 类型混淆（refresh 当 access）→ 401 ✅；篡改签名 → 401 ✅
- **JWT 使用 docker-compose 默认密钥，可伪造任意用户 token（高危，见缺陷 #3）**
- 登录/注册/忘记密码无限流（高危，见缺陷 #6）

### 6.5 其他
- `/uploads/*.html` 静态直达 text/html（见缺陷 #2）
- `?token=` 查询串鉴权通道（见缺陷 #8）
- 安全响应头全部缺失（见缺陷 #7）
- 下载 file_key 路径穿越：正则白名单拦截，未发现真实越权读文件（仅 SPA 兜底返回 index.html）✅

---

## 7. 异常 / 容错 与 数据测试

### 7.1 异常输入
| 输入 | 结果 |
|---|---|
| 非法 JSON / 类型错误 / 空 body | ✅ 422 精确定位字段 |
| 弱密码 / 非法邮箱 / 超长分页参数 | ✅ 422 |
| **非法 UUID 格式路径参数** | ❌ 500（企业/预案/数据字典等多端点）|
| **超大字段（200KB 企业名）** | ❌ 500 |
| 缺 AI 配置的生成接口 | ✅ 400/404 中文提示（依赖配置，符合预期）|
| 空预案导出 | ✅ 返回缺章清单 |

### 7.2 数据准确性
- 新企业驾驶舱计数（风险 0/隐患 0/完成度）与真实数据一致 ✅
- 创建预案后 enterprise-summary 计数准确 ✅
- 10 并发创建企业无冲突/无数据串扰 ✅

### 7.3 大数据量
- 化学品库 2997 条：分页正确，第 50 页超界返回空集不报错 ✅
- 法规库 123 条分页正常 ✅

---

## 8. 自动化测试套件

### 8.1 后端 pytest（容器内，1403 收集）
- ≈1398 通过；3 失败（均为**既有环境类问题**，非本轮回归）：
  - `test_baseline_migrations_match_pre_upgrade_scripts`（迁移基线）
  - `test_build_risk_management_context_risk_events_without_event_name`（diagram 上下文 mock）
  - 另 1 项位于套件 5% 位置（批次间偶发，待定位）
- 1 集合错误：`test_autofill_research.py` 缺 `scrapling` 依赖
- ⚠️ 全量套件约在 97% 处收尾挂起：末尾文件单独跑均通过，疑似会话级清理器（asyncio/Playwright teardown）问题，需排查

### 8.2 前端 vitest
- **36 文件 220 用例全部通过** ✅

### 8.3 Playwright E2E（宿主 Chromium @ 5173）
- **18 通过 / 19 失败**
- 失败分类：
  - `comprehensive.spec.ts` 11 条：**测试基建问题**——硬编码 `localhost:5174`（已死的端口），非产品缺陷
  - `risk-hierarchy-tree` 5 条 + `risk-mapping-workbench` 部分 + `smart-guide-import` 1 条：**mock 用例依赖拦截 API 与假 token，真实后端 401 导致页面未渲染**，需 mock 补全 auth 相关接口后回归

---

## 9. 缺陷清单（按严重级别）

### 严重（P0）
| # | 缺陷 | 位置 |
|---|---|---|
| 1 | 移动端登录无法输入（onChange 签名不匹配，TypeError），手机端无法登录 | `frontend/src/mobile/components/ui/Input.tsx`、`mobile/screens/LoginScreen.tsx` |
| 2 | 上传接口无类型白名单，`.html` 可上传并 text/html 静态访问 → 存储型 XSS/钓鱼面 | `backend/app/main.py` upload 路由 |
| 3 | JWT 使用 docker-compose 默认密钥，可伪造任意用户 token 认证成功 | `docker-compose.yml` SECRET_KEY / `config.py` |

### 高（P1）
| # | 缺陷 |
|---|---|
| 4 | 路径参数非法 UUID → 500（应 404/422），覆盖企业/预案/数据字典等端点 |
| 5 | 超大字段（200KB）→ 500，无输入长度限制 |
| 6 | 登录/注册/忘记密码无任何限流防刷（暴力破解风险）|

### 中（P2）
| # | 缺陷 |
|---|---|
| 7 | 安全响应头缺失（CSP / X-Frame-Options / HSTS / X-Content-Type-Options）|
| 8 | `?token=` 查询串鉴权通道，token 有泄露面（日志/Referer/历史）|
| 9 | 普通用户可读全部提示词模板（73 条，知识产权暴露）|
| 10 | 数据库 `idle in transaction` 连接持续存在，疑似事务/连接泄漏 |

### 低 / 待确认
| # | 项 |
|---|---|
| 11 | `/configs` 业务配置普通用户可读（无密钥）|
| 12 | 企业名等文本后端存储不转义，XSS 防御依赖前端转义（markdown/DOCX 链路需复核 DOMPurify 覆盖）|
| 13 | 注册需 `password_confirm` 前后端字段对齐；企业名后端无最小长度校验 |
| 14 | 风格偏好 PUT `style` 字段未生效（字段命名不一致）|
| 15 | `/export/download/` 未知 file_key 返回 200 但为 SPA 页面（语义应 4xx）|
| 16 | 登出无状态，token 过期前仍有效 |

---

## 10. 测试残留与说明

- **未清理测试数据**：注册测试用户 2 个（`bytester_853802@test.com`、`priv_876543@test.com`）、上传测试文件 `uploads/cff40de394504a28b59954247b898e41.html`、qa_e2e 账号下历史 E2E_* 测试企业
- **未覆盖/受限项**：
  - AI 生成端到端（预案生成/风险评估/报告）需管理员配置与 token 消耗，本轮仅验证缺配置时的友好报错
  - 断网/断电场景未实机模拟（前端有 offlineFirst 与断线横幅设计，未验证）
  - Firefox / Safari 浏览器未覆盖
  - 生产 4 worker 并发表现未复测（环境为单 worker）

---

## 11. 结论与优先建议

系统核心功能（认证、企业、预案 CRUD、权限隔离、分页、统计、公开页、错误处理）通过测试；前端单测 220 全绿；但有 **3 个严重缺陷**（移动端登录不可用、上传任意文件存储型 XSS 面、JWT 默认密钥可伪造）需优先修复：

1. **#1 移动端登录**（优先修复，一行级：onChange 传值方式统一）
2. **#2 上传白名单**（限制扩展名 + Content-Type + Content-Disposition attachment）
3. **#4 非法 UUID 全局兜底**（路由此参数校验或全局异常处理器）
4. **#6 登录限流**（slowapi/内存滑动窗口）
5. **#3 生产密钥整改**（环境变量注入强随机密钥，移除默认值）
6. 排查 #10 事务/连接泄漏与内存增长（关联 LLM 生成链路）
7. 回归通道：修复后补跑 vitest 220 + E2E（需先修复 E2E 基建：5174 硬编码与 mock 鉴权）

---

*本报告基于对生产/开发环境的一次性完整测试，测试过程中未修改任何源代码。*