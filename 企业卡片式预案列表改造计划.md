# 企业卡片式预案列表改造计划

## 摘要

将扁平的 `/plans` 预案列表页改造为企业卡片总览 → 企业专属预案列表的两层结构。数据模型无需改动（`PlanProject.enterprise_id` 已就绪），改动集中在 UI 路由层级和一条新统计 API。

## 实现变更

### 后端：新增预案统计汇总端点

- 在 `backend/app/routers/plans.py` 新增 `GET /plans/enterprise-summary`
- 返回当前用户所有企业在各预案类型下的数量和最后更新时间
- 同时返回 Schema `EnterprisePlanSummary` 到 `backend/app/schemas/plan.py`

### 前端：新增企业卡片总览页 PlanCardsPage

- 新建 `frontend/src/pages/Plan/PlanCardsPage.tsx`
- 路由 `/plans` 指向此页（替换原 PlanListPage）
- 用 Grid + Card 展示每家企业
- 每张卡片有「新建预案」按钮（跳转 `/plans/new?enterprise_id=xxx`）
- 点击卡片主体跳转 `/enterprises/:id/plans`

### 前端：改造预案列表页为企业专属列表

- 修改 `PlanListPage.tsx`：`enterprise_id` 从 `useParams()` 获取
- 路由配置为 `/enterprises/:enterprise_id/plans`
- 页面标题显示企业名 +「← 返回预案总览」链接

### 前端：路由调整

- `/plans` → `PlanCardsPage`（新）
- `/enterprises/:enterprise_id/plans` → `PlanListPage`（改造后）
- `/plans/new` → `PlanCreatePage`（支持 query param `?enterprise_id=xxx` 预填）
- `/plans/:id/edit`、`/plans/:id/versions`、`/plans/:id/preview` → 不变

### 前端：顶栏 EnterpriseSwitcher

- 组件本身不变
- 语义上不再控制预案列表的过滤范围

## 测试验证

- 后端 summary 端点返回正确统计
- 多企业卡片页正确渲染
- 点击卡片进入专属列表
- 创建/编辑/删除预案功能不受影响
- Playwright 完整流程通过
