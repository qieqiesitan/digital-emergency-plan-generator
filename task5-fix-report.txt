修复完成，已提交独立 commit `48be874`。

## 状态

✅ 完成。commit `48be874`（`fix(risk-mapping): align frontend workbench types with backend contract`）已提交到 `codex/protego-integration`，未推送、未执行 `git finish`。

## 修改文件（仅 3 个前端任务 5 文件）

1. [riskMappingWorkbenchService.ts](C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/services/riskMappingWorkbenchService.ts)：删除未使用的 `RawOverviewResponse` 导入（消除审查点 1 的 ESLint error）。
2. [riskManagement.ts](C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/types/riskManagement.ts)：
   - `RiskZoneCreate` 增加 `floor_id?: string | null`，`floor_plan_polygon` 改为共享的 v2 `RiskZoneFloorPlanPolygon`（替代旧 `{points}` 内联结构）；
   - `RiskObjectCreate` 增加 `floor_id?: string | null`；
   - 多边形/等级/颜色类型收敛为本文件共享定义（`RiskZoneFloorPlanPolygon`、`RiskPolygon`、`RiskPolygonPoint`、`ColorSource`、`RiskLevel`），`RiskZone.floor_plan_polygon` 改为引用共享类型，消除内联复制漂移；
   - `HierarchyObject`/`HierarchyZone` 改为显式接口，后端尚未提供的字段全部改为可选。
3. [riskMappingWorkbench.ts](C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/types/riskMappingWorkbench.ts)：
   - `BatchSaveZoneItem`/`BatchSaveRiskPointItem` 改为 discriminated union：以 `zone_id`/`id` 的 `null` vs `string` 为判别字段，新建分支必填 `client_id`/`name`，既有分支必填 `updated_at`；
   - 5 处内联 `import(...)` 改为常规 `import type`；共享类型通过 `export type` 从本文件 re-export，保证任务 6 的 `riskMappingGeometry.ts` 等既有消费者不受影响（未改任务 6 文件）。

## Hierarchy 字段处理选择

选了「改为可选」这一更安全的方式。当前后端 `HierarchyObjectResponse`/`HierarchyZoneResponse`（backend/app/schemas/risk_management.py）只返回 id/name/category/is_risk_point/units/events 与 id/name/description/objects，尚无 floor_id、floor_name、floor_plan_polygon、max_risk_level、effective_color、location_x/y。保持必填会形成「类型说必有、数据说没有」的假安全。已核对现有 5 个消费组件（RiskOverviewPage、RiskHierarchyTree、RiskOverviewMatrix、RiskOverviewStats、RiskManagementTab）只使用 id/name/category/is_risk_point/units/events/objects，改为可选对编译和运行均无影响。任务 4 后端补齐字段后再改回必填。

## 测试结果

- `cd frontend && npx tsc -b`：✅ 通过（0 error）
- `npx eslint` 三个修改文件：✅ 0 error 0 warning（routes/index.tsx 的既有 react-refresh error 与本次无关，未触碰）
- 判别联合约束：用临时验证文件做了正/负用例编译验证，缺 client_id/name/updated_at 的非法构造均被编译期拦截，验证后已删除临时文件
- `git show --check HEAD`：无空白或冲突标记错误

## 自审

- 提交内容仅 3 个前端文件；TASKS.md（工作区既有 M 状态）、计划文件、后端文件、任务 6 文件均未修改也未包含在提交内。
- 审查中的次要意见未全部纳入本次必改范围：`RiskZone.max_risk_level` 与 `WorkbenchZone` 统一为 `RiskLevel`、`getRiskMappingOverview` 显式返回类型、`EnterpriseFloor.created_at` 补齐——前两者涉及列出的文件清单之外的改动，如需要可另行处理。
- 按铁律二未执行 codegraph/graphify 同步（本次为纯类型级修改，未生成可查询的新图谱节点；如需同步可后续补跑 `codegraph sync .`）。