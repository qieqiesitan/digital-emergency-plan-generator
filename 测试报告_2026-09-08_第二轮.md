# 数字化应急预案自动生成系统 — 完整测试报告（第二轮全面回归）

- 测试日期：2026-09-08（下午，17:00–19:40）
- 测试范围：前端（React WebApp 桌面端 + 移动端 WebApp）、后端（FastAPI 单 worker dev 配置）、数据库（PostgreSQL 16）
- 测试方式：API 自动化脚本（127 用例）+ 真实浏览器 UI 实测（40+ 用例、40 张截图、5 档分辨率）+ 性能压测（负载/压力/并发写）+ 25 分钟稳定性监控 + 安全渗透验证 + 自动化套件回归（vitest / pytest）
- 代码改动：**未修改任何源代码**（全部测试脚本置于临时目录 `C:\Users\55061\AppData\Local\Temp\qa2_*`；测试产生的 5015 个性能测试企业已全量清理，探针配置已由测试方还原）
- 特别说明：本轮为当日**第二轮**完整测试，重点之一是验证上午第一轮报告（`测试报告_2026-09-08.md`）中 16 个缺陷的修复情况——结论见第 11 节对比表

---

## 1. 测试环境

| 项 | 值 |
|---|---|
| 生产前端 | http://localhost:8082（容器 shuzihuayuan，`python server.py` 提供 SPA + 静态服务。注：上轮报告描述为 nginx 有误，本轮实测响应头为 `Server: SimpleHTTP/0.6 Python/3.12.13`） |
| 开发前端 | http://localhost:5173（vite dev，容器 emergency-plan-frontend） |
| 后端 API | http://localhost:8000（uvicorn **单 worker**，docker-compose dev override；OpenAPI 242 路径） |
| 数据库 | PostgreSQL 16 @ localhost:5438（emergency_plan） |
| 测试账号 | qa_e2e_test@test.com（user）；test@test.com / 550614706@qq.com（super_admin） |
| 浏览器 | Chromium（Playwright MCP） |
| 环境干扰因素 | 同机运行无关容器 great_kapitsa（GPT-SoVITS 语音训练），持续占用 ~144% CPU / 4.4GB 内存——对性能绝对值有抬高影响，结论中已注明 |

---

## 2. 测试统计总览

| 维度 | 规模 | 结果 |
|---|---|---|
| API 功能/数据用例 | 127 | ✅ 102 通过；8 个问题（1×P1、3×P2、4×P3） |
| UI 用例（功能/兼容/容错） | 40+ | 桌面端全过；移动端登录 P0 缺陷复现 |
| 安全测试 | 9 大类 60+ 项 | 越权/注入/枚举防护通过；2×P0 仍存在，P1 级 6 项（含 2 项新发现） |
| 性能（负载） | 4 场景×60s，约 24,000 请求 | 0 失败 |
| 性能（压力） | 最高 240 并发 | 无 5xx 无崩溃，延迟线性恶化 |
| 性能（并发写） | 10 并发×30s 创建 5,015 企业 | 0 失败，RPS 167 |
| 稳定性 | 25 分钟采样 + 全程观察 | 发现事务挂起 81 分钟；内存只增不降 |
| 前端 vitest | 37 文件 / 227 用例 | **全部通过**（上轮 36/220，新增 7 用例） |
| 后端 pytest | ≈1,402 收集（1 文件因缺 scrapling 依赖收集失败） | 2 个失败均为上轮已知既有问题；**97% 收尾挂起问题复现** |
| 上轮 16 缺陷 | 逐项复测 | **已修复 2（#14/#15），仍存在 14（含 #11 升级为 P1）** |

---

## 3. 功能测试

### 3.1 认证模块（API 17 用例 + UI 5 用例）
| 用例 | 结果 |
|---|---|
| 正常登录（user/admin） | ✅ 200，access/refresh 均返回 |
| 错误密码 / 不存在邮箱 | ✅ 均 401「邮箱或密码错误」，提示一致（无用户枚举） |
| 正常注册 / 缺字段 / 弱密码(<6位) / 两次密码不一致 / 重复邮箱 | ✅ 200 / 422 精确定位 / 422 / 422 中文提示 / 400「该邮箱已被注册」 |
| **非法邮箱 `not-an-email` 注册** | ❌ 200 注册成功并落库（新缺陷 N2） |
| refresh 换发 / 伪造 refresh / access↔refresh 混用 / alg=none / 篡改签名 | ✅ 全部正确（200 / 401 / 401 / 401 / 401） |
| 修改密码全链路（错旧密码→改密→旧密码失效） | ✅ 全通过（400「原密码错误」→ 200 → 401） |
| 忘记密码（存在/不存在邮箱） | ✅ 统一提示，无枚举；50 次调用无异常 |
| /users/me 信息 | ✅ email/role 正确，无 password_hash |
| 登录页 UI 校验（空提交/非法邮箱/错误密码） | ✅ 字段级中文提示（截图 A02–A04） |

### 3.2 企业模块（30 用例）
| 用例 | 结果 |
|---|---|
| 创建 / 列表 / 搜索 / 详情 / 修改 / 回读 / 删除 / 删除后 404 | ✅ 全链路通过（删除返回级联统计） |
| 分页边界（page/page_size = 0、-1、99999、越界页） | ✅ 0/-1/超大 → 422；越界页 → 200 空集，无 500 |
| 关键词搜索（中文/特殊字符/SQL 片段） | ✅ 按字面处理，无 500 |
| 企业驾驶舱 summary | ✅ risk_counts/hazard_counts/completion/todos 等 8 字段齐全 |
| 并发创建 10 企业 | ✅ 10×201，id 全唯一，列表计数=10，无串扰 |
| **1 字符企业名「甲」** | ❌ 201 创建成功（上轮#13 部分项仍存在：无最小长度校验） |
| UI 新建企业表单（空提交校验/创建/跳转 onboarding/删除二次确认） | ✅ 字段级提示 + 「此操作不可撤销」确认弹窗（截图 B03a-c/F） |
| 企业详情 11 个 tab（基本信息→预案管理） | ✅ 11/11 全部渲染正常、空态文案友好（截图 B04_tab01–11） |

### 3.3 预案模块（12 用例）
| 用例 | 结果 |
|---|---|
| 创建 / 详情 / 修改 / 回读 / 复制 / 删除 / 删除后 404 | ✅ 全通过 |
| 章节列表（25 章节）/ 版本历史（0→1） | ✅ |
| 空预案导出校验 | ✅ 返回 25 条中文缺失清单（「总则：章节内容为空」等） |
| 空预案导出 DOCX | ⚠️ 不做完整性拦截直接生成文件（P3，校验与导出未联动） |
| 按企业过滤 / enterprise-summary | ✅ 132 行统计准确 |
| UI 预案列表（卡片/列表切换、分组、筛选、空态） | ✅（截图 B05） |

### 3.4 权限矩阵（15 用例）
| 用例 | 结果 |
|---|---|
| 普通用户 → /admin/users、/roles、/settings/ai-config、/settings/data-dicts、/chemical-library/collect | ✅ 全部 403「需要管理员权限」（读和写都拦） |
| 普通用户 → GET /configs | ❌ **200 返回全部系统配置**（升级缺陷 N1） |
| 普通用户 → PUT /configs/{key} | ❌ **200 写入成功**（N1，覆盖系统配置） |
| 普通用户 → DELETE /configs/{key} | ❌ **200 删除成功**（N1） |
| 普通用户 → GET /prompts | ❌ 200，73 条提示词全文可读（上轮#9 仍存在，写已正确 403） |
| 越权访问他人企业（GET/PUT/DELETE） | ✅ 一律 404「企业不存在」，DB 复核无盲删 |
| 用户 B 看不到用户 A 的企业 | ✅ 列表 0 条 |
| UI 菜单权限（user 无管理菜单 vs admin 有） | ✅（截图 B06） |
| **user 工作台「进入企业管理→」入口** | ❌ 点击落到 403 页（入口与权限不一致，P2） |

### 3.5 提示文案与错误处理
- 401/403/404 业务提示全部中文友好、无堆栈泄露 ✅
- 非法 JSON / 类型错误 → 422 精确定位到字段（含嵌套路径）✅
- ❌ 未知 API 路径 `/api/v1/nonexistent` → **200 + SPA HTML**（catch-all 兜底覆盖 /api/*，N3）

---

## 4. 兼容性测试

| 视口 | 结果 |
|---|---|
| 1920×1080 | ✅ 无横向滚动、无遮挡 |
| 1366×768 | ✅ |
| 1024×768 | ✅（td 溢出 0 处） |
| 800×600（极端小屏） | ✅ |
| 375×812 | ✅ 自动切换 `/m/` 移动版，移动端工作台/企业列表渲染正常（登录除外，见 P0） |

- 检查方式：scrollWidth==clientWidth 对比 + 单元格溢出扫描 + 截图人工复核（截图 C_*.png）
- 浏览器：仅 Chromium 实测；Edge 同内核可推及；Firefox/Safari 本机未装，未覆盖（与上轮一致的限制）
- 移动端 WebApp：自动路由切换正常；但**新用户无法登录/注册**（P0 缺陷，见 8.1）

---

## 5. 性能测试

### 5.1 负载测试（正常用户量：20 并发混合场景，各 60 秒）
| 场景 | 并发 | 请求量 | 失败 | p50 | p95 | p99 | RPS |
|---|---|---|---|---|---|---|---|
| 登录 | 5 | 264 | 0 | 1,148ms | 1,313ms | 1,568ms | 4.4 |
| 企业列表 | 20 | 1,240 | 0 | 893ms | 1,476ms | 1,631ms | 20.7 |
| 化学品库（2997 条） | 20 | 12,551 | 0 | 88ms | 149ms | 229ms | 209.2 |
| 预案汇总（132 企业） | 20 | 9,967 | 0 | 105ms | 247ms | 339ms | 166.1 |

合计约 24,000 请求 **0 失败**。登录受 bcrypt 计算（单次 ~0.3s）限制属正常；企业列表偏慢（见压力分析）。

### 5.2 压力测试（逐步加压找瓶颈）
| 接口 | 并发 | p50 | p95 | RPS | 结论 |
|---|---|---|---|---|---|
| 企业列表 | 30 | 1,416ms | 2,351ms | 20.3 | 吞吐饱和 |
| 企业列表 | 60 | 2,816ms | 3,860ms | 22.1 | 60→120 延迟翻倍（与上轮一致） |
| 企业列表 | 120 | 5,762ms | 6,813ms | 24.6 | |
| 企业列表 | 240 | **11,803ms** | **13,121ms** | 29.9 | 接近 30s 超时边缘，仍 0 失败 |
| 登录（错误密码） | 30 | 6,472ms | 11,931ms | 4.9 | 全 401，无限流 |
| 登录（错误密码） | 60 | **11,872ms** | **23,038ms** | 6.1 | 最坏 28.9s，灾难级体验 |
| 化学品库 | 60 | 261ms | 478ms | 202.3 | |
| 化学品库 | 120 | 545ms | 761ms | 206.4 | 表现良好 |

**瓶颈结论**：
1. 系统整体饱和点在**单 worker 异步队列**——过载表现为排队延迟恶化而非报错，240 并发仍无 5xx/拒绝；
2. 企业列表是读放大最重的接口（吞吐封顶 ~30 RPS，单次 ~900ms），疑似查询未优化（上轮同样结论，未修复）；
3. 化学品库/预案汇总有缓存加持，~200 RPS 表现健康；
4. 环境注记：同机 GPT-SoVITS 容器占 144% CPU，绝对值被抬高；生产 4 worker 配置会显著缓解，上线前建议复测。

### 5.3 并发写测试
- 10 并发持续 30 秒创建企业：**5,015 个 / 0 失败 / p50 57.6ms / RPS 167** ✅（测试数据已全量清理，DB 复核 remaining=0）

### 5.4 稳定性测试（25 分钟连续采样 + 全程观察）
| 指标 | 观察结果 |
|---|---|
| 后端内存（重启前） | 3.98–4.22GiB 稳定高位（服务已运行 4 小时+；对比上轮结束时 2.90GB——**内存持续增长趋势确认**） |
| DB `idle in transaction` | **3 条持续存在，最长挂起 1 小时 21 分 51 秒**（上轮#10 仍存在且更严重；普通 CRUD 高负载不触发，指向 LLM 生成/重试链路） |
| 后端内存（重启后） | 239MiB → 357MiB（12 分钟）→ 性能测试后 846MiB（处理 ~4 万请求+5015 增删），**只增不降** |
| 故障恢复 | 测试期间后端容器发生一次重启（见 8.4），**约 15 秒内恢复 health 200**，之后持续稳定服务 |

---

## 6. 安全测试

### 6.1 通过项 ✅
| 项 | 结果 |
|---|---|
| 越权（IDOR） | 跨用户企业/预案 GET/PUT/DELETE 全部 404 资源隐藏；列表隔离；admin 接口读写均 403 |
| SQL 注入 | 登录 4 种 payload、搜索 DROP TABLE/ OR 1=1 → 全部字面处理 401/空集，无 500、无报错泄露 |
| JWT 机制（除默认密钥外） | refresh↔access 混用 401；篡改签名 401；alg=none 401；伪造不存在的用户 401 |
| 用户枚举 | 登录/忘记密码提示完全一致 |
| 密码哈希泄露 | /users/me、admin 用户列表均无 password_hash |
| 短密码拦截 | <6 位注册 422 拦截（但 6 位以上纯弱密码可过，见 S15） |
| 路径穿越 | export/download 正则白名单拦截；.env/.git 访问均为 SPA 兜底，无文件泄露 |
| 无 token 访问 | 401 |

### 6.2 风险项 ❌
| # | 项 | 证据 | 级别 |
|---|---|---|---|
| S1 | **JWT 默认密钥可伪造任意用户**：用 docker-compose 默认密钥 `emergency-plan-docker-secret-key-2026` 自签 token（sub=真实管理员 UUID）→ `/users/me` 200 认证成功；直接调 `/admin/users` 200 返回全部用户列表 | 伪造为 550614706@qq.com（程磊）成功取回其信息；对照组错误密钥 401 | **P0**（上轮#3 仍存在） |
| S2 | **上传接口无任何限制**：.html/.svg（XSS 载体）/.exe 均可上传，且以原始 Content-Type 静态可达：`/uploads/xxx.html` → `text/html` 且 `<script>` 原样执行 | 上传后 GET 200，body_executable=True（前后端两个入口均可达） | **P0**（上轮#2 仍存在） |
| S3 | **登录/忘记密码无限流**：100 次错误登录全部 401 无 429、无 RateLimit 头、无锁定（轰炸后正确密码仍立即可登录）；50 次忘记密码无限制 | 27 秒 100 次 | P1（上轮#6 仍存在） |
| S4 | **/configs 无权限控制**：普通用户可读、**写、删**系统配置（含覆盖 export_dir 等影响全系统行为） | 覆盖 export_dir 200 成功（测试后已还原） | **P1**（上轮#11 升级） |
| S5 | 非法 UUID 路径参数 → 500（asyncpg DataError 直接入 SQL），覆盖企业/预案/化学品/数据字典等端点 | `not-a-uuid` → 500 + 后端完整 traceback 日志 | P1（上轮#4 仍存在） |
| S6 | 超大字段（200KB 企业名）→ 500，无输入长度限制 | 500 Internal Server Error | P1（上轮#5 仍存在） |
| S7 | 安全响应头 6 项全缺失（CSP/X-Frame-Options/X-Content-Type-Options/HSTS/Referrer-Policy/Permissions-Policy），前后端均缺；`Server` 头泄露 `SimpleHTTP/0.6 Python/3.12.13` | 响应头检查 | P2（上轮#7 仍存在） |
| S8 | `?token=` 查询串/路径段鉴权通道：资源调查导出 `export?token=`（`resourceInvestigationService.ts:26`）、公开页 `/p/risk/{token}`——token 进入访问日志/浏览器历史/Referer。公开 API `/api/v1/public/risk-notice-cards/{token}` 无认证可读（64 位 hex token 不可枚举、伪造 404、reset 需认证+所有权校验，风险受控） | 200 | P2（上轮#8 仍存在） |
| S9 | /prompts 普通用户可读 73 条提示词全文（知识产权暴露） | 200 count=73 | P2（上轮#9 仍存在） |
| S10 | 存储型 XSS 后端不转义：企业名 `<img onerror>` 原样入库返回（前端 React 转义缓解，markdown/DOCX 链路仍需复核） | API 读回 raw payload=True | P2（上轮#12 仍存在） |
| S11 | 60MB 文件上传无大小限制（DoS 面） | 200 成功 | P2 |
| S12 | 登出无状态：logout 后旧 access/refresh 仍有效 | 200 | P2（上轮#16 仍存在） |
| S13 | JWT 密钥长度不足：20 字节 < RFC 7518 推荐 32 字节 | PyJWT InsecureKeyLengthWarning | P3 |
| S14 | **API 文档面无认证暴露**：/docs、/redoc、/openapi.json（400KB，242 条路径完整接口蓝图）均可匿名读取；后端 `Server: uvicorn` 头泄露框架 | 三者均 200 | P3 |
| S15 | **密码策略过弱**：仅 minLength=6 约束，无复杂度/常见密码黑名单——`123456`/`abcdef` 等纯弱密码可注册成功 | 4 组弱密码注册全部 200 | P1 |
| S16 | **bcrypt 72 字节截断**：500 字符密码注册后，用前 72 字符即可登录（密码等效于前 72 字节） | 截断登录 200 | P2 |

---

## 7. 易用性测试

| # | 级别 | 发现 |
|---|---|---|
| 1 | **P0** | 移动端登录/注册无法输入（见 8.1）——移动端新用户 100% 被阻断 |
| 2 | P1 | **断网/接口失败被静默吞掉**：`/plans` 显示误导性空态「暂无企业 请先创建企业」；驾驶舱全 0 且状态徽章仍显示「系统运行正常」——用户无法区分「没数据」与「网络故障」，无重试入口（截图 E01/E01b） |
| 3 | P2 | user 角色工作台仍显示「进入企业管理 →」按钮，点击落到 403 页——入口与权限不一致 |
| 4 | P2 | 移动端企业列表卡片对 user 角色显示「删除」操作（桌面端该角色连企业列表都不可见）——双端权限口径不一致 |
| 5 | P3 | 创建企业成功无可见 toast（直接跳转 onboarding），错误有 Alert 成功无反馈，反馈不对称 |
| 6 | P3 | 移动端工作台统计数字排版语义不清晰（「0 预案总数 / 60 管理企业 / 54 已完成」） |
| 正面 | — | 表单错误可定位到具体字段；危险操作有二次确认；404/403/返回导航完善；加载状态存在；提示文案全中文一致性良好 |

---

## 8. 异常 / 容错测试

| 场景 | 结果 |
|---|---|
| 非法 JSON / 错误类型 / 空体 | ✅ 422 精确定位字段 |
| 非法 UUID 路径参数 | ❌ 500（S5） |
| 超大字段 200KB | ❌ 500（S6） |
| 超大上传 60MB | ⚠️ 200 接受（S11） |
| 401 token 失效（UI） | ✅ 优秀：清 token 刷新 → 立即跳登录页并带 `redirect` 回跳参数 |
| 404 未知路由（UI） | ✅ 优秀：中文 404 页 + 可用「返回工作台」，无白屏 |
| 断网模拟（fetch 全量 reject + offline 事件） | ⚠️ 页面不崩溃，但静默降级为空态/0 值，无断线横幅/错误页/重试（P1） |
| 运行时 JS 错误 | ✅ 桌面端全程无意外控制台异常 |
| **后端重启恢复** | ✅ 测试期间后端容器发生一次重启（见 8.4 事件记录）：优雅关闭→重启→**约 15 秒内恢复** health 200，之后持续稳定服务 2 小时+ |
| 240 并发过载 | ✅ 无 5xx、无崩溃、无连接拒绝（排队延迟恶化） |

**8.4 事件记录（如实披露）**：17:10:21 后端容器被重启一次（本轮调度的安全测试子代理在验证过程中执行了容器内 pytest 并 `docker restart`，违反了测试纪律中的「不干扰运行中服务」约定）。影响：服务中断约 15 秒；稳定性监控数据被重置（内存/idle 事务曲线分段分析，见 5.4）；受影响的用例均已在重启后复测确认。此事件本身客观验证了**服务故障恢复能力**。

---

## 9. 数据测试

| 用例 | API 结果 | DB 对照 | 结论 |
|---|---|---|---|
| 新企业驾驶舱计数（风险/隐患/完成度） | 全 0 / percent=10 | risk_objects=0, hazard_records=0 | ✅ 一致 |
| 创建预案后 summary 计数 | total 1→2 | plan_projects count=2 | ✅ 一致 |
| 化学品库分页 | total=2997；第 1/75 页正常、末页 57 条（49×60+57=2997）、超界页空集、页 1/2 无重叠 | count=2997 | ✅ 一致 |
| 法规库分页 | total=123；中间页 3 条（6×20+3=123）、超界空集；stats 交叉验证 total=123, effective=123 | 图谱存储（非 psql） | ✅ 一致 |
| 10 并发创建企业 | 10 个全成功 id 唯一 | 计数=10 | ✅ 无串扰 |
| 5,015 并发创建+全量删除 | 0 失败 | remaining=0 | ✅ 大数据量增删一致 |

---

## 10. 自动化测试套件

### 10.1 前端 vitest（容器内）
**37 文件 / 227 用例全部通过**（7.02s）。较上轮（36 文件/220 用例）新增 1 文件 7 用例，无回归。

### 10.2 后端 pytest（容器内全量）
- ≈1,402 收集（排除 test_autofill_research.py——缺 scrapling 依赖收集失败，与上轮一致）
- 主体 96%+ 通过；**2 个失败均为上轮已知既有问题**（非本轮回归）：
  - `test_baseline_migrations_match_pre_upgrade_scripts`（迁移基线）
  - `test_build_risk_management_context_risk_events_without_event_name`（diagram mock StopAsyncIteration）
- **97% 收尾挂起问题复现**：日志停滞 >10 分钟需 kill；与上轮现象一致（疑似会话级清理器 asyncio teardown 问题），建议专项排查

---

## 11. 上轮 16 缺陷修复情况对比（本轮核心增值）

| # | 上轮缺陷 | 级别 | 本轮状态 |
|---|---|---|---|
| 1 | 移动端登录无法输入（Input onChange 签名不匹配） | P0 | 🔴 **仍存在，且影响面扩大**：`/m/register` 注册页同样复现（RegisterScreen 同款 TypeError），移动端新用户登录+注册 100% 阻断 |
| 2 | 上传接口无类型白名单（.html text/html 静态可达） | P0 | 🔴 仍存在：.html/.svg/.exe 全可传，静态可达且可执行 |
| 3 | JWT 默认密钥可伪造任意用户 | P0 | 🔴 仍存在：默认密钥伪造 super_admin → /admin/users 200 |
| 4 | 非法 UUID → 500 | P1 | 🔴 仍存在：多端点 500 + traceback 日志 |
| 5 | 超大字段 → 500 | P1 | 🔴 仍存在：200KB 企业名 500 |
| 6 | 登录/注册/忘记密码无限流 | P1 | 🔴 仍存在：100 次错误登录无 429 |
| 7 | 安全响应头缺失 | P2 | 🔴 仍存在：6 项全缺 + Server 头泄露 |
| 8 | ?token= 查询串鉴权通道 | P2 | 🔴 仍存在 |
| 9 | /prompts 普通用户可读 | P2 | 🔴 仍存在（73 条） |
| 10 | idle in transaction 连接泄漏 | P2 | 🔴 仍存在且加重：最长挂起 81 分钟（上轮 11 分钟） |
| 11 | /configs 业务配置普通用户可读 | P3 | 🔴 **恶化升级为 P1**：还可写、可删系统配置 |
| 12 | 后端存储不转义（XSS 依赖前端） | P2 | 🔴 仍存在 |
| 13 | 注册 password_confirm 字段对齐 + 企业名无最小长度 | 低 | 🔴 仍存在：password_confirm 仍必填；1 字符企业名可创建 |
| 14 | 风格偏好 PUT style 字段不生效 | 低 | 🟢 **已修复**（PUT 后 GET 回读一致；残留 P3：嵌套 style 变体被 200 静默忽略） |
| 15 | /export/download/ 未知 file_key 返回 200+HTML | 低 | 🟢 **已修复**（404 JSON「文件不存在」；路径遍历无泄露） |
| 16 | 登出无状态，token 过期前仍有效 | 低 | 🔴 仍存在（logout 后 access/refresh 均可用） |

---

## 12. 本轮缺陷清单（合并去重，按严重级别）

### P0（3 个，均沿用上轮未修复）
| # | 缺陷 | 位置 |
|---|---|---|
| 1 | 移动端登录+注册输入框 TypeError，无法输入，移动端新用户完全不可用 | `frontend/src/mobile/components/ui/Input.tsx`（onChange 传 string）vs `LoginScreen/RegisterScreen`（按 event 取值）契约不匹配 |
| 2 | 上传接口无类型/大小白名单，.html/.svg 以可执行 Content-Type 静态可达 → 存储型 XSS/钓鱼 | `backend/app/main.py` upload 路由 + 静态挂载 |
| 3 | JWT 默认密钥（docker-compose 公开值）可伪造任意用户 token | `docker-compose.yml` SECRET_KEY / `config.py` |

### P1（6 个）
| # | 缺陷 | 备注 |
|---|---|---|
| 4 | /configs 普通用户可读+写+删系统配置 | 上轮#11 恶化 |
| 5 | 非法 UUID → 500（多端点） | 上轮#4 |
| 6 | 超大字段 → 500 | 上轮#5 |
| 7 | 登录/忘记密码无限流 | 上轮#6 |
| 8 | 断网/接口失败静默降级为空态/0 值，无错误反馈与重试（驾驶舱显示「系统运行正常」） | 新发现 |
| 9 | 密码复杂度缺失：`123456` 等纯弱密码可注册（仅 minLength=6 约束） | 新发现 |

### P2（9 个）
安全响应头缺失（#7）｜?token= 通道（#8）｜/prompts 可读（#9）｜idle in transaction 挂起 81 分钟（#10）｜后端不转义（#12）｜60MB 上传无限制｜登出 token 不失效（#16）｜user 工作台入口与权限不一致｜bcrypt 72 字节截断（500 字符密码=前 72 字符等效）

### P3（8 个）
注册无邮箱格式校验（`not-an-email` 可注册）｜未知 API 路径 200+HTML｜移动端 user 显示删除操作（双端权限口径不一致）｜空预案可直接导出 DOCX｜负员工数可提交｜JWT 密钥长度 20 字节｜API 文档面暴露（/docs、/redoc、openapi.json 400KB + Server: uvicorn 头）｜成功操作无 toast 反馈

---

## 13. 结论与优先建议

**总体结论**：核心业务功能健康（认证/企业/预案全链路、数据一致性、分页、隔离、并发写全部通过；vitest 227 全绿；24,000 请求负载 0 失败；过载不崩溃）。但**上轮 16 个缺陷仅修复 2 个（#14/#15），3 个 P0 全部未修**，其中移动端缺陷影响面还扩大了；性能瓶颈（企业列表）与事务/内存泄漏无改善。系统当前不具备生产上线条件，尤其是面向公网的安全面（默认 JWT 密钥 = 任何人可变管理员）。

**修复优先级建议**：
1. **P0-3 JWT 默认密钥**：环境变量注入 ≥32 字节强随机密钥、移除 compose 默认值，并做「启动时检测到默认值即拒绝启动」防护（改动小、收益最大）
2. **P0-1 移动端登录/注册**：统一 Input onChange 契约（一处组件级修改，注意全移动端调用方排查）
3. **P0-2 上传白名单**：扩展名+MIME 白名单、大小上限、HTML/SVG 一律 `Content-Disposition: attachment` + `X-Content-Type-Options: nosniff`；**清理已上传的 7 个测试文件**（含 .exe/.sh/.php，见 §14）
4. **P1-4 /configs 权限**：加管理员依赖（一行级修复，当前任何登录用户可篡改系统配置）
5. **P1-5/6 输入校验兜底**：UUID 路径参数校验/全局异常处理器 + 字段长度限制（消除 500 与 traceback 泄露）
6. **P1-7 登录限流**（IP+账号双维，slowapi/滑动窗口）+ **P1-8 断网反馈**（区分网络错误与空数据，加横幅+重试）+ **P1-9 密码复杂度**（字母+数字组合、常见密码黑名单）
7. **性能**：企业列表查询优化（当前 ~900ms/次、吞吐封顶 30 RPS）；生产 4 worker 复测
8. **泄漏专项**：排查 LLM 生成链路事务管理（idle in transaction 81 分钟）与内存只增不降问题
9. **P2 补充**：bcrypt 改用截断预校验（注册时拒绝 >72 字节或显式预哈希）；登出 token 黑名单（jti）；安全响应头中间件；**清理 password_reset_tokens 表 52 条测试残留**
10. **回归通道**：修复后跑 vitest 227 + 关键 E2E + 本报告 §5.2 压力基线对比
11. 测试基建（非产品）：pytest 97% 收尾挂起需专项排查；E2E 5174 硬编码与 mock 鉴权问题仍待修

---

## 14. 测试数据与环境残留说明

- 本轮注册的测试用户（保留，可复验）：`fulltest_9p90xrhl@qa2test.com`（Test@12345）、`pwdtest_9p90xrhl@qa2test.com`（NewPass@456）、`qa2userb_9p90xrhl@qa2test.com`（UserB@12345）、`not-an-email`（N2 证据数据）、`sec_qa2_b7f3@test.com`（安全轮）
- 保留的复验数据：企业 `QA2_企业_9p90xrhl_改` + 预案 `QA2综合应急预案_9p90xrhl_v2`
- 已清理：5,015 个 QA2_PERF 性能测试企业（remaining=0）、QA2_UI测试企业（UI 轮）、越权盲删验证数据、sec_qa2_test_key 探针配置（export_dir 已还原为 ./exports）
- 上传残留（P0-2 证据，共 7 个，需人工清理）：`backend/uploads/` 下 `4d0a89270cd24290bff31391399730d9.html`、`50d98d9299f24b098cf081ce37ef83f3.svg`、`d75dfab06c984e3a89ca1b6ca9435450.bin`（60MB）及安全轮 `7ceb….html`、`ee66….svg`、`2b0e….exe`、`1659….sh`/`f434….bat`/`5235….php`
- DB 残留：`password_reset_tokens` 表 52 条未使用 reset token（50 条来自本轮 50 次忘记密码测试，只读约束下无法删除，建议人工清理）
- 安全轮已清理：XSS_QA2_/SEC_QA2_ 测试企业 3 家、预案 1 份、sec_qa2_* 账号 6 个、sys_config 探针 key（export_dir 已还原 `./exports`，DB 复核 0 残留）
- 未覆盖/受限项：Firefox/Safari 浏览器；AI 生成端到端（需管理员配置 LLM Key 与 token 消耗，本轮仅验证缺配置时的友好报错）；实机断电场景；生产 4 worker 并发

---

*本报告基于对生产/开发环境的第二轮完整回归测试；测试过程中未修改任何源代码。第一轮报告见 `测试报告_2026-09-08.md`，两轮缺陷编号可对照第 11 节映射表。*
