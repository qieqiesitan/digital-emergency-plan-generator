# 反例（修复前的网关配置形态）：无 SSE 配置、无 client_max_body_size、无真实 IP 透传
location /emergency-plan-migration/ {
    alias /etc/nginx/html/emergency-plan-migration/;
    try_files $uri $uri/ /emergency-plan-migration/index.html;
}

location /api/ {
    proxy_pass http://192.168.3.17:8000/api/;
}
