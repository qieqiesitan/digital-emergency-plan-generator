审查完成。测试后工作树仅 TASKS.md 有实施者既有的未提交快照改动（本次审查未改任何文件），上传测试仅留下空目录残留。

## ✅ 符合规格

对照计划任务 3（3.1~3.7）逐项核验 e44e126，7 个步骤全部落实，无缺失需求；用户指定的 4 个测试文件 **36 passed**（upload 2 + cascade 1 + service 21 + migration 12）。

- 依赖：`backend/requirements.txt:30-33` 追加 Pillow/pytest/pytest-asyncio，与计划逐字一致
- 存储服务：`backend/app/services/floor_plan_storage_service.py:8-44` 与计划代码一致（UPLOAD_DIR 兜底、PNG/JPEG/WebP 白名单、20MB/12000px 上限、save/remove）
- Floors CRUD（位于 Zones 段之前，`risk_management.py:39-137`）：GET/POST/PUT/DELETE/上传 5 个路由齐全，名称唯一 409、首个默认楼层、唯一默认楼层不可删除、有数据楼层不可删除、默认楼层同步写回 `enterprises.floor_plan_url` 均符合计划；`_floor_response` 返回 zone_count/risk_point_count
- 企业清理：`enterprise_cleanup_service.py:6-20` 与计划逐字一致，删除顺序 措施→事件→单元→对象→分区→楼层→企业，与设计文档 4.7 完全吻合；`enterprises.py:165-171` 已改走 `delete_enterprise_complete`
- 提交文件清单与计划 3.7 完全一致，无多余文件

## 审查发现（均低风险，不构成不通过）

1. **多余实现**：`risk_management.py:30-35` 的 `_validate_zone_polygon` 被接入 `create_zone`/`update_zone`（214、224 行）。计划任务 3 未要求此改动，`validate_polygon_v2` 的路由调用点计划只安排在任务 4 批量保存（计划 1095 行）；且 `RiskZoneFloorPlanPolygon` 的 schema 校验器（`schemas/risk_management.py:35-62`）已在 Pydantic 层拒绝非法多边形，路由层为冗余，并给 create/update zone 新增了计划外 `422 POLYGON_INVALID` 行为。建议归入任务 4 一并评审。
2. **改进型偏差**：`_default_floor`（`risk_management.py:39-44`）复用 `risk_mapping_service.ensure_default_floor` + IntegrityError 回滚重试，替代计划中的本地实现，与 TASKS.md 记录的"重复实现需合并"遗留一致，且会复用 `enterprises.floor_plan_url`（符合设计文档 4.1）。注意其并发防重依赖迁移 SQL 的 `uq_ef_default_per_enterprise` 部分唯一索引，目标库未应用迁移时无实际防重。
3. **优于计划**：`create_floor`（66-75 行）用 values 字典显式写入 `is_default`，规避了计划伪代码中 pydantic `exclude_unset` 丢弃 mutation 的潜在缺陷。
4. **无功能影响的偏差**：计划导入清单含 `RiskZoneFloorPlanPolygon`，实现未导入（未使用）。
5. **观察（非任务 3 缺陷）**：设计文档 4.7 的"企业删除返回待清理数量、前端二次确认、平面图文件事务后清理"未包含在计划任务 3 给定代码中，实现与计划任务 3 一致，确认后续任务（4/7）覆盖即可。
6. **测试残留**：运行 `test_save_valid_png_then_remove` 后留下空目录 `backend/uploads/enterprises/e-1/floors/f-1`（文件已被测试删除，我尝试清理被策略拦截），无害，可手动删除。

按"只审查不改文件"要求，本次未修改任何文件、未更新 TASKS.md、未提交。