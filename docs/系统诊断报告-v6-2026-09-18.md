# 数字化预案系统诊断报告 v6（三处"半成品/断链"功能补齐 + 全量门禁复验）

- 日期：2026-09-18
- 范围：v5 收尾后继续推进"把所有问题修完"。本轮改扫描口径——不再看报告清单，
  而是直接在代码里找三类痕迹：**文档/注释自认的遗留项、孤儿接口（后端有前端从不调用）、
  前端为绕开后端限制而静默降级的功能**。共命中 4 处功能断链，另清掉 1 类升级债。
- 结论：**4 处功能断链已闭环且各有端到端证据；AntD v6 弃用 API 迁完 6 类（仅剩 List 组件，见 §四）；剩余 4 项仍是外部运维动作。**

---

## 一、本轮发现与处置

### N-5（P1 功能断链）作业票审批对"非企业主审批人"完全不可达，且没有"我的待办"

**取证**

- 前端 `WorkTicketApprovalPage.tsx` 注释自认遗留："后端也没有'我的待办'接口……按角色收窄待办需要后端补接口"；
- 运行时复核：`ensure_enterprise_owned` 只认 `Enterprise.user_id == 当前用户`，**绑定为审批成员的账号连列表都是 404**；
- 数据复核：`enterprise_members` 中 `user_id` 非空的绑定数为 **0** → GB 30871 的多单位会签在任何真实账号上都跑不起来（审批人进不去，能进的只有企业主一人）。

**修复**

| 层 | 改动 | 关键点 |
| --- | --- | --- |
| 服务 | `access_control.ensure_enterprise_visible()` | 所有者 → `(企业, True)`；`enterprise_members` 里已绑定且 `enabled` 的成员 → `(企业, False)`；其余仍 404 |
| 服务 | `work_ticket_service.tickets_pending_for_user()` / `member_can_view_ticket()` | 按"当前节点可签人 × 是否已签"筛选；成员对自己签过的票仍可回看/打印 |
| 路由 | `GET /work-ticket/tickets` 新增 `assigned_to_me` | **成员在任何情况下都只返回与自己有关的票**（不传参也不放行全量）；企业主传 `true` 才是"轮到我签" |
| 路由 | 详情 / 打印 / `node-action` 改走 `_visible_ticket()` | 读与签字放宽到成员；**开票、提交、气体检测仍仅企业主**（不放宽写面） |
| 前端 | 审批工作台新增「只看我的待办」开关（默认开）+ 空态文案；删除过时遗留注释 | 关掉后企业主可看全部审批中票据 |

**证据**：`output/playwright/e2e-20260918/scripts/_work_ticket_member_todo_probe.py` → **21/21 PASS**
（临时提权建两个账号 → 建组织树 → 绑定审批成员 → 开票 → 气体检测 → 提交 → 成员待办命中 → 非成员 404 → 匿名 401 → 成员签字 → 待办清空但票仍可回看）。
探针自带清理：临时用户 / 成员绑定 / 组织树 / 探针票据全部归零，QA 角色还原为 `user`。

### N-6（P1 功能断链）"AI 清单补全"只有半条链路

**取证**：`POST /hazard-inspection/ai/checklist` 端点存在、前端 `aiChecklistSuggestion()` service 也存在，
但**没有任何页面调用它**；后端也没有"把勾选的建议项写进任务"的端点。
服务端 docstring 写着"页面勾选后与既有清单项合并去重"——那一跳从来没实现，
`hazard_service.py` 里对应位置还留着一句 `TODO(task 12)`。

**修复**

| 层 | 改动 |
| --- | --- |
| 后端（新端点） | `POST /hazard-inspection/tasks/{task_id}/items`：按 `content` 去重、单次 ≤20 条、纯空白内容 422；权限 = 任务责任人 / 企业主 / 企业管理员（其余 403，非成员仍 404） |
| 后端（状态一致性） | 已 `done` 的任务被追加 pending 项时回退为 `processing` 并清 `completed_at`——消除"已完成但清单里还有未核对项"的自相矛盾 |
| 前端 | 任务执行弹窗新增「AI 补全清单」→ 建议勾选弹窗 → 「加入清单」；重复项提示"已全部跳过" |
| 文档性修正 | 清掉 `hazard_service.py` 里已过期的 TODO，写明"建议 → 勾选 → 落库"两段式是**刻意设计**（生成任务时不自动调 LLM：避免成本/延迟，也不让 AI 静默改写清单） |

**证据**：`_hazard_ai_checklist_probe.py` → **8/8 PASS**（真实浏览器点按钮 → 弹窗展示建议 → 落库成功提示 → 复查清单含新项且重复项只出现 1 次）。
AI 响应用 route 拦截伪造，**零 LLM 费用**；后端新增 6 条单测（去重、状态回退、责任人/管理员/非成员三种权限、空白 422、跨企业 404）。

### N-7（P2 功能不一致）移动端批量生成"选了风格也不生效"

**取证**：`generate_batch_background` 只读预案保存的 `p.style_preference`，
移动端为此**直接隐藏**了批量模式下的风格选择（组件注释自认）——用户看不到选择，也谈不上生效。

**修复**：两个 batch 端点（SSE 与 background）接受请求级 `style_preference` / `advanced_prompt_overrides`
（非 dict 一律忽略，缺省回落到预案风格，既有调用方零改动）；解析逻辑抽成 `_parse_batch_request()` 便于单测；
前端 service / 类型透传；移动端批量模式恢复风格选择。

**证据**：`_v6_ui_probe.py` → **11/11 PASS**，其中真实浏览器捕获到的批量请求体里
`style_preference.detail_level == "concise"`（此前该字段根本不会出现在请求里）。

### N-8（P2 功能断链）"AI 一键生成排查计划"后端与 service 都在，界面没有入口

**取证**：`POST /hazard-inspection/ai/plan-builder` 端点与 `aiPlanBuilder()` service 都已存在，
但**没有任何页面调用**——端点 docstring 写的"页面整批确认或逐条调整后映射为企业成员 id 与分区 id，
再经 POST /plans 落库"这一跳从未实现。同一份文件里 `aiSetupWizard()` 也处于同样的孤儿状态（见 §四）。

**修复**：计划页新增「AI 生成计划」按钮 → 输入区域/工艺说明 + 频次偏好 → 建议卡片列表 →
「采用」后自动把**分区名映射为分区 id**、责任人姓名映射为成员 id，并预填到既有「新建计划」弹窗，
由用户复核后走原保存路径落库（未匹配到的分区名会明确提示手动补选）。

**证据**：`_hazard_plan_builder_probe.py` → **9/9 PASS**（浏览器点击 → 建议 → 采用 → 名称/分区预填 →
保存 → 复查落库的分区 id 正是探针分区）。AI 响应用 route 拦截伪造，零 LLM 费用。

### N-9（P3 升级债）AntD v6 弃用 API 全量清理（6 类 137 处）

真实浏览器冒烟暴露的弃用告警（会在下一个 antd 大版本直接移除），本轮按类型清理：

| 弃用项 | 替换 | 处数 |
| --- | --- | --- |
| `Alert message` | `title` | 81 |
| `Space direction` | `orientation` | 32 |
| `Drawer width` / `height` | `size`（antd 6 的 `size` 支持数字） | 14 |
| `Modal destroyOnClose` | `destroyOnHidden` | 2 |
| `Input bordered={false}` | `variant="borderless"` | 1 |
| `Table rowKey(record, index)` 用 index | 预计算 `withRowKeys()`（新增 util + 2 例单测） | 7 |

重写用 JSX 括号感知的脚本完成（跳过字符串/嵌套表达式，避免正则误伤），逐文件 diff 复核后
由 `tsc` + `eslint` + `vitest` + 37 页浏览器冒烟兜底：
**冒烟里带 console error 的页面从 10 页降到 5 页**（其中 4 页是既有的"报告未生成 404"，
1 页只剩 `List` 组件弃用，见 §四）。

### N-10（P3 日志噪音）passlib 每次登录都打一条 bcrypt 回溯

**取证**：后端日志里每次登录/改密都会出现
`(trapped) error reading bcrypt version` + `AttributeError: module 'bcrypt' has no attribute '__about__'`。
根因是 `passlib 1.7.4` 的后端自检读取 `bcrypt.__about__.__version__`，而 `bcrypt>=4.1` 已移除该属性；
passlib 会捕获它（功能正常），但 ERROR 级回溯会持续污染日志、掩盖真实错误。

**处置**：不改依赖版本（重建镜像会按 `>=x,<y` 范围拉到新版本，引入未验证的漂移），
改为在 `app/services/auth_service.py` 里补一个只读属性 shim，让 passlib 的自检回到预期路径；
**哈希算法与密文格式不变（仍是 `$2b$`）**。

**证据**：重启后执行真实登录 → `login=200`、近 30 秒日志里 `bcrypt version|__about__|AttributeError` 计数 **0**
（修复前每次登录必现）；容器内直接跑 `hash_password/verify_password` → `prefix=$2b$ verify=True wrong=False`；后端 75 条 auth 相关单测 + 全量 1921 条通过。

### N-11（复核）前后端接口契约漂移检查：83/83 全部匹配

写了一个小求值器把前端 service 里的 API 调用（含 `BASE`/`BASE(eid)` 这类本地常量拼接）
展开成规范路径，与后端运行实例的 OpenAPI（279 条路径）逐条比对：
**83 个调用点全部命中后端路径，0 处漂移**（无"前端调了但后端没有"的死链）。

### N-12（P3 死代码）清理 3 个无引用文件 + 判定 4 个待确认

全仓扫描"导出但无任何引用"的模块后：

| 文件 | 判定 | 处置 |
| --- | --- | --- |
| `pages/Hazard/HazardPlaceholderPage.tsx` | 占位页，13–16 任务的真页面已实现且无路由引用 | ✅ 删除 |
| `components/enterprise/RiskMatrixHeatmap.tsx` | 已被在用的 `RiskOverviewMatrix.tsx` 取代 | ✅ 删除 |
| `components/common/MarkdownViewer.tsx` | 薄包装；Chat / PlanEditorPage / reportMarkdown 都直接用 markdown-it | ✅ 删除 |
| `components/enterprise/RiskSourceForm.tsx`、`mobile/screens/RiskSourceListScreen.tsx` | 旧"风险源"模型的 UI，当前全站无路由挂载（新"风险管控"用 risk_objects/measures） | ⏸ 保留待你确认：删除还是重新挂载 |
| `mobile/components/plan/PlanCard.tsx`、`mobile/hooks/useStreamGeneration.ts` | 移动端无引用（批量生成已改后台任务+轮询） | ⏸ 保留：与并行会话的移动端改动同目录，避免冲突 |

### 运行时日志审计（本轮）

- 近 90 分钟内后端 **6 条 500 全部来自"修复前"的并发开票探针**（`POST /work-ticket/tickets`，端口 415xx 同一批），
  修复后至今 **0 条新 500**；
- 其余 `数据库参数校验失败（已转 422）` 来自烟测脚本故意使用的占位 id（非 UUID）→ 属预期的 422 转换路径。

---

## 一之补：完整预案导出专项（用户核心诉求，本轮抓到 2 个内容丢失缺陷）

用户最初的诉求是"**肯定要让能导出企业的完整预案**"。本轮用一个新探针
（`_export_completeness_probe.py`：新建综合预案 → 把模板 25 章全部写满 + 其中一章内嵌 Mermaid
→ 导出前校验 → 导出 DOCX → 用 python-docx 逐项核对标题/正文/图/占位符/体量）实测，
**当场抓到 2 个"内容被静默丢弃"的真缺陷**：

### N-13（P1 内容丢失）正文里提到章节名，整段被删 —— 导出的预案会"只剩标题"

**现象**：25 章全部填满的预案，导出后的 DOCX **只有封面、目录和章节标题，一条正文都没有**；
导出预览（HTML）同样空白。而只填 1–2 章时正文正常。

**定位过程**：二分复现 → 与章节数量无关，与**正文内容**有关：

| 章节名 | 正文 | 结果 |
| --- | --- | --- |
| 总则 | `<p>AAA 组正文段落。</p>` | 保留 |
| 总则 | `<p>正文包含总则二字</p>` | **被删** |
| 总则 | `<p>探针正文：总则。</p>` | **被删** |
| 总则 | `<p>总则概述</p>` | **被删** |

**根因**：预览与 DOCX 共用的 `app/services/plan_section_content.py::strip_section_heading`
用**子串包含**判断"这一行是不是重复的章节标题"（`section_title in text`），
于是任何 ≤80 字的开头段落只要提到章节名就被当作"重复标题"删掉；
循环还会连续删除多段。真实正文里"本节依据《安全生产法》编写""本章介绍应急组织机构及职责"
这类句子非常常见 → 导出内容静默缺失，且没有任何提示。

**修复**：改为**归一化等值**判定——去掉标签、编号前缀（`1.2`、`一、`、`第3章`、`（1）`）、
首尾标点后要求与章节名完全相等才剥离；未传章节名时保留旧的宽松行为（既有调用方契约）。
补 12 条参数化 + 4 条专项单测（`backend/tests/test_plan_section_heading_strip.py`）。

### N-14（P1 内容丢失）手工编辑的流程图在导出时被静默丢弃

**现象**：章节里内嵌 Mermaid 代码，但该章节没有预渲染 SVG 缓存时，导出的 DOCX 里**没有这张图**，
也没有任何提示（实测 `inline_shapes=0`）。

**根因**：`generate_plan_docx` 里把 `<code class="language-mermaid">` 的 hash 只记为"已见过"，
不生成渲染项；能用的 SVG 只有两个来源——内容里的内联 `<div class="mermaid-rendered">`
或 `PlanSection.mermaid_svgs` 缓存。生成期预渲染只覆盖 AI 产出的内容，
用户手工编辑/粘贴的 mermaid 块两边都不占 → 被丢弃。

**修复**：新增纯函数 `missing_mermaid_codes(codes, mermaid_svgs, covered_hashes)`
并在导出循环里对未覆盖的代码**就地渲染**（mermaid.js + Playwright 的渲染能力此前已具备，
只是没被调用）；渲染失败仍是"不阻断导出"。
（自审：第一版兜底我误把"见过 hash"的集合当成"已覆盖"传进去，导致兜底永不触发——
 已用 `covered_hashes` 修正，并补一条调用点守护测试。）

### 专项验收结果

| 检查项 | 结果 |
| --- | --- |
| 新建综合预案并填满模板章节 | **25/25** 写入成功 |
| 导出前校验接口 | 200；`valid=true`、`issues=[]`（6 条档案类 warning 属该企业未登记组织/资源，非缺陷） |
| DOCX 导出 | 200，48,054 字节 |
| 每个章节标题都在文档里 | **缺失 0** |
| 每章正文都在（无截断/丢失） | **缺失 0**（修复前 25/25 全缺） |
| 含图章节产出内嵌图片 | **inline_images=1**（修复前 0） |
| 全文占位符残留（待补充/XXX/{{…}}） | **0** |
| 探针清理 | 自动删除探针预案（DELETE 200） |

**结论**：修复后"填满的预案"可以完整导出——章、正文、流程图、附件占位策略都符合预期。

### N-15（P2 票面规范）作业票打印件标题只印内部缩写

**取证**：作业票打印 DOCX 的标题是 `f"{ticket_type} 安全作业票"` → 打出来是 **"DHZY 安全作业票"**；
而模板表里本来就有法定中文票名（`动火安全作业票`）。票面是交给监管与作业现场的法律文书，
只印内部缩写既不合 GB 30871-2022 附录A 的票面要求，现场人员也未必认识。

**修复**：`build_snapshot` 增加 `template_name`（模板中文名），`render_ticket_docx` 优先用它渲染标题，
缺省才回退"类型码 + 安全作业票"；补 2 条单测（含回退分支），并更新快照字段守护测试。

### N-16（P1 功能断点）作业票批准之后没有任何出口：不能开始作业 / 完工 / 归档

**取证**：

- 状态机 `work_ticket_flow.TRANSITIONS` 定义了 `approved →working →finished →closed`
  （还有 `cancel`/`expire`），前端类型里也早就写好"作业中/已完工/已归档"的中文标签；
- 但后端路由**只有 8 个端点**（模板/列表/开票/气体检测/提交/审批/详情/打印），
  没有任何端点能触发 start/finish/close；前端详情页也只有「提交审批」和「打印票面」两个按钮。
- 结果：票批准后永远停在"已批准"，**完工与归档留痕无从产生**——而 GB 30871-2022 要求作业票至少保存一年，
  归档动作本身就是要留痕的事实。

**修复**：

| 层 | 改动 |
| --- | --- |
| 状态机 | `work_ticket_flow` 增 `LIFECYCLE_ACTIONS`（start→working / finish→finished / close→closed / cancel→cancelled）与 `lifecycle_target()`，与 `TRANSITIONS`（能不能做）配套 |
| 服务 | `transition_ticket()`：状态机校验、**超期保护**（批准后已过 `valid_to` → 置 `expired` 并留痕后拒绝开工）、每步写 `work_ticket_audit_logs`、完工/归档时清空当前审批节点指针 |
| 路由 | 新端点 `POST /work-ticket/tickets/{id}/transition`（仅企业主；审批节点动作仍走 `/node-action`，两者语义分开） |
| 详情接口 | 新增 `audit_logs`（全量流转留痕：开票/提交/审批/开始作业/完工/归档/作废/过期） |
| 前端 | 详情页按状态给出「开始作业 / 完工 / 归档」主按钮 + 「作废」（二次确认）；时间线改为渲染全量流转留痕（带 状态→状态、办理人、时间） |

**证据**：

- `_work_ticket_member_todo_probe.py` → **29/29 PASS**：成员审批通过后，企业主 `start→working`、`finish→finished`、
  `close→closed`，已归档再作废返回 **409**，`audit_logs` 覆盖 `open/submit/approve/start/finish/close`；
- `_work_ticket_lifecycle_ui_probe.py` → **4/4 PASS**：draft / approved / working / finished 四种状态下
  页面只出现该状态应有的动作按钮（在 8082 产物上复跑通过）；
- 后端新增 6 条生命周期单测；全量 **1950 passed**。

> 附带收获：ruff 在提交前抓到我自己的一处真错误——详情端点新代码用了 `WorkTicketAuditLog` 但没 import，
> 运行时会 500（单测不覆盖该分支）。已修；这也说明"静态门禁 + 端到端探针"两道都要有。

### 导出族验收（本轮实测，覆盖 4 类对外文档）

| 导出 | 探针 | 结果 |
| --- | --- | --- |
| 预案 DOCX（合成 25 章 + 流程图） | `_export_completeness_probe.py` | **10/10**：标题 0 缺、正文 0 缺、图 1 张、占位符 0、48KB |
| 预案 DOCX（真实 AI 内容，4 章非空） | `_export_real_plan_check.py` | **4/4 指纹命中**（最长段落逐一核对） |
| 风险评估报告 DOCX | `_report_export_probe.py`（E2E_KIND=risk） | **4/4 章**标题+正文齐全（39KB） |
| 资源调查报告 DOCX | `_report_export_probe.py`（E2E_KIND=resource） | **4/4 章**标题+正文齐全（39KB） |
| 作业票票面 DOCX（法定票面） | `_work_ticket_print_probe.py` | **8/8**：法定中文票名、票号、气体检测、安全措施表（107 行）、三联标注、保存期注、无占位 |

> 探针全部自带清理：合成预案/报告行/票据在 finally 里删除（本轮实测 DELETE 200 / 库内 DELETE 1，残留 0）。

---

## 一之补二：批量导入专项（又抓到 3 处真缺陷，其中资源导入整体不可用）

同样的"扫一个功能族"思路用到**Excel 批量导入**（HR/安全员一次导几十上百行，坏了影响面很大），
写了成员导入与资源导入两个端到端探针，结果抓到 3 个真缺陷：

### N-17（P1 路由遮蔽）资源导入模板下载被参数化路由抢先匹配 → 422

**现象**：`GET /enterprises/{id}/resources/template` 返回 422「请求参数无效（如 ID 格式错误…）」。

**根因**：`enterprise_sub.router` 在 `resources_ext.router` **之前**注册，而它的详情路由写成
`/{enterprise_id}/resources/{resource_id}`（无类型约束）——"template" 被当成 `resource_id`
匹配进去，UUID 转换失败 → 422。前端「第一步：下载模板」因此必失败，资源导入实际上手都上不了。

**修复**：三条详情路由约束为 `{resource_id:uuid}`、参数类型改 `uuid.UUID`（查询显式 `str()`）；
补 `test_route_shadowing_guard.py`（守护"字面量路径不得被无约束参数路径吞掉"）。

### N-18（P1 功能整体不可用）资源导入上传必 500

**现象**：修好模板下载后，上传任何 xlsx 都返回 500。

**根因**：`wb = Workbook(io.BytesIO(contents))` —— openpyxl 的 `Workbook()` 第一个参数是
`write_only`，传入 BytesIO 只会**新建一个空白只写工作簿**、完全忽略上传内容，
于是 `wb.active` 为 None → `AttributeError: 'NoneType' object has no attribute 'iter_rows'`。
同一误用还有第二处（`risk_sources_ext.py` 的风险源导入）。

**修复**：两处都改为 `load_workbook(io.BytesIO(contents), data_only=True)`，并对损坏文件返回
**400「导入文件格式无效，请使用模板」**（不再 500）；补 `test_excel_import_workbook_usage.py`
（扫全部 router，禁止 `Workbook(BytesIO)` 写法 + 要求 400 兜底存在）。

### N-19（P2 数据质量）模板自带的示例行会被整批导进真实台账

**取证**：下载的模板第 2 行是示例数据（干粉灭火器 / 张三 / 13800001111），
而前端确认步骤是 `validItems = previewItems.filter(errors.length === 0)`（**没有逐行勾选**）
→ 用户只要不手动删掉示例行，它就会被当成真实资源写进台账。

**修复**：示例行改为自标识（`【示例】干粉灭火器` / `示例-张三`），导入端按 `【示例】` 前缀跳过，
并在预览响应里返回 `skipped_examples` 计数（既不静默消失、也不误计为错误）。

### 批量导入族验收

| 探针 | 结果 | 覆盖点 |
| --- | --- | --- |
| `_member_import_probe.py`（组织成员） | **8/8** | 模板表头契约、2 行成功入库、错误行（用户不存在/姓名必填）准确报出、部门与班组节点按需创建、错误行不入库 |
| `_resource_import_probe.py`（应急资源） | **10/10** | 模板、预览统计（1 有效/1 错误/1 示例跳过）、错误原因、预览不落库、批量落库、台账可查 |

> 两个探针都自带清理：删除导入的成员/资源并把组织树恢复为导入前快照（实测残留 0）。

### N-20（P1 路由遮蔽，同 N-17）风险源导入模板下载同样被 422 吞掉

把"上传端点"整族扫一遍时发现：`/enterprises/{id}/risk-sources/template` 同样返回
422「请求参数无效」——`enterprise_sub.router` 的 `/{enterprise_id}/risk-sources/{risk_id}`
（GET/PUT/DELETE）无类型约束且**先注册**，把 `template` 当 `risk_id` 吃掉。
**风险源导入模板下载因此也不可用**（与 N-17 是同一根因的第二处）。

修复：三条 `risk_id` 详情路由一并约束为 `{risk_id:uuid}`；守护测试扩成两组
（`resource_id` / `risk_id` 各 3 条 + 对应字面量路由存在性）。

### N-21（P2 数据质量，同 N-19）风险源模板示例行同样会被整批导入

模板第 2 行是"原料仓库 / 仓库东区 / 大量可燃物堆积…"，预览里 `valid_count=2`（示例 + 真实行），
而前端确认步骤同样是"所有有效行整批落库" → 示例数据会被导进台账。
修复方式与 N-19 一致：示例行标 `【示例】`、导入跳过、返回 `skipped_examples` 计数。

### 上传端点整族盘点（8 个）

| 端点 | 验证结果 |
| --- | --- |
| `POST /org/members/import` | **8/8** |
| `POST /resources/import` | **10/10**（修掉 N-18 必 500、N-19 示例行污染） |
| `POST /risk-sources/import` | **10/10**（修掉 N-20 模板 422、N-21 示例行污染） |
| `POST /floors/{id}/plan` | **9/9**：上传解析宽高、替换生成新 URL 且**旧文件已删**、非图片 422、删除清 URL；磁盘残留 **0** 文件 |
| `POST /extraction/parse-file`、`POST /onboarding/import(/batch)`、`POST /floors/{id}/four-color/analyze` | 依赖 AI/文档解析，未做零费用验证（列入 §四 待验清单） |

---

## 一之补三：法规库专项（读写两侧 + 图谱数据修复）

### N-22（P2 数据/磁盘膨胀）删除法规只删图谱节点，条文子节点与源文件全部残留

**取证**：

- 入库时除法规节点外，还会为**每条条文**建 `art_{reg_id}_*` 子节点（本库 `node_type=article` 共 **7414** 个）；
  而 `DELETE /regulations/{id}` 只调 `graph.delete_node(法规节点)` → 条文子节点变成孤儿。
  实测本机图谱里**已遗留 109 个孤儿条文节点**（如 `reg_11f5d398_1` 名下 26 个）。
- 同时 `data/texts/{id}.md`（条文原文）与 `data/uploads/{id}/`（源文件）在删除后**从不清理**。
- 影响判定：孤儿**不会被检索到**（检索路径要么按 live 法规 id 取条文、要么显式跳过 `article` 节点），
  所以不是"引用已删除法规"的错误答案，而是**图谱与磁盘持续膨胀**——
  最直接的证据是：删除后"节点数回不到基线"。

**修复**：

| 项 | 改动 |
| --- | --- |
| 图谱写操作 | 新增 `RegulationGraph.delete_regulation()`：一次删除法规节点 + 其全部条文子节点（`parent_regulation` 匹配），只落盘一次 |
| 路由 | `DELETE /regulations/{id}` 改用级联删除，并把 `article_nodes` 计数写进 `history.jsonl` 事件 |
| 文件清理 | 新增 `sync.remove_regulation_files()`：删 `texts/{id}.md` 与 `uploads/{id}/`（目录清空后一并删除），DELETE 时调用 |
| 存量数据 | 新增 `backend/scripts/purge_orphan_articles.py`（`--dry-run` / `--apply`，自动写 `graph.json.bak`，该后缀已被 `.gitignore` 覆盖）；本机执行：**7563 → 7454 节点（-109 孤儿），边 7821 → 7818** |
| 守护 | 单测 5 条（级联删除、不误删别家条文、落盘后重新加载仍干净、文件清理幂等） |

**证据**：

- `_regulations_write_probe.py` → **16/16 PASS**：入库 200（id 可预期）、条文文本落盘、图谱 +1 节点、
  重复入库 409、详情 200、废止后 `abolished`、影响分析 200、删除 200 → 详情 404、
  **条文文本与源文件目录已清理**、**节点数/边数精确回到基线**、**无残留节点（含条文子节点）**；
  探针跑完把 `history.jsonl` 还原为原始字节（基线为空）。
- `_regulations_read_probe.py` → **11/11 PASS**：`/graph-data`、`/stats/data`、`/history/global`
  等字面量路由都未被 `/{regulation_id}` 吞掉（同类路由遮蔽问题的法规侧回归）。
- `graph.json` 的改动是纯删除：`-1419` 行、`0` 新增，且文件在 git 里可回滚。

---

## 一之补四：四色图工作台 + 聊天助手工具链（各自抓到问题）

### N-23（P1 助手工具层）一个坏工具会毒化整轮对话，且 `list_templates` 必然失败

**方法**：聊天助手的 37 个工具由模型按需调用，出问题时只表现为"助手答不出来"。
本轮写了 `backend/scripts/check_chat_tools.py`：**绕过 LLM 直接 `dispatch()` 每个工具**
（零额度），把"工具层"与"模型层"分开验证。

**结果（修复前 8/18）**：`list_templates` 报
`'PlanTemplate' object has no attribute 'description'`（模型上根本没有该列，该工具 100% 失败）；
**紧随其后**的 9 个关系型工具（`list_plans`/`get_enterprise`/`get_plan`/`list_resources`/
`list_risk_sources`/`list_resource_investigations`/`list_risk_assessments`/
`get_generation_progress`/`query_enterprise_knowledge`）统统一致地报
`greenlet_spawn has not been called; can't call await_only() here`。

**根因（两个）**：

1. `_list_templates` 读取不存在的 `t.description`；
2. `dispatch()` 的兜底是"任何异常都 `await db.rollback()`"——普通 Python 异常本不需要回滚，
   而回滚会把会话里**已加载对象全部置为过期态**，后续工具再访问关系属性就走同步懒加载 →
   `MissingGreenlet`。**一个坏工具把同一轮里其它工具全带崩**，且报错信息完全误导
   （像是并发/事件循环问题，实际是工具 bug + 恢复策略不当）。

**修复**：`_list_templates` 只返回真实列（`id/name/plan_type/version/section_count`）；
`dispatch()` 仅对 `SQLAlchemyError` 回滚，并在回滚后 `expunge_all()` 清 identity map，
避免后续工具拿到过期对象。

**复验**：`check_chat_tools.py` → **18/18 PASS**（含修复前失败的 10 个）；
新增 `backend/tests/test_chat_dispatch_resilience.py`（4 例：普通异常不回滚、DB 异常回滚+清 map、
模板字段契约、删除预案提示带标题）。

### N-24（P3 提示文案）`delete_plan` 提示"预案「」已删除"

`_generic_delete` 统一用 `getattr(entity, "name", "")` 取显示名，而 `PlanProject` 的字段是 `title`
→ 删除预案时提示的名称是空的。改为按配置取显示字段（`_PLAN_CFG` 增加 `label_field="title"`）。

### 四色图工作台验收（`_risk_workbench_probe.py` → 17/17 PASS）

楼层 → 分区 → 风险对象（带坐标的"风险点"）→ 单元 → 事件（LS 法）→ **重算得到 `R=12`/等级** →
措施；工作台读取、层级树、**管控清单**、**管控清单 Excel 导出（表头 + 1 数据行）**、
风险公示 token、事件转换参考；最后 **删除楼层级联清掉分区/对象/单元/事件**（实测四张表残留 0）。
该族**未发现缺陷**（缺省口径"管控清单按企业默认楼层"属设计，探针显式传 floor_id 后一致）。

### 账号/角色权限 + 数据字典验收（`_admin_dict_probe.py` → 30/30 PASS）

| 覆盖点 | 结果 |
| --- | --- |
| 管理员用户：列表（分页结构）/详情/改名/删除后 404 | 全部通过 |
| **管理员重置密码闭环** | 重置后**新密码可登录（200）、旧密码失效（401）** |
| 越权收口 | 普通用户访问 `/admin/users` → **403**；`/roles/my-menus` → 200 |
| 角色：列表/权限清单/建（绑定权限子集）/详情/改/删 | 全部通过 |
| 数据字典（设置级 + 企业级）：建/查/改/删 | 全部通过；重复 `(dict_type, code)` → **409** |
| 字典缓存 | 写入与更新后**立即读到新值**（`invalidate_dict_cache` 生效，无 60s 陈旧窗口） |

该族**未发现缺陷**；探针两次自伤（角色 `code` 不允许数字、用户列表是分页对象不是数组）均为探针断言问题，已修正。

### 免登录公开面验收（`_public_surface_probe.py` → 19/19 PASS）

三条无鉴权链路（风险公示、隐患公示、风险点二维码扫码上报）是攻击面最外层，逐项验证：

| 检查 | 结果 |
| --- | --- |
| 令牌强度 | 三类 token 均为 `secrets.token_hex(32)` → **64 hex** |
| **重置即失效** | 轮换风险公示 token 后：旧 token → **404**、新 token → 200 |
| 伪造/枚举 token | 三处伪 token 全部 **404**（不泄露是否存在） |
| 数据脱敏 | 风险公示行不含责任/电话字段；隐患公示**企业名脱敏（`Q**`）**、公示行仅 6 个白名单字段（code/level/rectification/source_type/status/title） |
| 匿名扫码上报 | `POST /public/hazard/report/{风险点二维码 token}` → 落库 `source_type=report`、`created_by` 为空 |
| nonce 防重 | 同 nonce 重复提交 → **409 请勿重复提交**（跨 worker 共享状态） |

该族**未发现缺陷**。注：探针会轮换测试企业的公示 token（重置语义的验证需要），已记录在 TASKS。

---

## 一之补五：外部对接、版本回滚（各抓到一处真缺陷）

### N-25（P1 路由遮蔽，第三次）预案「版本对比」整体不可用

**取证**：`GET /plans/{id}/versions/compare?a=2&b=3` 返回 **422**（"请求参数无效"）。
原因是 `versions.py` 里字面量路由 `/versions/compare` 声明在参数路由 `/versions/{version_id}`
**之后**，`compare` 被当成版本 id 去查库 → Postgres UUID 转换失败 → 应用中间件把 DB 错误统一转成 422。
也就是说，**版本对比功能在页面上点一次就会报错**（探针实测：修复前 compare 恒 422）。

**修复**：①把 `/versions/compare` 路由前移到参数路由之前；②给详情路由加 `{version_id:uuid}` 约束
（同一个坑已是第三次出现——每一次都是"字面量路径被无约束参数路径吞掉"，因此这次连防御性约束一起加），
③守护测试断言"compare 在参数路由之前"且"参数路由必须 `:uuid`"。

**复验**：`_plan_versions_probe.py` → **11/11 PASS**：存 V1/V2 快照 → 列表倒序 `[3,2]` →
**对比识别出 `sec_1: modified`（V1→V2 内容）** → 回滚到 V1 后**章节内容真的变回 V1**、
`current_version=2`、快照历史未被删改；探针预案删除后版本残留 0。

### N-26（P2 外部对接幂等）外部下单接口重复调用会重复开单 + 重复烧 AI

**取证**：`POST /api/external/plans` 每次调用都新建 `PlanProject` 并 `spawn` 一次生成任务；
而 HMAC 中间件只校验签名与 **±5 分钟时间窗**、不做重放防护（`_external_hmac_probe.py` 实测：
同一签名重发仍被接受）。因此外部商城**超时重试**（或签名被抓包重放）会导致
同一订单开出两张预案、跑两次 AI 生成。

**修复**：按外部必填字段 `external_order_id` 做幂等——先用 `try_acquire_lease` 抢占订单租约：
抢到 → 正常创建并把 `plan_id` 写入 `external_order:{id}`（TTL 24h）；没抢到且已有 `plan_id` →
**原样返回同一 task_id**；没抢到且首单仍在创建中 → **409 稍后查询**。
创建过程异常时 `release_lease` 释放租约，避免订单号被"毒化"24 小时。

**证据**：`backend/tests/test_external_idempotency.py` 3 例（重复订单不再进创建分支且不 spawn、
首单进行中 409、失败释放租约）；`_external_hmac_probe.py` **8/8 PASS**（未配置→**503**、
缺头/错签名/过期/非数字时间戳→**401**、有效签名放行、同签名重发仍被接受＝幂等由业务兜底）。
> 未在开发环境真跑 `POST /external/plans`：那会触发真实 AI 生成（费用），
> 幂等分支已由单测覆盖，中间件边界由探针覆盖。

### 复核（v5 结论未回退）

| 复核项 | 结果 |
| --- | --- |
| 作业票并发发号（v5 N-1） | 8 并发 → 8/8 成功、编号不重复 |
| 新功能跨租户越权 | 16/16 PASS（对照 200 / 跨租户读 404 / 跨租户写 403 / 管理员模块 403 / 匿名 401） |
| 后端 112 个 GET 端点 | 0 个 5xx |
| 前端产物 | 新构建 `main-C0EJWNk4.js` 已部署 8082，产物内含本轮新增 UI 文案 |

---

## 二、设计取舍（为什么这样修）

1. **成员只放宽"读 + 签字"**：开票、提交、气体检测仍要求企业主。会签要的是"审批人进来签字"，
   不是把写权限摊开；越权面保持最小。
2. **失败仍返回 404 而不是 403**：沿用项目既有约定（不用状态码探测资源是否存在）。
3. **AI 清单补全保持"人工勾选"两段式**：不在任务生成期自动调 LLM——每次生成都付一次调用成本与延迟，
   且 AI 静默改写清单会让"谁加的这条"说不清。落库端点负责去重与状态回退，前端只做勾选。
4. **批次风格覆盖缺省回落**：`style_override or p.style_preference`，既有 Web 调用方无需改动。

---

## 三、门禁（本轮实测）

| 项目 | 结果 |
| --- | --- |
| 后端单测 | **1972 passed / 0 failed**（v5 的 1908 + 本轮累计新增 64 条） |
| 后端静态检查 | `ruff check app tests scripts` **All checks passed** |
| 前端类型/规范 | `tsc -b` 0 error；`eslint .` **0 error / 0 warning**；棘轮 0-0「未新增债务」 |
| 前端单测 | `vitest run` **278 passed**（44 文件，含新增 `stableRowKey` 2 例） |
| 前端构建 | `npm run build` OK（PWA 142 条预缓存），产物 `main-Dnx-dI7G.js` 已部署 8082 |
| 浏览器广域冒烟 | **37 页：页面异常 0 / 5xx 0 / 带 console error 的页 5**（4 页既有 404 + 1 页 List 弃用） |
| 端点烟测 | 112 个 GET 端点：65 个 2xx/3xx（+2：资源/风险源模板路由修复后不再 422）、47 个预期 4xx、**0 个 5xx** |
| 端到端探针 | 成员审批+生命周期 **29/29**、生命周期 UI **4/4**、AI 清单补全 **8/8**、AI 生成计划 **9/9**、桌面+移动 UI **11/11**、跨租户越权 **16/16**（均在 8082 产物上复跑） |
| 完整预案导出 | **10/10**（25 章填满 + 1 张流程图：标题/正文/图片/占位符/体量全通过，见"一之补"） |
| 导出族（报告/票面） | 风险评估报告 **4/4**、资源调查报告 **4/4**、作业票票面 **8/8**、真实内容预案 **4/4** |
| 批量导入族 | 组织成员导入 **8/8**、应急资源导入 **10/10** |
| 上传端点族 | 风险源导入 **10/10**、楼层平面图上传/替换/删除 **9/9**（磁盘残留 0） |
| 法规库族 | 只读 **11/11**（字面量路由不被吞）、读写 **16/16**（入库→查重→废止→删除零残留 + 图谱计数回基线） |
| 四色图工作台 | **17/17**（层级 CRUD、LS 重算 R=12、工作台/清单/Excel 导出、公示 token、**删除楼层级联**） |
| 聊天助手工具层 | 只读 **18/18**（修复前 8/18）、写入闭环（资源/预案/企业 建→查→改→删）全通过（`backend/scripts/check_chat_tools.py`，零模型额度） |
| 账号/角色/字典 | **30/30**（改密后新密码生效+旧密码失效、普通用户 403、角色 CRUD、字典 CRUD + **缓存失效即时可见**） |
| 免登录公开面 | **19/19**（token 64hex、重置即失效、伪 token 404、脱敏、匿名上报落库、nonce 409） |
| 外部对接 HMAC | **8/8**（未配置 503、缺头/错签名/过期/非法时间戳 401、有效签名放行；创建侧按订单号幂等） |
| 预案版本闭环 | **11/11**（存快照→倒序列表→**对比识别变更**→回滚后内容回到目标版本且历史不删改） |
| 接口契约一致性 | 前端 83 个 API 调用点 ↔ 后端 OpenAPI 279 条路径：**83/83 命中，0 漂移** |
| 运行时日志 | 近 90 分钟 6 条 500 全部来自并发开票的**修复前**探针；修复后新增 500 = **0**；登录不再产生 bcrypt 回溯 |

复现命令：

```powershell
python output\playwright\e2e-20260918\scripts\_work_ticket_member_todo_probe.py
python output\playwright\e2e-20260918\scripts\_hazard_ai_checklist_probe.py
python output\playwright\e2e-20260918\scripts\_hazard_plan_builder_probe.py
$env:E2E_BASE="http://localhost:8082"; python output\playwright\e2e-20260918\scripts\_v6_ui_probe.py
python output\playwright\e2e-20260918\scripts\_new_features_authz_probe.py
python output\playwright\e2e-20260918\scripts\_api_import_smoke.py
cd backend; $env:SECRET_KEY="<48+ 字符>"; python -m pytest tests -q; python -m ruff check app tests scripts
```

---

## 四、剩余待办（与 v5 相同，全部为外部/运维动作）

| # | 事项 | 操作 |
| --- | --- | --- |
| 1 | 公司库 `ENCRYPTION_KEY` 仍是公开默认值 + 1 条历史坏密文 | `check_encryption_health.py` → `rotate_encryption_key.py --dry-run/--apply` → AI 配置页重存 `ai_configs 511639fd…` |
| 2 | 公司网关 nginx 缺 SSE / 真实 IP / 上传体积 | `scripts/check-gateway-config.sh` 全 PASS 后按 `deploy/gateway-nginx.conf.example` 落地 |
| 3 | 0.3.x 升级包未实机演练 | `scripts/rehearsal.sh` 15/15 后执行升级 |
| 4 | 其他服务器 `.env` 的 QCC 两行必须独立 | 后端已对"黏行"值打 warning 并按未配置处理 |
| 5 | `aiSetupWizard()`（智能引导：组织树 + 计划 + 检查表三合一建议）后端与 service 就绪、**界面未建** | 属"未开工的功能"而非缺陷：需要产品先确认是否要建这个多步向导，再单独排期（不阻塞当前使用） |
| 6 | AntD `List` 组件弃用（**7 个文件 12 处**：`DashboardPage`×2 / `RiskNoticeCardPreviewPage`×3 / `RiskMigrationWizard`×2 / `FourColorImportModal`×2 / `RiskEventForm` / `EvidencePanel` / `FloorManagementDrawer`） | 需替换为自定义列表并做**视觉回归**，建议单独一轮；v6 内仍可正常工作（附：更正 §一 N-9 里"5 文件 8 处"的旧计数） |
| 7 | 4 个无引用 UI 文件（旧风险源表单/移动端风险源页/移动端预案卡片/移动端流式 hook） | 见 §一 N-12：删除或重新挂载需你拍板 |
| 8 | 3 个依赖 AI/文档解析的上传端点未做零费用验证（`/extraction/parse-file`、`/onboarding/import(/batch)`、`/floors/{id}/four-color/analyze`） | 需要一次真实 AI 配置下的联调（会消耗供应商额度），是否安排由你决定 |

> v1–v5 的全部问题状态总表见 `docs/系统诊断报告-v5-2026-09-18.md` 第四节；本报告只覆盖 v5 之后新增的处置。
