# 数字化应急预案自动生成系统 · 修复验收报告 v3

**报告时间**：2026-09-18
**上游报告**：`docs/系统诊断报告-2026-09-17.md`（v1）、`docs/系统诊断报告-v2-2026-09-18.md`（v2）
**本轮范围**：按 v2 修复路线图执行 W0–W4（安全止血 → 新模块功能 → 架构级 → 工程底座 → 验收）
**代码基线**：本报告写作时 HEAD 含 24 个修复 commit（W0–W3）；`frontend/dist` 已重建为当日新构建（入口 `main-7tCBqKCV.js`）

---

## 一、总体结论

| 维度 | v2 诊断 | 当前 | 证据 |
|---|---|---|---|
| 鉴权/越权 | 34 个新端点无鉴权 + v1 四个 P0 开放 | **全部收口** | 匿名 6 类端点 401；跨企业 404；越权浏览器复测通过 |
| 单元测试 | 9 failed + 2 collection errors | **全绿** | `1850 passed / 1 skipped / 0 failed / 0 errors` |
| 静态检查 | 无 ruff / 无 CI | **ruff 全绿 + CI（ruff+pytest+tsc+vitest+eslint 棘轮）** | `ruff check app tests` → All checks passed |
| 前端类型 | 26 个 `@ts-nocheck`、245 eslint error | **棘轮封顶（只减不增）** | `eslint-ratchet` 基线 245 errors / 20 warnings；nocheck ≤26 |
| 多 worker 一致性 | 进程内状态 + 4 worker 冲突 | **全部迁移共享存储** | 生成/报告租约/登出/限流/nonce/外部任务全走 `app_runtime_state`，实测通过 |
| 数据加密 | 公开默认密钥 + AES-ECB | **AES-GCM + 不停机轮换能力** | 本机库 9 条密文迁移 GCM，迁移后真实 AI 调用 200 |
| 部署运维 | 备份脚本实际不可用 | **备份/回滚全链路可用** | backup 产出 7.1MB dump + 12.7MB 归档；rollback 实测恢复成功 |

**一句话**：v2 列出的代码侧问题已全部修完并验证；剩余的是**前端 245 条历史 eslint 债（已封顶）**与**必须由你/公司服务器执行的四件事**。

---

## 二、验收证据（本机 Docker 栈，全部实测）

### 2.1 接口层

| 检查 | 结果 |
|---|---|
| 匿名访问 `platform/capabilities`、`ingest/sources`、`work-ticket/templates`、`major-hazard/units`、`export/download/_regen_real.pdf`、`plans/{id}/generate/stop` | 全部 **401** |
| 8082 路径穿越 `curl --path-as-is /../../etc/passwd` | **404**（正常页 200） |
| 普通用户访问管理员端点（platform/ingest/extraction/法规写操作） | **403** |
| 普通用户访问他企业作业票/重大危险源 | **404**（此前可读 28 张票） |

### 2.2 浏览器验收（目标：`http://shuzihuayuan:8080` 当日新构建）

| 范围 | 结果 |
|---|---|
| 桌面 1440：43 项检查（29 路由 + 403 断言 + 公开页 + 移动） | **42 通过**；1 项脚本内选择器告警（chat 探针；chat 已由 SSE 探针与浏览器回复验证） |
| 移动 390：17 条 `/m/*` 路由 | 全部加载；仅 2 个预期的"报告不存在"404 空态 |
| 新模块 12 页（平台总览/AI 能力/DataHub/重大危险源/作业票） | 普通用户对管理员页 **403 拦截**；自有企业页面正常；**他企业页面 404** |
| 移动端设置页账户信息 | 显示真实账号 `qa_e2e_test@test.com`（假邮箱已消除） |
| 隐患驾驶舱 | 无 `registered` 技术词；单位重复消除（`0条`） |
| XSS 注入复验 | 章节注入 `<img onerror>` → `window.__xss` 未执行、正文正常显示 |
| 聊天（真实 AI） | 回复命中校验串；SSE 直连分块计时正常 |

### 2.3 质量门禁

```bash
cd backend && ruff check app tests          # All checks passed
cd backend && pytest tests -q               # 1850 passed / 1 skipped
docker exec -w /app emergency-plan-frontend npx tsc -b          # 0 error
docker exec -w /app emergency-plan-frontend node scripts/eslint-ratchet.mjs  # 未新增债务
```

---

## 三、v1/v2 问题清单逐项状态

### P0

| 编号 | 问题 | 状态 | 验证 |
|---|---|---|---|
| P0-1 | 8082 路径穿越 | **已修复** | curl 404 |
| P0-2 | 导出下载无鉴权 | **已修复**（鉴权+归属；预案导出改内存返回） | 匿名 401 / 他人 404 / 本人 200 |
| P0-3 | 停止生成无鉴权 | **已修复**（+归属校验） | 匿名 401 |
| P0-4 | 法规写操作无管理员校验 | **已修复**（6 端点 require_admin） | 普通用户 403 |
| P0-5 | 进程内状态 vs 4 worker | **已修复**（共享状态表 + 租约 + 调度器选主） | KV/租约/计数/nonce 实测；stop 状态落库可跨进程读 |
| P0-6 | 公开默认加密密钥 + AES-ECB | **代码侧已修复**（GCM + 轮换工具）；**公司服务器轮换待执行** | 本机库迁移 9 条 + 调用成功 |

### P1/P2 与补测项

| 项目 | 状态 |
|---|---|
| 网关 nginx SSE/真实 IP | **模板与手册已给出**；公司服务器配置待执行 |
| 连接池 4×(10+20) | **已修复**：4×(5+10)=60，压测 60/60 成功 |
| 存储型 XSS | **已修复**：9 处渲染全过 DOMPurify + Mermaid strict + 守护测试 |
| 权限种子/菜单与后端一致 | **已修复**：补 `menu:regulations`、user 补 `menu:enterprises`、移除 user 的 `menu:ai_config` |
| 忘记密码半成品 | **已修复**：管理员重置口径 + 令牌 SHA-256 哈希 + 历史令牌作废 |
| uploads/exports 暴露面 | exports **已修复**；uploads 保留 UUID 能力 URL 模型（不可枚举），已在报告注明为可接受风险 |
| 内部错误外泄 | **已修复**：26 处 500 改通用文案 + 细节只进日志 |
| 卡死 generating（4 条） | **已修复**：启动自愈复位 4 条 + 阈值 2h 自愈机制 |
| 测试 9 失败 + 2 收集错误 | **全绿** |
| `.env` QCC key 拼接 / 测试污染 | **已修复**（测试侧清 settings 脏值）；本机 `.env` 仍建议修正为两行 |
| AI 能力开关无效 | **已修复**：6 个能力码接线 + 停用/恢复实测 |
| LLM 调用无留痕 | **已修复**：对话后 `llm_call_logs` 4→5（module/token/耗时/截断/失败） |
| 超时错误码 | **已修复**：502 → **504** |
| 移动端 M1/M3/M4/M6 | M1 **已修**（真实账号）；M2/M4 复验为**截图伪影非问题**；M3（大标题+Tab 文案）为设计样式 |
| A19/A9 文案 | **已修复** |
| 前端 245 eslint / 26 @ts-nocheck | **棘轮封顶**，逐文件补类型列为后续专项（26 文件实测 106 个类型错误） |

---

## 四、剩余事项（需要你或公司服务器）

1. **公司库加密密钥轮换**（脚本与手册已就绪）：`scripts/rotate_encryption_key.py --dry-run/--apply`，轮换窗口内 `ENCRYPTION_KEY_LEGACY` 保留旧值。
2. **公司 nginx 补 SSE/真实 IP 配置**（`deploy/gateway-nginx.conf.example` + 部署手册第 6/11 节）。
3. **0.3.x 升级包实机演练**（含 `scripts/backup.sh`、`scripts/rollback.sh`）。
4. **1 条历史坏密文**：`ai_configs` id `511639fd-…` 所有已知密钥都解不开，请在「设置 → AI 配置」重新保存该条。
5. **前端 eslint 债收敛**：当前 245 errors（no-explicit-any 56 / no-unused-vars 48 / react-hooks-refs 41 / only-export-components 28 / ban-ts-comment 26 / set-state-in-effect 24）；棘轮保证不再增加，后续按文件推进。

---

## 五、本轮修复 commit 一览（代码侧）

| 阶段 | commit | 内容 |
|---|---|---|
| W0 | `528275b` `9583cbb` | 新老端点鉴权/越权收口、作业票审批资格、抽取限流 |
| W1 | `69d8371` | AI 能力接线、调用留痕、超时 504 |
| W2 | `fba9e78` `1d261ed` `af7c683` `c864a6d` `60e8613` `6bcfcd5` `2da430a` | 连接池/自愈/A19、调度器选主、权限种子、XSS、共享状态、加密轮换、密码找回、移动端 |
| W3 | `c742091` `8a98bc1` `1fc50cf` `6335641` `b060d8d` `7354bbf` | 测试全绿、ruff+CI、依赖锁/PWA、备份回滚、eslint 棘轮、去内泄 |

---

*本报告所有结论均来自本机 Docker 栈实测（命令与输出见上）；涉及公司服务器的四项已明确标注为待执行。*
