# 数字化应急预案自动生成系统 — 技术方案

> **版本**：1.0 | **日期**：2026-06-05 | **依据标准**：GB/T 29639-2020

---

## 一、系统概述

面向企业安全管理人员的 Web 应用，以 **GB/T 29639-2020** 为合规基础，通过"结构化表单采集企业数据 + 模板框架约束章节结构 + AI 大模型生成/润色内容"，自动生成三种预案（综合应急预案、专项应急预案、现场处置方案），最终导出为标准格式的 Word 文档。

**核心技术选型：**

- 后端：Python 3.11+ / FastAPI / SQLAlchemy / Alembic / Celery
- 前端：React 18 / TypeScript / Ant Design / TipTap 富文本编辑器
- 数据库：PostgreSQL 15+
- 文档生成：python-docx + Jinja2
- AI 层：多模型适配器（OpenAI / 通义千问 / 文心一言 / DeepSeek），用户自行配置 API Key

---

## 二、系统架构

```
┌─────────────────────────────────────────────────┐
│                    Nginx (反向代理)                │
├──────────────────────┬──────────────────────────┤
│   React SPA (静态)    │   FastAPI (API 服务)       │
│   Ant Design UI       │   ├─ auth_router          │
│   TipTap 编辑器        │   ├─ enterprise_router    │
│                       │   ├─ plan_router          │
│                       │   ├─ generation_router    │
│                       │   ├─ template_router      │
│                       │   └─ export_router        │
│                       │                           │
│                       │   Services:               │
│                       │   ├─ PlanService          │
│                       │   ├─ GenerationService    │
│                       │   ├─ TemplateService      │
│                       │   └─ ExportService        │
│                       │                           │
│                       │   AI Layer:               │
│                       │   ├─ LLMAdapter (abstract)│
│                       │   ├─ OpenAIProvider       │
│                       │   ├─ QwenProvider         │
│                       │   └─ WenxinProvider       │
│                       │                           │
├──────────────────────┴──────────────────────────┤
│              PostgreSQL 15                        │
│   users │ enterprises │ risk_sources │ plans ...  │
└─────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 用户与权限模块

- JWT Token 认证（access + refresh token），bcrypt 密码哈希
- 注册、登录、个人信息管理
- 每个用户只能访问自己的企业和预案数据（行级隔离：所有查询带 `user_id` 过滤）

### 3.2 企业管理模块

用户录入结构化企业信息，作为 AI 生成预案的核心数据源：

- **企业基本信息**：名称、地址、行业分类、经营范围、员工人数、建筑/厂区概况
- **组织架构与职责**：应急指挥部结构、各小组（抢险、疏散、医疗、通讯、后勤）负责人及成员
- **风险源清单**：风险类型（火灾、爆炸、触电、中毒、机械伤害、高处坠落等）、具体位置、可能后果、风险等级
- **应急资源清单**：消防设施、急救物资、防护装备、通讯设备、避难场所、外部救援联系方式
- **周边环境信息**：邻近单位、交通状况、敏感目标（学校、医院等）

### 3.3 预案模板管理模块

**模板存储形式**：JSON 结构文件 + 数据库记录，定义三类预案的章节结构。

每类预案模板包含：
- 元数据：预案类型、名称、依据标准
- 章节树：每个章节的 key、标题、层级、序号
- GB/T 要求说明：该章节应包含的内容要点
- AI 提示词模板：Jinja2 模板，注入企业数据后发送给 LLM
- 数据依赖声明：生成该章节需要哪些企业字段

**章节可操作性属性（每节可独立配置）：**
- `ai_generatable`：是否允许 AI 生成
- `user_editable`：是否允许用户手动编辑
- `required`：是否强制必填
- `auto_fill`：是否从企业数据自动填充（如组织架构表）

### 3.4 AI 生成引擎

**适配器模式：**

```python
class BaseLLMProvider(ABC):
    async def generate(self, messages: list[Message], **kwargs) -> str: ...
    async def generate_stream(self, messages: list[Message]) -> AsyncIterator[str]: ...

class OpenAIProvider(BaseLLMProvider): ...
class QwenProvider(BaseLLMProvider): ...
class WenxinProvider(BaseLLMProvider): ...
```

- 用户在前端配置模型类型、API Key、模型名称
- API Key 使用 AES-256 加密存储
- 支持流式输出（SSE），前端实时显示生成内容

**提示词管理：**

- 系统级提示词模板：预置在 `backend/templates/prompts/` 目录下，按预案类型和章节组织
- 系统提示词设定 AI 角色："你是一位持有国家注册安全工程师资格的专业应急预案编制专家，熟悉 GB/T 29639-2020 标准"
- 章节提示词动态注入企业数据后发送给 LLM

**章节级生成策略（混合模式）：**

- **强约束章节**（如批准页、组织架构表）：直接从企业数据填充，不调 AI
- **半约束章节**（如编制依据、适用范围）：模板生成骨架，AI 润色完善
- **自由撰写章节**（如风险评估描述、应急处置措施）：AI 根据企业数据 + 标准要求完整撰写

### 3.5 预案编辑模块

- 预案项目列表页：按企业分组，显示进度状态（草稿/生成中/待审核/已完成）
- 章节编辑器：左侧章节树导航 + 右侧 TipTap 富文本编辑器
- AI 生成面板：可选中单个章节点"AI 生成"，支持重新生成
- 支持一键批量生成所有可生成章节
- 实时字数统计、GB/T 合规检查（必填章节是否为空）

### 3.6 文档导出模块

- 前端预览：将章节内容渲染为 HTML 预览，模拟 Word 排版效果
- Word 导出：python-docx 按 GB/T 29639-2020 格式生成 .docx
  - 封面页（预案名称、企业名称、版本号、发布日期）
  - 批准页（批准人签字位）
  - 自动目录
  - 正文（多级标题编号、正文样式、表格、页眉页脚）
  - 附件页（应急资源清单表、组织架构图、疏散路线图占位）
- 样式严格遵循公文格式：标题黑体、正文仿宋、固定行距

### 3.7 版本管理模块

- 每次手动保存或 AI 生成后自动创建快照
- 版本列表可查看、对比（diff 高亮）、回滚
- 导出时可选择当前版本或历史版本

---

## 四、数据库核心表设计

```sql
-- 用户
users (id, email, password_hash, name, role, created_at, updated_at)

-- 企业
enterprises (id, user_id FK, name, address, industry, scale, employee_count,
              org_structure JSONB, surrounding_info JSONB, ...)

-- 风险源
risk_sources (id, enterprise_id FK, category, name, location, description,
              likelihood, severity, risk_level, control_measures)

-- 应急资源
emergency_resources (id, enterprise_id FK, category, name, spec, quantity,
                     location, responsible_person, contact)

-- 预案项目
plan_projects (id, user_id FK, enterprise_id FK, plan_type ENUM,
               title, status, current_version, created_at, updated_at)

-- 预案章节
plan_sections (id, plan_project_id FK, section_key, title, content TEXT,
               ai_generated BOOLEAN, sort_order, updated_at)

-- 预案版本快照
plan_versions (id, plan_project_id FK, version_number, snapshot JSONB,
               created_at, created_by)

-- 预案模板
plan_templates (id, plan_type, name, version, structure JSONB, is_active)

-- AI 模型配置
ai_configs (id, user_id FK, provider, api_key_encrypted BYTEA,
            model_name, base_url, is_active, created_at)
```

关键设计决策：
- `org_structure` 用 JSONB 存储灵活的组织架构层级
- `plan_sections.content` 存 Markdown，前端 TipTap 编辑器渲染，导出时转为 docx 格式
- `plan_versions.snapshot` 存完整 JSON 快照（所有章节内容 + 元数据），保证可回滚

---

## 五、前端页面设计

| 页面 | 功能 |
|------|------|
| 登录/注册 | 邮箱注册登录，密码找回 |
| 工作台 | 概览面板：预案数量、企业数量、最近编辑、快捷入口 |
| 企业列表 | 管理多个企业档案，支持搜索、筛选 |
| 企业详情 | 分标签页展示：基本信息、风险源、应急资源、组织架构 |
| 新建预案向导 | 选择企业 → 选择预案类型 → 确认模板 → 进入编辑 |
| 预案编辑器 | 左侧章节树 + 右侧富文本编辑区 + AI 生成按钮 |
| AI 配置 | 选择模型提供商、填写 API Key、测试连接 |
| 导出预览 | 全文档 HTML 预览，确认后下载 .docx |

---

## 六、开发阶段规划

### 第一阶段：核心闭环（预计 4-6 周）

1. 项目脚手架搭建：FastAPI 工程结构、React 工程结构、Docker 开发环境
2. 用户系统：注册、登录、JWT 认证
3. 企业管理 CRUD：基本信息 + 风险源 + 应急资源
4. 模板系统：预置 GB/T 29639-2020 三类预案章节结构 JSON
5. AI 适配层：OpenAI 单模型对接、提示词管理、SSE 流式输出
6. 预案编辑器：章节树、富文本编辑、AI 章节生成、保存
7. Word 导出：python-docx 生成符合国标格式的 .docx

### 第二阶段：增强完善（预计 2-3 周）

8. 多模型支持：通义千问、文心一言、DeepSeek 适配器
9. AI 模型配置 UI
10. 版本管理：快照、对比、回滚
11. 一键批量生成
12. 导出预览功能
13. 提示词模板优化与调优

### 第三阶段：进阶功能（按需）

14. 预案审核工作流
15. PDF 导出
16. 风险矩阵可视化
17. 应急资源分布图
18. 多企业批量管理
19. 法规库智能引用

---

## 七、测试策略

- **单元测试**：pytest 覆盖 Service 层、AI 适配层、文档导出逻辑
- **API 测试**：httpx + pytest 覆盖所有 API 端点，含认证和权限校验
- **前端组件测试**：Vitest + React Testing Library
- **集成测试**：完整流程测试（注册 → 创建企业 → 录入数据 → AI 生成 → 编辑 → 导出）
- **GB/T 合规测试**：自动化检查导出 docx 的章节结构是否与国标一致

---

## 八、假设与约束

1. 用户具备自行申请和管理 AI API Key 的能力，系统不代持 Key
2. GB/T 29639-2020 模板结构由开发阶段手动梳理录入 JSON，不做自动解析
3. 单用户并发请求量有限（企业自用场景），不做复杂的请求队列和限流
4. Word 导出样式以"基本合规可用"为目标，不做像素级公文排版
5. 初期不涉及移动端适配
6. 企业组织架构图、疏散路线图等图形类内容暂时以文字描述占位，后续版本支持
