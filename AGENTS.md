# 项目核心规则

**首要指令：每次会话开始时，必须先读取 TASKS.md 了解当前任务进度，再继续工作。**

## 自动记忆积累
每次会话中用 `write_memory` 工具主动写入记忆（详见全局规则）：
- 修复 bug → `global/common-bugs` 或 `project-bugs`
- 架构决策 → `project-decisions`
- 会话结束前 → 汇总到 `global/patterns`、`global/commands`
