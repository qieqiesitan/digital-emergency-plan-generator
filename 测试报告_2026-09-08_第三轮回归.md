# 数字化应急预案自动生成系统 — 修复回归报告（第三轮）

- 回归日期：2026-09-08（晚，21:30–22:20）
- 修复来源：commits `f56b1a4`（P0-P1 安全）、`d950064`（限流/密码复杂度）、`4c2e979`（P2/P3 收尾）、`e049ef2`（前端断网降级/P2/P3）
- 回归方式：独立黑盒 HTTP 复测 + Playwright 真实浏览器 UI 复测 + 测试套件独立重跑
- 代码改动：**未修改任何代码**

---

## 一、总体结论

**代码修复全面且绝大多数真实生效**（黑盒复测 25 项中 23 项通过）。但有 **1 个关键问题：P0-1 移动端登录/注册修复未构建到生产包**——源码已改对，`frontend/dist` 仍是修复提交前的旧构建，8082 线上实际仍在跑坏代码，移动端新用户依旧 100% 无法登录/注册。**需重新 `npm run build` 并更新 dist 后再次回归**。

---

## 二、逐项回归结果

### P0 级
| 缺陷 | 复测方法 | 结果 |
|---|---|---|
| P0-3 JWT 默认密钥可伪造 | 用旧默认密钥自签管理员 token 调 /users/me | ✅ **已修复**：401（config.py 黑名单 + <32 字节拒绝 + 未显式注入自动生成强随机密钥） |
| P0-2 上传无白名单 | 上传 .html/.exe / 50MB / 合法 png | ✅ **已修复**：html/exe → 400「不支持的文件类型（仅图片及 pdf/doc/docx/xls/xlsx）」；50MB → 413「文件超过 20MB 上限」；png 正常放行；uploads 危险类型静态访问守卫（attachment/nosniff）；历史恶意文件已清理 |
| P0-1 移动端登录/注册无法输入 | Playwright 375×812 真实输入 + 逐键 | ⚠️ **源码已修、生产包未生效**：`LoginScreen.tsx` 已改为 `onChange={(v) => setEmail(v)}`，但 8082 加载的仍是旧构建 `LoginScreen-CjBzhVjl.js`（构建于 17:21，修复 commit 21:26）→ 输入框依旧被清空、按钮恒 disabled、TypeError 同栈复现（截图 3 张）。**需重新构建 dist** |

### P1 级
| 缺陷 | 复测方法 | 结果 |
|---|---|---|
| /configs 普通用户可读可写可删 | 用户 GET/PUT /configs | ✅ **已修复**：GET 403、PUT 403「需要管理员权限」；管理员可读 200 |
| 非法 UUID → 500 | /enterprises/not-a-uuid、/plans/123 | ✅ **已修复**：422（中间件识别 asyncpg DataError） |
| 超大字段 → 500 | 300 字符企业名 | ✅ **已修复**：422（schema max_length 与 DB 列宽一致） |
| 登录/忘记密码无限流 | 26 次连续错误登录 | ✅ **已修复**：第 27 次起 429「请求过于频繁」+ `Retry-After: 900`；认证成功请求正常（回归脚本自身在探测后触发 429，真实佐证限流生效） |
| 密码复杂度缺失 | 弱密码 `123456` 注册 | ✅ **已修复**：422（≥8 位 + 字母数字 + 弱密码黑名单） |
| 断网静默降级 | 前端代码层检查 | ✅ **已修复**：PlanListPage/DashboardPage 请求失败渲染 Alert+重试，不再误导性显示空态/全 0 |

### P2/P3 级
| 项 | 结果 |
|---|---|
| 登出撤销 | ✅ 已修复：登出后旧 access / 旧 refresh 均 401（token 加 jti + 进程内黑名单） |
| /prompts 普通用户可读 | ✅ 已修复：普通用户 403、管理员 200 |
| 注册邮箱格式校验 | ✅ 已修复：`not-an-email` → 422 |
| bcrypt 72 字节截断 | ✅ 已修复：80 字符密码注册 → 422 |
| 负员工数 | ✅ 已修复：422 |
| 未知 /api/* 路径返回 SPA HTML | ✅ 已修复：404 JSON |
| API 安全响应头 | ⚠️ 基本修复：4/5 已补（nosniff/X-Frame-Options/Referrer/Permissions-Policy），**content-security-policy 仍未添加** |
| 8082 安全头 + Server 头 | ⚠️ 基本修复：4/5 已补（同缺 CSP）；`Server` 头已隐藏（显示为 `EmergencyPlan`，不再泄露 Python/SimpleHTTP） |
| 空预案导出拦截 | ✅ 已修复（test_export_empty_guard 通过） |
| 未知 file_key | ✅ 404 JSON「文件不存在」 |
| ?token= 查询串鉴权通道 | ⚠️ 未修复（此轮 commit 未涉及；上轮已确认 token 为 64 位 hex 不可枚举，风险受控） |
| /docs、/redoc、/openapi.json 无认证暴露 | ⚠️ 未修复（P3，信息收集面，openapi 402KB） |
| 单字符企业名（min_length） | ⚠️ 未验证（受限流窗口影响未能重测；commit 只补了 max_length） |
| idle in transaction 挂起 81 分钟 | ✅ 修复：DB `idle_in_transaction_session_timeout=30min` 生效，当前 DB 无挂起事务；后端连接池补 `pool_recycle=1800 + pool_pre_ping` |

---

## 三、自动化套件独立复跑

| 套件 | 结果 |
|---|---|
| 前端 vitest | ✅ **37 文件 / 227 用例全绿**（独立重跑确认） |
| 新增安全测试（7 文件） | ✅ 51 passed / 1 批量失败（`test_logout_revokes_access_and_refresh_api`，**单个重跑 passed** — 测试间状态污染，属测试隔离性基建问题，非产品缺陷；黑盒实测登出撤销正确） |
| 上轮遗留 pytest 失败 | ⚠️ 2 项仍失败（迁移基线 + diagram mock，均为既有环境/测试基建问题，非本次修复范围） |

---

## 四、结论与建议

1. **修复质量高**：P0-3、P0-2 及全部 P1（除移动端发布问题外）、多数 P2/P3 均已真实生效，安全面从「可被任意伪造管理员/上传执行 XSS」收敛到位。
2. **唯一阻塞项**：P0-1 移动端——源码已修，但 `frontend/dist` 未重建，8082 生产包仍是旧版。**执行 `npm run build` 重建前端并更新 dist（shuzihuayuan 容器挂载点）后，需重跑移动端 UI 回归确认输入框可输入、按钮解除 disabled、无控制台异常。**
3. **低危残留**（可择期处理）：CSP 安全头（前后端均缺）；/docs//openapi.json 文档面暴露；?token= 查询串通道（风险受控）。
4. **测试基建**（非产品）：pytest logout 用例批量跑状态污染；上轮 2 个既有失败未修；全量 pytest 97% 收尾挂起未验证是否随连接池修复改善。

---

*本报告为修复后的黑盒回归；测试过程中未修改任何代码。前两轮报告见 `测试报告_2026-09-08.md`、`测试报告_2026-09-08_第二轮.md`。*