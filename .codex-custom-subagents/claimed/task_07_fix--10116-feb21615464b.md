# Codex Custom Subagents task handoff v1

Task: task_07_fix

## 修复任务：任务 7 质量审查 2 项重要 + 3 项次要建议

### 背景

任务 7（AI 优化 + 快照端点）已通过规格审查，质量审查 ❌ 需修复（2 项重要 + 3 项次要）。实现提交 `0901c75`。

### 修复 1（重要·安全）：PUT snapshot 校验 object_id 归属

`backend/app/routers/risk_notice_card.py` 的 PUT snapshot 端点当前只做 `_get_ent`（企业归属），未校验 object_id 存在性与归属——任意/他人企业的 object_id 可创建或覆盖快照（越权写）。请与 ai-optimize 对齐：保存前按 `RiskObject.id == object_id AND RiskObject.enterprise_id == enterprise_id` 查询，不存在 → 404「风险点不存在」。

### 修复 2（重要）：ai-optimize 异常处理与日志

`ai_optimize` 端点当前裸 `except Exception` 把一切异常归 502 且无日志。请改为：

```python
from fastapi import HTTPException
import logging
logger = logging.getLogger(__name__)

try:
    optimized = await optimize_right_column(...)
except HTTPException:
    raise  # AI 未配置等业务错误保留原语义（如 400）
except Exception:
    logger.exception("风险告知卡 AI 优化失败: enterprise=%s object=%s", enterprise_id, object_id)
    raise HTTPException(502, "AI 优化失败，请稍后重试或保留原版")
```

### 修复 3（次要）：JSON 解析健壮性

`backend/app/services/risk_notice_card_ai.py` 的 `optimize_right_column`：
* 解析前剥离 ```json 代码块（参考 `risk_ai_service._parse_ai_json` 的处理方式，可直接 import 复用或实现等价轻量逻辑）
* 解析失败时 `logger.warning` 记录 raw[:200]
* 字段值类型归一：control_measures/emergency_measures 非 list 或 None 时回落原值；hazard_description 非 str 时回落原值
* prompt 补充 system message（「你是安全生产专家」人设，与项目其他 AI 服务一致）

### 修复 4（次要）：补测试

`backend/tests/test_risk_notice_card_api.py` 补充：
* 跨企业 object_id 保存快照 → 404（回归防线）
* AI 字段缺失回落原值
* 非法 JSON / ```json 包裹 → 502（或按实现回落）
* AI 未配置（_get_ai_config 抛 HTTPException 400）→ 400 而非 502
* ai-optimize 测试断言 prompt 含企业名/对象名/原版文本

### 修复 5（次要）：风格一致性

* `backend/app/routers/risk_notice_card.py` 模块 docstring 更新（AI 优化/快照已完成，仅剩导出/token 由任务 8-9 补充）
* 快照响应建议定义专用 schema（如 `SnapshotResponse{version, source}`）替代裸 dict（可选）

### 范围与限制

* 只改 `backend/app/routers/risk_notice_card.py`、`backend/app/services/risk_notice_card_ai.py`、`backend/app/schemas/risk_notice_card.py`（如需）、`backend/tests/test_risk_notice_card_api.py`。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_api.py tests/test_risk_notice_card_service.py -v` 全部 PASS。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/ -q` 无回归（全量 390+ passed）。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 0901c75），消息：`fix(risk-notice-card): guard snapshot ownership and harden ai errors`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
