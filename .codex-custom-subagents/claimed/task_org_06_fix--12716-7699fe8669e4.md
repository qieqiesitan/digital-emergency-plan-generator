# Codex Custom Subagents task handoff v1

Task: task_org_06_fix

## 目标

按组织任务 6 规格审查的 1 条必须修复 + 3 条建议修改修复，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD=`963dab2`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1（必须修复）：treeData 只映射根节点**（`frontend/src/pages/Enterprise/EnterpriseOrgPage.tsx`）

`treeData` 改为 `nodes.filter(n => !n.parent_id)` 的映射（与 AI 预览 `buildTreeData` 一致），`buildChildren` 按 parent_id 嵌套；孤儿节点（parent_id 指向缺失节点）处理明确：挂根并注释，或丢弃（选清晰方案并说明）。

**2（建议）：validateNodes 补成员名校验**（同文件）

`validateNodes` 增加对 `members` 的校验：成员为对象且 name 非空，否则返回可读错误。

**3（建议）：delete_member 响应对齐 ApiResponse 惯例**（`backend/app/routers/enterprise_org.py` + 测试）

后端 `delete_member` 返回改为 `ApiResponse(data=None, message="已删除")`（与 data_dicts.py 惯例一致）；前端 `deleteMember` 保持 `.then(r=>r.data.data)` 解包（得 null）；同步测试断言。

**4（建议）：buildChildren 环防护**（同文件）

`buildChildren` 加 `seen` 集合防环（环形数据不无限递归，未访问节点跳过并可由后端校验兜底）。

## 验证

- 后端：`python -m pytest tests/test_enterprise_org.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- 前端：`npx tsc -b`、eslint（改动文件）、`npx vitest run` 全部通过（可补 1 条 treeData 纯函数单测如已抽函数，否则说明）；
- `git diff --check` 干净。

## Commit

```bash
git add frontend/src/pages/Enterprise/EnterpriseOrgPage.tsx backend/app/routers/enterprise_org.py backend/tests/test_enterprise_org.py
git commit -m "fix(org): render root-only tree nodes and align delete response"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_org_06_fix --claim-id <claim_id> --exit-code 0 --summary "组织任务6树渲染修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的文件；阻塞时停下汇报。
