# Codex Custom Subagents task handoff v1

Task: task_b1_t4

## 任务：数据防幻觉护栏（缺失标注 + system prompt 护栏）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成 TDD 实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

这是 git 分支 `codex/plan-generation-enhancement` 的隔离 worktree。当前 HEAD 应为 `3cb49c8`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_generation_enterprise_data.py -v
```

### 背景

现有 `backend\app\routers\generation.py::_collect_enterprise_data` 把企业字段原样注入提示词，空值/None 也注入，AI 可能凭先验知识编造地址等（历史案例：地址「湖北大厦」被写成「湖北武汉」）。`risk_assessment_service.py` 已有「缺失标（待补充）」模式可复用。

### 步骤 1：追加失败的测试

在 `backend\tests\test_generation_enterprise_data.py` 末尾追加：

```python
def test_collect_enterprise_data_marks_missing_fields():
    ent = MagicMock()
    ent.name = "测试企业"
    ent.address = None
    ent.industry = ""
    ent.business_scope = "生产"
    ent.employee_count = 100
    ent.building_overview = ""
    ent.org_structure = []
    ent.surrounding_info = None
    ent.legal_representative = ""
    ent.credit_code = None
    ent.economic_type = ""
    ent.established_date = None
    ent.registered_capital = None
    ent.phone = ""
    ent.land_area = None
    ent.building_area = None
    ent.safety_officer = ""
    ent.safety_standardization = ""
    ent.fire_approval = ""
    ent.main_products = ""
    ent.hazardous_chemicals = ""
    ent.special_equipment = ""

    data = _collect_enterprise_data(ent, {"risk_sources": []}, [])
    assert data["address"] == "（待补充）"
    assert data["industry"] == "（待补充）"
    assert data["legal_representative"] == "（待补充）"
    assert data["business_scope"] == "生产"  # 非空值保持原样


def test_compliance_block_contains_truth_guard():
    from app.services.prompt_cache import COMPLIANCE_BLOCK
    assert "数据真实性护栏" in COMPLIANCE_BLOCK
    assert "禁止推断" in COMPLIANCE_BLOCK
```

### 步骤 2：运行测试验证失败

运行上面的 pytest 命令。
预期：新增测试 FAIL（`assert data["address"] == "（待补充）"` 处失败，当前为 None）；原有测试仍 PASS。

### 步骤 3：实现缺失标注

修改 `backend\app\routers\generation.py`：

1. 在 `_collect_enterprise_data` 前新增辅助函数：

```python
def _missing(v):
    """缺失字段统一标注，防止 LLM 编造。"""
    return v if v not in (None, "") else "（待补充）"
```

2. 重写 `_collect_enterprise_data`，字符串/文本字段用 `_missing` 包裹（name/address/industry/business_scope/building_overview/surrounding_info/legal_representative/credit_code/economic_type/phone/safety_officer/safety_standardization/fire_approval/main_products/hazardous_chemicals/special_equipment）；`employee_count/established_date/registered_capital/land_area/building_area` 数值/日期字段保持原样；`org_structure` 保持原样；`risk_sources` 与 `emergency_resources` 列表逻辑保持不变（可参考当前函数体的其余部分原样保留）。

### 步骤 4：追加 system prompt 护栏

修改 `backend\app\services\prompt_cache.py` 的 `COMPLIANCE_BLOCK`，在末尾追加：

```text
5. 【数据真实性护栏——必须严格遵守】
   5.1 企业档案中以"（待补充）"标注的信息一律视为缺失，禁止推断、禁止编造。
   5.2 严禁编造地址、法定代表人、联系电话、统一社会信用代码、注册资本等企业基本信息。
   5.3 正文涉及缺失信息时，直接书写"（待补充）"，不得用其他文字替代。
   5.4 全部正文内容必须以企业档案数据为唯一事实来源，不得引入档案之外的企业信息。
```

注意：`COMPLIANCE_BLOCK` 是 Python 字符串拼接，需保证语法正确（原字符串以 `"4. 请直接输出章节正文内容，不要重复章节标题作为正文第一行。"` 结尾，在其后加 `\n` 与第 5 条）。

### 步骤 5：运行测试验证通过

运行 pytest 命令。
预期：PASS。

### 步骤 6：全量回归

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\ -q --ignore=tests\test_autofill_research.py
```

预期：全部通过（注意 `test_autofill_research.py` 因环境缺 `scrapling` 需忽略，这是既有环境问题，不是本次引入）。

### 步骤 7：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/routers/generation.py backend/app/services/prompt_cache.py backend/tests/test_generation_enterprise_data.py
git commit -m "feat(plan): add data hallucination guard rails (batch1)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出（几 passed）与全量回归结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
