# Codex Custom Subagents task handoff v1

Task: task_org_03

## 目标

实现「企业组织与成员管理」计划任务 3：组织树读写 + 成员 CRUD 接口（schema/router/鉴权），按 TDD 完成并提交。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD=`11b6ae5`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 文件

- 创建：`backend/app/schemas/enterprise_org.py`
- 创建：`backend/app/routers/enterprise_org.py`
- 修改：`backend/app/main.py`（注册 router）
- 测试：`backend/tests/test_enterprise_org.py`（追加端点测试）

## 关键契约

**schema（`enterprise_org.py`）**：

```python
from typing import Literal
from pydantic import BaseModel, Field

class OrgMember(BaseModel):
    name: str
    user_id: str | None = None
    position: str | None = None

class OrgNode(BaseModel):
    id: str
    type: Literal["dept", "team", "position"]
    name: str
    parent_id: str | None = None
    members: list[OrgMember] = Field(default_factory=list)

class OrgTreeUpdate(BaseModel):
    nodes: list[OrgNode]

class MemberCreate(BaseModel):
    user_id: str
    org_node_id: str | None = None
    position: str | None = None
    role: Literal["enterprise_admin", "team_leader", "member"] = "member"

class MemberUpdate(BaseModel):
    org_node_id: str | None = None
    position: str | None = None
    role: Literal["enterprise_admin", "team_leader", "member"] | None = None
    enabled: bool | None = None

class MemberResponse(BaseModel):
    id: str
    enterprise_id: str
    user_id: str
    email: str | None = None
    name: str | None = None
    org_node_id: str | None = None
    position: str | None = None
    role: str
    enabled: bool
    model_config = {"from_attributes": True}
```

**端点（`routers/enterprise_org.py`，前缀 `/enterprises/{enterprise_id}/org`）**：

- `GET /nodes` → 返回 `enterprise.org_structure`（数组，原样）
- `PUT /nodes`（body `OrgTreeUpdate`）→ `validate_org_tree`（422 返回 `{"code":"ORG_TREE_INVALID","message":"；".join(errors)}`）→ `sync_org_structure` 写回 + commit
- `POST /members` → user_id 存在校验（404「用户不存在」）；(enterprise_id,user_id) 已存在 → 409「该用户已是企业成员」；创建 EnterpriseMember（role 默认 member）
- `PUT /members/{member_id}` → 不存在/非本企业 404；`exclude_unset` 更新
- `DELETE /members/{member_id}` → 硬删（404 语义同上）
- `GET /members` → 列表（join users 返回 email/name；未绑定到企业 user 的名称取 user.name）
- **写权限**：仅企业主（`enterprise.user_id == current_user.id`）可写（PUT nodes/POST/PUT/DELETE members）；读（GET）放开给登录用户（企业归属校验仍执行 `_get_ent` 同款：企业存在且属于当前用户或为成员——本项目企业均为 user 所有，用 `_get_ent(enterprise_id, current_user.id)` 校验归属即可，读也要求企业属于当前用户，说明该取舍）
- `main.py`：`app.include_router(enterprise_org.router, prefix="/api/v1")`

**测试**（`dependency_overrides` + SQL 文本分发 mock，参照 `test_risk_control_list.py` 模式）：

- `GET/PUT /nodes`：读取返回结构；写入合法树 200；非法树 422（错误含「重复」）；非企业主 PUT 403
- `POST /members`：201 且 role 默认 member；user 不存在 404；重复 409；非企业主 403
- `PUT /members`：更新字段生效（exclude_unset 不误清）；404
- `DELETE /members`：删除成功；404
- `GET /members`：列表含 email/name join

## 步骤

- [ ] **步骤 1：失败测试**（追加到 `backend/tests/test_enterprise_org.py`，端点测试用 FastAPI + dependency_overrides）
- [ ] **步骤 2：确认失败 → 步骤 3：实现 schema/router/main → 步骤 4：通过 + `python -m pytest tests/ -q` 无回归**
- [ ] **步骤 5：Commit**

```bash
git add backend/app/schemas/enterprise_org.py backend/app/routers/enterprise_org.py backend/app/main.py backend/tests/test_enterprise_org.py
git commit -m "feat(org): org tree and member CRUD endpoints"
```

不要提交 TASKS.md；消息精确匹配；`git diff --check` 干净。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_org_03 --claim-id <claim_id> --exit-code 0 --summary "组织树+成员CRUD接口完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、自审结论（含写权限取舍说明）。

## 规则

- 严格 TDD；用 `apply_patch` 编辑；只改列出的 4 个文件；阻塞时停下汇报。
