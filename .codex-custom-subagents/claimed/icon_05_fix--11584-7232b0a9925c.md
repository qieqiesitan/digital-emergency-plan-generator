# Codex Custom Subagents task handoff v1

Task: icon_05_fix

## 目标

修复任务 5 质量审查发现的 Minor 视觉问题：`RegulationList.tsx` TYPE_CONFIG 的 4 个 AppIcon 未传 size，默认 16px，而 AntD Tag 内文字字号为 12px（旧 AntD 图标 1em=12px），导致图标比原渲染大 33%。改为 `size={12}` 像素级还原。

## 工作目录

所有工作、git 操作在隔离工作区：
`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\icon-system`

当前 HEAD 为 `8802b46`。前端命令在 `frontend/` 目录下运行。

## 任务步骤

### 步骤 1：修改 4 处 AppIcon

`frontend/src/components/regulation/RegulationList.tsx` TYPE_CONFIG（约 26-30 行）：

```tsx
  law: { label: "法律", color: "#1677ff", icon: <AppIcon name="law" size={12} /> },
  standard: { label: "标准", color: "#52c41a", icon: <AppIcon name="standard" size={12} /> },
  policy: { label: "政策", color: "#faad14", icon: <AppIcon name="policy" size={12} /> },
  topic: { label: "主题", color: "#eb2f96", icon: <AppIcon name="topic" size={12} /> },
```

只加 `size={12}`，其余一律不动。

### 步骤 2：门禁

在 `frontend/` 运行：
- `npx tsc -b` → exit 0
- `npx vitest run` → 130 passed
- `npx eslint src/components/regulation/RegulationList.tsx` → exit 1（5 项**既有 lint 债**，与本修复无关，不要清理）

### 步骤 3：Commit

```bash
git add frontend/src/components/regulation/RegulationList.tsx
git commit -m "fix(icon-system): align regulation type icon size with tag text"
```

提交后确认 `git show --stat HEAD` 恰 1 文件、`git show --check HEAD` 干净。

## 约束

- 只改 4 行；不清理既有 lint 债；不提交临时文件。

## 认领方式（必须先做）

在目录 `C:\Users\55061\Documents\数字化预案自动生成 2` 下运行：

```
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace . --agent deepseek_anthropic_worker --model deepseek-v4-flash --provider deepseek_anthropic
```

无子命令即原子认领一个 pending 任务，返回 JSON 含 `path`；**先读该文件**再开始。认领 status 非成功立即上报。

## 汇报格式

- **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 改动内容、门禁结果、提交 SHA、任何疑虑
