# Codex Custom Subagents task handoff v1

Task: icon_07_impl

## 目标

实现图标系统计划**任务 7**：位置（EnvironmentOutlined 9 处 → `AppIcon name="location"`）、通知（NotificationOutlined 1 处 → `AppIcon name="notice"`）、安全（SafetyOutlined 1 处 → `AppIcon name="safety"`）。前置：AppIcon 已可用（HEAD `1b55674`）。

## 工作目录

所有工作、git 操作在隔离工作区：
`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\icon-system`

当前 HEAD 为 `1b55674`。前端命令在 `frontend/` 目录下运行。

## 任务描述（计划任务 7 全文）

**文件（修改，共 9 个）：**
- 位置：
  - `frontend/src/pages/Enterprise/EnterpriseCreatePage.tsx:105`
  - `frontend/src/components/enterprise/RiskZoneForm.tsx:129`
  - `frontend/src/components/enterprise/FloorPlanPicker.tsx:99`
  - `frontend/src/components/enterprise/RiskSourceForm.tsx:72,149`
  - `frontend/src/components/enterprise/RiskObjectForm.tsx:180`
  - `frontend/src/components/enterprise/EnterpriseInfoWorkspace.tsx:245`
  - `frontend/src/components/enterprise/riskMapping/WorkbenchToolbar.tsx:30`
- 通知：`frontend/src/pages/Enterprise/RiskManagementTab.tsx:367`
- 安全：`frontend/src/layouts/AuthLayout.tsx:30`

### 步骤 1：逐文件替换

每个文件顶部加 `import AppIcon from "@/components/common/AppIcon";`（合并去重）。替换规则（行号以实际为准，按 JSX 内容匹配，保留原 style/props）：

| 位置 | 原写法 | 新写法 |
|---|---|---|
| FloorPlanPicker.tsx:99 | `<EnvironmentOutlined style={{ fontSize: 28, color: "#ff4d4f", filter: "drop-shadow(0 2px 4px rgba(0,0,0,0.3))" }} />` | `<AppIcon name="location" size={28} style={{ color: "#ff4d4f", filter: "drop-shadow(0 2px 4px rgba(0,0,0,0.3))" }} />` |
| RiskSourceForm.tsx:72（行内标记） | `<EnvironmentOutlined style={{ color: "#ff4d4f", marginRight: 4 }} />` | `<AppIcon name="location" size={14} style={{ color: "#ff4d4f", marginRight: 4 }} />` |
| AuthLayout.tsx:30 | `<SafetyOutlined style={{ fontSize: 64, marginBottom: 24 }} />` | `<AppIcon name="safety" size={64} style={{ marginBottom: 24 }} />` |
| 其余 6 处按钮/工具图标（EnterpriseCreatePage:105、RiskZoneForm:129、RiskSourceForm:149、RiskObjectForm:180、EnterpriseInfoWorkspace:245、WorkbenchToolbar:30） | `<EnvironmentOutlined />` | `<AppIcon name="location" size={14} />` |
| RiskManagementTab:367（按钮图标） | `<NotificationOutlined />` | `<AppIcon name="notice" size={14} />` |

说明：`size={14}` 与 AntD 按钮/文本图标槽位 1em@14px 对齐（沿用任务 4/5/6 先例）；`size={28}`/`size={64}` 还原原 fontSize。替换后若某文件不再使用对应 AntD 图标，从 import 移除（以 eslint unused 为准；注意 RiskSourceForm 的 EnvironmentOutlined 在 :72 与 :149 两处都换后若不再用才删）。

### 步骤 2：类型与 lint

在 `frontend/` 运行：`npx tsc -b`
预期：exit 0

运行：`npx eslint` 上述 9 个文件
预期：exit 0（若个别文件存在既有 lint 债，与父版本对比确认非本次引入后如实上报，不要顺手清理）

### 步骤 3：测试回归

在 `frontend/` 运行：`npx vitest run`
预期：16 文件 / 130 测试全部通过

### 步骤 4：截图目检

抽查登录页安全盾（64px）、四色图工作台工具栏定位图标、楼层平面图红色定位标记（28px 红）、企业编辑页 GIS 按钮、风险管控页「重大风险公示」按钮。确认渲染正常、颜色/阴影样式保留。截图保存到临时目录并附路径；如个别页面不便打开，至少覆盖 2 处并说明。

### 步骤 5：Commit

```bash
git add <上述 9 个文件全路径>
git commit -m "feat(icon-system): replace location notice and safety icons with AppIcon"
```

## 约束

- 只改上述 9 个文件；不得改动其他文件；
- 不提交临时文件；提交信息精确；`git show --check` 干净；
- 若某处替换后图标明显异常（尺寸/颜色/布局），以 BLOCKED 上报附截图。

## 验证清单（提交前逐项确认）

1. `npx tsc -b` → exit 0
2. `npx eslint` 9 个文件 → 无本次新增（既有债需对比父版本并说明）
3. `npx vitest run` → 130 passed
4. `git show --stat HEAD` 恰含 9 个文件
5. `git show --check HEAD` → 干净
6. 截图抽查 ≥2 处（附路径）

## 认领方式（必须先做）

在目录 `C:\Users\55061\Documents\数字化预案自动生成 2` 下运行：

```
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace . --agent deepseek_anthropic_worker --model deepseek-v4-flash --provider deepseek_anthropic
```

无子命令即原子认领一个 pending 任务，返回 JSON 含 `path`；**先读该文件**再开始。认领 status 非成功立即上报。

## 汇报格式

完成后汇报：
- **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 实现了什么、验证清单逐项结果（含截图路径/现象）
- 提交 SHA 与文件清单
- 自审发现（如有）
- 任何问题或疑虑
