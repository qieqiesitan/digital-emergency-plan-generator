# Codex Custom Subagents task handoff v1

Task: task_09_fix

## 目标

按任务 9 规格审查的 5 条建议修改修复（含少量后端配合），提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`3d3b21e`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1（跨端）：公示表补「位置」「告知卡入口」**

- 后端 `backend/app/services/risk_control_list_service.py`：`_row` 增加 `location`（obj.location 或 "-"）；公开脱敏 `PUBLIC_FIELDS` 增加 `location`；`risk_management.py` 的 `_strip_internal_keys` 保留 `object_id`（仅内部公示 items 用）或改为输出 `object_id`；
- 前端 `RiskPublicityPage.tsx`：Table 增加「位置」列与「告知卡入口」列（链接跳 `/enterprises/:id/risk-notice-cards/:objectId`，若无 object_id 则不渲染链接）；
- 后端测试补 location 断言；前端门禁通过。

**2：清单页补「联系电话」列**（`RiskControlListPage.tsx`）

在后端已返回 `phone` 基础上加列（无值显示 "-"）。

**3（跨端）：导出透传筛选**

- 后端 `control_list_export` 增加与 control-list 一致的筛选参数（zone_id/level/control_level/keyword；floor_id 已有）并应用过滤；
- 前端 `exportControlList(enterpriseId, params)` 传当前筛选；`RiskControlListPage` handleExport 传 filters；
- 后端测试补导出筛选断言（level 过滤后 xlsx 行数）。

**4：重置 token 错误处理**（`RiskPublicityPage.tsx`）

`handleResetToken` 的 onOk 加 try/catch：成功 message + refetch + 关闭 Modal；失败 message.error 且保持 Modal 打开（或提示后关闭，选清晰方案并说明）。

**5：公开页文案**（`PublicRiskPage.tsx`）

底部提示「数据来自系统快照」改为「数据为实时生成」或去掉快照表述，与规格「数据动态组装（非快照）」一致。

## 验证

- 后端：`python -m pytest tests/test_risk_control_list.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- 前端：`npx tsc -b`、eslint（改动文件）、`npx vitest run` 全部通过；
- `git diff --check` 干净。

## Commit

```bash
git add backend/app/services/risk_control_list_service.py backend/app/routers/risk_management.py backend/tests/test_risk_control_list.py frontend/src/pages/Enterprise/RiskPublicityPage.tsx frontend/src/pages/Enterprise/RiskControlListPage.tsx frontend/src/pages/PublicRiskPage.tsx frontend/src/services/riskManagementService.ts frontend/src/services/riskManagementService.test.ts
git commit -m "feat(risk): publicity location and notice links, export filters and polish"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_09_fix --claim-id <claim_id> --exit-code 0 --summary "任务9规格建议修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的文件；阻塞时停下汇报。
