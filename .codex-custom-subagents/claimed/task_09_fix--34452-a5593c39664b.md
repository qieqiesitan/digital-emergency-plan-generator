# Codex Custom Subagents task handoff v1

Task: task_09_fix

## 修复任务：任务 9 质量审查 3 项次要建议

### 背景

任务 9（公开 API + token 重置）已通过规格审查与质量审查（✅ 通过，附 3 项次要 + 3 项参考建议）。实现提交 `563e08f`。

### 修复 1（次要）：公开端点复用已加载对象，避免重复回查

`backend/app/routers/public_risk_notice.py`：RiskObject 查询后 `lazy="selectin"` 已自动加载 units/events/measures 全图，但随后 `load_events_and_measures` 按 id 再查一次（SELECT + 级联全部重发）。请改为复用已加载对象：直接调 service 的纯函数 `merge_object_events(obj)` / `collect_measures(events)`（在 `risk_notice_card_service.py` 已有），不再调用 `load_events_and_measures`。

### 修复 2（次要）：全企业 objects 查询改投影，避免 selectin 放大

公开端点查询全企业 objects 仅为 `compute_code` 取 id 与排序。请改为投影查询：`select(RiskObject.id, RiskObject.created_at).where(...).order_by(RiskObject.created_at)`，返回 row 列表传给 `compute_code`（其实现只访问 `.id`，如类型注解需要调整可在 service 内兼容）。

### 修复 3（次要）：公开端点加 Cache-Control

公开端点响应加 `Cache-Control: public, max-age=300`（二维码重复扫码场景减负；FastAPI 用 `response: Response` 参数设置头）。

### 修复 4（次要·参考）：TokenResetResponse 强类型

`backend/app/schemas/risk_notice_card.py` 新增 `TokenResetResponse{public_url: str}`，token 重置端点 response_model 改用它（与 SnapshotResponse 惯例一致）。

### 范围与限制

* 只改 `backend/app/routers/public_risk_notice.py`、`backend/app/routers/risk_notice_card.py`、`backend/app/schemas/risk_notice_card.py`、`backend/app/services/risk_notice_card_service.py`（如 compute_code 类型调整）、`backend/tests/test_public_risk_notice.py`（如有断言需同步）。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_public_risk_notice.py tests/test_risk_notice_card_api.py -v` 全部 PASS。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/ -q` 无回归（全量 405+ passed）。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 563e08f），消息：`fix(risk-notice-card): optimize public query and add cache header`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
