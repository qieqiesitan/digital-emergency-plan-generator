# Codex Custom Subagents task handoff v1

Task: task_d3_t4

## 任务：导出接入（预览 + docx）（diagrams batch3 任务 4）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams`

这是 git 分支 `codex/plan-diagrams-enhancement` 的隔离 worktree。当前 HEAD 应为 `7be69ab`（batch3 任务 1-3 完成）。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令（必须挂 2_chroma_cache 卷）

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_diagrams_api.py -v
```

### 步骤 1：预览接入

修改 `backend\app\routers\export.py::get_export_preview`：在每个章节处理完 mermaid 后追加 diagram_svgs 渲染。先读当前函数（约 126-210 行），在 `html_parts.append(content)` 后追加：

```python
        # 附图：非占位 SVG 内嵌，占位转文字
        for key, meta in (section.diagram_svgs or {}).items():
            if isinstance(meta, dict) and meta.get("placeholder"):
                html_parts.append(
                    f'<p class="diagram-placeholder">【{key}】待补充数据后生成'
                    f"（{meta.get('reason','')}）</p>"
                )
            elif isinstance(meta, dict) and meta.get("svg"):
                svg = meta["svg"]
                m = re.search(r"<svg[^>]*>.*?</svg>", svg, re.DOTALL)
                if m:
                    html_parts.append(
                        '<div class="mermaid-diagram" style="margin:16px 0;padding:16px;'
                        'background:#fafafa;border:1px solid #e8e8e8;border-radius:6px;'
                        'text-align:center;">' + m.group(0) + "</div>"
                    )
```

注意：`key`/`meta.get('reason')` 来自后端数据，插入 HTML 前需 `html.escape`（`import html` 已有或补）：

```python
        import html as _html
        ...
        html_parts.append(
            f'<p class="diagram-placeholder">【{_html.escape(str(key))}】待补充数据后生成'
            f"（{_html.escape(str(meta.get('reason','')))}）</p>"
        )
```

### 步骤 2：docx 接入

修改 `backend\app\services\docx_template.py`：

1. `generate_plan_docx` 的章节循环中，在 Mermaid 图片插入后追加附图处理。先读现有 `generate_plan_docx` 的 Mermaid 处理段（约 400-490 行），在其后追加：

```python
        # 附图：非占位 SVG → PNG 插入；占位 → 文字行
        for key, meta in (section.get("diagram_svgs") or {}).items():
            if isinstance(meta, dict) and meta.get("placeholder"):
                add_normal_paragraph(doc, f"【{key}】待补充企业数据后生成")
            elif isinstance(meta, dict) and meta.get("svg"):
                try:
                    from app.services.mermaid_renderer import render_svg_to_png
                    png_bytes = render_svg_to_png(meta["svg"])
                    img_stream = io.BytesIO(png_bytes)
                    doc.add_picture(img_stream, width=Cm(14.6))
                    if doc.paragraphs:
                        doc.paragraphs[-1].alignment = WD_ALIGN_PARAGRAPH.CENTER
                except Exception as e:
                    logger.warning("Diagram %s render failed: %s", key, e)
                    add_normal_paragraph(doc, f"【{key}】附图渲染失败")
```

（`render_svg_to_png` 为同步函数，直接调用；`io`/`Cm`/`WD_ALIGN_PARAGRAPH` 已导入。）

2. `export.py::export_plan_docx` 的 `sections_data` 构建追加：

```python
        sections_data.append({
            "title": s.title,
            "level": s.level,
            "content": content,
            "mermaid_svgs": _ms,
            "diagram_svgs": s.diagram_svgs or {},
        })
```

### 步骤 3：追加测试

在 `backend\tests\test_plan_diagrams_api.py` 追加：

```python
def test_export_preview_includes_diagram_placeholder_and_svg():
    from unittest.mock import MagicMock, AsyncMock
    # 直接测试 get_export_preview 的渲染逻辑较复杂（依赖 DB 会话），
    # 改为测试 export.py 中新增的辅助函数（若有）或通过构造 sections 验证预览字符串。
    # 若实现为内联逻辑，则测试调整为：构造含 diagram_svgs 的 Section mock，
    # 调用预览构建函数（如 _build_preview_html）断言含「待补充数据后生成」与 "<svg"。
```

实现者根据实际代码结构决定测试方式：若内联在路由函数中，可提取 `_build_preview_section_html(section)` 辅助函数便于测试；或直接用 TestClient + 内存 DB 验证。保证测试真实断言占位与 SVG 渲染。

### 步骤 4：运行测试验证通过 + 全量回归

运行：`docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_diagrams_api.py -v`
预期：PASS

运行：`docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/ -q --ignore=tests/test_autofill_research.py`
预期：全部通过

### 步骤 5：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams
git add backend/app/routers/export.py backend/app/services/docx_template.py backend/tests/test_plan_diagrams_api.py
git commit -m "feat(plan): export diagram_svgs in preview and docx (diagrams batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出与全量回归结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
