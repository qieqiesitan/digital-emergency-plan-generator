# Codex Custom Subagents task handoff v1

Task: task_b2_t2

## 任务：创建预案自动生成编号

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成 TDD 实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

这是 git 分支 `codex/plan-generation-enhancement` 的隔离 worktree。当前 HEAD 应为 `43db521`（批 2 任务 1 完成：PlanProject 已有 plan_number/version_number 字段与 `_generate_plan_number`）。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_plan_number.py -v
```

### 步骤 1：追加失败的测试

在 `backend\tests\test_plan_number.py` 末尾追加：

```python
def test_create_plan_schema_accepts_numbers():
    from app.schemas.plan import PlanCreate
    p = PlanCreate(
        enterprise_id="e1", plan_type="special", title="测试",
        accident_type="火灾", plan_number="自定义-001", version_number="A-2026-08",
    )
    assert p.plan_number == "自定义-001"
    assert p.version_number == "A-2026-08"
```

### 步骤 2：运行测试验证失败

运行 pytest 命令。
预期：新增测试 FAIL（`TypeError: PlanCreate.__init__() got an unexpected keyword argument 'plan_number'`）；其余测试 PASS。

### 步骤 3：schema 加字段

修改 `backend\app\schemas\plan.py`：

1. `PlanCreate` 追加：

```python
    plan_number: str | None = None
    version_number: str | None = None
```

2. `PlanResponse` 追加：

```python
    plan_number: str | None = None
    version_number: str | None = None
```

### 步骤 4：create_plan 自动生成编号

修改 `backend\app\routers\plans.py` 的 `create_plan`。当前实现有两处 PlanProject 构造分支（继承用户默认风格 / 直接构造），需先合并 `plan_data` 统一构造。先读当前 `create_plan`（约 127-158 行）确认结构，然后：

1. 在函数开头 `ent` 校验后，构造 `plan_data = data.model_dump(exclude_none=True)`。
2. `plan_number` 为空时自动生成：

```python
    if not plan_data.get("plan_number"):
        existing_count = (
            await db.execute(
                select(func.count()).select_from(PlanProject).where(
                    PlanProject.enterprise_id == data.enterprise_id,
                    PlanProject.plan_type == data.plan_type,
                )
            )
        ).scalar() or 0
        plan_data["plan_number"] = _generate_plan_number(ent.name, data.plan_type, existing_count + 1)
```

3. `version_number` 为空时默认 `A-{year}-{month:02d}`：

```python
    if not plan_data.get("version_number"):
        plan_data["version_number"] = f"A-{datetime.now().year}-{datetime.now().month:02d}"
```

4. 风格继承逻辑保持（`style_preference` 为 None 时读用户默认风格），然后统一 `p = PlanProject(user_id=current_user.id, **plan_data)`。
5. 确认 `plans.py` 顶部已导入 `from datetime import datetime`（若无则补）；`func`/`select`/`User` 已导入。

注意：保持 create_plan 其余行为不变（章节模板创建、commit、返回 _build_plan）。

### 步骤 5：运行测试验证通过

运行 pytest 命令。
预期：PASS（4 passed，含新增 schema 测试）。

### 步骤 6：全量回归

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\ -q --ignore=tests\test_autofill_research.py
```

预期：全部通过。

### 步骤 7：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/routers/plans.py backend/app/schemas/plan.py backend/tests/test_plan_number.py
git commit -m "feat(plan): auto-generate plan number on create (batch2)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出与全量回归结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
