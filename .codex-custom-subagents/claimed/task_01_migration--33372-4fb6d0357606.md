# Codex Custom Subagents task handoff v1

Task: task_01_migration

## 实现任务 1：数据库迁移 + RiskObject 模型字段

### 任务描述（来自实现计划 2026-08-11-risk-notice-card.md 任务 1）

**文件：**
- 创建：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\db_migration_risk_notice_card.sql`
- 修改：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\app\models\risk_management.py`（RiskObject 类，约 45-75 行）
- 测试：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\tests\test_risk_notice_card_service.py`（新建，先建模型字段断言）

**步骤 1：编写失败测试（模型字段）**

创建 `backend/tests/test_risk_notice_card_service.py`：

```python
"""风险告知卡服务测试。"""
from app.models.risk_management import RiskObject


def test_risk_object_has_notice_card_fields():
    cols = {c.name for c in RiskObject.__table__.columns}
    assert {"responsible_unit", "responsible_person", "contact_phone", "public_token"} <= cols
```

**步骤 2：运行测试验证失败**

运行：`cd backend && python -m pytest tests/test_risk_notice_card_service.py -v`
预期：FAIL（字段不存在）

**步骤 3：编写迁移 SQL**

创建 `backend/db_migration_risk_notice_card.sql`：

```sql
-- 风险告知卡：risk_objects 新增字段
ALTER TABLE risk_objects
    ADD COLUMN IF NOT EXISTS responsible_unit VARCHAR(255),
    ADD COLUMN IF NOT EXISTS responsible_person VARCHAR(100),
    ADD COLUMN IF NOT EXISTS contact_phone VARCHAR(50),
    ADD COLUMN IF NOT EXISTS public_token VARCHAR(64);

-- 存量行补随机 token（迁移幂等：仅空值行）
UPDATE risk_objects
   SET public_token = substr(md5(random()::text || clock_timestamp()::text), 1, 64)
 WHERE public_token IS NULL OR public_token = '';

ALTER TABLE risk_objects ALTER COLUMN public_token SET NOT NULL;
CREATE UNIQUE INDEX IF NOT EXISTS uq_risk_objects_public_token ON risk_objects(public_token);
```

**步骤 4：更新模型**

在 `backend/app/models/risk_management.py` 的 `RiskObject` 类中（`image_url` 与 `is_risk_point` 之间）添加：

```python
    # 风险告知卡：责任信息与公开页 token
    responsible_unit: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    responsible_person: Mapped[Optional[str]] = mapped_column(String(100), nullable=True)
    contact_phone: Mapped[Optional[str]] = mapped_column(String(50), nullable=True)
    public_token: Mapped[str] = mapped_column(String(64), nullable=False, unique=True, default=lambda: __import__("secrets").token_hex(32))
```

**步骤 5：运行测试验证通过**

运行：`cd backend && python -m pytest tests/test_risk_notice_card_service.py -v`
预期：PASS

**步骤 6：Commit**

```bash
git add backend/db_migration_risk_notice_card.sql backend/app/models/risk_management.py backend/tests/test_risk_notice_card_service.py
git commit -m "feat(risk-notice-card): add risk object notice fields and migration"
```

### 范围与限制

- 只做任务 1 定义的内容：迁移 SQL、RiskObject 模型 4 字段、测试文件 `test_risk_notice_card_service.py`（只含模型字段断言）。
- 不创建快照模型、不创建服务、不修改其他文件。
- 不要重构 RiskObject 或任何无关代码。
- 提交前确认 `git status` 只含上述 3 个文件（worktree 内）。

### 验证

- `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_service.py -v` 通过。
- git commit 成功，提交信息精确为 `feat(risk-notice-card): add risk object notice fields and migration`。

### 上下文

- 项目 worktree：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card`（分支 codex/risk-notice-card，独立于主工作区）。
- 设计规格：`docs/superpowers/specs/2026-08-11-risk-notice-card-design.md`（§6.1 定义 4 个字段）。
- 后续任务会扩展该测试文件与模型，本任务只做第一步。
- 后端测试框架：pytest（conftest 已处理 sys.path），测试命令在 worktree 的 backend 目录下执行。

---

## 追加指令（2026-08-11，控制者）：修复质量审查提出的重要项

你的任务 1 已通过规格审查和质量审查（✅ 通过），但质量审查提出 2 项「重要」改进建议，需要你修复后提交。请按以下要求修复：

### 修复 1（重要）：import secrets

`backend/app/models/risk_management.py` 顶部已有 `from uuid import uuid4` 等导入，请添加 `import secrets`（模块级），并把 public_token 的默认值从 `default=lambda: __import__("secrets").token_hex(32)` 改为 `default=lambda: secrets.token_hex(32)`。

### 修复 2（重要）+ 2 项次要：迁移 SQL 统一 token 长度

`backend/db_migration_risk_notice_card.sql` 中：

1. 存量行 token 生成改为 64 位 hex，与模型默认一致：`SET public_token = encode(gen_random_bytes(32), 'hex')`（PG13+ 内置函数）。
2. 追加 server-side DEFAULT：`ALTER TABLE risk_objects ALTER COLUMN public_token SET DEFAULT encode(gen_random_bytes(32), 'hex');`
3. 用 `BEGIN;` ... `COMMIT;` 包裹整个迁移（幂等语句可保留 IF NOT EXISTS 写法）。

### 验证

- `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_service.py -v` 仍 PASS。
- `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
- 提交新 commit（不要 amend 原提交 80a56ed），消息：`fix(risk-notice-card): unify public token generation and import style`

### 汇报

- 状态：DONE | BLOCKED | NEEDS_CONTEXT
- 修改的文件与行
- 测试结果
- 新提交 SHA
