# Codex Custom Subagents task handoff v1

Task: task_04_fix

## 修复任务：任务 4 质量审查 1 项重要 + 3 项次要建议

### 背景

任务 4（SVG 标志资产）已通过规格审查，质量审查 ❌ 需修复（1 项重要 + 3 项次要）。实现提交 `7c744ce`。

### 修复 1（重要）：warning-explosion.svg 改为爆裂星形

`backend/app/static/signs/warning-explosion.svg` 当前是「竖线+圆点」感叹号形，可辨识为「注意」而非「爆炸」。GB 2894-2025 当心爆炸标准图形为爆裂星形。请改为：黄底黑边正三角内放**黑色爆裂星形**（中心圆 + 6-8 条放射短线，或标准爆裂星 path），保持 viewBox `0 0 28 26`、`#FFD100` 底、`#000` 图形。

### 修复 2（次要）：instruction-ventilate.svg 删冗余 transform

`backend/app/static/signs/instruction-ventilate.svg` 中 `transform="rotate(0 14 14)"` 旋转 0 度无意义，删除（仅保留 120°/240° 旋转片）。

### 修复 3（次要）：instruction-anti-static-clothes.svg 强化防静电语义

在衣服图形上加闪电/接地符号（如衣服左上角加黄色闪电或黑色接地箭头），避免被误读为普通衣物；保持蓝底 #005EB8 + 白色图形。

### 修复 4（次要）：test_static_signs.py 增强

`backend/tests/test_static_signs.py`：
1. 用 `xml.etree.ElementTree` 解析断言每个 SVG XML 合法（新增测试或并入现有测试）。
2. 对非四类前缀的文件（未知前缀孤儿文件）报错（循环加 else 分支）。
3. 补一条爆炸图形特征断言：`warning-explosion.svg` 内容含星形放射特征（如多个放射线 path/line，或 `polygon` 星形），防止回归成感叹号。

### 范围与限制

* 只改上述 SVG 文件与 test_static_signs.py。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_static_signs.py tests/test_risk_notice_card_data.py -v` 全部 PASS。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 7c744ce），消息：`fix(risk-notice-card): redraw explosion sign and harden svg tests`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
