# Codex Custom Subagents task handoff v1

Task: task_10_fix

## 目标

修复任务 10 规格审查的 1 条必须修复（`menu:data_dicts` 权限未种子化导致系统菜单不可见）+ 2 条建议修改，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`4d0ec3c`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 背景

- `MainLayout.tsx` 菜单项受 `hasMenu("/settings/data-dicts")`（权限码 `menu:data_dicts`）门控，但权限种子/迁移未含该权限，任何角色侧栏都看不到入口；
- 先例：`menu:regulations` 权限行在历史 backup SQL 中手工维护；请先读 `backend` 下 `seed_roles.sql`、各 `db_migration_*.sql` 及角色/权限模型（`models/role.py`、`routers/roles.py`）确认权限种子与角色分配机制，再按既有模式补种。

## 修复清单

**1（必须修复）：补 `menu:data_dicts` 权限种子 + 存量库补种**

- 按项目既有模式（参考 `menu:regulations`/`menu:prompts` 的插入方式）在合适位置（迁移 SQL 或 seed 文件）补 `permissions` 行：`code='menu:data_dicts'`、`name='数据字典管理'`、`resource='menu'`、`action='view'`、`category='menu'`；
- 确保 super_admin（及 admin，若项目如此约定）能获得该菜单：按既有角色分配模式补 `role_permissions`（若角色与权限是显式分配）或确认超管权限机制；
- 存量库补种：新增/追加 `INSERT ... ON CONFLICT DO NOTHING`（幂等），文件名与项目迁移惯例一致（如追加到 `db_migration_data_dicts.sql` 或新建 `db_migration_data_dicts_permission.sql`，选清晰方案）；
- 验证：`/roles/permissions` 列表含该权限、超管 `my-menus` 含 `menu:data_dicts`（mock/只读探针或测试断言）。

**2（建议）：编辑提交只发更新字段**（`frontend/src/pages/Settings/DataDictManagePage.tsx`、`EnterpriseDictConfigPage.tsx`）

Drawer/编辑提交 payload 只含 label/value/sort_order/enabled/description（不发 dict_type/code）。

**3（建议）：覆盖按钮按 code 去重**（`EnterpriseDictConfigPage.tsx`）

同一 dict_type 下已存在企业条目（enterprise scope 同 code）时，系统默认行隐藏或禁用「覆盖」按钮（Tooltip「已覆盖，可编辑企业条目」），避免 409。

## 验证

- 后端：`python -m pytest tests/ -q` 无回归；权限相关测试（若新增）通过；
- 前端：`npx tsc -b`、eslint（改动文件）、`npx vitest run` 全部通过；
- `git diff --check` 干净。

## Commit

```bash
git add backend/db_migration_data_dicts.sql（或新建迁移） backend/tests/test_data_dict.py frontend/src/pages/Settings/DataDictManagePage.tsx frontend/src/pages/Enterprise/EnterpriseDictConfigPage.tsx
git commit -m "fix(data-dict): seed menu permission and dedupe override actions"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_10_fix --claim-id <claim_id> --exit-code 0 --summary "任务10权限种子+覆盖去重修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改上述文件（如需额外文件请说明理由）；阻塞时停下汇报。
