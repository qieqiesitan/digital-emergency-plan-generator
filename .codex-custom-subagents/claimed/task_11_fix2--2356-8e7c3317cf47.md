# Codex Custom Subagents task handoff v1

Task: task_11_fix2

## 目标

按任务 11 代码质量审查的 2 条建议修改修复，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`86a747a`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1（建议）：DIRECT 采用后手动修改让位**

`frontend/src/utils/eventPayload.ts`：`adoptedInherent` 的 DIRECT 分支加门控——仅当表单当前固有等级（或 `DIRECT_LEVELS` label）与 `adoptedInherent.level` 一致（用户未改）时才覆盖；用户已改则让位给用户值（不覆盖）。同步调整 `eventPayload.test.ts` 的 DIRECT 用例语义：「DIRECT 已改仍覆盖」改为「DIRECT 已改让位用户值」；保留「DIRECT 未改显式携带」。若 `RiskEventForm.tsx` 的提示文案与门控语义冲突，一并修正（注明「DIRECT 采用后修改固有等级将覆盖建议值」或等价措辞）。

**2（建议）：归属校验提取复用**

`backend/app/routers/risk_management.py`：把 conversion-reference 与 AI 端点重复的 object/unit 链归属校验提取为私有 helper（如 `async def _event_owned_by_enterprise(db, event, enterprise_id) -> bool`），两处调用；行为不变；AI 端点补 unit 链归属测试（可选，至少确认提取后现有测试全绿）。

## 验证

- 后端：`python -m pytest tests/test_risk_dual_level.py tests/test_risk_conversion_api.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- 前端：`npx tsc -b`、eslint（改动文件）、`npx vitest run src/utils/eventPayload.test.ts` 全部通过；
- `git diff --check` 干净。

## Commit

```bash
git add backend/app/routers/risk_management.py backend/tests/test_risk_dual_level.py frontend/src/utils/eventPayload.ts frontend/src/utils/eventPayload.test.ts frontend/src/components/enterprise/RiskEventForm.tsx
git commit -m "fix(risk): yield direct inherent edits and reuse event ownership check"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_11_fix2 --claim-id <claim_id> --exit-code 0 --summary "任务11 DIRECT让位+归属复用修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改上述文件；阻塞时停下汇报。
