# Codex Custom Subagents task handoff v1

Task: task_14_form

## 实现任务 14：风险对象表单新增责任信息字段（前后端同步）

### 任务描述（来自实现计划 2026-08-11-risk-notice-card.md 任务 14 + 设计规格 §6.1/§10.4）

**文件：**
* 修改：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend\src\components\enterprise\RiskObjectForm.tsx`
* 修改：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend\src\types\riskManagement.ts`（RiskObjectFormValues 加 3 字段）
* 修改：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\app\schemas\risk_management.py`（RiskObjectCreate/RiskObjectUpdate/RiskObjectResponse 加 responsible_unit/responsible_person/contact_phone）
* 测试：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\tests\test_risk_notice_card_service.py`（追加 schema 字段断言，可选）

### 前端表单（`RiskObjectForm.tsx`）

在「位置描述」Form.Item 之后添加「责任信息（用于风险告知卡）」分组：

```tsx
<div style={{ fontSize: 13, fontWeight: 600, color: "#1677ff", margin: "16px 0 8px", borderLeft: "3px solid #1677ff", paddingLeft: 8 }}>
  责任信息（用于风险告知卡）
</div>
<Form.Item name="responsible_unit" label="责任单位">
  <Input placeholder="如：储运车间" />
</Form.Item>
<Form.Item name="responsible_person" label="责任人">
  <Input placeholder="如：张三" />
</Form.Item>
<Form.Item name="contact_phone" label="联系电话">
  <Input placeholder="如：13800000000" />
</Form.Item>
<div style={{ fontSize: 12, color: "#999", background: "#fafafa", border: "1px dashed #e0e0e0", borderRadius: 4, padding: "6px 8px", marginBottom: 8 }}>
  这三个字段会显示在风险告知卡左栏。留空时，卡片自动使用企业信息中的安全负责人及电话兜底。
</div>
```

`RiskObjectFormValues` 接口加 `responsible_unit?/responsible_person?/contact_phone?: string`（在 `frontend/src/types/riskManagement.ts` 对应类型或组件内接口）。

### 后端 schema（`backend/app/schemas/risk_management.py`）

`RiskObjectCreate`/`RiskObjectUpdate`/`RiskObjectResponse` 各加：

```python
responsible_unit: str | None = None
responsible_person: str | None = None
contact_phone: str | None = None
```

（Update 中为 `str | None = None` 与现有可选字段一致。）确认 `risk_management.py` 路由的 create/update 用 `model_dump(exclude_unset=True)` 或全量 dump 透传（若是全量 dump，默认 None 会覆盖旧值——请核对并采用与既有字段一致的处理）。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx tsc -b` 0 错误 + `npx vitest run` 全通过
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/ -q` 无回归（407+ passed）
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净
* 提交 commit，消息：`feat(risk-notice-card): add responsibility fields to risk object form`，只含上述文件

### 范围与限制

* 只改表单、前端类型、后端 schema（+可选测试）。
* 不修改路由逻辑（除非确认需要适配字段透传）。

### 上下文

* worktree：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card`（分支 codex/risk-notice-card）。
* 任务 1-13 已完成（最新 HEAD：b740201）。
* 任务 10 修复已说明：responsible_* 字段待任务 14 后端扩展 Response/Update schema 后再加回前端类型——本次前后端同步完成。
* 设计规格：`docs/superpowers/specs/2026-08-11-risk-notice-card-design.md` §6.1（字段定义）与 §10.4（表单交互）。
