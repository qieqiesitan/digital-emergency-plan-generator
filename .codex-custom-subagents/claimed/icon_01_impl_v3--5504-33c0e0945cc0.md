# Codex Custom Subagents task handoff v1

Task: icon_01_impl_v3

## 目标

图标系统计划**任务 1** 的收尾：前两次尝试因 iconfont 接口参数问题 BLOCKED，主控已定位根因（`fills` 应为空串而非 `0`，配合 `line=1` 风格过滤）并修正脚本、成功抓取 24 个 SVG 资产。本任务由你**独立复核**后提交（提交本身也是实现的一部分）。

## 工作目录

所有工作、git 操作都在隔离工作区：
`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\icon-system`

当前 HEAD 为 `b00fd49`。`git status` 应只有以下未跟踪内容（不要动其他文件）：
- `frontend/src/assets/icons/`（24 个 SVG）
- `scripts/fetch_icons.py`
- `scripts/test_fetch_icons.py`

## 背景（根因，供理解）

iconfont 公开搜索接口按更新时间排序，且结果集取决于请求参数：技能 CLI 的 `--line` 实际发送 `fills=""`（空串）+ `line="1"`；之前 fetch_icons.py 写死 `fills="0"`，导致返回另一套结果集、查不到选定 id。修正后 24 个 id 全部可获取，资产已成功生成。

## 任务步骤

### 步骤 1：独立复核验证清单

1. `python -m unittest scripts.test_fetch_icons -v` → 预期 PASS（1 用例）
2. `python scripts/fetch_icons.py --verify` → 预期 `OK: 24 assets verified`
3. 抽查 `frontend/src/assets/icons/` 至少 5 个文件：非空、含 `viewBox`、无 `class=` / `style=` / `fill="#` 残留
4. 复核 `scripts/fetch_icons.py` 关键点：`MAPPING` 24 项与设计文档 §4.3 一致；`search()` 参数含 `"fills": ""`、`"line": "1"`；`clean_svg()` 去除 class/style/version/硬编码 fill；分页 `MAX_PAGES=5`/`PAGE_SIZE=60` 与按 term 所需 id 全集判断
5. 文件行尾统一 CRLF（如 git 提示 LF→CRLF，先转换）

如任一项不符：以 BLOCKED 上报并附具体证据；不要自行改设计（24 个 id 与文件名是锁定的）。

### 步骤 2：Commit

```bash
git add scripts/fetch_icons.py scripts/test_fetch_icons.py frontend/src/assets/icons
git commit -m "feat(icon-system): fetch and clean 24 iconfont svg assets"
```

提交后确认：
- `git show --stat HEAD` 恰含 3 类文件（2 个脚本 + 24 个 svg）
- `git show --check HEAD` 干净
- `git status` 只剩预期的未跟踪/已提交状态，无临时文件

## 约束

- 只允许上述 3 类文件的 add/commit；不得改动其他文件；
- 不提交 `TASKS.md`（本工作区无此文件）、不提交任何诊断/临时文件；
- 提交信息必须精确为 `feat(icon-system): fetch and clean 24 iconfont svg assets`。

## 认领方式（必须先做）

在目录 `C:\Users\55061\Documents\数字化预案自动生成 2` 下运行（主工作区根目录，不是 worktree）：

```
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace . --agent deepseek_anthropic_worker --model deepseek-v4-flash --provider deepseek_anthropic
```

该命令无子命令即原子认领一个 pending 任务，返回 JSON 含 `path`（认领后的任务文件）。**先读该文件**再开始工作。认领输出中若 `status` 不是成功，立即上报。

## 汇报格式

完成后汇报：
- **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 验证清单逐项结果
- 提交 SHA 与 `git show --stat HEAD` 摘要
- 自审发现（如有）
- 任何问题或疑虑
