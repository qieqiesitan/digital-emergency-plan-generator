# Codex Custom Subagents task handoff v1

Task: cockpit_08_fix

你正在修复「企业驾驶舱」任务 8 质量审查发现的 2 项重要回归（+1 项连带）。只改下列内容，提交单独 commit。

## 工作目录（重要）
C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\enterprise-cockpit
（git worktree，分支 codex/enterprise-cockpit。前端命令用 workdir 进入 frontend 子目录。）

## 缺陷 1（重要）：RiskRedirect 丢弃 query 参数（?ai=1 AI 优化入口失效、?mode=edit 丢失）

文件：`frontend/src/routes/index.tsx`

修复：RiskRedirect 保留原 query。顶部 import 追加 `useLocation`：

```tsx
import { createBrowserRouter, Navigate, useLocation, useParams } from "react-router-dom";
```

RiskRedirect 改为：

```tsx
function RiskRedirect({ to, params = [] }: { to: string; params?: Array<"objectId" | "methodId"> }) {
  const { id, objectId, methodId } = useParams<{ id: string; objectId?: string; methodId?: string }>();
  const location = useLocation();
  let suffix = "";
  if (params.includes("objectId") && objectId) suffix += `/${objectId}`;
  if (params.includes("methodId") && methodId) suffix += `/${methodId}`;
  return <Navigate to={`/enterprises/${id}${to}${suffix}${location.search}`} replace />;
}
```

效果：`/enterprises/:id/risk-notice-cards/:objectId?ai=1` 重定向后保留 `?ai=1`（RiskNoticeCardPreviewPage 读取 ai 参数执行 AI 优化）；`?mode=edit` 同理保留。

## 缺陷 2（重要）：两处「返回」落点仍用旧 ?tab= URL，现会落到驾驶舱首页

文件 1：`frontend/src/pages/Enterprise/RiskMappingWorkbenchPage.tsx`

`goBack` 内：

```tsx
navigate(`/enterprises/${enterpriseId}?tab=risk-management`);
```

改为：

```tsx
navigate(`/enterprises/${enterpriseId}/risk-management`);
```

文件 2：`frontend/src/pages/Hazard/HazardRecordDetailPage.tsx`

`backTarget` 常量：

```tsx
const backTarget = `/enterprises/${enterpriseId}?tab=hazard-inspection`;
```

改为：

```tsx
const backTarget = `/enterprises/${enterpriseId}/hazard`;
```

## 验证

运行（工作目录 worktree\frontend）：
- `npx tsc -b` → exit 0
- `npx eslint src/routes/index.tsx src/pages/Enterprise/RiskMappingWorkbenchPage.tsx src/pages/Hazard/HazardRecordDetailPage.tsx` → exit 0
- `npx vitest run` → 全量通过
- `git diff --check` 干净

## Commit

```bash
git add frontend/src/routes/index.tsx frontend/src/pages/Enterprise/RiskMappingWorkbenchPage.tsx frontend/src/pages/Hazard/HazardRecordDetailPage.tsx
git commit -m "fix(cockpit): preserve query on risk redirect and fix module back targets"
```

## 项目规则
- TASKS.md 永不提交；不要修改任务范围外文件；你不是孤立的，不要 revert 他人修改。
- 按 AGENTS.md 铁律一，在 TASKS.md 顶部追加「当前状态快照」（不提交）。

## 汇报格式
- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修复内容、验证结果、commit SHA、自审发现
