# Codex Custom Subagents task handoff v1

Task: task_c14_review_spec

## 任务：规格合规审查——task_c14_onboarding_shell

你是一个规格合规审查子智能体。目的：验证实现者是否构建了所要求的内容（不多不少）。关键：不要信任实现者的报告，必须独立阅读实际代码验证。

### 实现位置

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`（分支 codex/usability-overhaul）。审查提交 `873107e`：

git show 873107e --stat 与 git show 873107e

### 要求的内容（任务 C1-4 原文摘要）

1. OnboardingPage 骨架：左侧 6 步列表（当前/已完成/可选配色）、右侧内容、enterprise_id 缺失提示、稍后继续回 dashboard、统一 Step props。
2. StepEnterprise：EnterpriseInfoCards + onSaved 后失效 enterprise/completion 查询；企业不存在提示。
3. StepOrg：概况输入 → POST /onboarding/candidates（org）→ 候选组卡片 + 成员表格（角色/姓名/职位/电话待填）→ 全部采纳合并去重 → updateOrgStructure 保存。
4. 第 3-6 步最小占位（同 Props）。
5. tsc + eslint 通过（无 any）。
6. Commit：feat(onboarding): page skeleton and step 1-2 (enterprise info, org structure)。

### 实现者声称构建了什么

- 7 文件 +397/-1（骨架 + 第1/2步 + 4 占位）
- tsc + eslint 通过
- 5 处合理偏离（useQuery 导入补、completion 显示、StepDef 类型、OrgCandidate 扩展、axios 类型守卫）

### 你的工作

阅读实际代码验证：骨架/第1步/第2步与要求一致？偏离是否合理（模板缺陷修复）？占位可用？只改 Onboarding 目录？tsc/eslint 通过？

### 汇报格式

- ✅ 符合规格（如果经过代码检查后一切匹配）
- ❌ 发现问题：[具体列出缺失或多余的内容，附带 file:line 引用]
