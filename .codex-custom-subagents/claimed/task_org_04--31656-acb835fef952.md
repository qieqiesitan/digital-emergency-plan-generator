# Codex Custom Subagents task handoff v1

Task: task_org_04

## 目标

实现「企业组织与成员管理」计划任务 4：Excel 导入成员 + 责任人选择器，按 TDD 完成并提交。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD=`4aa59d5`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 文件

- 修改：`backend/app/routers/enterprise_org.py`、`backend/app/services/enterprise_org_service.py`
- 测试：`backend/tests/test_enterprise_org.py`（追加）

## 关键契约

**服务层（`enterprise_org_service.py` 追加）**：

```python
ROLE_LABEL_MAP = {"企业管理员": "enterprise_admin", "班组长": "team_leader", "员工": "member"}

def build_member_import_template() -> Workbook:
    """openpyxl 模板：表头 姓名/邮箱/部门/班组/岗位/角色；角色列数据校验下拉。"""

def parse_member_rows(rows: list[dict]) -> list[dict]:
    """rows 为 {列名: 值}；邮箱必填且格式校验；角色映射 ROLE_LABEL_MAP（缺省 member）；返回 [{name,email,department,team,position,role,error?}]"""
```

**端点（`routers/enterprise_org.py` 追加，写权限同任务 3 `_get_owned_ent`）**：

- `POST /members/import`（`UploadFile`，xlsx）→ `load_workbook` → 逐行 `parse_member_rows` → 对每行：按邮箱查用户（不存在 → error 行）→ 在 `org_structure` 按部门/班组名查找或创建节点（节点 id 复用 `normalize_org_nodes` 规则）→ 创建 EnterpriseMember（重复 → skipped）→ 返回 `{imported, skipped, errors: [{row, reason}]}`
- `GET /members/available` → 返回启用成员列表 `[{id, name, email, role, position, org_path}]`（org_path 由 org_node_id 沿树拼部门/班组），供隐患模块责任人选择器复用

**测试**：

- `build_member_import_template` 表头断言；`parse_member_rows` 角色映射/缺邮箱 error/邮箱格式 error；
- 导入端点：构造 xlsx 字节 → 成功导入计数、用户不存在 error 行、重复 skipped；非企业主 403；
- available：启用/停用过滤、org_path 拼接；非企业主读（按现有读权限）语义。

## 步骤

- [ ] **步骤 1：失败测试**（追加）
- [ ] **步骤 2：确认失败 → 步骤 3：实现服务/端点 → 步骤 4：通过 + `python -m pytest tests/ -q` 无回归**
- [ ] **步骤 5：Commit**

```bash
git add backend/app/routers/enterprise_org.py backend/app/services/enterprise_org_service.py backend/tests/test_enterprise_org.py
git commit -m "feat(org): excel member import and available member picker"
```

不要提交 TASKS.md；消息精确匹配；`git diff --check` 干净。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_org_04 --claim-id <claim_id> --exit-code 0 --summary "Excel导入+责任人选择器完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、自审结论。

## 规则

- 严格 TDD；用 `apply_patch` 编辑；只改列出的 3 个文件；阻塞时停下汇报。
