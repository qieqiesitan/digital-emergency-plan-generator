# Codex Custom Subagents task handoff v1

Task: task_02_fix

## 修复任务：任务 2 质量审查 2 项次要建议

### 背景

任务 2（RiskNoticeCard 快照模型）已通过规格审查与质量审查（✅ 通过），质量审查提出 2 项次要建议需要修复。原实现提交：`1ef31a4`（worktree `.worktrees\risk-notice-card`）。

### 修复 1（次要）：迁移 DDL 的 id 补 DEFAULT

文件：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\db_migration_risk_notice_card.sql`

快照表 DDL 中 `id UUID PRIMARY KEY` 改为 `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`（与项目既有迁移惯例一致，见 db_migration_risk_overhaul.sql）。

### 修复 2（次要）：模型 object_id 去掉冗余 index=True

文件：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\app\models\risk_notice_card.py`

`object_id` 的 `index=True` 删除（唯一约束 uq_risk_notice_cards_object 已覆盖相等查询，避免与 DDL 产生双索引；enterprise_id 的 index=True 保留）。

### 范围与限制

* 只改上述 2 个文件。
* 不修改其他文件、不重构、不加新功能。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_service.py -v` 仍 2 passed。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 原提交 1ef31a4），消息：`fix(risk-notice-card): align snapshot ddl and model index`，只含上述 2 个文件。

### 上下文

* worktree：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card`（分支 codex/risk-notice-card）。
* 任务 1 已完成（80a56ed + 88d28b5），任务 2 已提交（1ef31a4）。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
