# Codex Custom Subagents task handoff v1

Task: task_b3_t2

## 任务：validate 接口接入 check_plan + 前端质量报告

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

这是 git 分支 `codex/plan-generation-enhancement` 的隔离 worktree。当前 HEAD 应为 `e56893a`（批 3 任务 1 完成：check_plan 含 6 条规则）。启动时 `cd` 到该目录，`git status` 确认干净。

### 背景

`backend\app\routers\export.py::validate_plan_export` 目前只检查空章节 + Mermaid 类型声明。`check_plan` 已覆盖全部规则（含 Mermaid）。需将 validate 改为调用 check_plan，响应结构保持兼容（warnings 为字符串列表），并确认 export.py 中 enterprise 查询存在。

### 验证命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\ -q --ignore=tests\test_autofill_research.py
```

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\frontend
npx tsc -b
npx vitest run
```

### 步骤 1：validate_plan_export 改为调用 check_plan

修改 `backend\app\routers\export.py::validate_plan_export`（约 306-341 行）：

1. 先读当前函数，确认已查询 plan 与 sections（参考 export_plan_docx 的 enterprise 查询方式），补查 enterprise：

```python
    enterprise = (
        await db.execute(
            select(Enterprise).where(Enterprise.id == plan.enterprise_id)
        )
    ).scalar_one_or_none()
```

（`Enterprise` 已在 export.py 顶部导入。）

2. 函数体替换为：

```python
    from app.services.plan_quality_service import check_plan
    result = check_plan(plan, enterprise, sections)
    # 兼容既有响应：warnings 为字符串列表
    warnings = [
        f"「{w['section_title']}」{w['warning']}"
        for w in result["warnings"]
    ]
    return ApiResponse(data={
        "valid": result["valid"],
        "issues": result["issues"],
        "warnings": warnings,
    })
```

注意：sections 为空时 check_plan 返回 valid=True（无 issue）——检查规格：规格说「空章节预案 valid=false」针对有章节但内容空的情况；sections 列表本身为空时应保持原行为（原实现 issue「Plan has no sections」）。因此 sections 为空时，在调用 check_plan 前保留原逻辑：

```python
    if not sections:
        return ApiResponse(data={
            "valid": False,
            "issues": [{"section_key": "", "section_title": "All", "issue": "预案没有章节"}],
            "warnings": [],
        })
```

### 步骤 2：前端展示质量报告

修改 `frontend\src\pages\Plan\ExportPreviewPage.tsx`：

1. 确认 `exportService` 已有 `validateExport`（若没有则新增，调用 `POST /plans/{id}/export/validate`，参照现有 exportService 写法）。
2. 新增查询：

```typescript
  const { data: validation } = useQuery({
    queryKey: ["exportValidate", id],
    queryFn: () => validateExport(id!),
    enabled: !!id,
  });
```

3. 预览容器上方渲染质量报告（需从 antd 导入 Alert）：

```typescript
        {validation && !validation.valid && (
          <Alert
            type="error"
            showIcon
            message="导出前请修复以下问题"
            description={
              <ul>
                {validation.issues.map((i, idx) => (
                  <li key={idx}>「{i.section_title}」{i.issue}</li>
                ))}
              </ul>
            }
            action={<Button onClick={() => navigate(`/plans/${id}/edit`)}>去编辑</Button>}
          />
        )}
        {validation && validation.warnings.length > 0 && (
          <Alert
            type="warning"
            showIcon
            message="质量提示"
            description={
              <ul>
                {validation.warnings.map((w, idx) => <li key={idx}>{w}</li>)}
              </ul>
            }
            style={{ marginTop: 8 }}
          />
        )}
```

### 步骤 3：验证

运行后端全量测试（忽略 test_autofill_research.py），预期全部通过。
运行 `npx tsc -b` 与 `npx vitest run`，预期通过。

### 步骤 4：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/routers/export.py frontend/src/pages/Plan/ExportPreviewPage.tsx
git commit -m "feat(plan): surface quality report in export preview (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. 后端全量 + 前端 tsc/vitest 结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
