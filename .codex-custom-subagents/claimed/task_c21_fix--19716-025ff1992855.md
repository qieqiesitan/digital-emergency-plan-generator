# Codex Custom Subagents task handoff v1

Task: task_c21_fix

## 任务：修复 C2-1 重要问题（GIS/平面图回归、抽屉字段、徽标状态、类型断言）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 425a725。启动时 `cd` 到该目录，git status 确认干净。

### 重要 1：创建/编辑页补 GIS 定位 + 平面图上传

`frontend/src/pages/Enterprise/EnterpriseCreatePage.tsx` 与 `EnterpriseEditPage.tsx`：在 `EnterpriseInfoCards` 下方保留 GIS 定位（复用 `GisMapPicker`）与平面图上传（复用 `uploadFile` + `floor_plan_url`）区块——参照被重写前原页面的实现（git show 425a725^:frontend/src/pages/Enterprise/EnterpriseCreatePage.tsx 可查原代码）。

- 创建页：GIS 选点 + 平面图上传后把 `gis_lat/gis_lng/floor_plan_url` 并入 `onCreate` payload。
- 编辑页：GIS/平面图随 `onSaved` 一并提交（或单独保存）。

（若原实现较复杂，最简做法：在卡片下方放 GIS 定位按钮（打开 GisMapPicker Modal）+ 平面图 Upload；值存 state，创建/保存时合并进 payload。）

### 重要 2：EnterpriseInfoCards 抽屉补全字段

`frontend/src/components/enterprise/EnterpriseInfoCards.tsx` 抽屉补 10 个缺失字段（参考 EnterpriseEditPage 原表单）：

- 联系与场地：fax（传真）、postal_code（邮编）、land_area（占地面积）、building_area（建筑面积）、building_overview（建筑概况）
- 安全管理：safety_staff_count（安全管理人员数）、fire_approval_date（消防验收日期）、last_plan_filing_date（上次备案日期）、last_plan_filing_authority（上次备案部门）
- 生产与物料：annual_capacity（年生产能力）

（fire_approval_date/last_plan_filing_date 用 DatePicker + dayjs；其余 Input/InputNumber。字段名与 Enterprise 类型一致。）

### 重要 3：报告徽标状态修正 + 刷新联动

`frontend/src/pages/Enterprise/EnterpriseDetailPage.tsx`：

1. 徽标状态映射改为实际可达三态：无报告（404）→「未生成」（橙）；draft →「待合并」（橙）；completed →「已完成」（绿）。移除不可达的「生成中」态（后端 GET 过滤 completed/draft，generating 永不返回）。
2. 报告状态查询加 `refetchOnWindowFocus: true`（覆盖全局 false），用户生成/合并报告后切回页面自动刷新徽标。
3. 若 RiskAssessmentTab/ResourceInvestigationTab 生成/合并后会 invalidate 详情页相关 query key（若它们已 invalidate 通用 key 则无需改；若没有，可在详情页查询 key 保持不变的前提下接受"切页刷新"）。

### 重要 4：values as never 显式类型

`EnterpriseCreatePage.tsx` / `EnterpriseEditPage.tsx`：`onCreate`/`onSaved` 的 payload 用显式类型（`EnterpriseCreate`/`EnterpriseUpdate`）转换而非 `as never`：

```tsx
onCreate={async (values) => {
  const payload = values as unknown as EnterpriseCreate;
  mutation.mutate({ ...payload, gis_lat, gis_lng, floor_plan_url });
}}
```

（GIS/平面图 state 并入 payload；类型转换 `as unknown as` 明确标注边界。）

### 步骤 5：验证 + Commit

运行：`cd frontend && npx tsc -p tsconfig.app.json --noEmit`

再运行：`cd frontend && npx eslint src/pages/Enterprise/ src/components/enterprise/EnterpriseInfoCards.tsx`

预期：无类型/ESLint 错误（无 no-explicit-any）。

```bash
git add frontend/src/pages/Enterprise/ frontend/src/components/enterprise/EnterpriseInfoCards.tsx
git commit -m "fix(enterprise): restore GIS/floor plan inputs, complete drawer fields, fix report badges"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 先读被重写前原页面（git show 425a725^）确认 GIS/平面图原实现，读 GisMapPicker/uploadFile 签名
2. 按步骤修复
3. tsc + eslint 验证
4. 提交
5. 自审：创建/编辑可设 GIS/平面图？抽屉 10 字段可编辑？徽标三态正确 + 切页刷新？无 as never/as any？
6. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、tsc/eslint 结果、提交 SHA、自审发现
