# Codex Custom Subagents task handoff v1

Task: task_org_07

## 目标

执行「企业组织与成员管理」计划任务 7 回归门禁：后端全量、前端全量、迁移幂等、API 冒烟（组织树/成员/导入/AI 建树）；如发现缺陷则修复并提交。**只做验证与必要修复，不做新功能。**

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD=`9306c65`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 验证清单

1. 后端全量：`python -m pytest tests/ -q`（预期约 551+），记录总数与失败；
2. 前端门禁：`npx tsc -b`、`npx vitest run`（约 97）；eslint 以分支改动文件口径（`git diff master...HEAD --name-only` 定位，零新增）；
3. git：`git diff --check`、`git show --check HEAD`；
4. 迁移幂等：`db_migration_enterprise_org.sql` 本地库复跑两遍无报错无重复；
5. API 冒烟（既有测试覆盖引用即可，关键路径未覆盖可补 1-2 个）：组织树读写 422、成员 CRUD 403/404/409、Excel 导入（损坏 400/表头 400/成功）、available、AI 建树降级、search/template；
6. 缺陷修复：如有，提交 `fix(org): regression fixes from org smoke test`。

## 输出

门禁数字、迁移结论、冒烟覆盖、修复 commit（如有）、遗留风险清单。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_org_07 --claim-id <claim_id> --exit-code 0 --summary "组织成员回归门禁完成"
```

## 规则

- 只做验证与必要缺陷修复；不要重构、不要新增功能；阻塞时停下汇报。
