# Codex Custom Subagents task handoff v1

Task: task_b3_t4_fix

## 任务：修复前端重试的两个缺陷

你是一个实现子智能体。代码质量审查发现 `frontend\src\pages\Plan\PlanEditorPage.tsx` 两个缺陷：

1. `startRealtimeGeneration` 新增 `keys?: string[]` 参数后，直接绑定在 `onClick={startRealtimeGeneration}` 的位置会把 MouseEvent 当 keys 传入，导致生成 0 个章节。
2. 成功 batch_done（无失败章节）时 `failedSections` 未清空，旧的失败提示残留。

请修复并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

当前 HEAD 应为 `272f0b6`。启动时 `cd` 到该目录，`git status` 确认干净。

### 验证命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\frontend
npx tsc -b
npx vitest run
```

### 步骤 1：修复 onClick 传参

`startRealtimeGeneration` 签名改为：

```typescript
  const startRealtimeGeneration = useCallback((keys?: string[]) => {
    ...
```

所有直接绑定 onClick 的位置改为箭头函数包裹：

```typescript
                onClick={() => startRealtimeGeneration()}
```

（搜索 `onClick={startRealtimeGeneration}` 全部替换；重试按钮处保持 `onClick={() => { startRealtimeGeneration(failedSections.map(...)); setFailedSections([]); }}`。）

为防御未来误用，在函数开头加类型守卫：

```typescript
    if (keys && !Array.isArray(keys)) {
      keys = undefined;
    }
```

### 步骤 2：成功时清空 failedSections

`batch_done` 分支中，无失败章节时也 `setFailedSections([])`（在 else 分支加）；`error`/`onError` 分支同样清空或保留失败提示由实现者判断（推荐：生成失败但无失败清单时清空，避免残留误导）。

### 步骤 3：验证

运行 `npx tsc -b` 与 `npx vitest run`，预期通过。

### 步骤 4：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add frontend/src/pages/Plan/PlanEditorPage.tsx
git commit -m "fix(plan): guard retry keys param and clear failed sections on success (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. tsc / vitest 结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
