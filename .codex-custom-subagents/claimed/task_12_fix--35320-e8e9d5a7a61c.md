# Codex Custom Subagents task handoff v1

Task: task_12_fix

## 修复任务：任务 12 质量审查 3 项次要建议

### 背景

任务 12（卡片组件 + 预览页）已通过规格审查与质量审查（✅ 通过，附 4 项次要建议，其中二维码占位符合规格不处理）。实现提交 `b941b14`。

### 修复 1（次要）：EMPTY_TEXT 常量抽取

`frontend/src/components/enterprise/RiskNoticeCard.tsx` 与 `frontend/src/pages/Enterprise/RiskNoticeCardPreviewPage.tsx` 的「暂无，请先完善风险评估数据」常量重复。建议在 `frontend/src/components/enterprise/RiskNoticeCard.tsx` 导出该常量（或新建共享常量文件），预览页复用。

### 修复 2（次要）：?ai=1 自动触发时序改进

`RiskNoticeCardPreviewPage.tsx` 的自动触发改为更稳健模式：进入页面时若 `ai=1`，用一次性 state 标志（如 `autoAiTriggered` ref）+ useEffect 触发一次 `runAiOptimize()`，并在触发后清除 URL 参数（可保留 setTimeout 但确保触发先于清参；或以 `useEffect` + 依赖 `[searchParams]` 判断一次后置标志）。

### 修复 3（次要）：保存快照后 refetch await

采用优化版保存快照后：`await refetch()`（失败时 message.error 提示），再关闭对比面板。

### 范围与限制

* 只改 `frontend/src/components/enterprise/RiskNoticeCard.tsx`、`frontend/src/pages/Enterprise/RiskNoticeCardPreviewPage.tsx`。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx tsc -b` 0 错误。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx vitest run` 全部通过。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx eslint src/components/enterprise/RiskNoticeCard.tsx src/pages/Enterprise/RiskNoticeCardPreviewPage.tsx` 0 问题。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend b941b14），消息：`fix(risk-notice-card): dedupe empty text and harden auto ai trigger`，只含上述 2 个文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
