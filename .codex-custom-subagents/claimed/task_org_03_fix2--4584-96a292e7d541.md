# Codex Custom Subagents task handoff v1

Task: task_org_03_fix2

## 目标

按组织任务 3 质量审查的 2 条建议修改修复，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD=`d4bdd58`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1：PUT /members 显式 null 的 role/enabled 不再触发 500**（`backend/app/schemas/enterprise_org.py` + `backend/app/routers/enterprise_org.py`）

- `MemberUpdate` 的 `role`/`enabled` 显式传 `null` 时拒绝 422（文案「role/enabled 不能为 null」），或路由更新时忽略这两个字段的 None（保留 position/org_node_id 的 null 清空语义）。选清晰方案并补测试：
  - 测试 1：`PUT /members` 显式 `{"role": null}` → 422（或 role 不变）；
  - 测试 2：`PUT /members` 显式 `{"position": null}` → 清空生效（保留语义）。

**2：PUT /nodes 透传扩展字段（兼容 onboarding 数据）**（`backend/app/schemas/enterprise_org.py`）

- `OrgNode` 与 `OrgMember` 增加 `model_config = {"extra": "allow"}`，`model_dump()` 保留扩展字段（如成员 `role`/`phone`、节点 `description`），避免整树保存后静默清除 onboarding 数据；
- 补测试：构造含扩展字段的 OrgNode/OrgMember，`model_dump()` 后扩展字段仍在。

## 验证

- `python -m pytest tests/test_enterprise_org.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；`git diff --check` 干净。

## Commit

```bash
git add backend/app/schemas/enterprise_org.py backend/app/routers/enterprise_org.py backend/tests/test_enterprise_org.py
git commit -m "fix(org): reject null role/enabled and preserve extra org node fields"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_org_03_fix2 --claim-id <claim_id> --exit-code 0 --summary "组织任务3质量建议修复2完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的 3 个文件；阻塞时停下汇报。
