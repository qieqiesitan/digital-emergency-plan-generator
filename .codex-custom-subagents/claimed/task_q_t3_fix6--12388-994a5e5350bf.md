# Codex Custom Subagents task handoff v1

Task: task_q_t3_fix6

## 任务：L2 降级为「提取不判定」（用户确认方案 A）

你是一个实现子智能体。经用户确认，L2 法规引用存在性判定因法规库数据质量（7413 个 article 节点、测试节点混入、法规名写法差异）无法可靠实现，**降级为只提取引用、不做存在性判定**。请实施降级并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check`

当前 HEAD 应为 `3e13e3d`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令（必须挂 2_chroma_cache 卷）

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_quality_compliance.py tests/test_plan_quality.py -v
```

### 降级内容

**1. `plan_quality_service.py`：移除 L2 存在性判定**

- 删除 `_load_regulation_index`、`_REG_NODE_TYPES`、`_reg_index_cache`/`_reg_index_loaded` 全局变量、`_regulation_exists`（若已删则忽略）。
- `check_plan` 中删除「L2：法规引用真实性」整段（`reg_index = _load_regulation_index()` 到循环结束）。
- **保留** `_extract_regulation_refs` 函数（供未来法规库治理后启用），并保留 L2 注释说明「暂缓：法规库数据质量不足以支撑存在性判定，待治理后启用」。

```python
# L2 法规引用真实性 —— 暂缓（2026-08-11 用户确认方案 A）
# 法规库 graph.json 含大量 article 节点与测试节点、法规名写法不统一，
# 纯规则无法可靠判定「引用是否存在」；仅保留 _extract_regulation_refs 供治理后启用。
```

**2. `test_plan_quality_compliance.py`：移除存在性判定测试**

- 删除依赖 `_load_regulation_index`/`_REG_NODE_TYPES`/mock 存在性判定的测试：
  - `test_l2_regulation_ref_not_in_library_warning`
  - `test_l2_abolished_regulation_warning`
  - `test_l2_empty_full_name_nodes_do_not_make_refs_exist`
  - `test_l2_law_nodes_included_in_index`
  - `test_l2_standard_number_ref_not_false_positive`
  - `test_l2_short_key_nodes_do_not_mask_missing_refs`
  - `test_l3_more_term_pairs` 保留（L3 不受影响）
- **保留** `test_extract_regulation_refs`、`test_extract_regulation_refs_order_number_1digit`（提取函数仍保留）。
- 确认文件不再 import `_regulation_exists`（若引用则删）。

**3. 规格文档标注暂缓**

修改 `docs\superpowers\specs\2026-08-10-plan-quality-check-enhancement-design.md` §3.2 L2 部分，在规则描述后追加：

```markdown
> **暂缓说明（2026-08-11）**：L2 存在性判定因法规库数据质量（大量 article 节点、测试节点混入、法规名写法不统一）暂缓启用；仅保留引用提取函数 `_extract_regulation_refs`，待法规库数据治理后恢复存在性/废止判定。
```

**4. 验证**

运行：`docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_quality_compliance.py tests/test_plan_quality.py -v`
预期：PASS（L1/L3 测试保留并通过）

运行：`docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/ -q --ignore=tests/test_autofill_research.py`
预期：全部通过

### Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check
git add backend/app/services/plan_quality_service.py backend/tests/test_plan_quality_compliance.py docs/superpowers/specs/2026-08-10-plan-quality-check-enhancement-design.md
git commit -m "fix(plan): defer L2 regulation existence check pending regulation-library data cleanup (quality)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. 降级内容确认（删除项/保留项）
3. pytest 结果
4. commit SHA

不要提交其他文件；不要推送；不要动 TASKS.md。
