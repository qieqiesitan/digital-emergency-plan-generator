# Codex Custom Subagents task handoff v1

Task: t06_nginx_subpath

## 任务：部署可交付性计划任务 6 —— frontend/nginx.conf 增加子路径 location

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。不要读计划文件——本任务文件已包含完整任务文本。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\deploy-readiness`

git 分支 `codex/deploy-readiness` 的隔离 worktree。启动时 `cd` 到该目录，`git status` 确认工作区干净（TASKS.md 若有未提交改动属正常，不要动它）。

### 背景

前端容器 nginx（`frontend/nginx.conf`，监听 8080）需要支持子路径访问 `/emergency-plan-migration/`。此配置与公司网关侧模板（任务 8 的 `deploy/gateway-nginx.conf.example`）不同：本文件用于前端容器自托管形态，dist 位于容器 html 根目录。

### 步骤 1：加入子路径 location

文件：`frontend/nginx.conf`。在 `location /m/ { ... }` 块之后、`location / { ... }` 块之前插入：

```nginx
    # 应用子路径部署：dist 位于容器 html 根目录（alias 指向容器内路径，勿写宿主机路径）
    location /emergency-plan-migration/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /emergency-plan-migration/index.html /emergency-plan-migration/m.html;
    }
```

### 步骤 2：nginx 配置语法校验（docker 容器）

在 worktree 根目录执行：

```bash
docker run --rm -v "${PWD}/frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro" nginx:stable-alpine nginx -t
```

预期：`syntax is ok` / `test is successful`。

### 步骤 3：Commit

```bash
git add frontend/nginx.conf
git commit -m "feat(deploy): add subpath location to frontend nginx config"
```

### 门禁

1. `docker run ... nginx -t` 通过（若本机无 Docker 或无 nginx 镜像，说明原因并记录，待任务 13 端到端演练时一并验证）；
2. `git diff --check` 干净；
3. 提交只含 `frontend/nginx.conf`，提交消息精确匹配步骤 3。

### 汇报格式

完成后汇报：**状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT；你实现了什么；验证结果（nginx -t 输出）；修改的文件；自审发现；任何疑虑。
