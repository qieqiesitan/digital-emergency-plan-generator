# Codex Custom Subagents task handoff v1

Task: task_11_fix2

## 修复任务：任务 11 质量审查 1 项重要 + 若干次要建议

### 背景

任务 11（管理页 + 后端快照供给）已通过规格审查（含复审）与质量审查（✅ 通过，附 1 项重要 + 5 项次要 + 3 项参考建议）。实现提交 `7dab40e` + `9f647a7`。

### 修复 1（重要）：导出 loading 与防重入

`frontend/src/pages/Enterprise/RiskNoticeCardPage.tsx`：加 `const [exporting, setExporting] = useState(false)`；`doExport`/`exportAll` 内 try/finally 控制；「批量导出 Word」与「导出选中卡片 Word」按钮 `loading={exporting} disabled={exporting}`。

### 修复 2（次要）：时间戳计算移到快照分支内

`backend/app/routers/risk_notice_card.py` list_cards：`timestamps`/`max` 计算移到 `if snap:` 分支内（无快照对象不计算）；如方便可抽 service helper（`_source_updated_at(obj, events, measures)`）供 list_cards 与 build_card_data 复用（可选，做简单版即可）。

### 修复 3（次要）：快照标签按 source 显示

`RiskNoticeCardPage.tsx` 快照状态列：`snapshot.source === "rule" ? "规则" : "AI"`（或直接显示 source）。

### 修复 4（次要）：列表查询失败错误提示

`RiskNoticeCardPage.tsx`：useQuery 的 `isError` 时 `message.error("加载失败，请稍后重试")`；空态文案区分错误与无数据。

### 修复 5（次要）：token 重置避免误标快照 stale

`backend/app/routers/risk_notice_card.py` reset_token：当前直接改 `obj.public_token` 后 commit，ORM `onupdate=func.now()` 会刷新 `RiskObject.updated_at`，导致卡片内容未变却被标「数据已变更」。请改为 `await db.execute(update(RiskObject).where(RiskObject.id == object_id).values(public_token=new_token))` + commit（绕过 ORM onupdate，不触碰 updated_at），或采用等效方案，并确认测试仍通过。

### 修复 6（参考·次要）：未评估色值统一 + 下载 URL 复用

* `RiskNoticeCardPage.tsx` 统计行未评估色改用后端返回的 `level_color`（或与后端 `LEVEL_COLORS` 的 #d9d9d9 对齐，删除本地 UNEVALUATED_COLOR 常量）
* 下载 URL 复用 `frontend/src/services/exportService.ts` 的 `getDownloadUrl` helper（如已存在）

### 范围与限制

* 只改 `frontend/src/pages/Enterprise/RiskNoticeCardPage.tsx`、`backend/app/routers/risk_notice_card.py`、`backend/tests/test_risk_notice_card_api.py`（如 reset 方案需要）。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx tsc -b` 0 错误 + `npx vitest run` 全通过。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/ -q` 无回归（407+ passed）。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend），消息：`fix(risk-notice-card): guard export reentry and reset token without stale`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
