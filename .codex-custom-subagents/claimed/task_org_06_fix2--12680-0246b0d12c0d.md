# Codex Custom Subagents task handoff v1

Task: task_org_06_fix2

## 目标

按组织任务 6 质量审查的 3 条低优先建议修复，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD=`1419272`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1：name 类型守卫**（`frontend/src/pages/Enterprise/EnterpriseOrgPage.tsx`）

`validateNodes` 中成员/节点 name 判断改为 `typeof name === "string" && name.trim()`（避免非字符串抛 TypeError）；`handleSaveTree` 的 validate 包 try/catch（校验失败 message.error 而非静默）。

**2：环/自环校验**（`backend/app/services/enterprise_org_service.py` + `frontend/src/pages/Enterprise/EnterpriseOrgPage.tsx`）

- 后端 `validate_org_tree`：新增 `parent_id != 自身` 报错 + 环检测（沿 parent 链 seen 集，发现环报「节点 X 存在循环引用」）；
- 前端 `validateNodes` 同步补自环检查；
- 补测试：自环、双向环被前后端校验拒绝。

**3：service 类型一致性**（`frontend/src/services/enterpriseOrgService.ts`）

- `payload: object` 改为具体类型（`OrgNode`/`MemberCreate`/`MemberUpdate` 等）；
- `r.data.data as T` 解包改为 `api.get<ApiResponse<T>>` 泛型（参照项目惯例）；测试保持通过。

## 验证

- 后端：`python -m pytest tests/test_enterprise_org.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- 前端：`npx tsc -b`、eslint（改动文件）、`npx vitest run` 全部通过；
- `git diff --check` 干净。

## Commit

```bash
git add backend/app/services/enterprise_org_service.py backend/tests/test_enterprise_org.py frontend/src/pages/Enterprise/EnterpriseOrgPage.tsx frontend/src/services/enterpriseOrgService.ts
git commit -m "fix(org): type-guard names, detect cycles and type service payloads"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_org_06_fix2 --claim-id <claim_id> --exit-code 0 --summary "组织任务6类型+环校验修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的文件；阻塞时停下汇报。
