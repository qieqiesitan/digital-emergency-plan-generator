# Codex Custom Subagents task handoff v1

Task: task_q_t3_fix2

## 任务：修复 L1-L3 代码质量审查问题

你是一个实现子智能体。代码质量审查发现 `backend\app\services\plan_quality_service.py` 的 L1-L3 存在以下问题，请修复并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check`

当前 HEAD 应为 `a27df46`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令（必须挂 2_chroma_cache 卷）

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_quality_compliance.py -v
```

### 需修复的问题

**1（死代码）：删除未使用的 `_regulation_exists`**

L2 已改为内联比对，`_regulation_exists` 无调用者。删除该函数。

**2（bug）：令号正则漏 1-2 位号、误抓全角括号**

当前 `（[^）]{0,20}?第?\s*\d{3,4}\s*号）` 只匹配 3-4 位号且限定全角括号。修复为兼容 1-4 位、全/半角括号：

```python
    refs += re.findall(r"[（(][^）)]{0,20}?第?\s*\d{1,4}\s*号[）)]", text)
```

**3（误报）：法规索引含非法规节点**

`graph.json` 的 nodes 可能含 topic/非 standard 节点。`_load_regulation_index` 仅收 `node_type == "standard"` 或含 `full_name` 且非空：

```python
        _reg_index_cache = {
            n.get("full_name", ""): n.get("status", "effective")
            for n in data.get("nodes", [])
            if n.get("full_name") and n.get("node_type") in ("standard", "regulation", None)
        }
```

**4（漏报）：L3 术语对不全**

对照 `prompt_cache.COMPLIANCE_BLOCK` 术语表补全：

```python
    TERM_PAIRS = [
        ("应急救援指挥部", "应急指挥部"),
        ("应急救援小组", "应急小组"),
        ("抢险救援组", "抢险组"),
        ("通讯联络组", "通信联络组"),
        ("疏散引导组", "疏散组"),
    ]
```

**5（范围）：L1 仅检查顶层 required**

规格 §3.2 L1 说「required 顶层章节」。当前已只取顶层（export.py 中 `[item.get("key") for item in tpl.structure if item.get("required")]`）——确认实现确实只取顶层即可，无需改（若 export.py 递归取了子章节需改为仅顶层）。

### 步骤：补充测试

在 `backend\tests\test_plan_quality_compliance.py` 追加：

```python
def test_extract_regulation_refs_order_number_1digit():
    refs = _extract_regulation_refs("依据（应急管理部令第2号）要求")
    assert any("第2号" in r for r in refs)


def test_l3_more_term_pairs():
    enterprise = MagicMock(address="地址", legal_representative="刘昕野", safety_officer="刘昕野")
    plan = MagicMock(plan_type="special")
    result = check_plan(plan, enterprise, [
        _section("sec_2", "应急指挥", "<p>抢险救援组负责，抢险组协调。</p>"),
    ])
    assert any("术语" in w["warning"] for w in result["warnings"])
```

### 完成标准

1. 5 个问题修复（第 5 项为确认）
2. 新增测试通过
3. 全量回归：`docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/ -q --ignore=tests/test_autofill_research.py` 全部通过

### Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check
git add backend/app/services/plan_quality_service.py backend/tests/test_plan_quality_compliance.py
git commit -m "fix(plan): remove dead code, widen order-number regex, filter index nodes, complete term pairs (quality)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. 5 个问题处理方式
3. pytest 结果
4. commit SHA

不要提交其他文件；不要推送；不要动 TASKS.md。
