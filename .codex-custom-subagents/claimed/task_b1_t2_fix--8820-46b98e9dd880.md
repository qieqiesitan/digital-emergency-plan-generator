# Codex Custom Subagents task handoff v1

Task: task_b1_t2_fix

## 任务：补 duplicate_plan 元数据保留测试（修复规格审查 FAIL）

你是一个实现子智能体。规格审查发现任务 2 的 `duplicate_plan` 元数据复制缺少测试覆盖，请补充测试并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

当前 HEAD 应为 `1415de0`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_plan_section_metadata.py -v
```

### 步骤 1：编写失败的测试（duplicate_plan 元数据保留）

在 `backend\tests\test_plan_section_metadata.py` 末尾追加：

```python
from app.models.enterprise import PlanProject, PlanSection
from app.routers.plans import duplicate_plan


def test_duplicate_plan_copies_section_metadata(monkeypatch):
    """duplicate_plan 复制章节时应保留 4 个元数据字段。"""
    # 构造带元数据的原预案与章节
    src = PlanProject(id="src", user_id="u1", enterprise_id="e1", plan_type="onsite", title="原预案")
    sec = PlanSection(
        id="s1", plan_project_id="src", section_key="sec_3_4", title="紧急联系电话",
        level=2, sort_order=0, content="<p>x</p>", ai_generated=False,
        ai_generatable=False, auto_fill=True, auto_fill_source="org_structure",
        data_dependencies=["org_structure"],
    )
    src.sections = [sec]

    # 模拟 db：flush 后给 dup 赋 id；query 返回原预案
    db = MagicMock()
    dup_holder = {}

    async def fake_flush():
        dup_holder["dup"] = db.add.call_args.args[0]
        dup_holder["dup"].id = "dup-id"

    db.flush.side_effect = fake_flush

    # execute 返回带 scalar_one_or_none 的 result
    result = MagicMock()
    result.scalar_one_or_none.return_value = src
    db.execute.return_value = result

    import asyncio
    asyncio.run(duplicate_plan("src", current_user=MagicMock(), db=db))

    dup = dup_holder["dup"]
    added_sections = [c.args[0] for c in db.add.call_args_list if isinstance(c.args[0], PlanSection)]
    contact = next(s for s in added_sections if s.section_key == "sec_3_4")
    assert contact.ai_generatable is False
    assert contact.auto_fill is True
    assert contact.auto_fill_source == "org_structure"
    assert contact.data_dependencies == ["org_structure"]
```

注意：`test_plan_section_metadata.py` 顶部需导入 `MagicMock`（若之前仅在函数内导入则保持函数内导入即可，确保可用）。测试中 `duplicate_plan` 需要真实可调用，`db.flush` 用 async 模拟，`db.commit` 也需 async（可 `db.commit = AsyncMock()`）。请根据实际需要调整 mock 细节，确保测试真实验证「duplicate 后的章节保留元数据」，而不是空跑。

如果 `duplicate_plan` 的调用签名与 mock 不匹配，先读 `backend\app\routers\plans.py` 的 `duplicate_plan` 实现再适配 mock。

### 步骤 2：运行测试验证

运行 pytest 命令。
预期：新增测试 PASS，全部 4 passed。

### 步骤 3：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/tests/test_plan_section_metadata.py
git commit -m "test(plan): verify duplicate_plan preserves section metadata (batch1)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出（几 passed）
3. commit SHA
4. 如有疑虑，说明具体内容

不要运行全量测试；不要提交其他文件；不要推送；不要动 TASKS.md。
