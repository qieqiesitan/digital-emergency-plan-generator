# Codex Custom Subagents task handoff v1

Task: task_b3_t1_fix

## 任务：修复质量校验空白归一化 + 补档案字段匹配测试

你是一个实现子智能体。规格审查发现 `backend\app\services\plan_quality_service.py` 规则 3（关键档案信息未体现）未做空白归一化，正文含换行/空白时会误报；且缺档案字段匹配的显式测试。请修复并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

当前 HEAD 应为 `88164f0`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_plan_quality.py -v
```

### 步骤 1：追加失败的测试

在 `backend\tests\test_plan_quality.py` 末尾追加：

```python
def test_archive_field_match_ignores_whitespace():
    enterprise = MagicMock(address="西安市高新区一路1号", legal_representative="张三", safety_officer="李四")
    plan = MagicMock()
    # 正文中地址被换行/空格打断，应视为已体现（归一化后匹配）
    result = check_plan(plan, enterprise, [
        _section("sec_1", "总则", "<p>企业地址：西安市高新区\n一路1号，法人：张三</p>"),
    ])
    assert not any("地址" in w["warning"] for w in result["warnings"])
```

### 步骤 2：运行测试验证失败

运行 pytest 命令。
预期：新增测试 FAIL（当前未归一化，产生「正文未体现企业档案地址」warning）。

### 步骤 3：实现空白归一化

修改 `backend\app\services\plan_quality_service.py`：

1. 新增辅助函数：

```python
def _normalize(text: str) -> str:
    """去除所有空白字符，用于档案字段模糊匹配。"""
    return re.sub(r"\s+", "", text or "")
```

2. 规则 3 的字段匹配改为归一化比较：

```python
        norm_text = _normalize(text)
        for field, label in [
            (getattr(enterprise, "address", None), "地址"),
            (getattr(enterprise, "legal_representative", None), "法定代表人"),
            (getattr(enterprise, "safety_officer", None), "安全负责人"),
        ]:
            if field and field not in ("（待补充）",) and _normalize(field) not in norm_text:
                warnings.append({
                    "section_key": s.section_key,
                    "section_title": s.title,
                    "warning": f"正文未体现企业档案{label}：{field}",
                })
```

注意：占位符检测与疑似地址检测仍用原始 `text`，不受影响。

### 步骤 4：运行测试验证通过

运行 pytest 命令。
预期：PASS（5 passed）。

### 步骤 5：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/services/plan_quality_service.py backend/tests/test_plan_quality.py
git commit -m "fix(plan): normalize whitespace in archive field matching (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出（几 passed）
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
