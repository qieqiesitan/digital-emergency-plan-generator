# Codex Custom Subagents task handoff v1

Task: task_b1_fix2

## 任务：修复 B1 质量审查问题（唯一索引 + 错误文案 + test 端点认证）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 40bf552。启动时 `cd` 到该目录，git status 确认干净。

### 问题 1：系统配置唯一性保护（关键）

迁移 `backend/db_migration_ai_config_system.sql` 末尾追加：

```sql
-- 系统级配置唯一（user_id 为 NULL 的行）
CREATE UNIQUE INDEX IF NOT EXISTS uq_ai_configs_system ON ai_configs (is_system) WHERE user_id IS NULL;
```

模型 `backend/app/models/enterprise.py` 的 AIConfig 增加唯一索引声明（在 __table_args__ 或类内）：

```python
__table_args__ = (
    Index(
        "uq_ai_configs_system",
        "is_system",
        unique=True,
        postgresql_where=text("user_id IS NULL"),
    ),
)
```

（确保 `Index` 与 `text` 已从 sqlalchemy 导入；若 AIConfig 目前无 __table_args__ 则新增。）

### 问题 2：错误文案统一（重要）

统一为「系统未配置 AI 模型，请联系管理员」，替换以下残留：

- backend/app/routers/risk_assessment.py：`请先配置 AI 模型` → 统一文案
- backend/app/routers/surrounding_ai.py（2 处）：`请先在系统设置中配置 AI 模型` → 统一文案
- backend/app/routers/resource_investigation.py：`"ERROR"` 占位符 → 统一文案（该行本次改动已触碰）

其余文件中本次改动已统一的文案不动；若同文件还有其它 `"ERROR"` 占位符（251/270/441/444 等）属于存量问题，仅当与本次改动同一函数且低成本时一并替换，否则记录不动。

### 问题 3：/ai-config/test 端点加认证（重要）

`backend/app/routers/ai_config.py` 的 `test_ai_connection` 增加 `_=Depends(require_admin)`：

```python
@router.post("/ai-config/test", response_model=ApiResponse[AITestResult])
async def test_ai_connection(data: AITestRequest, _=Depends(require_admin)):
```

### 步骤 4：补迁移 SQL 内容测试

新建 `backend/tests/test_ai_config_migration.py`：

```python
def test_migration_sql_has_system_unique_index():
    from pathlib import Path
    sql = Path(__file__).resolve().parents[1] / "db_migration_ai_config_system.sql"
    text = sql.read_text(encoding="utf-8")
    assert "DROP NOT NULL" in text
    assert "ADD COLUMN IF NOT EXISTS is_system" in text
    assert "CREATE UNIQUE INDEX IF NOT EXISTS uq_ai_configs_system" in text
    assert "user_id IS NULL" in text
```

（路径按实际迁移文件位置调整。）

### 步骤 5：全量后端测试

运行：`cd backend && python -m pytest tests/ -q`

预期：全部 PASS（与基线一致；新增迁移测试通过）。

### 步骤 6：Commit

```bash
git add backend/db_migration_ai_config_system.sql backend/app/models/enterprise.py backend/app/routers/risk_assessment.py backend/app/routers/surrounding_ai.py backend/app/routers/resource_investigation.py backend/app/routers/ai_config.py backend/tests/test_ai_config_migration.py
git commit -m "fix(ai): enforce system config uniqueness, unify error copy, auth test endpoint"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 逐一实现 3 个修复 + 迁移测试
2. 全量测试
3. 提交
4. 自审：唯一索引在迁移与模型两侧一致？错误文案全部统一？test 端点已认证？迁移测试通过？
5. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、测试结果、提交 SHA、自审发现
