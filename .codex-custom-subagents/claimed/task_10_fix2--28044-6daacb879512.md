# Codex Custom Subagents task handoff v1

Task: task_10_fix2

## 目标

按任务 10 代码质量审查建议修复 2 项小改（第 2 项页面抽取共享组件按项目「页面自包含」惯例接受为债务，不重构），提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`f1940f6`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1：提取 `_max_level` 辅助**（`backend/app/services/risk_notice_card_service.py`）

`compute_level` 与 `compute_inherent_level` 仅字段与默认值不同，提取私有辅助：

```python
def _max_level(events: list, attr: str, default: str) -> str | None:
    values = {getattr(ev, attr) for ev in events if getattr(ev, attr, None)}
    return max(values, key=lambda v: LEVEL_ORDER.get(v, 0)) if values else default
```

（`LEVEL_ORDER` 按现有实现；保持两个公开函数行为不变，内部调用辅助；测试保持通过。）

**2：权限种子 action 用 slug 惯例**（`backend/db_migration_data_dicts_permission.sql`）

`permissions` 行 `action` 由 `'view'` 改为 `'data_dicts'`（与 seed_roles.sql 菜单权限惯例一致：`menu:prompts`→`'prompts'`、`menu:regulations`→`'regulations'`）；同步更新 `backend/tests/test_data_dict.py` 权限断言中的 action 值。

**3（记录，不重构）：两页共享抽取**——`DataDictManagePage`/`EnterpriseDictConfigPage` 约 40-50% 重复，符合项目「页面自包含」既有惯例，质量审查亦标注不阻塞；接受为债务，在最终报告中说明即可。

## 验证

- 后端：`python -m pytest tests/test_risk_notice_card_service.py tests/test_data_dict.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- 前端：`npx tsc -b`、eslint（如有前端改动）、`npx vitest run` 通过；
- `git diff --check` 干净。

## Commit

```bash
git add backend/app/services/risk_notice_card_service.py backend/db_migration_data_dicts_permission.sql backend/tests/test_data_dict.py backend/tests/test_risk_notice_card_service.py
git commit -m "refactor(risk): dedupe level helpers and align permission action slug"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_10_fix2 --claim-id <claim_id> --exit-code 0 --summary "任务10小改修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的文件；阻塞时停下汇报。
