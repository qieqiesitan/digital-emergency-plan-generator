# Codex Custom Subagents task handoff v1

Task: task_b23_fix

## 任务：修复 B2-3 质量审查问题（测试 RuntimeWarning + groups 类型检查）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 f204355。启动时 `cd` 到该目录，git status 确认干净。

### 问题 1（必须）：测试 RuntimeWarning

`backend/tests/test_onboarding_org.py`：仿照 `test_onboarding_extract.py` 的 fake_config 模式，monkeypatch `get_system_ai_config`：

```python
def test_generate_org_candidates_parses_llm_json(monkeypatch):
    async def fake_llm(messages, ai_config, timeout=120):
        return '{"groups": [{"group_key": "cmd", "group_name": "应急救援指挥部", "members": [{"role": "总指挥", "name": "", "phone": ""}]}, {"group_key": "rescue", "group_name": "抢险救援组", "members": []}]}'
    async def fake_config(db):
        return object()
    monkeypatch.setattr("app.services.onboarding_service.llm_text_completion", fake_llm)
    monkeypatch.setattr("app.services.onboarding_service.get_system_ai_config", fake_config)
    result = asyncio.run(generate_org_candidates({"name": "甲公司", "industry": "化工"}, AsyncMock()))
    assert result[0]["group_key"] == "cmd"
    assert result[0]["members"][0]["name"] == ""
```

（去掉裸 `db = AsyncMock()` 的协程泄漏，改用 fake_config。）

### 问题 2（可选，顺手）：groups 类型检查

`backend/app/services/onboarding_service.py` 的 `generate_org_candidates` 防御解析与 `classify_modules` 对称：

```python
    raw_groups = parsed.get("groups") or []
    if not isinstance(raw_groups, list):
        return []
    return [g for g in raw_groups if isinstance(g, dict)]
```

### 步骤 3：测试验证 + Commit

运行：`cd backend && python -m pytest tests/test_onboarding_org.py -v`

再全量：`python -m pytest tests/ -q`（与基线一致，无新增失败，无新 RuntimeWarning）。

```bash
git add backend/app/services/onboarding_service.py backend/tests/test_onboarding_org.py
git commit -m "fix(onboarding): clean org test mock and guard groups type"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 按步骤实现
2. 测试验证（确认无 RuntimeWarning）
3. 提交
4. 自审：无协程泄漏警告？groups 类型守卫对称？
5. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、测试结果、提交 SHA、自审发现
