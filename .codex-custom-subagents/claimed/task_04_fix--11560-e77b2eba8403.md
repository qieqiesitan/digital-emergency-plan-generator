# Codex Custom Subagents task handoff v1

Task: task_04_fix

## 修复任务：任务 4 质量审查 2 项重要 + 3 项次要建议

### 背景

任务 4（ai-review-signs 端点）已通过规格审查与质量审查（✅ 通过，附 2 项重要 + 5 项次要建议）。实现提交 `dcbc54d`。

### 修复 1（重要）：响应组装移入 try，畸形元素 502 兜底

`backend/app/routers/risk_notice_card.py`：`SignSuggestion(**suggestion)` 与 `original_signs=current_signs` 组装目前在 try/except 之外——AI 返回畸形元素（add: [123]）或畸形快照 signs（缺 name/svg_name）会裸 500。请：
* 将响应组装移入 try（`except Exception` → logger + 502「AI 审查失败，请稍后重试或保留原版」）
* current_signs 在组装前用 `normalize_signs` 规范化（过滤非法元素，与规格 §10「非法静默丢弃」一致）

### 修复 2（重要）：回退基线对齐卡片展示

`build_right_column(events, measures)` 改为传入快照 content（与 `build_card_data` 一致）：存在旧快照（无 signs）时，端点 current_signs 应基于快照 accident_types 而非源数据，避免建议删除卡片未展示的标志。建议抽共享 helper（快照 content 选择）供 router 与 service 复用（可选，至少保证语义一致）。

### 修复 3（次要）：502 文案统一

统一为设计规格 §10 口径：「AI 审查失败，已保留原版」（替代「请稍后重试或保留原版」）；同步更新计划文档相关处（如需要）。

### 修复 4（次要）：测试补强

`backend/tests/test_risk_notice_card_api.py` 补：端点 404（风险点不存在）、502（review_signs 抛异常）、HTTPException 透传、快照优先（original_signs 返回快照标志）；原测试断言 `original_signs is not None` 改为断言非空且含预期 svg_name。

### 范围与限制

* 只改 `backend/app/routers/risk_notice_card.py`、`backend/tests/test_risk_notice_card_api.py`（如需 service 小改可含）。
* 不修改前端。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\ai-sign-review\backend && python -m pytest tests/test_risk_notice_card_api.py -v` 全部 PASS。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\ai-sign-review\backend && python -m pytest tests/ -q` 无回归（425+ passed）。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\ai-sign-review show --check HEAD` 干净。
* 提交新 commit（不要 amend dcbc54d），消息：`fix(risk-notice-card): harden review endpoint assembly and align baseline`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
