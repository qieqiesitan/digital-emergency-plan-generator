# Codex Custom Subagents task handoff v1

Task: task_13_fix

## 修复任务：任务 13 质量审查 1 项重要 + 2 项次要建议

### 背景

任务 13（公开只读页）已通过规格审查，质量审查 ❌ 需修复（1 项重要 + 2 项次要）。实现提交 `10803c4`。

### 修复 1（重要）：移动端纵向布局

`frontend/src/components/enterprise/RiskNoticeCard.tsx` 的 RNC_CSS 增加 `@media (max-width: 520px)`：
* `.rnc-body` 改为纵向堆叠（`flex-direction: column`，左右栏 `width: 100%`）
* 安全标志缩至 48px
* 其余样式适配窄屏（头部/表格/信息块保持可读）

公开页（手机扫码主场景）与预览页共享组件，一并生效。此修复同时在任务 15 回归的手工冒烟中验证。

### 修复 2（次要）：网络错误图标区分与重试

`frontend/src/pages/PublicRiskNoticePage.tsx`：`isError` 时按 `error.response?.status === 404` 区分——404 用 `Result status="404"`「卡片不存在或链接已失效」；其他错误用 `Result status="warning"` 同文案 + 「重新加载」按钮（`onClick={() => refetch()}`）。

### 修复 3（次要）：页面测试

检查项目前端测试基础设施：若已配置 @testing-library/react（package.json / 既有组件测试），为 PublicRiskNoticePage 补 RTL 测试（token 渲染成功 / 404 错误分支）；若无 RTL 基础设施，按项目惯例（仅 utils/services/store 有测试）不引入新依赖，在任务 15 回归以手工冒烟覆盖，并如实说明。

### 范围与限制

* 只改 `frontend/src/components/enterprise/RiskNoticeCard.tsx`、`frontend/src/pages/PublicRiskNoticePage.tsx`（如需测试文件则新增）。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx tsc -b` 0 错误。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx vitest run` 全部通过。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx eslint src/components/enterprise/RiskNoticeCard.tsx src/pages/PublicRiskNoticePage.tsx` 0 问题。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 10803c4），消息：`fix(risk-notice-card): add mobile layout and error retry for public page`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
