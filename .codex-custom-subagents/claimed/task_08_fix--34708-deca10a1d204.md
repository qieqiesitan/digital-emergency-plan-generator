# Codex Custom Subagents task handoff v1

Task: task_08_fix

## 修复任务：任务 8 质量审查 1 项重要 + 5 项次要建议

### 背景

任务 8（docx 导出 + 二维码）已通过规格审查与质量审查（✅ 通过，附 1 项重要 + 5 项次要建议）。实现提交 `61458ff`。

### 修复 1（重要）：导出循环内重复查询提出循环

`backend/app/routers/risk_notice_card.py` 的 export 端点：`select(RiskObject).where(enterprise_id==...).order_by(created_at)` 在 `for oid` 循环体内重复执行 N 次。请把全企业 objects 查询提出循环只执行一次（`compute_code` 需要），并在循环内复用它。归属校验与 `load_events_and_measures` 可保持（或合并为一次带 selectinload 查询，若实现简单）。

### 修复 2（次要）：qrcode pin 版本

`backend/requirements.txt`：`qrcode` 改为 `qrcode==8.2`（当前已装版本）。

### 修复 3（次要）：导出异常日志与友好错误

export 端点 `render_cards_docx` 异常时：`logger.exception(...)` 后抛 `HTTPException(500, "导出失败，请稍后重试")`（对齐 export.py 惯例）。

### 修复 4（次要）：文件名避免同秒覆盖

文件名加微秒或随机后缀：`risk-notice-{enterprise_id[:8]}-{YYYYMMDDHHMMSS}-{微秒或短随机}.docx`。

### 修复 5（次要）：docx 渲染用 to_thread

export 端点调用 `render_cards_docx` 用 `await asyncio.to_thread(render_cards_docx, ...)` 避免阻塞事件循环（对齐 export.py 惯例）。

### 修复 6（次要）：占位盲区测试改进

`backend/tests/test_risk_notice_card_docx.py` 集成测试：校验标志 drawing 实际渲染尺寸（解包内嵌 PNG 宽高 > 1），避免占位 PNG 掩盖真实渲染失败。

### 范围与限制

* 只改 `backend/app/routers/risk_notice_card.py`、`backend/requirements.txt`、`backend/tests/test_risk_notice_card_docx.py`。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_docx.py tests/test_risk_notice_card_api.py -v` 全部 PASS。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/ -q` 无回归（全量 400+ passed）。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 61458ff），消息：`fix(risk-notice-card): dedupe export queries and harden file naming`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
