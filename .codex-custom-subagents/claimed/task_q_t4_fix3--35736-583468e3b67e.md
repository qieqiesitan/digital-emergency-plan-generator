# Codex Custom Subagents task handoff v1

Task: task_q_t4_fix3

## 任务：E1 收敛为仅组织架构电话 + 修复 E2/E3（用户确认方案 2）

你是一个实现子智能体。经用户确认，E1 正文电话格式检查**完全去掉**（正文数字语义判断纯正则不可靠，易误报身份证/证件号），只保留「组织架构成员无电话」检查。同时修复代码质量复审发现的 E2/E3 问题。请实施并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check`

当前 HEAD 应为 `1798848`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令（必须挂 2_chroma_cache 卷）

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_quality.py -v
```

### 需修复的问题

**1（E1 收敛）：删除正文电话格式检查**

`check_plan` 中「E1：联系电话格式」的正文循环（`for s in sections: ... re.findall(r"\d{5,}", cleaned) ...`）整段删除，仅保留组织架构成员无电话检查：

```python
    # ── E1：组织架构联系电话完整性（正文电话格式检查已收敛移除：
    #    正文数字语义判断纯正则不可靠，易误报身份证/证件号）──
    for g in (getattr(enterprise, "org_structure", None) or []):
        for m in g.get("members", []):
            if m.get("name") and not m.get("phone"):
                warnings.append({
                    "section_key": "",
                    "section_title": "",
                    "warning": f"企业组织架构中{m.get('name')}（{m.get('position','') or m.get('role','')}）无联系电话",
                })
```

删除相关测试 `test_e1_invalid_phone_format`、`test_e1_landline_with_hyphen_no_false_positive`、`test_e1_id_number_not_flagged`（或改写为仅组织架构场景）。

**2（E2 拼接误报）：position/role 分别检查**

当前 `org_positions` 用 `(position or "") + (role or "")` 拼接，导致「position=组长、role=总指挥」拼接成「组长总指挥」，`"总指挥" in org_positions` 为 True 误判已有总指挥。修复：分别取两字段、各自作为候选：

```python
    org_roles = {
        role
        for g in (getattr(enterprise, "org_structure", None) or [])
        for m in g.get("members", [])
        for role in (m.get("position") or "", m.get("role") or "")
        if role
    }
```

**3（E2 重复告警）：规则 1/2 合并**

当前规则 1（档案缺总指挥/副总指挥）与规则 2（正文提及指挥机构但档案无总指挥）在同一情形重复告警。修复：合并为一条综合告警：

```python
    # ── E2：关键岗位覆盖 ──
    has_commander = "总指挥" in org_roles
    has_deputy = "副总指挥" in org_roles
    if not has_commander or not has_deputy:
        # 规则 1：档案缺岗位
        missing = []
        if not has_commander:
            missing.append("总指挥")
        if not has_deputy:
            missing.append("副总指挥")
        warnings.append({
            "section_key": "",
            "section_title": "",
            "warning": f"企业组织架构缺少{'、'.join(missing)}",
        })
        # 规则 2：正文提及指挥机构但档案缺总指挥（仅当规则 1 已报且缺总指挥时补充）
        if not has_commander:
            for s in sections:
                text = _strip_html(s.content)
                if ("应急指挥" in text or "指挥机构" in text) and text.strip():
                    warnings.append({
                        "section_key": s.section_key,
                        "section_title": s.title,
                        "warning": "正文提及应急指挥机构，但企业组织架构未设置总指挥",
                    })
```

**4（E3 NULL 数量）：NULL 视为未知不报**

E3 第三条数量检查中，`quantity` 为 None 时不应报「数量为 0」。确认逻辑只统计 `quantity == 0`（不统计 None）。若当前实现 `getattr(r, "quantity", 0)` 会把 None 保留为 None，`None == 0` 为 False → 已不报，但确认即可；若实现把 None 当 0 则修正。

### 步骤：调整测试

- 删除/改写 E1 相关测试（见问题 1）。
- 保留 `test_e2_role_field_counts_as_position`（org_roles 应能识别 role="总指挥"）。
- 追加：

```python
def test_e2_combined_position_role_no_false_commander():
    enterprise = MagicMock(address="地址", legal_representative="刘昕野", safety_officer="刘昕野")
    enterprise.org_structure = [
        {"group_name": "指挥部", "members": [
            {"name": "李四", "role": "总指挥", "position": "组长", "phone": "13800000000", "responsibilities": ""},
        ]},
    ]
    plan = MagicMock(plan_type="special")
    result = check_plan(plan, enterprise, [
        _section("sec_1", "事故风险分析", "<p>内容</p>"),
    ])
    # 有 role=总指挥 → 不应报缺总指挥
    assert not any("总指挥" in w["warning"] and "缺少" in w["warning"] for w in result["warnings"])
```

### 完成标准

1. 4 个问题修复
2. 测试通过
3. 全量回归：`docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/ -q --ignore=tests/test_autofill_research.py` 全部通过

### Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check
git add backend/app/services/plan_quality_service.py backend/tests/test_plan_quality.py
git commit -m "fix(plan): converge E1 to org phone completeness, merge E2 rules, guard E3 null quantities (quality)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. 4 个问题处理方式
3. pytest 结果
4. commit SHA

不要提交其他文件；不要推送；不要动 TASKS.md。
