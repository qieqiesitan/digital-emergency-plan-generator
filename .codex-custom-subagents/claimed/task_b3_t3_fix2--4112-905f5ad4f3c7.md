# Codex Custom Subagents task handoff v1

Task: task_b3_t3_fix2

## 任务：修复 SSE 批量生成完成后未重置 _active_generations

你是一个实现子智能体。规格复审发现 `backend\app\routers\generation.py::generate_batch`（SSE）的 `run_background` finally 只放 sentinel 关闭 SSE 流，未重置 `_active_generations[plan_id]`（background 版本有 `pop`），导致生成完成后该预案仍被判定「正在生成中」，`GET /generate/status` 的 generating 恒为 true，且 background 端点会拒绝再次生成。请修复并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

当前 HEAD 应为 `f414e5f`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_generation_batch_refactor.py -v
```

### 步骤 1：编写失败的测试

在 `backend\tests\test_generation_batch_refactor.py` 追加（验证 generate_batch 的 run_background finally 会清理 _active_generations；通过直接调用生成任务并捕获其 finally 行为不现实，改为验证模块级辅助函数，若无辅助函数则测试「generate_batch 定义中存在清理逻辑」的静态断言不可取——更优方案：把清理逻辑抽为模块级函数 `_clear_generation_state(plan_id)` 并测试它）：

先读 `generate_batch` 的 run_background finally 结构（约 585-595 行），确认当前只有 `await event_queue.put(None)`。然后：

```python
def test_clear_generation_state_resets_active_flag():
    import app.routers.generation as gen
    gen._active_generations["p1"] = True
    gen._failed_sections["p1"] = [{"section_key": "sec_1", "title": "总则"}]
    gen._clear_generation_state("p1")
    assert gen._active_generations.get("p1", False) is False
    assert gen._failed_sections.get("p1") == [{"section_key": "sec_1", "title": "总则"}]
```

### 步骤 2：运行测试验证失败

运行 pytest 命令。
预期：FAIL，`AttributeError: module 'app.routers.generation' has no attribute '_clear_generation_state'`。

### 步骤 3：实现辅助函数与 finally 清理

修改 `backend\app\routers\generation.py`：

1. 模块级新增（放在 `_failed_sections` 定义附近）：

```python
def _clear_generation_state(plan_id: str) -> None:
    """批量生成结束后清除生成中标记（保留失败清单供前端查询）。"""
    _active_generations[plan_id] = False
```

2. `generate_batch`（SSE）的 `run_background` finally 中，在 sentinel 前调用：

```python
        finally:
            _clear_generation_state(plan_id)
            await event_queue.put(None)  # Sentinel to close SSE
```

3. `generate_batch_background` 的 finally 中现有 `_active_generations.pop(plan_id, None)` 保持（或改为 `_clear_generation_state(plan_id)`，行为等价；注意不要 pop，因为 status 端点需要读 generating=False 的稳定值——确认 pop 后 `get(plan_id, False)` 也返回 False，两者等价，选择其一并保持一致即可）。

### 步骤 4：运行测试验证通过

运行 pytest 命令。
预期：PASS（2 passed）。

### 步骤 5：全量回归

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\ -q --ignore=tests\test_autofill_research.py
```

预期：全部通过。

### 步骤 6：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/routers/generation.py backend/tests/test_generation_batch_refactor.py
git commit -m "fix(plan): reset generating flag after SSE batch completes (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出与全量回归结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
