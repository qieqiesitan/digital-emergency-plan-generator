# Codex Custom Subagents task handoff v1

Task: task_c12_fix2

## 任务：修复 EnterpriseInfoCards 复审遗漏（established_date initialValue 转 Dayjs + 卡片时间戳）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 e296302。启动时 `cd` 到该目录，git status 确认干净。

### 关键：established_date initialValue 必须转 Dayjs

`frontend/src/components/enterprise/EnterpriseInfoCards.tsx` 的 `fieldInit`：对 `established_date` 返回 `dayjs(enterpriseRecord.established_date)`（无值返回 undefined），其它字段原样：

```tsx
const fieldInit = (key: string) => {
  const raw = (enterpriseRecord as Record<string, unknown>)[key];
  if (raw === null || raw === undefined) return undefined;
  if (key === "established_date") {
    return raw ? dayjs(raw as string) : undefined;
  }
  return raw;
};
```

（确认 `fieldInit` 当前实现并修正——DatePicker 的 initialValue 必须是 Dayjs，否则编辑模式打开抽屉崩溃。）

### 重要：日期卡片 dayjs 值显示 YYYY-MM-DD

`displayValue` 对 `established_date` 统一 `dayjs(raw).format("YYYY-MM-DD")`（无论 raw 是 Dayjs 还是字符串）：

```tsx
const displayValue = (key: string, raw: unknown) => {
  if (raw === null || raw === undefined || raw === "") return undefined;
  if (key === "established_date") {
    const d = dayjs(raw as string | dayjs.Dayjs);
    return d.isValid() ? d.format("YYYY-MM-DD") : undefined;
  }
  return String(raw);
};
```

### 次要（顺手）：payload 断言与 CARD_FIELDS 类型

- 保存序列化处：`payload.established_date = dayjs(payload.established_date as dayjs.Dayjs | string).format("YYYY-MM-DD")`（接受 string 或 Dayjs，类型诚实）。
- `CARD_FIELDS` 类型可保持 `key: string`（`keyof Enterprise | string` 实际等价 string，不构成缺陷，可不动）。

### 步骤 4：验证 + Commit

运行：`cd frontend && npx tsc -p tsconfig.app.json --noEmit`

再运行：`cd frontend && npx eslint src/components/enterprise/EnterpriseInfoCards.tsx`

预期：tsc/eslint 无错误。

```bash
git add frontend/src/components/enterprise/EnterpriseInfoCards.tsx
git commit -m "fix(enterprise): convert established_date to dayjs in init and display"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 先读 EnterpriseInfoCards.tsx 的 fieldInit/displayValue 当前实现
2. 按步骤修复
3. tsc + eslint 验证
4. 提交
5. 自审：编辑模式打开抽屉不再崩溃（initialValue 是 Dayjs）？卡片显示 YYYY-MM-DD？自动填充后卡片正确？
6. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、tsc/eslint 结果、提交 SHA、自审发现
