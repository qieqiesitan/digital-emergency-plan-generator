# Codex Custom Subagents task handoff v1

Task: icon_07_fix

## 目标

修复任务 7 质量审查发现的格式回归：`frontend/src/components/enterprise/RiskSourceForm.tsx:150` 的 `icon={<AppIcon name="location" size={14} />}` 缩进为 16 空格，应为 14 空格（与 151-152 行 onClick/disabled 对齐）。

## 工作目录

所有工作、git 操作在隔离工作区：
`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\icon-system`

当前 HEAD 为 `459dff3`。前端命令在 `frontend/` 目录下运行。

## 任务步骤

### 步骤 1：修正缩进

`RiskSourceForm.tsx:150` 该行行首缩进由 16 个空格改为 14 个空格，其余内容不变：

```tsx
<Button
  icon={<AppIcon name="location" size={14} />}
  onClick={() => setFloorPlanOpen(true)}
```

### 步骤 2：门禁

在 `frontend/` 运行：
- `npx tsc -b` → exit 0
- `npx vitest run` → 130 passed
- `npx eslint src/components/enterprise/RiskSourceForm.tsx` → 仅 2 项既有债（:79 `v as any`、:131 `_desc` 未用），不得清理

### 步骤 3：Commit

```bash
git add frontend/src/components/enterprise/RiskSourceForm.tsx
git commit -m "fix(icon-system): align RiskSourceForm icon line indentation"
```

提交后确认 `git show --stat HEAD` 恰 1 文件、`git show --check HEAD` 干净。

## 约束

- 只改这一行缩进；不清理既有 lint 债；不提交临时文件。

## 认领方式（必须先做）

在目录 `C:\Users\55061\Documents\数字化预案自动生成 2` 下运行：

```
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace . --agent deepseek_anthropic_worker --model deepseek-v4-flash --provider deepseek_anthropic
```

无子命令即原子认领一个 pending 任务，返回 JSON 含 `path`；**先读该文件**再开始。认领 status 非成功立即上报。

## 汇报格式

- **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 改动内容、门禁结果、提交 SHA、任何疑虑
