# Codex Custom Subagents task handoff v1

Task: task_b1_t2

## 任务：模板元数据复制到章节（_create_sections_from_template + duplicate_plan）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成 TDD 实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

这是 git 分支 `codex/plan-generation-enhancement` 的隔离 worktree。当前 HEAD 应为 `8cf653a`（任务 1 已完成：PlanSection 已有 ai_generatable/auto_fill/auto_fill_source/data_dependencies 4 个字段）。启动时 `cd` 到该目录，运行 `git status` 确认干净，`git log --oneline -1` 确认 HEAD。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_plan_section_metadata.py -v
```

### 步骤 1：追加失败的测试

在已有文件 `backend\tests\test_plan_section_metadata.py` 末尾追加：

```python
from unittest.mock import MagicMock
from app.routers.plans import _create_sections_from_template


def test_create_sections_copies_metadata_recursively():
    db = MagicMock()
    structure = [{
        "key": "sec_3", "title": "应急组织", "level": 1, "sort_order": 0,
        "ai_generatable": True, "auto_fill": False, "auto_fill_source": None,
        "data_dependencies": [],
        "subsections": [{
            "key": "sec_3_4", "title": "紧急联系电话", "level": 2, "sort_order": 0,
            "ai_generatable": False, "auto_fill": True,
            "auto_fill_source": "org_structure", "data_dependencies": ["org_structure"],
            "subsections": [],
        }],
    }]
    _create_sections_from_template(db, "plan-1", structure)
    added = [c.args[0] for c in db.add.call_args_list]
    contact = next(s for s in added if s.section_key == "sec_3_4")
    assert contact.ai_generatable is False
    assert contact.auto_fill is True
    assert contact.auto_fill_source == "org_structure"
    assert contact.data_dependencies == ["org_structure"]
```

### 步骤 2：运行测试验证失败

运行上面的 pytest 命令。
预期：新增测试 FAIL（`assert contact.ai_generatable is False` 处失败，当前默认 True）；原有 2 个测试仍 PASS。

### 步骤 3：实现元数据复制

修改 `backend\app\routers\plans.py` 的 `_create_sections_from_template` 函数（约 39-57 行），在 PlanSection 构造中追加 4 个字段，从模板 item 读取：

```python
        section = PlanSection(
            id=str(uuid4()),
            plan_project_id=plan_id,
            section_key=key,
            title=title,
            level=level,
            sort_order=sort_order,
            content=None,
            ai_generated=False,
            ai_generatable=item.get("ai_generatable", True),
            auto_fill=item.get("auto_fill", False),
            auto_fill_source=item.get("auto_fill_source"),
            data_dependencies=item.get("data_dependencies", []),
        )
```

### 步骤 4：duplicate_plan 复制元数据

修改 `backend\app\routers\plans.py` 的 `duplicate_plan` 函数（约 177-192 行），在 PlanSection 复制构造中追加：

```python
        ns = PlanSection(
            plan_project_id=dup.id, section_key=s.section_key,
            title=s.title, level=s.level, sort_order=s.sort_order,
            content=s.content, ai_generated=s.ai_generated,
            ai_generatable=s.ai_generatable, auto_fill=s.auto_fill,
            auto_fill_source=s.auto_fill_source,
            data_dependencies=s.data_dependencies,
        )
```

### 步骤 5：运行测试验证通过

运行 pytest 命令。
预期：PASS（3 passed）。

### 步骤 6：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/routers/plans.py backend/tests/test_plan_section_metadata.py
git commit -m "feat(plan): copy template metadata into sections (batch1)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出（几 passed）
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

注意：不要运行全量测试；不要提交其他文件；不要推送；不要动 TASKS.md。
