# Codex Custom Subagents task handoff v1

Task: task_03_fix

## 修复任务：任务 3 规格审查发现的问题（SVG 路径少一层 backend）

### 背景

任务 3（常量数据）实现提交 `94960e9` 已通过大部分规格核对，但规格审查发现 1 个问题：

`backend/tests/test_risk_notice_card_data.py` 第 29 行：

```python
sign_dir = Path(__file__).resolve().parents[2] / "app" / "static" / "signs"
```

测试文件位于 `backend/tests/`，`parents[2]` 解析到 worktree 根目录，路径变成 `<根>/app/static/signs`（**缺少 backend 层**）。而 SVG 资产将建在 `backend/app/static/signs/`。不改的话，任务 4 建好资产后该测试仍不会转绿。

### 修复内容

1. `backend/tests/test_risk_notice_card_data.py` 第 29 行：`parents[2]` 改为 `parents[1]`（解析到 backend 目录 → `backend/app/static/signs`）。
2. 计划文档 `C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\docs\superpowers\plans\2026-08-11-risk-notice-card.md` 中任务 3 的测试代码块里同样的 `parents[2]` 写法一并改为 `parents[1]`（搜索 "parents[2]" 全部替换为该文件中的正确层级；注意只改测试代码块中指向 app/static/signs 的那处）。

### 范围与限制

* 只改上述 2 个文件。
* 不修改其他代码。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_data.py -v` 仍 3 passed 1 failed（failed 仍是 SVG 未创建，属预期）。
* `rg -n "parents\[2\]" backend/tests/test_risk_notice_card_data.py docs/superpowers/plans/2026-08-11-risk-notice-card.md` 确认无残留（计划文档中若无其他 parents[2] 引用则无输出）。
* 提交新 commit（不要 amend 94960e9），消息：`fix(risk-notice-card): correct sign dir path in tests and plan`，只含上述 2 个文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
