# Codex Custom Subagents task handoff v1

Task: task_09_pages

## 目标

实现「风险分级管控增强（A 阶段）」任务 9：管控清单页 + 重大风险公示页 + 公开脱敏页（前端），按门禁完成并提交。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD 以任务 8 复审后实际为准）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 背景

任务 8 后端已就绪：`GET /control-list`（筛选+分页）、`GET /control-list/export`（xlsx）、`GET /risk-publicity`（token/enterprise_name/items/zones/generated_at）、`POST /risk-publicity/token`、`GET /public/risk/{token}`（脱敏 items）。前端约定：vitest 仅 service/utils；组件靠 tsc/lint + 手工冒烟（任务 12）。

## 文件

- 创建：`frontend/src/pages/Enterprise/RiskControlListPage.tsx`
- 创建：`frontend/src/pages/Enterprise/RiskPublicityPage.tsx`
- 创建：`frontend/src/pages/PublicRiskPage.tsx`
- 修改：`frontend/src/services/riskManagementService.ts`（4 个新方法 + fetchPublicRisk）
- 修改：`frontend/src/services/riskManagementService.test.ts`（新方法 URL 断言，可选）
- 修改：`frontend/src/App.tsx`（3 个路由）
- 修改：`frontend/src/pages/Enterprise/RiskManagementTab.tsx`（顶部「管控清单」「重大风险公示」按钮）

## 步骤

- [ ] **步骤 1：service 封装**（`frontend/src/services/riskManagementService.ts`）

```typescript
export async function getControlList(enterpriseId: string, params: object) {
  return api.get(`/enterprises/${enterpriseId}/risk-management/control-list`, { params });
}
export async function exportControlList(enterpriseId: string) {
  return api.get(`/enterprises/${enterpriseId}/risk-management/control-list/export`, { responseType: "blob" });
}
export async function getRiskPublicity(enterpriseId: string) {
  return api.get(`/enterprises/${enterpriseId}/risk-management/risk-publicity`);
}
export async function resetRiskPublicityToken(enterpriseId: string) {
  return api.post(`/enterprises/${enterpriseId}/risk-management/risk-publicity/token`);
}
export async function fetchPublicRisk(token: string) {
  return api.get(`/public/risk/${token}`);
}
```

（按现有 api 封装风格；补 1-2 个 service 测试断言 URL。）

- [ ] **步骤 2：管控清单页**（`RiskControlListPage.tsx`）

- `useQuery(["risk-control-list", enterpriseId, filters, page], () => getControlList(...))`；
- 筛选行：楼层/分区/等级/管控层级 Select + 关键字 Input（按后端参数名 floor_id/zone_id/level/control_level/keyword）；
- Table 列：分区/风险点/单元/事故类型/固有等级（Tag 色）/现有等级（Tag 色，复用 `RISK_LEVEL_COLORS`）/管控层级/管控措施（ellipsis tooltip）/责任单位/责任人；
- 分页（total 来自响应）；右上「导出 Excel」：`exportControlList` → blob 下载（按现有项目下载先例）+ message；
- 顶部「返回」。

- [ ] **步骤 3：公示页与公开页**

`RiskPublicityPage.tsx`：
- `useQuery(["risk-publicity", enterpriseId], getRiskPublicity)`；
- 展示四色图（现有模式，复用 `RiskDistributionStage`，数据由后端 `zones` 转换或直接按组件契约传入；若组件契约不匹配，做最小适配并说明）；
- 重大风险清单 Table（items：分区/风险点/事故类型/现有等级/管控层级/责任单位/主要措施）；
- 公开链接卡片：`${location.origin}/p/risk/${token}` + 复制按钮（message 提示）+「重置链接」（Modal 二次确认调 `resetRiskPublicityToken` 后刷新 query）；`generated_at` 展示（本地时间换算）；
- `@media print` 样式隐藏按钮只留公告内容；
- 顶部「返回」。

`PublicRiskPage.tsx`：
- `useParams` 取 token → `useQuery(["public-risk", token], () => fetchPublicRisk(token), { retry: false })`；
- 加载 Spin；错误 `isAxiosError && status===404` → Result 404「链接已失效」，其他错误 → Result warning + 重试按钮；
- 成功：企业名 + 生成时间 + 脱敏清单表（分区/风险点/事故类型/现有等级/管控层级/责任单位/主要措施）；
- 顶部提示条「公开只读页面 · 数据已脱敏 · 无需登录」。

`App.tsx` 路由：`/enterprises/:id/risk-control-list`、`/enterprises/:id/risk-publicity`（ProtectedRoute 内）；`/p/risk/:token`（公开，无守卫，参考 `/r/:token` 位置）。

`RiskManagementTab.tsx`：顶部加「管控清单」「重大风险公示」按钮（跳转对应路由，参考「风险告知卡」按钮先例）。

- [ ] **步骤 4：门禁**

在 `frontend` 目录：`npx tsc -b`、`npx eslint`（上述页面/service/Tab 文件）、`npx vitest run`，全部通过；`git diff --check` 干净。

- [ ] **步骤 5：Commit**

```bash
git add frontend/src/pages/Enterprise/RiskControlListPage.tsx frontend/src/pages/Enterprise/RiskPublicityPage.tsx frontend/src/pages/PublicRiskPage.tsx frontend/src/services/riskManagementService.ts frontend/src/services/riskManagementService.test.ts frontend/src/App.tsx frontend/src/pages/Enterprise/RiskManagementTab.tsx
git commit -m "feat(risk): control list page, publicity page and public token page"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_09_pages --claim-id <claim_id> --exit-code 0 --summary "管控清单+公示+公开页完成"
```

最终回复报告：task_id、claim_id、commit SHA、门禁结果、自审结论。

## 规则

- 用 `apply_patch` 编辑；只改上述文件（如需额外文件请说明理由）；阻塞时停下汇报。
