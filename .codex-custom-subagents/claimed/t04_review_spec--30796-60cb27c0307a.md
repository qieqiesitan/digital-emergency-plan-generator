# Codex Custom Subagents task handoff v1

Task: t04_review_spec

## 任务：规格合规审查 —— 任务 4（入口与布局剥前缀）

你是一个规格合规审查子智能体。验证实现者是否构建了所要求的内容（不多不少）。**不要信任实现者的报告**，必须独立阅读实际代码逐项核对。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\deploy-readiness`

git 分支 `codex/deploy-readiness`。相关提交：`b470cf1`（`feat(deploy): strip app base in entry, menu and mobile tab paths`），父提交 `3c1dca4`。

### 要求的内容（任务 4 规格）

1. `frontend/src/entry.tsx`：import `stripAppBase`；`isMobilePath` 改为 `const p = stripAppBase(window.location.pathname); return p === "/m" || p.startsWith("/m/");`；
2. `frontend/src/main.tsx`：同上；
3. `frontend/src/layouts/MainLayout.tsx`：import `stripAppBase`；`selectedKeys={[stripAppBase(location.pathname)]}`；
4. `frontend/src/mobile/layouts/MainTabsLayout.tsx`：import `stripAppBase`；`const pathname = stripAppBase(location.pathname);` 用于 `shouldHideTabBar(pathname)`、`pattern.test(pathname)`、依赖数组 `[pathname, ...]`、`key={pathname}`；
5. 提交只含上述 4 个文件，提交消息精确匹配；门禁：tsc 0、vitest 52、eslint 无新增。

### 实现者声称

4 文件按规格修改；提交 b470cf1 仅 4 文件；tsc 0、eslint 0 error（1 个既有 warning）、vitest 52；集成验证确认 `/emergency-plan-migration/m/login` 剥前缀后直接加载移动端 App，MobileRedirect 自循环消除。

### 你的工作

1. `git show b470cf1 --stat` 确认提交范围；
2. 通读全量 diff，逐项对照上述 5 项要求（注意 MainTabsLayout 的 4 处 pathname 使用点是否全部替换）；
3. 运行 `cd frontend && npx tsc -b`、`npx vitest run` 确认门禁；
4. 检查是否有规格外改动（如 MobileRedirect 被擅自修改）。

### 汇报格式

- ✅ 符合规格（经代码检查后一切匹配）
- ❌ 发现问题：[具体列出缺失/多余/理解偏差，附带 file:line 引用]
