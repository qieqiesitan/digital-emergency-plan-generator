# Codex Custom Subagents task handoff v1

Task: task_b1_t3

## 任务：SectionResponse schema 加元数据字段

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成 TDD 实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

这是 git 分支 `codex/plan-generation-enhancement` 的隔离 worktree。当前 HEAD 应为 `cbc75aa`（任务 1、2 已完成：PlanSection 已有 4 个元数据字段并随模板复制）。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_plan_section_metadata.py -v
```

### 步骤 1：追加失败的测试

在 `backend\tests\test_plan_section_metadata.py` 末尾追加：

```python
from app.schemas.plan import SectionResponse


def test_section_response_has_metadata_fields():
    resp = SectionResponse(
        id="s1", section_key="sec_3_4", title="紧急联系电话", level=2,
        sort_order=0, content="", ai_generated=False,
        updated_at="2026-08-08T00:00:00",
        ai_generatable=False, auto_fill=True,
        auto_fill_source="org_structure", data_dependencies=["org_structure"],
    )
    assert resp.ai_generatable is False
    assert resp.auto_fill is True
    assert resp.auto_fill_source == "org_structure"
    assert resp.data_dependencies == ["org_structure"]
```

### 步骤 2：运行测试验证失败

运行上面的 pytest 命令。
预期：新增测试 FAIL（`TypeError: SectionResponse.__init__() got an unexpected keyword argument 'ai_generatable'`）；其余测试 PASS。

### 步骤 3：实现 schema 字段

修改 `backend\app\schemas\plan.py` 的 `SectionResponse` 类（约 21-26 行），追加：

```python
    ai_generatable: bool = True
    auto_fill: bool = False
    auto_fill_source: str | None = None
    data_dependencies: list = []
```

### 步骤 4：运行测试验证通过

运行 pytest 命令。
预期：PASS（5 passed）。

### 步骤 5：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/schemas/plan.py backend/tests/test_plan_section_metadata.py
git commit -m "feat(plan): expose section metadata in API schema (batch1)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出（几 passed）
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

注意：不要运行全量测试；不要提交其他文件；不要推送；不要动 TASKS.md。
