# Codex Custom Subagents task handoff v1

Task: task_b2_fix

## 任务：补齐 diagrams.py / external.py 调用点的化学品数据（B2 补充修复）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 2463ab8。启动时 `cd` 到该目录，git status 确认干净。

### 背景

B2 给 `_collect_enterprise_data` 增加了 `chemicals` 参数（默认空 dict），但两个调用点未传真实化学品数据：

- `backend/app/routers/diagrams.py`（约 53 行，图表再生成）
- `backend/app/routers/external.py`（约 99 行，外部批量生成）

这两个路径的生成上下文缺少化学品清单，导致这些场景的预案生成不含危化品信息。

### 步骤 1：补齐两个调用点

逐一读这两个文件，找到 `_collect_enterprise_data(...)` 调用处，在调用前查询化学品并传参（与 generation.py 内既有模式一致）：

```python
from app.models.hazardous_chemicals import HazardousChemical
chemicals_rows = (await db.execute(
    select(HazardousChemical).where(HazardousChemical.enterprise_id == enterprise_id)
)).scalars().all()
chemicals = {c.id: c for c in chemicals_rows}
```

然后调用改为 `_collect_enterprise_data(enterprise, risk_context, resources, chemicals)`（按该文件实际变量名调整）。

注意：若 external.py 的调用签名与 generation.py 不同（如不同参数名/顺序），按实际调整；确保 chemical 数据进入生成上下文。若该文件已有化学品查询则复用。

### 步骤 2：相关测试 + 全量后端测试

运行相关测试（diagrams/external/generation 相关）：`cd backend && python -m pytest tests/test_plan_diagram_service.py tests/test_generation_enterprise_data.py tests/test_batch_context.py -q`

再全量：`python -m pytest tests/ -q`

预期：全部 PASS（与基线一致，无新增失败）。

### 步骤 3：Commit

```bash
git add backend/app/routers/diagrams.py backend/app/routers/external.py
git commit -m "fix(generation): pass chemical data in diagrams and external batch generation"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 逐一读文件确认调用签名再改
2. 测试验证
3. 提交
4. 自审：两个调用点都传了 chemicals？无签名残留？
5. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、测试结果、提交 SHA、自审发现
