# Codex Custom Subagents task handoff v1

Task: task_07_toggle

## 目标

实现「风险分级管控增强（A 阶段）」任务 7：四色图双模式切换（工作台 + 总览前端），按门禁完成并提交。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`；任务 6 完成后 HEAD 会是其最新提交，以实际为准）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 背景

任务 6 已让后端 Zone/Workbench/Hierarchy 响应带 `inherent_max_level`/`inherent_effective_color`（默认 None 兼容）。本任务在前端加「现有风险图 / 固有风险图」切换。前端约定：vitest 仅 service/utils（无组件测试），组件靠 tsc/lint + 手工冒烟（任务 12）。

## 文件

- 修改：`frontend/src/types/riskManagement.ts`（HierarchyZone / WorkbenchZone 类型加 2 字段）
- 修改：`frontend/src/pages/Enterprise/RiskMappingWorkbenchPage.tsx`
- 修改：`frontend/src/pages/Enterprise/RiskOverviewPage.tsx`
- 修改：`frontend/src/components/enterprise/riskMapping/RiskDistributionStage.tsx`（入参支持 mode）
- 修改：`frontend/src/components/enterprise/RiskOverviewMatrix.tsx`（入参支持 mode）
- 如有其他消费 zone 颜色的组件（工作台分布图渲染），按需同步（说明）。

## 步骤

- [ ] **步骤 1：类型扩展**

`frontend/src/types/riskManagement.ts`：`HierarchyZone` 与 workbench zone 类型增加 `inherent_max_level?: string | null`、`inherent_effective_color?: string | null`。

- [ ] **步骤 2：工作台切换**（`RiskMappingWorkbenchPage.tsx`）

- 顶部渲染 `Segmented`：`[{label:"现有风险图", value:"current"}, {label:"固有风险图", value:"inherent"}]`，state `colorMode`（默认 current，可存 localStorage 提升体验，非必须）；
- 区域填充色：`colorMode === "inherent" ? (z.inherentEffectiveColor ?? z.effectiveColor) : z.effectiveColor`（无固有数据时回退现有色）；
- 图例/说明文案随模式切换（「区域颜色 = 该区域 现有/固有 最大风险等级」）；
- 切换不清空已绘制内容（仅展示层变化）。

- [ ] **步骤 3：总览切换**（`RiskOverviewPage.tsx` + 两个子组件）

- `RiskOverviewPage.tsx`：在现有视图 Segmented 旁加第二组 Segmented（现有/固有）；`colorMode` state；传入 `RiskDistributionStage` 与 `RiskOverviewMatrix` 的 `mode`；切换时清除 `highlightZone`/`treeSelectedKeys`；
- `RiskDistributionStage` / `RiskOverviewMatrix`：`mode: "current" | "inherent"` 入参，取对应 max level 与 color（`inherentMaxLevel ?? maxLevel`、`inherentEffectiveColor ?? effectiveColor` 回退），等级标签/颜色映射沿用现有逻辑。

- [ ] **步骤 4：门禁**

在 `frontend` 目录：`npx tsc -b`、`npx eslint src/pages/Enterprise/RiskMappingWorkbenchPage.tsx src/pages/Enterprise/RiskOverviewPage.tsx src/components/enterprise/riskMapping/RiskDistributionStage.tsx src/components/enterprise/RiskOverviewMatrix.tsx`、`npx vitest run`，全部通过（既有用例不回归）。

- [ ] **步骤 5：Commit**

```bash
git add frontend/src/types/riskManagement.ts frontend/src/pages/Enterprise/RiskMappingWorkbenchPage.tsx frontend/src/pages/Enterprise/RiskOverviewPage.tsx frontend/src/components/enterprise/riskMapping/RiskDistributionStage.tsx frontend/src/components/enterprise/RiskOverviewMatrix.tsx
git commit -m "feat(risk): add inherent/current four-color map toggle in workbench and overview"
```

不要提交 TASKS.md；消息精确匹配；`git diff --check` 干净。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_07_toggle --claim-id <claim_id> --exit-code 0 --summary "四色图双模式前端切换完成"
```

最终回复报告：task_id、claim_id、commit SHA、门禁结果、自审结论。

## 规则

- 用 `apply_patch` 编辑；只改上述列出的文件（如需额外文件请说明理由）；阻塞时停下汇报。
