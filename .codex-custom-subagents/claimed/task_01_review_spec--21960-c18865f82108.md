# Codex Custom Subagents task handoff v1

Task: task_01_review_spec

## 规格合规审查：任务 1（数据库迁移 + RiskObject 模型字段）

你正在审查一个实现是否与其规格匹配。**不要信任实现者的报告，必须独立阅读实际代码验证。**

### 要求的内容（任务 1 规格）

**文件：**

* 创建：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\db_migration_risk_notice_card.sql`
* 修改：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\app\models\risk_management.py`（RiskObject 类，image_url 与 is_risk_point 之间）
* 测试：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\tests\test_risk_notice_card_service.py`（新建，含模型字段断言）

**测试**：`test_risk_object_has_notice_card_fields` 断言 RiskObject 表列包含 `responsible_unit`、`responsible_person`、`contact_phone`、`public_token`。

**迁移 SQL** 要求：

* `ALTER TABLE risk_objects ADD COLUMN` 四字段（responsible_unit VARCHAR(255)、responsible_person VARCHAR(100)、contact_phone VARCHAR(50)、public_token VARCHAR(64)），幂等（IF NOT EXISTS）
* 存量空值行补随机 token（md5(random()||clock_timestamp()) 截 64）
* `ALTER COLUMN public_token SET NOT NULL`
* 唯一索引 `uq_risk_objects_public_token`

**模型**：RiskObject 类 4 个 mapped_column 字段（3 个可空 + public_token 非空唯一，默认随机 token）。

**范围限制**：只改这 3 个文件；不创建快照模型/服务；不重构无关代码；commit 信息精确 `feat(risk-notice-card): add risk object notice fields and migration`。

### 实现者声称构建了什么

* commit `80a56ed`（worktree `.worktrees\risk-notice-card`，分支 codex/risk-notice-card），3 文件 26 行
* 按 TDD：先建测试失败，迁移 SQL，模型 4 字段，pytest 1 passed，提交后工作区干净
* 声称范围合规（未创建其他文件）

### 你的工作

1. `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card`，运行 `git show 80a56ed` 查看 diff，逐行核对上述要求。
2. 检查：
* **缺失**：是否实现所有要求？字段类型/可空/唯一/默认值是否正确？迁移幂等性？
* **多余**：是否添加规格外内容？
* **理解偏差**：字段是否在 RiskObject 类内？（注意：`backend/app/models/risk_management.py` 第 110 行附近还有一处 responsible_person，需确认它属于哪个类、是否既有字段、是否被误改）
* **提交范围**：`git show --stat 80a56ed` 是否只含 3 个目标文件？提交消息是否精确匹配？
* **门禁实测**：`cd backend && python -m pytest tests/test_risk_notice_card_service.py -v` 是否 PASS？
3. 报告格式：
* ✅ 符合规格（经代码检查后一切匹配）
* ❌ 发现问题：[具体列出缺失/多余/偏差，附带 file:line 引用]

### 上下文

* worktree 是独立分支 codex/risk-notice-card，审查只读，不修改任何文件、不提交。
* 该测试文件后续任务会扩展（任务 2 会追加快照模型测试），本任务只审查任务 1 的实现。
* `__import__("secrets")` 属于代码风格问题（规格未禁止），归入质量审查范畴，规格审查不必作为失败项（可备注）。
