# Codex Custom Subagents task handoff v1

Task: task_c14_fix

## 任务：修复 C1-4 关键缺陷（企业信息保存/采纳覆盖/去重/完成状态一致）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 873107e。启动时 `cd` 到该目录，git status 确认干净。

### 关键 1：StepEnterprise 保存落库

`frontend/src/pages/Onboarding/StepEnterprise.tsx`：`onSaved` 改为调用 `updateEnterprise` 真正保存：

```tsx
import { updateEnterprise } from "@/services/enterpriseService";

<EnterpriseInfoCards
  enterprise={enterprise}
  onSaved={async (values) => {
    await updateEnterprise(enterpriseId, values as any);
    queryClient.invalidateQueries({ queryKey: ["enterprise", enterpriseId] });
    queryClient.invalidateQueries({ queryKey: ["completion", enterpriseId] });
  }}
/>
```

（`as any` 若触发 lint 用 `as EnterpriseUpdate` 或 Record 转换；保存失败应抛错让 EnterpriseInfoCards 的调用方感知——若组件内部已 catch 则此处可 message.error。）

### 重要 2：StepOrg 采纳保存防覆盖 + 失败反馈

`frontend/src/pages/Onboarding/StepOrg.tsx`：

1. `accepted` 使用前等待加载：`isLoading` 时禁用「全部采纳」按钮（`disabled={isLoading}`）。
2. `saveMut` 增加 `onError: () => message.error("保存失败，请重试")`。
3. 全部采纳改用 `mutateAsync`，成功后再 `setCandidates([])`：

```tsx
const adoptAll = async () => {
  if (isLoading) return;
  const merged = [...accepted];
  candidates.forEach(g => {
    const key = g.group_key || g.group_name || `g-${merged.length}`;
    if (!merged.some(x => x.group_key === key || x.group_name === key)) merged.push({ ...g, group_key: key });
  });
  try {
    await saveMut.mutateAsync(merged);
    setCandidates([]);
  } catch {
    // onError 已提示
  }
};
```

### 重要 3：去重 fallback 稳定 key

如上述：fallback key 用 `group_name`（稳定）而非依赖 `merged.length` 计数。

### 重要 4：completed 与后端 completion 一致

`frontend/src/pages/Onboarding/OnboardingPage.tsx`：

1. 定义 STEPS key 与后端 module key 映射：

```ts
const MODULE_KEY_MAP: Record<string, string> = {
  enterprise: "enterprise_info",
  org: "org_structure",
  risk: "risk_chemical",
  resources: "resources",
  surrounding: "surrounding",
  generate: "reports",
};
```

2. `completed` 初始化为 `new Set<string>()`，用 useEffect 根据 completion.modules 的 done 同步：

```tsx
useEffect(() => {
  if (!completion) return;
  const done = new Set<string>();
  completion.modules.forEach(m => {
    if (m.done) {
      const stepKey = Object.entries(MODULE_KEY_MAP).find(([, v]) => v === m.key)?.[0];
      if (stepKey) done.add(stepKey);
    }
  });
  setCompleted(done);
}, [completion]);
```

3. 侧栏完成度百分比显示加 loading 态（completion 未返回时显示「–」而非 0%）。

### 步骤 5：验证 + Commit

运行：`cd frontend && npx tsc -p tsconfig.app.json --noEmit`

再运行：`cd frontend && npx eslint src/pages/Onboarding/`

预期：tsc/eslint 无错误。

```bash
git add frontend/src/pages/Onboarding/
git commit -m "fix(onboarding): persist enterprise edits, safe adopt, align completion state"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 先读 StepEnterprise.tsx / StepOrg.tsx / OnboardingPage.tsx 现状
2. 按步骤修复
3. tsc + eslint 验证
4. 提交
5. 自审：企业信息保存落库？采纳不覆盖已保存数据（加载完成才可采纳）？去重稳定？completed 与 completion 一致？
6. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、tsc/eslint 结果、提交 SHA、自审发现
