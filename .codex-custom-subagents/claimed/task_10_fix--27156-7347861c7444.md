# Codex Custom Subagents task handoff v1

Task: task_10_fix

## 修复任务：任务 10 质量审查 1 项重要 + 3 项次要建议

### 背景

任务 10（前端类型 + service + 入口路由）已通过规格审查与质量审查（✅ 通过，附 1 项重要 + 3 项次要建议）。实现提交 `33a353d`。

### 修复 1（重要）：riskManagement.ts 移除未契约字段

`frontend/src/types/riskManagement.ts` 中 RiskObject 新增的 4 字段（responsible_unit/responsible_person/contact_phone/public_token）回退删除：后端 `RiskObjectResponse` 不返回这些字段，其中 `public_token` 放共享客户端类型有泄露风险（公开 URL 统一用卡片 API 的 public_url）。`responsible_*` 待任务 14 后端扩展 Response/Update schema 后再加。

### 修复 2（次要）：exportCards 返回 warnings

`frontend/src/services/riskNoticeCardService.ts`：`exportCards` 改为返回 `{ file_key, warnings }`（后端 ExportResponse 含 warnings），并更新 service 测试断言。

### 修复 3（次要）：补 service 测试覆盖

`frontend/src/services/riskNoticeCardService.test.ts` 补：fetchCardDetail（GET 详情 URL）、aiOptimize（POST）、saveSnapshot（PUT body content）、resetToken（POST → public_url）、fetchPublicCard 返回解包断言。

### 修复 4（次要）：路由参数命名统一

`frontend/src/routes/index.tsx` 新路由参数统一为 `:id/:objectId`（与兄弟风险路由 `:id` 风格一致；功能无影响，纯一致性）。注意：后续任务 11-13 页面 useParams 会按此参数名读取。

### 范围与限制

* 只改 `frontend/src/types/riskManagement.ts`、`frontend/src/services/riskNoticeCardService.ts`、`frontend/src/services/riskNoticeCardService.test.ts`、`frontend/src/routes/index.tsx`。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx tsc -b` 0 错误。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\frontend && npx vitest run` 全部通过。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 33a353d），消息：`fix(risk-notice-card): align frontend contracts and route params`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
