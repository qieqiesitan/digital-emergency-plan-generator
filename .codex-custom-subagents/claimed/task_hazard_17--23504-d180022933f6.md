# Codex Custom Subagents task handoff v1

Task: task_hazard_17

## 目标

执行隐患管理任务 17「回归门禁」自动化部分（不含浏览器手工冒烟——那部分由用户在主控协调下验证），并核对迁移幂等与字典种子。

## 工作目录

- **代码与 git 只读验证目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，HEAD=`c8dff5b`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 门禁清单（逐项执行并记录证据）

1. **后端全量**：`python -m pytest tests/ -q` 全绿（预期 952 passed，Event loop ResourceWarning/closed pipe 为既有非失败噪音；若全量数有出入以实际为准并说明）。汇总本分支后端测试文件清单与各文件用例数（`tests/test_hazard_*.py`）。
2. **前端全量**：`npx tsc -b` exit 0；`npx vitest run` 全绿（预期 13 文件 111 passed）；`npx eslint` 对分支新增/修改的前端文件逐一 exit 0（列出文件清单）。
3. **迁移幂等**：`backend/db_migration_hazard_management.sql` 在本地 PostgreSQL 容器（如 `emergency-plan-db`，按任务 1 先例）复跑两遍均 exit 0；程序化核对：hazard_* 10 张表 + hazard_checklist_templates 全部对象存在且无重复；字典种子行数核对（deadline_rules 3 / publicity_scope 3 / source_type 5 / record_status_label 7）；系统检查表模板 5 张（enterprise_id NULL + is_system TRUE）；`reminder_notified_at` 列存在（任务 8 补列）。
4. **分支审计**：`git log --oneline master..HEAD` 列出本分支全部 commit（预期 12-15 个 feat/fix，消息与任务契约一致）；`git diff master..HEAD --check` 干净；确认 TASKS.md 未在任何 commit 中（`git log --oneline -- TASKS.md master..HEAD` 应为空）。
5. **不修改任何源码**：只读验证；若发现必须修复的缺陷，停下汇报（不要自行提交，由主控安排 fix 任务）。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_hazard_17 --claim-id <claim_id> --exit-code 0 --summary "隐患回归门禁通过"
```

最终回复报告：task_id、claim_id、逐项门禁证据（命令+输出摘要）、迁移核对结果、分支 commit 清单、结论（✅ 通过 / ❌ 发现缺陷需修复，列缺陷清单）。

## 规则

- 全程只读；不修改任何源码/迁移/测试文件；阻塞或发现缺陷时停下汇报。
- 全程用简体中文交流。
