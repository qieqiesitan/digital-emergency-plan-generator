# Codex Custom Subagents task handoff v1

Task: task_b22_fix

## 任务：修复 B2-2 质量审查重要问题（防御式解析/测试 mock/错误类型）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 7d16d41。启动时 `cd` 到该目录，git status 确认干净。

### 问题 1（重要）：LLM 输出防御

`backend/app/services/onboarding_service.py`：

- `extract_candidates`：`parsed.get("items") or []`，并对每项做 `isinstance(item, dict)` 过滤（非 dict 项丢弃）。
- `classify_modules`：`parsed.get("modules") or []`，并确保是 list（若为 None/非 list 返回 []）。

```python
    raw_items = parsed.get("items") or []
    return [item for item in raw_items if isinstance(item, dict)]
```

```python
    raw_modules = parsed.get("modules") or []
    if not isinstance(raw_modules, list):
        return []
    return [m for m in raw_modules if isinstance(m, str) and m in MODULE_SCHEMA_HINTS]
```

### 问题 2（重要）：测试 mock 修正 + 补无配置测试

`backend/tests/test_onboarding_extract.py`：

- 用 `monkeypatch` 掉 `get_system_ai_config`：`async def fake_config(db): return object()`（或 AsyncMock(return_value=object())），替代当前 AsyncMock 子属性 coroutine 写法。
- 追加无配置测试：

```python
def test_extract_raises_when_no_ai_config(monkeypatch):
    async def fake_config(db):
        return None
    monkeypatch.setattr("app.services.onboarding_service.get_system_ai_config", fake_config)
    import pytest
    with pytest.raises(ValueError):
        asyncio.run(extract_candidates("risk_chemical", "文本", AsyncMock()))
```

- 将测试中的模块 key `"chemical"` 改为 `"risk_chemical"`（真实模块 key）。

### 问题 3（重要，顺手）：统一未配置错误语义

在 `backend/app/services/onboarding_service.py` 抽一个内部 helper（或直接用 `from app.services.risk_ai_service import _get_ai_config` 获取配置——若可行且无循环依赖），让 extract/classify 未配置时与 `_get_ai_config` 一致抛 `HTTPException(400, "系统未配置 AI 模型，请联系管理员")`。若引入 HTTPException 会造成服务层与 FastAPI 耦合（项目内 risk_ai_service 已如此），保持一致即可；否则保留 ValueError 并在注释说明由路由层转 400。

### 步骤 4：测试验证 + Commit

运行：`cd backend && python -m pytest tests/test_onboarding_extract.py -v`

再全量：`python -m pytest tests/ -q`（与基线一致，无新增失败）。

```bash
git add backend/app/services/onboarding_service.py backend/tests/test_onboarding_extract.py
git commit -m "fix(onboarding): defensive LLM output parsing, fix config mock, align error semantics"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 按步骤实现
2. 测试验证
3. 提交
4. 自审：items/modules 防御完整？无配置测试通过？无 RuntimeWarning？模块 key 已改真实值？
5. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、测试结果、提交 SHA、自审发现
