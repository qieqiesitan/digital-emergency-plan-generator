# Codex Custom Subagents task handoff v1

Task: task_05_fix

## 目标

修复任务 5 规格审查发现的 2 条必须修复 + 3 条建议修改，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`9104d4f`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 背景（规格审查结论摘要）

- 必须修复 1：编辑回显缺失 + 编辑保存会用默认参数覆盖固有/现有等级（`eventInitialValues` 只给 accident_type/chemical_id，树 meta 不带方法/固有字段，表单从默认值初始化，handleFinish 无条件重算提交）；
- 必须修复 2：「采用为现有风险」只写 `adoptedRef` 状态，`handleFinish` 不消费，保存后与参考结果不一致；
- 建议 1：conversion-reference 端点未校验事件归属企业；
- 建议 2：COAL_LS 无配置时折算恒「低」（compute_risk 有内置默认阈值而折算没有）；
- 建议 3：DIRECT 保存键不匹配（表单提交 `method_params={level: 数值}`，后端 DIRECT 读 `params.get("risk_level")`）。

## 修复清单

**A（必须修复 1）编辑回显 + 保存不覆盖**

- `frontend/src/components/enterprise/RiskHierarchyTree.tsx`（及所用 TreeNodeMeta 类型）：事件节点 meta 携带 `method_type`、`method_params`、`risk_level`、`risk_score`、`inherent_risk_level`、`inherent_risk_score`、`control_level`（沿现有 meta 结构扩展，保持既有字段兼容）；
- `frontend/src/pages/Enterprise/RiskManagementTab.tsx`：`eventInitialValues` 用 meta 字段填充表单 initialValues（method_type/method_params/inherent_*（能拿到的字段）/control_level）；
- `frontend/src/components/enterprise/RiskEventForm.tsx`：
  - 记录初始参数（`initialMethodParams` / `initialInherentParams`，编辑模式来自 initialValues；新建为 null）；
  - `handleFinish` 提交前判断：若「现有参数」与初始一致 → 不重算、不提交 risk_level/risk_score（保持已存值）；若「固有参数」与初始一致 → 不重算、不提交 inherent_risk_level/inherent_risk_score（保持已存值）；仅当对应参数被用户改动（或新建）才重算提交；
  - 固有原始参数（L/S/E/C）未持久化时，编辑模式固有参数组按初始值显示；无初始值时保持默认并遵守上述「未改动不覆盖」规则。

**B（必须修复 2）采用为现有风险落库**

- 后端（`backend/app/schemas/risk_management.py`）：`RiskEventCreate`/`RiskEventUpdate` 增加可选 `risk_level: str | None = None`、`risk_score: str | None = None`（显式覆盖当前等级）；
- 后端（`backend/app/routers/risk_management.py`）：create/update 路径——若 `body.risk_level` 提供，则当前等级取该值（不按 method_params 重算；仍执行 `validate_dual_level`）；否则保持现行为；
- 前端 `RiskEventForm.tsx`：`handleAdoptConversion` 保存参考等级/分值（含 DIRECT 的 Select 值）；`handleFinish` 在存在采用值时把 `risk_level`/`risk_score` 放入提交 payload；DIRECT 的固有等级照常提交 `inherent_risk_level`；
- 后端测试：补「显式 risk_level 覆盖」用例（create 与 update 各一，mock compute 不调用或结果被覆盖），并确认校验仍生效。

**C（建议 1）conversion-reference 归属校验**

- `backend/app/routers/risk_management.py` 端点：查询加 `RiskEvent.enterprise_id == enterprise_id`，无结果 404「风险事件不存在」；补跨企业 404 测试。

**D（建议 2）COAL_LS 默认阈值**

- `backend/app/services/risk_method_engine.py`：把 COAL_LS 内置默认阈值提为模块常量 `COAL_LS_DEFAULT_THRESHOLDS`，`compute_risk` 复用它；
- 端点：`thresholds = (config or {}).get("risk_thresholds", [])` 为空且 `method_type == "COAL_LS"` 时用 `COAL_LS_DEFAULT_THRESHOLDS`；
- 补测试：COAL_LS 无配置时折算参考不再恒「低」（如固有 R=20 × 0.5=10 → 一般）。

**E（建议 3）DIRECT 保存键**

- `frontend/src/components/enterprise/RiskEventForm.tsx`：DIRECT 方法提交 `method_params={"risk_level": <等级>}`（与后端 `compute_risk` DIRECT 一致）；固有等级提交 `inherent_risk_level`。

## 验证

- 后端：在 `backend` 目录 `python -m pytest tests/test_risk_conversion_api.py tests/test_risk_dual_level.py tests/test_risk_conversion.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- 前端：`npx tsc -b`、eslint（改动文件）、`npx vitest run` 全部通过；
- `git diff --check` 干净。

## Commit（可分两个）

```bash
git add backend/app/schemas/risk_management.py backend/app/routers/risk_management.py backend/app/services/risk_method_engine.py backend/tests/test_risk_conversion_api.py backend/tests/test_risk_dual_level.py
git commit -m "fix(risk): ownership check, explicit current-level override and coal_ls defaults"

git add frontend/src/components/enterprise/RiskEventForm.tsx frontend/src/components/enterprise/RiskHierarchyTree.tsx frontend/src/pages/Enterprise/RiskManagementTab.tsx frontend/src/types/riskManagement.ts
git commit -m "fix(risk): edit echo with no-overwrite save and adopt conversion persists"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_05_fix --claim-id <claim_id> --exit-code 0 --summary "任务5编辑回显+采用落库等修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复清单逐项说明。

## 规则

- 用 `apply_patch` 编辑；只改上述列出的文件（如树 meta 类型定义在其它文件，可同步修改并说明）；阻塞时停下汇报。
