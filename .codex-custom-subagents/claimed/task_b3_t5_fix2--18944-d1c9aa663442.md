# Codex Custom Subagents task handoff v1

Task: task_b3_t5_fix2

## 任务：修复 Diff 弹窗的 2 个代码质量问题

你是一个实现子智能体。代码质量审查发现 `frontend\src\components\plan\AIGenerateButton.tsx` 2 个重要问题：

1. `done` 事件分支中 `setTimeout(() => setStatus("idle"), 1500)` 与弹窗逻辑的顺序问题：若分支提前 return 或状态切换导致组件重渲染，Diff 弹窗可能延迟出现。需确认 done 分支完整执行（弹窗 state 设置）后再安排状态复位，且不要提前 return。
2. `handleConfirm` 的 `useCallback` 依赖数组可能缺少 `oldContent`，导致闭包捕获旧的 oldContent 值。

请修复并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

当前 HEAD 应为 `dc4a56a`。启动时 `cd` 到该目录，`git status` 确认干净。

### 验证命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\frontend
npx tsc -b
npx vitest run
```

### 修复内容

修改 `frontend\src\components\plan\AIGenerateButton.tsx`：

1. `handleConfirm` 的 `useCallback` 依赖数组补上 `oldContent`（确保闭包使用最新旧内容）。
2. `done` 事件分支：先设置 diff state（`setDiffOld/setDiffNew/setDiffOpen`）与 `onGenerateComplete(newText)`，再安排 `setTimeout(() => setStatus("idle"), 1500)`；确认分支中没有提前 return（若当前实现因 `if (oldContent && newText !== oldContent)` 提前 return 导致后续逻辑跳过，重构为 if/else 完整执行）。
3. 确认 DiffPreviewModal 在状态从 loading→done→idle 切换过程中保持挂载（Modal 由 diffOpen 控制，不依赖 status）。

### 验证与提交

运行 `npx tsc -b` 与 `npx vitest run`，预期通过。

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add frontend/src/components/plan/AIGenerateButton.tsx
git commit -m "fix(plan): fix diff modal timing and stale oldContent closure (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. tsc / vitest 结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
