# Codex Custom Subagents task handoff v1

Task: t11_review_spec

## 任务：规格合规审查 —— 任务 11（deploy-check.sh 验证脚本）

你是一个规格合规审查子智能体。验证实现者是否创建了所要求的内容（不多不少）。**不要信任实现者的报告**，独立核对。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\deploy-readiness`

git 分支 `codex/deploy-readiness`。相关提交：`ec9b1ea`（`feat(deploy): add post-deploy verification script`），父提交 `01dd148`。

### 要求的内容（任务 11 规格）

`scripts/deploy-check.sh` 须包含：

1. usage（站点 URL + API URL + `--skip-api`）；
2. 8 项检查：
   - #1 首页 200；
   - #2 移动端 `/m/dashboard` 返回 m.html（body 含「移动端」）；
   - #3 从 index.html 提取 `assets/*.js|css` 逐个 200；
   - #4 manifest.webmanifest 200；
   - #5 API `/api/health` 200（**不是** /api/v1/health）；
   - #6 `/uploads/` 非 5xx；
   - #7 深链接返回桌面 index.html（含「数字化预案系统」且不含「移动端」）；
   - #8 无尾斜杠 301/308 → 带尾斜杠（或 200）；
3. PASS/FAIL 计数汇总，任一失败 `exit 1`，全部通过输出「部署验证通过」；
4. 提交只含该文件，提交消息精确匹配；`bash -n` 通过。

### 实现者声称

88 行与任务文本逐字节一致；bash -n 通过；可执行位 100755；提交 ec9b1ea 仅 1 文件。

### 你的工作

1. `git show ec9b1ea --stat` 确认提交范围与模式；
2. 通读全文逐项对照 8 项检查（重点：#2 与 #7 的 body 断言、#5 为 `/api/health`）；
3. 运行 `bash -n scripts/deploy-check.sh`；
4. 检查规格外改动。

### 汇报格式

- ✅ 符合规格（经检查后一切匹配）
- ❌ 发现问题：[具体列出，附带 file:line]
