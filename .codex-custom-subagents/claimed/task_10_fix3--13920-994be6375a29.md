# Codex Custom Subagents task handoff v1

Task: task_10_fix3

## 目标

修复任务 10 质量复审发现的新建议：`_max_level` 对不在 `LEVEL_ORDER` 的等级值（如「未评估」）抛 ValueError，改为「先过滤域内值再取最大」以保持旧循环的容错语义，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`a716dfe`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 文件

- 修改：`backend/app/services/risk_notice_card_service.py`
- 修改：`backend/tests/test_risk_notice_card_service.py`（补域外等级容错用例）

## 修复

`_max_level` 改为：

```python
def _max_level(events: list, attr: str, default: str | None) -> str | None:
    values = {getattr(ev, attr) for ev in events if getattr(ev, attr, None)}
    known = [v for v in values if v in LEVEL_ORDER]
    return max(known, key=lambda v: -LEVEL_ORDER.index(v)) if known else default
```

（域外值静默忽略，与旧循环一致；`compute_level`/`compute_inherent_level` 行为不变。）

测试补 1 个用例：事件等级含「未评估」（或域外值）时 `_max_level`/`compute_level` 不抛异常且返回最严重已知值或默认值。

## 验证

- 在 `backend` 目录 `python -m pytest tests/test_risk_notice_card_service.py tests/test_risk_notice_card_data.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- `git diff --check` 干净。

## Commit

```bash
git add backend/app/services/risk_notice_card_service.py backend/tests/test_risk_notice_card_service.py
git commit -m "fix(risk): tolerate unknown levels in notice card max level helper"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_10_fix3 --claim-id <claim_id> --exit-code 0 --summary "告知卡等级容错修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果。

## 规则

- 用 `apply_patch` 编辑；只改列出的 2 个文件；阻塞时停下汇报。
