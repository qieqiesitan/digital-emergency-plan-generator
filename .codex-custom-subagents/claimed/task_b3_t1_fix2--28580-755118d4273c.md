# Codex Custom Subagents task handoff v1

Task: task_b3_t1_fix2

## 任务：check_plan 补充 Mermaid 语法检查（规格 3.5 规则 5）

你是一个实现子智能体。规格复审发现 `backend\app\services\plan_quality_service.py::check_plan` 缺规格 3.5 规则 5（Mermaid 语法检查：缺失类型声明的代码块告警），而后续任务会把 `export.py::validate_plan_export` 改为调用 check_plan，若不加该规则会丢失现有 Mermaid 检查。请补充并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

当前 HEAD 应为 `93ab0b5`。启动时 `cd` 到该目录，`git status` 确认干净。

### 参考（现有 export.py 的 Mermaid 检查逻辑）

`backend\app\routers\export.py` 约 325-335 行：

```python
                codes = _extract_mermaid_code(section.content)
                for code in codes:
                    if not code.strip().startswith(("flowchart", "graph", "sequenceDiagram",
                        "classDiagram", "stateDiagram", "erDiagram", "gantt", "pie",
                        "gitGraph", "mindmap", "timeline", "journey")):
                        warnings.append(f"Mermaid in section '{section.title}' may be missing diagram type declaration")
```

`_extract_mermaid_code` 来自 `app.services.mermaid_renderer`。

### 测试命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\backend
.\.venv\Scripts\python.exe -m pytest tests\test_plan_quality.py -v
```

### 步骤 1：追加失败的测试

在 `backend\tests\test_plan_quality.py` 末尾追加：

```python
def test_mermaid_missing_type_declaration_warning():
    enterprise = MagicMock(address="西安市高新区一路1号", legal_representative="张三", safety_officer="李四")
    plan = MagicMock()
    content = '<pre><code class="language-mermaid">A --> B</code></pre>'
    result = check_plan(plan, enterprise, [_section("sec_5", "应急响应", content)])
    assert any("Mermaid" in w["warning"] for w in result["warnings"])
```

### 步骤 2：运行测试验证失败

运行 pytest 命令。
预期：新增测试 FAIL（当前无 Mermaid warning）。

### 步骤 3：实现 Mermaid 检查

修改 `backend\app\services\plan_quality_service.py`：

1. 顶部新增导入：

```python
from app.services.mermaid_renderer import _extract_mermaid_code
```

2. `check_plan` 循环内（占位符检测之后）新增规则 5：

```python
        codes = _extract_mermaid_code(s.content or "")
        for code in codes:
            if not code.strip().startswith((
                "flowchart", "graph", "sequenceDiagram", "classDiagram",
                "stateDiagram", "erDiagram", "gantt", "pie",
                "gitGraph", "mindmap", "timeline", "journey",
            )):
                warnings.append({
                    "section_key": s.section_key,
                    "section_title": s.title,
                    "warning": "Mermaid 代码块缺少图表类型声明",
                })
```

注意：先确认 `_extract_mermaid_code` 的导入路径与签名（读 `backend\app\services\mermaid_renderer.py` 确认），若函数名不同则按实际名称调用。

### 步骤 4：运行测试验证通过

运行 pytest 命令。
预期：PASS（6 passed）。

### 步骤 5：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add backend/app/services/plan_quality_service.py backend/tests/test_plan_quality.py
git commit -m "feat(plan): add mermaid syntax check to quality service (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出（几 passed）
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
