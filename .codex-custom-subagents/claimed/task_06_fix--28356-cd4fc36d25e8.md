# Codex Custom Subagents task handoff v1

Task: task_06_fix

## 修复任务：任务 6 质量审查 4 项次要建议

### 背景

任务 6（列表/详情路由）已通过规格审查与质量审查（✅ 通过，附 4 项次要建议）。实现提交 `661476d`。

### 修复 1（次要）：level 参数校验

`backend/app/routers/risk_notice_card.py` 的列表端点 `level` 参数：非法值（不在 LEVEL_ORDER + 「未评估」内）应显式返回 422 或 Query 限定。建议：用 `Query` 参数并加校验，非法值抛 HTTPException(422, ...)。合法值 = LEVEL_ORDER（["重大","较大","一般","低"]）+ "未评估"。

### 修复 2（次要）：详情端点合并重复查询

详情端点先按 enterprise_id 查归属（无关系加载），随后 `load_events_and_measures` 按 id 再查一次（带关系）。请合并为一次带 selectinload 的归属查询（查询 RiskObject 时直接带 units.events.measures 与 events.measures 关系），并将已加载对象传给事件/措施收集（可复用 service 的收集逻辑或新增共享函数）。

### 修复 3（次要）：提取共享事件收集函数

路由里的 `_collect_events` 与 `risk_notice_card_service.load_events_and_measures` 的「obj.events + unit.events 合并去重」逻辑重复。请在 `risk_notice_card_service.py` 提取共享纯函数（如 `merge_object_events(obj) -> list[RiskEvent]` 与 `collect_measures(events) -> list[RiskMeasure]`），路由与 service 都复用；`load_events_and_measures` 改为基于该函数（注意保持其查询语义不变）。

### 修复 4（次要）：补充测试用例

`backend/tests/test_risk_notice_card_api.py` 补充：
* 兜底用例：对象无责任字段 → fallback_used=True、责任单位/人/电话取企业值
* 空数据对象用例：无事件 → level=未评估、signs=[]、右栏显示兜底模板
* 非法 level 参数 → 422

### 范围与限制

* 只改 `backend/app/routers/risk_notice_card.py`、`backend/app/services/risk_notice_card_service.py`、`backend/tests/test_risk_notice_card_api.py`、`backend/tests/test_risk_notice_card_service.py`（如共享函数需要测试）。
* 不修改其他文件。

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_api.py tests/test_risk_notice_card_service.py -v` 全部 PASS。
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/ -v` 无回归（全量应 381+ passed）。
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净。
* 提交新 commit（不要 amend 661476d），消息：`fix(risk-notice-card): validate level filter and dedupe shared helpers`，只含上述文件。

### 汇报

* 状态：DONE | BLOCKED | NEEDS_CONTEXT
* 修改的文件与行
* 测试结果
* 新提交 SHA
