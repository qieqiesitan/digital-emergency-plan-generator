# Codex Custom Subagents task handoff v1

Task: task_08_fix2

## 目标

按任务 8 代码质量审查的 2 条建议修改修复，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`0b9647e`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1（建议）：提取共享筛选/口径逻辑**（`backend/app/services/risk_control_list_service.py`）

- 新增 `ZONE_TREE_OPTIONS` 常量（selectinload 选项元组）与 `is_major_publicity_row(row) -> bool`（`row["current"] == "重大" or row["control_level"] == "企业"`）；
- `risk_management.py` 与 `public_risk.py` 两处重复的 selectinload 选项改为引用共享常量，两处「重大 or 企业」筛选改调 `is_major_publicity_row`；
- 行为不变。

**2（建议）：补 4 个测试**（`backend/tests/test_risk_control_list.py`）

- `level` 筛选 inherent-only 分支（事件 current=一般 / inherent=较大，按 level=较大 过滤命中）；
- 显式传他企业 floor_id → 404「楼层不存在」；
- export 空清单（`max_row == 1` 且 sheetnames 双 sheet）；
- risk-publicity 有真实 floor 时 `floor_name` 非 None。

## 验证

- 在 `backend` 目录 `python -m pytest tests/test_risk_control_list.py -v` 全部 PASS（20 个）；`python -m pytest tests/ -q` 无回归；
- `git diff --check` 干净。

## Commit

```bash
git add backend/app/services/risk_control_list_service.py backend/app/routers/risk_management.py backend/app/routers/public_risk.py backend/tests/test_risk_control_list.py
git commit -m "refactor(risk): share publicity row filter and zone tree options with tests"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_08_fix2 --claim-id <claim_id> --exit-code 0 --summary "任务8共享逻辑+测试补齐完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的 4 个文件；阻塞时停下汇报。
