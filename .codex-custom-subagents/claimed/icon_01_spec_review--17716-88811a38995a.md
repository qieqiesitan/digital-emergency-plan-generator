# Codex Custom Subagents task handoff v1

Task: icon_01_spec_review

## 目标

审查任务 1（图标资产抓取）的实现是否与规格匹配——**不多不少**。你必须独立阅读实际代码验证，不要信任实现者报告。

## 审查对象

工作区：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\icon-system`（分支 codex/icon-system）
实现提交：`ddaed83`（父提交 `b00fd49`）「feat(icon-system): fetch and clean 24 iconfont svg assets」

## 要求的内容（规格）

1. 创建 `scripts/fetch_icons.py`：
   - `MAPPING` 恰 24 项（name → (search term, iconfont id)），与设计文档 §4.3 一致：archive 1490623、org 29865223、geo 2076231、chem 13209754、risk 32835841、hazard 12820186、rescue 45446276、assessment 3759366、investigation 4423489、plan-manage 8625443、dashboard 7215957、enterprise 11239041、plan-list 2959108、regulations 8329617、data-dict 1680700、prompt 2286510、ai 5387814、law 7991666、standard 3207743、policy 12031078、topic 3522456、safety 3029239、notice 577374、location 11372652；
   - `clean_svg()`：去除 class/style/version、硬编码 fill（hex 与 rgb）；保留 `fill="none"`、`stroke`、`viewBox`；
   - `search()` 请求参数：`fills=""`（空串）、`line="1"`、`page`/`pageSize`、`sortType=updated_at`、`fromCollection=1`、`ctoken=null`；
   - 分页：`MAX_PAGES=5`、`PAGE_SIZE=60`；按 term 聚合、按所需 id 全集判断是否停止；失败重试 3 次、间隔 2s；
   - `fetch_all()` 输出 24 个清洗后 SVG；`main()` 支持 `--verify`（缺失/脏文件报错）；
   - 仓库自包含：直接调用 iconfont 公开接口，不得依赖 `.codex` 技能目录或 `search_iconfont.py`。
2. 创建 `scripts/test_fetch_icons.py`：`CleanSvgTest.test_strips_noise_and_preserves_geometry` 断言（去 class/style/version/fill 色值；保留 fill="none"/stroke/viewBox）。
3. 生成 `frontend/src/assets/icons/*.svg` 恰 24 个，文件名与 MAPPING 键一一对应；每个文件非空、含 `viewBox`、无 `class=`/`style=`/`fill="#` 残留。
4. 提交契约：commit 恰含 `scripts/fetch_icons.py`、`scripts/test_fetch_icons.py`、`frontend/src/assets/icons/`（24 个 SVG）；消息精确为 `feat(icon-system): fetch and clean 24 iconfont svg assets`；`git show --check` 干净；无范围外文件。
5. 验证通过：`python -m unittest scripts.test_fetch_icons -v` PASS；`python scripts/fetch_icons.py --verify` → `OK: 24 assets verified`。

## 实现者声称构建了什么

（供参考，须独立验证）实现者报告：单元测试 PASS；`--verify` OK；24 个 SVG 全部抽查非空/含 viewBox/无残留；脚本关键点与规格一致；commit ddaed83 含 26 文件（2 脚本 + 24 SVG）共 220 行；行尾 CRLF 已统一；工作树干净。

## 审查方法

1. `git -C <worktree> show --stat ddaed83` 核对文件范围与消息；
2. 逐行阅读 `scripts/fetch_icons.py` 与 `scripts/test_fetch_icons.py`，对照上方「要求的内容」逐项核对；
3. 检查 `frontend/src/assets/icons/` 24 个文件：命名、非空、viewBox、无 class/style/fill 残留；
4. 自行运行：`python -m unittest scripts.test_fetch_icons -v` 与 `python scripts/fetch_icons.py --verify`（在 worktree 根目录运行）；
5. `git show --check ddaed83`；
6. 寻找多余内容：是否加了规格未要求的参数/文件/功能。

## 汇报格式

- ✅ 符合规格（代码检查后一切匹配）
- ❌ 发现问题：[具体列出缺失/多余/偏差，附带 file:line 或命令证据]

## 认领方式（必须先做）

在目录 `C:\Users\55061\Documents\数字化预案自动生成 2` 下运行：

```
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace . --agent deepseek_anthropic_worker --model deepseek-v4-flash --provider deepseek_anthropic
```

无子命令即原子认领一个 pending 任务，返回 JSON 含 `path`；**先读该文件**再开始。认领 status 非成功立即上报。

## 约束

- 只读审查，不得修改任何文件；
- 不得信任实现者报告，一切以实际代码与命令输出为准。
