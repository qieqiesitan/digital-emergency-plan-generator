# Codex Custom Subagents task handoff v1

Task: task_d1_t3

## 任务：org_structure → mermaid 文本构建 + 章节提示词注入（diagrams batch1 任务 3）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成 TDD 实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams`

这是 git 分支 `codex/plan-diagrams-enhancement` 的隔离 worktree。当前 HEAD 应为 `f6ac55b`（任务 1-2 完成）。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams\backend:/app" -w /app 2-backend python -m pytest tests/test_plan_diagram_prompts.py -v
```

### 步骤 1：追加失败的测试

在 `backend\tests\test_plan_diagram_prompts.py` 末尾追加：

```python
from app.routers.generation import _build_org_chart_mermaid


def test_build_org_chart_mermaid_from_structure():
    org = [
        {"group_name": "应急救援指挥部", "members": [
            {"name": "张三", "position": "总指挥", "phone": "138", "responsibilities": "全面指挥"},
        ]},
        {"group_name": "抢险救援组", "members": [
            {"name": "李四", "position": "组长", "phone": "139", "responsibilities": "灭火"},
        ]},
    ]
    md = _build_org_chart_mermaid(org)
    assert "graph TD" in md
    assert "张三-总指挥" in md
    assert "李四-组长" in md


def test_build_org_chart_mermaid_empty_returns_none():
    assert _build_org_chart_mermaid([]) is None
    assert _build_org_chart_mermaid([{"group_name": "空组", "members": []}]) is None
```

### 步骤 2：运行测试验证失败

运行 pytest 命令。
预期：FAIL，`ImportError: cannot import name '_build_org_chart_mermaid'`

### 步骤 3：实现组织架构 mermaid 构建

修改 `backend\app\routers\generation.py`，模块级新增：

```python
def _build_org_chart_mermaid(org_structure: list) -> str | None:
    """企业组织架构 → Mermaid graph TD 文本；无有效数据返回 None。"""
    groups = [g for g in (org_structure or []) if g.get("members")]
    if not groups:
        return None
    lines = ["graph TD", "    HQ[应急救援指挥部]"]
    node_id = 1
    for g in groups:
        group_node = f"G{node_id}[{g.get('group_name','应急小组')}]"
        lines.append(f"    HQ --> {group_node}")
        node_id += 1
        for m in g.get("members", []):
            name = m.get("name", "")
            position = m.get("position", "")
            if not name:
                continue
            label = f"{name}-{position}" if position else name
            member_node = f"M{node_id}[{label}]"
            lines.append(f"    {group_node} --> {member_node}")
            node_id += 1
    return "\n".join(lines)
```

### 步骤 4：`_build_section_prompt` 注入附加图指令

在 `_build_section_prompt` 中，mermaid 指令拼接后追加附加图指令。先读当前 `_build_section_prompt`（约 90-175 行），在函数返回前（两处：DB 模板路径与代码兜底路径）统一追加。参考实现（建议抽一个辅助函数避免两处重复）：

```python
def _append_additional_diagram_prompt(prompt: str, section_key: str | None, enterprise_data: dict) -> str:
    """按章节附加图类型追加提示词（组织架构图注入真实数据）。"""
    additional_key = SECTION_ADDITIONAL_DIAGRAM_MAP.get(section_key or "")
    if not additional_key:
        return prompt
    tmpl = get_additional_diagram_prompt(additional_key)
    if not tmpl:
        return prompt
    variables = {}
    if additional_key == "org_chart":
        variables = {"org_structure": json.dumps(
            enterprise_data.get("org_structure", []), ensure_ascii=False
        )}
    return prompt + "\n\n" + render_template(tmpl, variables)
```

并在 `_build_section_prompt` 两个返回点调用：`return _append_additional_diagram_prompt(prompt, section_key, enterprise_data)`。

`get_additional_diagram_prompt` 需加入 `generation.py` 顶部 `from app.services.prompt_cache import ...` 导入（与 `get_section_prompt` 等并列）。

### 步骤 5：运行测试验证通过

运行 pytest 命令。
预期：PASS（5 passed）

### 步骤 6：全量回归

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams\backend:/app" -w /app 2-backend python -m pytest tests/ -q --ignore=tests/test_autofill_research.py
```

预期：全部通过

### 步骤 7：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams
git add backend/app/routers/generation.py backend/tests/test_plan_diagram_prompts.py
git commit -m "feat(plan): inject additional diagram prompts into section generation (diagrams batch1)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出与全量回归结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md。
