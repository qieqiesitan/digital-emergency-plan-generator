# Codex Custom Subagents task handoff v1

Task: t12_review_spec

## 任务：规格合规审查 —— 任务 12（构建回归验证 + npm ci 兜底修复）

你是一个规格合规审查子智能体。验证验证型任务按要求执行、修复按规格落地。**不要信任实现者的报告**，独立核对。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\deploy-readiness`

git 分支 `codex/deploy-readiness`。任务 12 本身无代码改动；npm 兜底修复为 `t12_fix_npm_fallback`（提交消息 `fix(deploy): fall back to npm install when npm ci fails`）。HEAD 应为该修复提交。

### 要求的内容（任务 12 规格）

1. 根路径构建：产物资源引用以 `/assets/` 开头、不含 `/emergency-plan-migration/`；
2. 子路径构建（VITE_BASE_PATH）：资源引用带 `/emergency-plan-migration/` 前缀；manifest `start_url=/emergency-plan-migration/m/dashboard`、`scope=/emergency-plan-migration/`；
3. 恢复默认构建产物；
4. 不改任何代码。

另：npm 兜底修复要求 `scripts/package-release.sh` 与 `docs/deploy/README-DEPLOY.md` §3 的构建命令含 `(npm ci 2>/dev/null || npm install)`。

### 你的工作

1. `git log --oneline -3` 确认修复提交存在；
2. `git show <修复提交> --stat` 确认只含 2 个文件；
3. 抽查 `scripts/package-release.sh` 与 README §3：构建命令含兜底；README 有说明句；
4. 若 dist 产物仍在，抽查 `frontend/dist/index.html` 资源引用为根路径（无前缀，恢复态）；若已清理，记录即可；
5. 运行 `bash -n scripts/package-release.sh`。

### 汇报格式

- ✅ 符合规格（经检查后一切匹配）
- ❌ 发现问题：[具体列出，附带 file:line]
