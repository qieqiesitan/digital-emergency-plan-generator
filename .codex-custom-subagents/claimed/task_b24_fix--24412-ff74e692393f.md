# Codex Custom Subagents task handoff v1

Task: task_b24_fix

## 任务：修复 B2-4 质量审查重要问题（模块校验/端点测试/文件上限）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 7b31f6d。启动时 `cd` 到该目录，git status 确认干净。

### 问题 1（重要）：显式 module 校验已知模块

`backend/app/routers/onboarding.py` 的 `onboarding_import`：当 `module != "auto"` 时，校验 module 在 `MODULE_SCHEMA_HINTS` 中，否则 400「未知模块」：

```python
from app.services.onboarding_service import MODULE_SCHEMA_HINTS
...
    if module != "auto" and module not in MODULE_SCHEMA_HINTS:
        raise HTTPException(400, f"未知模块：{module}")
```

### 问题 2（重要）：补端点级测试

新建 `backend/tests/test_onboarding_routes.py`，用 FastAPI TestClient + `app.dependency_overrides` 覆盖 `get_current_user` 与 `get_db`（monkeypatch LLM 相关函数），覆盖：

- `GET /enterprises/{id}/completion` 本人 200 / 非本人 404（补上 B3 复审建议的 IDOR 端点测试）
- `POST /onboarding/candidates`：org 模块 200（monkeypatch generate_org_candidates）、非本人 404、非 org 模块 400
- `POST /onboarding/import`：auto 分类 200（monkeypatch classify_modules + extract_candidates）、显式未知模块 400、损坏文件 400
- `POST /onboarding/import/batch`：两文件返回两条（monkeypatch）、分类为空跳过

实现方式参考 FastAPI 测试惯例：

```python
from fastapi.testclient import TestClient
from app.main import app
from app.dependencies import get_current_user
from app.database import get_db


def _override_user():
    from app.models.user import User
    u = User(id="u1", email="a@b.c", name="A", role="admin")
    return u


def _override_db():
    from unittest.mock import AsyncMock
    db = AsyncMock()
    # 按需配置 db.execute 返回
    return db
```

（`app.dependency_overrides[get_current_user] = _override_user` 等；注意 main.py 导入可能因静态目录/前端产物失败——若如此，构造独立 FastAPI 应用只挂 onboarding router 再测试，或按实际可测方式实现。测试目标：核心端点行为入库，防回归。）

若 main.py 在测试环境导入失败（静态目录挂载 frontend/dist），优先用独立应用挂载 router 的方式（如 `FastAPI()` + `app.include_router(onboarding.router)`），并在测试内 override 依赖。

### 问题 3（语义说明）：batch 分类为空跳过 + 单文件 400

保持现有行为（batch 宽容跳过、单文件明确 400），在代码处加注释说明设计意图：

```python
# 批量导入为撒网模式：单文件识别不到模块则跳过，不拖累整批；单文件导入需明确反馈
```

### 问题 4（重要）：文件大小上限

`onboarding_import` 与 `onboarding_import_batch`：读取文件前检查大小（`file.size` 或读取后检查 `len(data)`），超过 20MB 抛 413：

```python
MAX_IMPORT_BYTES = 20 * 1024 * 1024
...
    data = await file.read()
    if len(data) > MAX_IMPORT_BYTES:
        raise HTTPException(413, "文件过大，最大支持 20MB")
```

（用 `len(data)` 读取后检查即可；UploadFile.size 可能为 None。）

### 步骤 5：测试验证 + Commit

运行：`cd backend && python -m pytest tests/test_onboarding_routes.py tests/test_onboarding_extract.py tests/test_onboarding_completion.py -v`

再全量：`python -m pytest tests/ -q`（与基线一致，无新增失败）。

```bash
git add backend/app/routers/onboarding.py backend/tests/test_onboarding_routes.py
git commit -m "fix(onboarding): validate import module, cap file size, add endpoint tests"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 先读 onboarding.py / main.py / dependencies.py 确认可测方式
2. 按步骤实现
3. 测试验证
4. 提交
5. 自审：module 校验生效？端点测试入库且真实覆盖？文件上限生效？batch/单文件语义注释清楚？
6. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、测试结果、提交 SHA、自审发现
