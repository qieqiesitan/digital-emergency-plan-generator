# Codex Custom Subagents task handoff v1

Task: task_d2_t1

## 任务：PlanSection 模型 + 迁移 SQL（diagrams batch2 任务 1）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成 TDD 实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams`

这是 git 分支 `codex/plan-diagrams-enhancement` 的隔离 worktree。当前 HEAD 应为 `e0227ed`（batch1 完成）。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令（必须挂 2_chroma_cache 卷，否则法规检索初始化会下载模型卡死）

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_diagram_service.py -v
```

### 步骤 1：编写失败的测试

新增文件 `backend\tests\test_plan_diagram_service.py`：

```python
from app.models.enterprise import PlanSection


def test_plan_section_has_diagram_svgs_column():
    cols = {c.name for c in PlanSection.__table__.columns}
    assert "diagram_svgs" in cols


def test_diagram_svgs_default():
    s = PlanSection(id="t", plan_project_id="p", section_key="s", title="t", level=1, sort_order=0)
    assert s.diagram_svgs == {}
```

### 步骤 2：运行测试验证失败

运行 pytest 命令。
预期：FAIL，`KeyError: 'diagram_svgs'` 或列缺失

### 步骤 3：实现模型字段

修改 `backend\app\models\enterprise.py` 的 `PlanSection` 类（`mermaid_svgs` 字段附近），追加：

```python
    diagram_svgs: Mapped[Optional[dict]] = mapped_column(JSONB, default=dict)
```

### 步骤 4：新增迁移 SQL

新增文件 `backend\db_migration_plan_diagram_svgs.sql`：

```sql
ALTER TABLE plan_sections
  ADD COLUMN IF NOT EXISTS diagram_svgs JSONB NOT NULL DEFAULT '{}';
```

### 步骤 5：运行测试验证通过

运行 pytest 命令。
预期：PASS（2 passed）

### 步骤 6：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-diagrams
git add backend/app/models/enterprise.py backend/db_migration_plan_diagram_svgs.sql backend/tests/test_plan_diagram_service.py
git commit -m "feat(plan): add diagram_svgs column to plan sections (diagrams batch2)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. pytest 最终输出（几 passed）
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要运行全量测试；不要提交其他文件；不要推送；不要动 TASKS.md。
