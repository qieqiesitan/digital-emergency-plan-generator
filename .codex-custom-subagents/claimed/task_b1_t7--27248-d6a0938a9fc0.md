# Codex Custom Subagents task handoff v1

Task: task_b1_t7

## 任务：移动端接入真实元数据 + 自动填充

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

这是 git 分支 `codex/plan-generation-enhancement` 的隔离 worktree。当前 HEAD 应为 `e9dfe62`。启动时 `cd` 到该目录，`git status` 确认干净。

### 背景

后端已返回章节元数据字段（ai_generatable/auto_fill/auto_fill_source/data_dependencies），桌面端已接入（任务 6）。移动端 `PlanEditorScreen.tsx` 仍硬编码 `aiGeneratable: true`，`ChapterTree.tsx` 的 `ChapterNode` 无 `autoFill` 字段，批量生成面板 `AIGenerationSheet.tsx` 未过滤不可生成章节。

### 验证命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\frontend
npx tsc -b
npx vitest run
```

### 步骤 1：ChapterNode 类型扩展

修改 `frontend\src\mobile\components\plan\ChapterTree.tsx`：

```typescript
export interface ChapterNode {
  key: string;
  title: string;
  level: number;
  aiGeneratable: boolean;
  autoFill: boolean;
  required: boolean;
  children?: ChapterNode[];
}
```

### 步骤 2：PlanEditorScreen 构建真实元数据

修改 `frontend\src\mobile\screens\PlanEditorScreen.tsx`：

1. chapters 构建处（约 60-70 行）`aiGeneratable: true` 改为 `aiGeneratable: sec.ai_generatable`，并新增 `autoFill: sec.auto_fill`。
2. 编辑态 NavBar `rightActions`：`selectedChapter?.aiGeneratable` 为 false 时不渲染 AI 生成按钮（`rightActions={selectedChapter?.aiGeneratable ? [{...}] : []}`，若 NavBar 不接受空数组则传 `undefined`，以类型检查为准）。
3. 编辑区为 `selectedChapter?.autoFill` 渲染「自动填充」按钮（放在编辑区顶部或工具栏区域均可，确保在移动端可见），点击调用 `autofillSection(planId!, selectedChapter.key)`（从 `@/services/planService` 导入），成功后更新本地内容并 autoSave，失败用 `showToast` 提示错误。参考结构：

```typescript
      {selectedChapter?.autoFill && (
        <button
          className="w-full h-10 bg-indigo-600 text-white text-body-sm font-medium"
          onClick={async () => {
            try {
              const sec = await autofillSection(planId!, selectedChapter!.key);
              setLocalContent(sec.content || "");
              autoSave(sec.content || "");
              showToast?.({ type: "success", message: "自动填充完成" });
            } catch (e: any) {
              showToast?.({ type: "error", message: e?.message || "自动填充失败" });
            }
          }}
        >
          自动填充
        </button>
      )}
```

### 步骤 3：AIGenerationSheet 章节过滤

修改 `frontend\src\mobile\components\plan\AIGenerationSheet.tsx`：组件本身通过 `chapters` prop 接收章节，过滤逻辑由调用方完成。确认 `PlanEditorScreen` 打开 sheet 前只传入 `aiGeneratable` 章节；若当前调用处传了全部章节，则过滤：

```typescript
const generatableChapters = chapters.filter(c => c.aiGeneratable);
```

（AIGenerationSheet 的 `onGenerate` 回调与章节勾选逻辑保持不变。）

### 步骤 4：类型检查与测试

运行 `npx tsc -b`，预期 PASS。
运行 `npx vitest run`，预期现有用例全部通过。

### 步骤 5：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add frontend/src/mobile/screens/PlanEditorScreen.tsx frontend/src/mobile/components/plan/ChapterTree.tsx frontend/src/mobile/components/plan/AIGenerationSheet.tsx
git commit -m "feat(plan): wire section metadata and autofill in mobile editor (batch1)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. tsc / vitest 结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md；不要运行后端测试。
