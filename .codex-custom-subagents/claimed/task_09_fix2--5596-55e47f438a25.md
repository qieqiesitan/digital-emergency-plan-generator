# Codex Custom Subagents task handoff v1

Task: task_09_fix2

## 目标

按任务 9 代码质量审查的 4 条建议修改修复，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`6a40bc1`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**1（UX）：分区下拉与后端默认楼层口径对齐**（`frontend/src/pages/Enterprise/RiskControlListPage.tsx`）

未选楼层时 `zoneOptions` 只列「默认楼层」分区（后端 `_resolve_zone_floor` 缺省解析默认楼层）。实现：页面初始查询默认楼层（或复用已有 floors 数据中 `is_default` 的楼层），zoneOptions 按「所选楼层 ?? 默认楼层」过滤；选中其他楼层分区但未选对应楼层时给出提示或自动带上 floor_id（选清晰方案并说明）。

**2：service 参数类型收窄**（`frontend/src/services/riskManagementService.ts`）

`getControlList`/`exportControlList` 的 `params: object` 改为 `Record<string, unknown>`。

**3：后端筛选逻辑去重**（`backend/app/routers/risk_management.py`）

提取 `_apply_control_list_filters(rows, zone_id, level, control_level, keyword)` 供 control-list 与 export 两处复用（行为不变）；补导出筛选既有测试保持通过。

**4：service 封装风格统一**（`frontend/src/services/riskManagementService.ts`）

新 5 个方法改为文件既有惯例（箭头函数 + `.then(r => r.data.data)` 解包返回数据；`exportControlList` 因需响应体保留返回 AxiosResponse 或按 blob 下载惯例处理）；同步更新 `RiskControlListPage`/`RiskPublicityPage`/`PublicRiskPage` 调用处解包方式；service 测试同步调整断言。

## 验证

- 后端：`python -m pytest tests/test_risk_control_list.py -v` 全部 PASS；`python -m pytest tests/ -q` 无回归；
- 前端：`npx tsc -b`、eslint（改动文件）、`npx vitest run` 全部通过；
- `git diff --check` 干净。

## Commit

```bash
git add frontend/src/pages/Enterprise/RiskControlListPage.tsx frontend/src/pages/Enterprise/RiskPublicityPage.tsx frontend/src/pages/PublicRiskPage.tsx frontend/src/services/riskManagementService.ts frontend/src/services/riskManagementService.test.ts backend/app/routers/risk_management.py backend/tests/test_risk_control_list.py
git commit -m "refactor(risk): align zone filter, service style and dedupe list filters"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_09_fix2 --claim-id <claim_id> --exit-code 0 --summary "任务9质量建议修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的文件；阻塞时停下汇报。
