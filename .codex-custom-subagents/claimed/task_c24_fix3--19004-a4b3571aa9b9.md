# Codex Custom Subagents task handoff v1

Task: task_c24_fix3

## 任务：修复 C2-4 质量审查 5 项重要问题

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 1cf236c。启动时 `cd` 到该目录，git status 确认干净。

### 重要 1：样章横幅生成期间禁用（防并发双批量流）

`frontend/src/pages/Plan/PlanEditorPage.tsx`：

- 样章确认横幅的两个按钮加 `disabled={isGenerating}`（或渲染条件加 `!isGenerating`）。
- `startRealtimeGeneration` 入口加重入守卫：

```tsx
const startRealtimeGeneration = useCallback((keys?: string[], onBatchDone?: () => void) => {
  if (isGenerating) return;   // 重入守卫
  ...
```

（`isGenerating` 在依赖数组中。）

### 重要 2：质量提示条不显示生成前陈旧数据

- `validateExport` 查询 `enabled` 改为 `!!id && !isGenerating && !sampleMode`（生成期间/样章模式不查询；样章完成后再查）。
- 或生成期间隐藏提示条（`{!isGenerating && validation && ...}`）。按简单有效方案实现。

### 重要 3：「查看要点清单」实际导航

`onClick` 改为 `navigate(\`/plans/${id}/preview\`)`（导入 useNavigate；页面已有 navigate？PlanEditorPage 有 useNavigate）。

### 重要 4：图例文案修正

`frontend/src/components/plan/SectionTree.tsx` 图例底部文案改为与实现一致：「空章节会列入导出校验清单」。

### 重要 5：样章流程刷新/异常恢复

`PlanEditorPage.tsx`：把 `sampleDone`（或 sampleMode+sampleDone）持久化到 sessionStorage（key 含 plan id，如 `plan_sample_done_${id}`），初始化时读取恢复；刷新后横幅可恢复。错误路径保留失败提示（error 分支不清空 failedSections——确认当前逻辑，若无问题保持）。

```tsx
const [sampleDone, setSampleDone] = useState(() => {
  try { return sessionStorage.getItem(`plan_sample_done_${id}`) === "1"; } catch { return false; }
});
const setSampleDoneSafe = (v: boolean) => {
  setSampleDone(v);
  try { sessionStorage.setItem(`plan_sample_done_${id}`, v ? "1" : "0"); } catch { /* ignore */ }
};
```

（把相关 `setSampleDone(true/false)` 调用替换为 `setSampleDoneSafe`；`sampleMode` 初始化也读取 sessionStorage（`plan_sample_mode_${id}`）或保留 URL 参数判断。）

### 步骤 6：验证 + Commit

运行：`cd frontend && npx tsc -p tsconfig.app.json --noEmit`

再运行：`cd frontend && npx eslint src/pages/Plan/PlanEditorPage.tsx src/components/plan/SectionTree.tsx`（对比基线无新增错误）

预期：tsc 通过；无新增 ESLint 错误。

```bash
git add frontend/src/pages/Plan/PlanEditorPage.tsx frontend/src/components/plan/SectionTree.tsx
git commit -m "fix(plan): guard concurrent generation, correct quality hints and sample recovery"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 先读 PlanEditorPage.tsx 的 startRealtimeGeneration/横幅/质量提示/错误处理 + SectionTree 图例
2. 按步骤修复
3. tsc + eslint 验证（对比基线无新增错误）
4. 提交
5. 自审：生成中横幅禁用/无并发流？质量提示不显示陈旧数据？查看要点导航？图例文案准确？刷新后样章恢复？
6. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、tsc/eslint 结果、提交 SHA、自审发现
