# Codex Custom Subagents task handoff v1

Task: task_03_fix

## 目标

修复任务 3 规格审查发现的 1 条必须修复 + 2 条建议修改，提交后复审。

## 工作目录

- **代码与 git 提交目录**：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\dual-prevention`（分支 `codex/dual-prevention`，当前 HEAD=`c1fcf8c`）。
- **任务池目录（认领/完成命令 cwd）**：`C:\Users\55061\Documents\数字化预案自动生成 2`。

## 修复清单

**必须修复 1：`update_event` 校验移出条件分支（`backend/app/routers/risk_management.py`）**

当前 `validate_dual_level` 调用位于 `if body.method_type or body.method_params:` 分支内，仅改固有等级时不校验。把校验移到 setattr 循环之后、commit 之前，**无条件**执行（此时两个字段均为最终值）：

```python
    # 在 setattr 循环之后、await db.commit() 之前
    try:
        validate_dual_level(ev.risk_level, ev.inherent_risk_level)
    except ValueError as exc:
        raise HTTPException(422, str(exc)) from exc
```

并删除原分支内的重复校验调用（若有）。

**建议修改 2：迁移测试路径锚定（`backend/tests/test_risk_dual_level.py`）**

`test_migration_contains_columns` 改用项目既有约定（`Path(__file__).resolve().parents[1]`）：

```python
from pathlib import Path

def test_migration_contains_columns():
    sql_path = Path(__file__).resolve().parents[1] / "db_migration_risk_control_enhancement.sql"
    sql = sql_path.read_text(encoding="utf-8")
    ...
```

**建议修改 3：Enterprise 模型声明 token 部分唯一索引（`backend/app/models/enterprise.py`）**

给 Enterprise 加 `__table_args__`（先例：`AIConfig.__table_args__`；`Index` 与 `text` 已在该文件 import）：

```python
    __table_args__ = (
        Index("uq_enterprises_public_risk_token", "public_risk_token",
              unique=True, postgresql_where=text("public_risk_token IS NOT NULL")),
    )
```

**补充测试（必须修复 1 的回归防线）**：在 `backend/tests/test_risk_dual_level.py` 增加一个路由级用例：构建含 `update_event` 的独立 FastAPI 应用（复用 `test_public_risk_notice.py` 的 `dependency_overrides` 模式：override `get_db`/`get_current_user`，mock `db.get` 返回一个 `RiskEvent(risk_level="重大", inherent_risk_level="重大", ...)` 且 `body.method_type/method_params` 均为空），发送仅含 `inherent_risk_level="一般"` 的 PATCH 请求，断言响应 422 且文案含「不应高于」。测试文件可参考既有 API 测试组织方式。

## 验证

- 在 `backend` 目录 `python -m pytest tests/test_risk_dual_level.py -v`，预期 5 passed（原 4 + 新路由用例；若新用例写法与预期有出入，以「能真实拦截仅改固有等级的 422」为准并说明）；
- **从仓库根目录运行** `python -m pytest backend/tests/test_risk_dual_level.py -v` 也应全部通过（验证路径锚定修复）；
- `python -m pytest tests/ -q`（backend 目录）无回归；
- `git diff --check` 干净。

## Commit

在 `.worktrees\dual-prevention` 内：

```bash
git add backend/app/routers/risk_management.py backend/app/models/enterprise.py backend/tests/test_risk_dual_level.py
git commit -m "fix(risk): enforce dual-level validation on event update and anchor migration test"
```

不要提交 TASKS.md；消息精确匹配。

## 完成协议

最终回复前在任务池目录执行：

```bash
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace "C:\Users\55061\Documents\数字化预案自动生成 2" complete --task-id task_03_fix --claim-id <claim_id> --exit-code 0 --summary "任务3更新路径校验修复完成"
```

最终回复报告：task_id、claim_id、commit SHA、测试结果、修复清单逐项说明。

## 规则

- 用 `apply_patch` 编辑；只改列出的 3 个文件；阻塞时停下汇报。
