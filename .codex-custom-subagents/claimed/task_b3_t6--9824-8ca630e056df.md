# Codex Custom Subagents task handoff v1

Task: task_b3_t6

## 任务：移动端批量生成接入后台接口

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

这是 git 分支 `codex/plan-generation-enhancement` 的隔离 worktree。当前 HEAD 应为 `94b2b16`。启动时 `cd` 到该目录，`git status` 确认干净。

### 背景

移动端 `PlanEditorScreen.tsx` 的「批量生成」按钮目前仅提示「批量生成功能请在桌面端使用」。后端 `POST /plans/{plan_id}/generate/batch/background` 已就绪（批 3 任务 3 完成，返回 `{code, message}`）。需让移动端批量生成接入该接口，成功后刷新章节。

### 验证命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\frontend
npx tsc -b
npx vitest run
```

### 步骤 1：generateBatchBackground 返回类型扩展

修改 `frontend\src\services\generationService.ts`：

```typescript
export async function generateBatchBackground(
  planId: string,
  sectionKeys: string[] | null
): Promise<{ code: number; message: string; failed_sections?: Array<{ section_key: string; title: string }> }> {
  // 现有实现不变，返回体直接透传（含可选 failed_sections）
}
```

（确认现有 `generateBatchBackground` 实现已返回 `res.json()`，只需更新返回类型注解。）

### 步骤 2：移动端批量按钮接入

修改 `frontend\src\mobile\screens\PlanEditorScreen.tsx`：

1. 从 `@/services/generationService` 导入 `generateBatchBackground`（确认该 service 已导入或新增）。
2. 「批量生成」按钮 onClick 替换为：

```typescript
      onClick={async () => {
        try {
          const generatable = chapters
            .flatMap((c) => [c, ...(c.children || [])])
            .filter((c) => c.aiGeneratable)
            .map((c) => c.key);
          if (generatable.length === 0) {
            showToast?.({ type: "info", message: "没有可生成的章节" });
            return;
          }
          const res = await generateBatchBackground(planId!, generatable);
          showToast?.({ type: "success", message: res.message || "已在后台开始生成" });
          setTimeout(() => queryClient.invalidateQueries({ queryKey: ["plan-sections", planId] }), 5000);
        } catch (e: any) {
          showToast?.({ type: "error", message: e?.message || "批量生成失败" });
        }
      }}
```

（`chapters`/`queryClient`/`showToast` 在组件内已存在。）

### 步骤 3：类型检查与测试

运行 `npx tsc -b` 与 `npx vitest run`，预期通过。

### 步骤 4：Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add frontend/src/mobile/screens/PlanEditorScreen.tsx frontend/src/services/generationService.ts
git commit -m "feat(plan): enable mobile batch generation via background API (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. tsc / vitest 结果
3. commit SHA（`git rev-parse --short HEAD`）
4. 如有疑虑，说明具体内容

不要提交其他文件；不要推送；不要动 TASKS.md；不要运行后端测试。
