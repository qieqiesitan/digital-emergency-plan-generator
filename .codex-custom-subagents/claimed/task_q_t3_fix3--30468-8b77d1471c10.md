# Codex Custom Subagents task handoff v1

Task: task_q_t3_fix3

## 任务：修复法规索引节点类型过滤（阻断级误报）

你是一个实现子智能体。代码质量复审发现 `backend\app\services\plan_quality_service.py::_load_regulation_index` 的节点过滤导致真实法律被误报不存在。

真实 `graph.json` 的 `node_type` 取值：`law`（74 个）、`article`、`policy`、`standard`、`topic`。当前过滤 `node_type in ("standard", "regulation", None)` 只留 34 个标准，`law` 类型的《安全生产法》等全部被过滤，导致正文引用真实法律时误报「疑似引用不存在的法规」。

请修复并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check`

当前 HEAD 应为 `3d02442`。启动时 `cd` 到该目录，`git status` 确认干净。

### 测试命令（必须挂 2_chroma_cache 卷）

```powershell
docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/test_plan_quality_compliance.py -v
```

### 修复方案

索引应包含**法规类节点**（law/policy/standard），排除 `article`（条文节点）与 `topic`（主题节点）：

```python
_REG_NODE_TYPES = ("law", "policy", "standard")


def _load_regulation_index() -> dict | None:
    """加载法规库 graph.json：{norm_full_name: status}；失败返回 None（静默跳过）。"""
    global _reg_index_cache, _reg_index_loaded
    if _reg_index_loaded:
        return _reg_index_cache
    _reg_index_loaded = True
    try:
        import json as _json
        from pathlib import Path
        p = Path(__file__).parent.parent / "regulations" / "data" / "graph.json"
        data = _json.loads(p.read_text(encoding="utf-8"))
        _reg_index_cache = {
            n.get("full_name", ""): n.get("status", "effective")
            for n in data.get("nodes", [])
            if n.get("full_name") and n.get("node_type") in _REG_NODE_TYPES
        }
    except Exception:
        _reg_index_cache = None
    return _reg_index_cache
```

### 步骤：追加测试

在 `backend\tests\test_plan_quality_compliance.py` 追加：

```python
def test_l2_law_nodes_included_in_index():
    from app.services.plan_quality_service import _REG_NODE_TYPES
    assert "law" in _REG_NODE_TYPES
    assert "standard" in _REG_NODE_TYPES
    assert "article" not in _REG_NODE_TYPES
    assert "topic" not in _REG_NODE_TYPES
```

并补充一个真实路径测试（不 mock，读真实 graph.json 断言包含《安全生产法》或至少 law 节点）——实现者按实际 graph.json 内容调整断言（先读文件确认有哪些 law 节点 full_name 再写断言，确保测试稳定）。

### 完成标准

1. 索引含 law/policy/standard，排除 article/topic
2. 真实法律不再误报不存在
3. 测试通过
4. 全量回归：`docker run --rm -v "C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check\backend:/app" -v "2_chroma_cache:/root/.cache/chroma" -w /app 2-backend python -m pytest tests/ -q --ignore=tests/test_autofill_research.py` 全部通过

### Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-quality-check
git add backend/app/services/plan_quality_service.py backend/tests/test_plan_quality_compliance.py
git commit -m "fix(plan): include law/policy nodes in regulation index to stop false non-existent warnings (quality)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. 修复方式与真实 graph.json 节点类型核对结果
3. pytest 结果
4. commit SHA

不要提交其他文件；不要推送；不要动 TASKS.md。
