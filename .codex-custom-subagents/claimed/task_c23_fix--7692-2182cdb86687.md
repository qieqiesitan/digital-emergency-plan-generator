# Codex Custom Subagents task handoff v1

Task: task_c23_fix

## 任务：修复 C2-3 质量审查问题（列表分页/搜索/状态列）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 34af0ac。启动时 `cd` 到该目录，git status 确认干净。

### 重要 1：列表视图服务端分页 + 搜索生效

`frontend/src/pages/Plan/PlanCardsPage.tsx` 的 `PlanListTable`：

1. 加分页状态（page），把 `listPlans` 参数改为服务端分页：

```tsx
const [listPage, setListPage] = useState(1);
const listQuery = useQuery({
  queryKey: ["plans", "list", listPage, listSearch],
  queryFn: () => listPlans({ page: listPage, page_size: 20, search: listSearch || undefined }),
});
```

2. 表格 `pagination` 用后端 `total`（`data?.data.total`）：

```tsx
pagination={{
  current: listPage,
  pageSize: 20,
  total: listQuery.data?.data.total || 0,
  showTotal: (t) => `共 ${t} 条`,
  onChange: setListPage,
}}
```

3. 顶部搜索 Input 同时作用于列表（列表视图时搜索预案标题）：加 `listSearch` state，列表视图时搜索框绑定 `listSearch`（或共用 search 但列表用 title 搜索）。行业 Select 在列表视图隐藏（列表为全部预案，无行业维度）。

### 重要 2：PlanListTable 补状态列

给 `PlanListTable` 补「状态」列（复用 `PlanStatusTag`），与 PlanListPage 对齐：

```tsx
{ title: "状态", dataIndex: "status", width: 100, render: (v: string) => <PlanStatusTag status={v as PlanStatus} /> },
```

（导入 PlanStatusTag/PlanStatus 类型。）

### 重要 3：auto_generate=sample 依赖记录

在 `PlanCreatePage.tsx` 的跳转处加注释，明确 C2-4 必须在 PlanEditorPage 支持 `auto_generate=sample`（否则创建后不自动生成样章）：

```tsx
// auto_generate=sample：C2-4（样章确认）在 PlanEditorPage 支持该参数前，创建后不自动生成；依赖记录见 TASKS.md
navigate(`/plans/${data.id}/edit?auto_generate=sample`);
```

### 步骤 4：验证 + Commit

运行：`cd frontend && npx tsc -p tsconfig.app.json --noEmit`

再运行：`cd frontend && npx eslint src/pages/Plan/PlanCardsPage.tsx src/pages/Plan/PlanCreatePage.tsx`

预期：无类型/ESLint 错误。

```bash
git add frontend/src/pages/Plan/PlanCardsPage.tsx frontend/src/pages/Plan/PlanCreatePage.tsx
git commit -m "fix(plan): server pagination and search in list view, add status column"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 先读 PlanCardsPage.tsx 现状 + planService.listPlans 参数类型
2. 按步骤修复
3. tsc + eslint 验证
4. 提交
5. 自审：列表服务端分页/搜索生效？状态列显示？行业 Select 列表视图隐藏？无 any？
6. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、tsc/eslint 结果、提交 SHA、自审发现
