# Codex Custom Subagents task handoff v1

Task: task_a1_review_spec

## 任务：规格合规审查——task_a1_reset_password 实现是否匹配任务要求

你是一个规格合规审查子智能体。目的：验证实现者是否构建了所要求的内容（不多不少）。关键：不要信任实现者的报告，必须独立阅读实际代码验证。

### 实现位置

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`（分支 codex/usability-overhaul）。审查提交 `068c028` 引入的改动：

git show 068c028 --stat 与 git show 068c028

### 要求的内容（任务 A1 原文）

后端管理员重置密码接口，TDD 实现：

1. backend/app/schemas/role.py 新增 AdminResetPassword(BaseModel)，字段 new_password: str = Field(..., min_length=6, max_length=128)。
2. backend/app/routers/admin_users.py 在 delete_user 之后新增路由：
   - @router.post("/{user_id}/reset-password", response_model=ApiResponse[AdminUserResponse])
   - 函数名 reset_user_password(user_id, data, _=Depends(require_admin), db=Depends(get_db))
   - 用户不存在抛 HTTPException(404, "用户不存在")
   - 用 hash_password(data.new_password) 更新 user.password_hash，db.commit() + db.refresh(user)，返回 ApiResponse(data=AdminUserResponse.model_validate(user))
   - 顶部导入追加 AdminResetPassword
3. backend/tests/test_admin_user_reset_password.py 新建，4 个测试：schema 拒绝短密码、接受合法密码、用户不存在 404、更新哈希并 commit。
4. 测试全部通过（python -m pytest tests/test_admin_user_reset_password.py -v，4 PASS）。
5. Commit 信息：feat(admin): add reset password endpoint for admin users。
6. 只改动上述 3 个文件，不得引入任务范围外的修改。

### 实现者声称构建了什么

- schema 新增 AdminResetPassword（长度 6-128）
- admin_users.py 新增 POST /{user_id}/reset-password 路由（require_admin 鉴权、404、hash_password、commit+refresh、返回 AdminUserResponse），更新导入
- 新建 4 个单元测试，任务测试 4/4 PASS
- 全量 221 passed, 26 failed（26 个为环境预存 pytest-asyncio 问题，基线同样 26 failed）
- 提交 068c028，仅 3 个文件
- 自审说明：测试代码对 mock 装配做了最小适配（Python 3.12 AsyncMock 行为），断言与任务描述一致

### 你的工作

阅读实际代码并验证：

缺失的需求：是否实现了所有要求？有没有跳过/遗漏？
多余的工作：是否构建了未被要求的内容（额外参数、额外文件、重构）？
理解偏差：是否以不同方式解读了需求？行为是否正确（404、哈希更新、鉴权）？
测试适配：实现者声称测试 mock 做了适配——审查测试是否仍真正验证了要求的行为（而非只验证 mock 行为）。

通过阅读代码来验证，而非信任报告。

### 汇报格式

- ✅ 符合规格（如果经过代码检查后一切匹配）
- ❌ 发现问题：[具体列出缺失或多余的内容，附带 file:line 引用]
