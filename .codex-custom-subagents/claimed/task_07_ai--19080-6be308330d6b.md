# Codex Custom Subagents task handoff v1

Task: task_07_ai

## 实现任务 7：AI 优化 + 快照端点

### 任务描述（来自实现计划 2026-08-11-risk-notice-card.md 任务 7）

**文件：**
* 创建：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\app\services\risk_notice_card_ai.py`
* 修改：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\app\routers\risk_notice_card.py`（追加 2 个端点）
* 测试：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend\tests\test_risk_notice_card_api.py`（追加）

### AI 优化服务（`backend/app/services/risk_notice_card_ai.py`）

```python
"""风险告知卡 AI 优化（可选路径，规则生成失败不影响）。"""
import json
from sqlalchemy.ext.asyncio import AsyncSession

from app.services.risk_ai_service import _get_ai_config
from app.services.llm_client import llm_text_completion
from app.schemas.risk_notice_card import RightColumn


async def optimize_right_column(
    db: AsyncSession,
    user_id: str,
    enterprise_name: str,
    object_name: str,
    original: RightColumn,
) -> RightColumn:
    ai_config = await _get_ai_config(user_id, db)
    prompt = (
        "你是安全生产专家。请优化风险告知卡右栏文案，输出严格 JSON："
        '{"hazard_description": "主要危险因素描述", "control_measures": ["① ...", "② ..."], "emergency_measures": ["① ...", "② ..."]}。'
        f"企业：{enterprise_name}；风险点：{object_name}；原版：{original.model_dump_json()}。"
        "要求：措施用①②③编号开头；事故类型不得改动；中文输出。"
    )
    raw = await llm_text_completion([{"role": "user", "content": prompt}], ai_config, timeout=60)
    data = json.loads(raw)
    return RightColumn(
        hazard_description=data.get("hazard_description", original.hazard_description),
        accident_types=original.accident_types,
        control_measures=data.get("control_measures", original.control_measures),
        emergency_measures=data.get("emergency_measures", original.emergency_measures),
    )
```

注意：`_get_ai_config` 与 `llm_text_completion` 是既有函数（risk_ai_service / llm_client），确认签名后使用；若 llm_text_completion 在 `app.services.llm_client` 下确认路径正确。

### 路由追加（`backend/app/routers/risk_notice_card.py`）

```python
@router.post("/{object_id}/ai-optimize", response_model=ApiResponse[AiOptimizeResponse])
async def ai_optimize(...):
    # 查企业 + 风险点归属 → load_events_and_measures → build_right_column(original)
    # optimize_right_column 成功 → AiOptimizeResponse(original, optimized)
    # 失败 → HTTPException(502, "AI 优化失败，请稍后重试或保留原版")

@router.put("/{object_id}/snapshot", response_model=ApiResponse[dict])
async def save_card_snapshot(...):
    # 查企业归属 → save_snapshot(db, eid, oid, user_id, body.content.model_dump())
    # → ApiResponse(data={"version": snap.version, "source": "ai"})
```

### 测试追加（`backend/tests/test_risk_notice_card_api.py`）

按既有 mock DB 模式：
* AI 优化成功：monkeypatch `risk_notice_card_ai.llm_text_completion` 返回合法 JSON（含 hazard_description/control_measures/emergency_measures），验证 200 且 optimized 字段正确、accident_types 不变
* AI 优化失败：monkeypatch 抛异常，验证 502
* 快照保存：PUT snapshot 返回 version=1（或按 mock 递增），验证响应
* save_snapshot 版本递增单测（mock DB，可放 service 测试文件）

### 验证

* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/test_risk_notice_card_api.py tests/test_risk_notice_card_service.py -v` 全部 PASS
* `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card\backend && python -m pytest tests/ -v` 无回归（全量 386+ passed）
* `git -C C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card show --check HEAD` 干净
* 提交 commit，消息：`feat(risk-notice-card): add ai optimize and snapshot endpoints`，只含上述文件

### 范围与限制

* 只创建 AI 服务、追加 2 个端点、追加测试。
* 不实现导出/token/公开端点（任务 8-9）。
* 不修改既有端点行为。

### 上下文

* worktree：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\risk-notice-card`（分支 codex/risk-notice-card）。
* 任务 1-6 已完成（最新 HEAD：bef308e）。
* service 已有 `save_snapshot`（任务 5 已实现，使用 SOURCE_AI 常量）；`build_right_column` 快照优先逻辑已就绪。
* 设计规格：`docs/superpowers/specs/2026-08-11-risk-notice-card-design.md` §12（AI 优化流程：对比确认后 PUT 存快照，版本 +1）。
