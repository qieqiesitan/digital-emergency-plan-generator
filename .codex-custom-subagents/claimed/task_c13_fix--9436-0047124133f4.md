# Codex Custom Subagents task handoff v1

Task: task_c13_fix

## 任务：CandidatesReview 改用 antd Button（一致性 + 可访问性）

你是一个实现子智能体。严格按以下步骤在指定 worktree 内完成实现并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\usability-overhaul`

分支 `codex/usability-overhaul`，当前 HEAD 应包含 90a8998。启动时 `cd` 到该目录，git status 确认干净。

### 步骤 1：替换为 antd Button

`frontend/src/pages/Onboarding/CandidatesReview.tsx`：

1. 导入 `Button`（antd），保留 `Empty`。
2. 候选操作「修改」「采纳 ✓」「删除」的 `<span onClick>` 改为：

```tsx
<Button type="link" size="small" onClick={() => onModify(item)}>修改</Button>
<Button type="link" size="small" style={{ color: "#52c41a" }} onClick={() => onAccept(item)}>采纳 ✓</Button>
<Button type="link" size="small" onClick={() => onDelete(item)}>删除</Button>
```

3. 底部「继续生成更多」原生 button 改为 antd `Button block loading={generating}`：

```tsx
<Button block loading={generating} onClick={onGenerateMore}>
  {generating ? "生成中…" : generateMoreLabel}
</Button>
```

（与仓库行内操作惯例一致：Button type="link"。）

### 步骤 2：验证 + Commit

运行：`cd frontend && npx tsc -p tsconfig.app.json --noEmit`

再运行：`cd frontend && npx eslint src/pages/Onboarding/CandidatesReview.tsx`

预期：tsc/eslint 无错误。

```bash
git add frontend/src/pages/Onboarding/CandidatesReview.tsx
git commit -m "fix(onboarding): use antd buttons for consistent actions"
```

## 开始之前

对需求有不清楚的地方，现在就问（报告 NEEDS_CONTEXT），不要猜测。

## 你的工作

1. 按步骤修改
2. tsc + eslint 验证
3. 提交
4. 自审：无原生 button/span onClick？antd Button 一致？键盘/读屏可操作？
5. 汇报

## 汇报格式

- 状态：DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 修改明细、tsc/eslint 结果、提交 SHA、自审发现
