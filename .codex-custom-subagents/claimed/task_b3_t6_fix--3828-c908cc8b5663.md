# Codex Custom Subagents task handoff v1

Task: task_b3_t6_fix

## 任务：补齐移动端批量生成的选章节与失败重试（规格 3.7）

你是一个实现子智能体。规格审查发现 `frontend\src\mobile\screens\PlanEditorScreen.tsx` 的批量生成实现（commit `faa9e28`）裁剪了规格 3.7 的两项要求：

1. 批量按钮应打开 `AIGenerationSheet`（batch 模式）供用户选择章节，而不是直接对全部 aiGeneratable 章节生成。
2. 后台生成后应调用 `GET /plans/{id}/generate/status` 获取失败清单并展示重试入口。

请补齐并提交。不要修改任务范围之外的文件。

### 工作目录

`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement`

当前 HEAD 应为 `faa9e28`。启动时 `cd` 到该目录，`git status` 确认干净。

### 参考组件

- `frontend\src\mobile\components\plan\AIGenerationSheet.tsx`：已有 batch 模式（props: open/onClose/mode/planId/enterpriseName/contextSummary/chapters/onGenerate），勾选章节后回调 `onGenerate(selectedKeys, styleParams)`。需要按组件实际 props 接入（先读组件确认 props 形状）。
- `frontend\src\services\generationService.ts`：已有 `generateBatchBackground`；需新增 `getGenerationStatus`：

```typescript
export async function getGenerationStatus(
  planId: string
): Promise<{ code: number; data: { generating: boolean; failed_sections: Array<{ section_key: string; title: string }> } }> {
  const token = localStorage.getItem("access_token");
  const res = await fetch(`/api/v1/plans/${planId}/generate/status`, {
    headers: { Authorization: `Bearer ${token}` },
  });
  if (!res.ok) throw new Error("查询生成状态失败");
  return res.json();
}
```

### 实现要求

修改 `frontend\src\mobile\screens\PlanEditorScreen.tsx`：

1. 新增 state：`batchSheetOpen`、`failedSections`（`Array<{section_key,title}>`）。
2. 「批量生成」按钮 onClick：打开 `AIGenerationSheet`（mode="batch"），传入 `chapters`（仅 aiGeneratable 章节，映射为组件期望的 `{key,name,aiGeneratable}` 形状——先读组件确认字段名）。
3. `AIGenerationSheet` 的 `onGenerate(selectedKeys)`：关闭 sheet → 调 `generateBatchBackground(planId, selectedKeys)` → toast 成功 → 延迟 5 秒后调 `getGenerationStatus(planId)` 获取 failed_sections → 有失败则存入 state 展示提示 + 「重试」按钮（重新打开 sheet 预选失败章节或直接重试失败 keys）。
4. 渲染：失败清单非空时在章节列表上方展示提示条（含章节名与重试按钮），重试直接 `generateBatchBackground(planId, failedKeys)` 并清空。
5. 保持单章流式生成不变。

若 `AIGenerationSheet` 现有 props 与需求不符（如 onGenerate 需 styleParams），按组件实际接口适配，不得修改 AIGenerationSheet 组件本身（除非确有必要且说明）。

### 验证命令

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement\frontend
npx tsc -b
npx vitest run
```

预期：通过。

### Commit

```powershell
cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\codex-plan-generation-enhancement
git add frontend/src/mobile/screens/PlanEditorScreen.tsx frontend/src/services/generationService.ts
git commit -m "feat(plan): add chapter selection and retry for mobile batch generation (batch3)"
```

### 完成报告

最终回复报告：
1. 状态：DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
2. tsc / vitest 结果
3. commit SHA（`git rev-parse --short HEAD`）
4. AIGenerationSheet 接入说明（props 适配情况）

不要提交其他文件；不要推送；不要动 TASKS.md。
