# Codex Custom Subagents task handoff v1

Task: icon_08_impl

## 目标

实现图标系统计划**任务 8**：全量门禁与收尾——跑完整前端门禁与 e2e、残留检查、更新设计文档实现状态并提交。前置：任务 1-7 已完成（HEAD `6436047`）。

## 工作目录

所有工作、git 操作在隔离工作区：
`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\icon-system`

当前 HEAD 为 `6436047`。前端命令在 `frontend/` 目录下运行。

## 任务描述（计划任务 8 全文）

### 步骤 1：全量前端门禁

在 `frontend/` 运行：
- `npx tsc -b` → 预期 exit 0
- `npx eslint src` → 预期 exit 0（**注意**：若报错全部为已知既有 lint 债（任务 5/6/7 已记录的 @ts-nocheck、set-state-in-effect、no-explicit-any、未用变量等），逐个确认属于既有债后如实上报，不要顺手清理）
- `npx vitest run` → 预期 16 文件 / 130 passed

### 步骤 2：驾驶舱 e2e

在 `frontend/` 运行：`npx playwright test e2e/enterprise-cockpit.spec.ts`
预期：1 passed

### 步骤 3：全仓残留检查

在仓库根运行：
- `rg "RobotOutlined|EnvironmentOutlined|NotificationOutlined" frontend/src -g "*.tsx" -g "*.ts"` → 预期零命中（排除 mobile 目录，即结果应为空）
- `rg "<AppIcon" frontend/src -g "*.tsx" -g "*.ts" | Measure-Object` → 统计并汇报总数（预期约 45 处：ModuleNav 10 + MainLayout 7 + RegulationList 4 + AI 14 + location 8 + notice 1 + safety 1）
- `git log --oneline 99120f5..HEAD` 列出本分支全部提交，核对消息链

### 步骤 4：更新文档与台账

1. 在设计文档 `docs/superpowers/specs/2026-08-16-icon-system-design.md` 末尾追加「实现状态」小节：

```markdown
## 10. 实现状态

（日期：2026-08-17）本设计已按实现计划落地：AppIcon + 24 个本地 SVG 资产、驾驶舱模块导航 10 项、主导航业务菜单 7 项、法规类型 4 项、AI 标识 14 处、位置/通知/安全 10 处全部替换完成；通用操作图标与 5 个通用菜单入口保留 AntD。已知遗留：部分文件存在既有 eslint lint 债（任务 8 记录）；移动端 lucide 统一为第二阶段。
```

2. 更新本工作区 `TASKS.md` 快照（如存在；若不存在则跳过，主工作区 TASKS.md 由主控维护）。
3. 提交文档更新：

```bash
git add docs/superpowers/specs/2026-08-16-icon-system-design.md
git commit -m "docs(icon-system): mark design spec as implemented"
```

### 步骤 5：图谱同步

- 若 `codegraph` 命令可用：运行 `codegraph sync .`（仓库根，超时上限 5 分钟）；不可用/失败则如实说明并跳过。
- `graphify update .`：尝试运行（超时上限 10 分钟）；若超时/失败，如实上报并标记为已延后（主控会另行处理），**不要**反复重试。

## 约束

- 除设计文档（与 TASKS.md 台账）外不得改动任何源码；发现门禁失败先判断是否既有债，不擅自修复；
- 不提交临时文件；提交信息精确；`git show --check` 干净；
- 若门禁出现**本次引入**的失败（非既有债），以 BLOCKED 上报附完整输出。

## 验证清单（提交后逐项确认）

1. `npx tsc -b`、`npx vitest run`、`npx playwright test e2e/enterprise-cockpit.spec.ts` 结果
2. `npx eslint src` 结果（如有既有债，附清单与对比证据）
3. 残留检查命令输出
4. 设计文档「实现状态」已追加并提交
5. `git show --stat HEAD` 恰含设计文档一个文件

## 认领方式（必须先做）

在目录 `C:\Users\55061\Documents\数字化预案自动生成 2` 下运行：

```
python "C:\Users\55061\.codex\skills\codex-custom-subagents\scripts\claim_task.py" --workspace . --agent deepseek_anthropic_worker --model deepseek-v4-flash --provider deepseek_anthropic
```

无子命令即原子认领一个 pending 任务，返回 JSON 含 `path`；**先读该文件**再开始。认领 status 非成功立即上报。

## 汇报格式

完成后汇报：
- **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 验证清单逐项结果（含命令输出摘要）
- 提交 SHA 与文件清单
- 图谱同步结果（codegraph/graphify）
- 自审发现（如有）
- 任何问题或疑虑
