# Codex Custom Subagents task handoff v1

Task: task_02_review_spec

## 规格合规审查：任务 2（normalize_signs 规范化函数）

你正在审查一个实现是否与其规格匹配。不要信任实现者的报告，必须独立阅读实际代码验证。

### 要求的内容（任务 2 规格）

**文件：**

* 修改：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\ai-sign-review\backend\app\services\risk_notice_card_service.py`
* 测试：`C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\ai-sign-review\backend\tests\test_risk_notice_card_service.py`

**要求**：`normalize_signs(signs, max_per_category=2, max_total=8)`——过滤非法 svg_name（对照 SIGN_GROUPS ∪ DEFAULT_SIGN_GROUP 的合法集）、去重、按 SIGN_CATEGORY_ORDER 排序、每类 ≤2、总数 ≤8。

**范围限制**：只改 2 文件；commit 消息 `feat(risk-notice-card): add sign normalization helper`。

### 你的工作

1. `cd C:\Users\55061\Documents\数字化预案自动生成 2\.worktrees\ai-sign-review`，`git show 5157f5e` 逐行核对。
2. 核对：VALID_SVG_NAMES 覆盖完整（含 DEFAULT_SIGN_GROUP）、过滤/去重/排序/限量逻辑正确、测试断言有效、提交范围与消息。
3. 门禁实测：`cd backend && python -m pytest tests/test_risk_notice_card_service.py -v`（预期全 PASS）+ 全量回归。
4. 报告：✅ 符合规格 或 ❌ 发现问题（file:line）。

### 上下文

* worktree 独立分支 codex/ai-sign-review，审查只读，不修改文件、不提交。
* 任务 5（快照透传）会调用 normalize_signs。
