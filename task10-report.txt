任务 10 已完成并提交。汇报如下：

## 执行报告

**计划：** `docs/superpowers/plans/2026-08-04-risk-mapping-workbench.md` 任务 10
**提交：** `6d79bad` feat(risk-mapping): connect overview and legacy form to workbench data（独立 commit，仅含任务 10 的 5 个文件）

### 修改文件

- 新建 [RiskDistributionStage.tsx](<C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/components/enterprise/riskMapping/RiskDistributionStage.tsx>)：Konva 只读渲染当前楼层底图（异步加载图片）、分区多边形（按 `effective_color` 填充描边）、风险点圆点、分区名标签与 `canvas_texts` 文字标注；支持 `floorId` 与 `onZoneClick`，另加了可选 `highlightZone` 用于回显高亮。
- 修改 [RiskOverviewPage.tsx](<C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/pages/Enterprise/RiskOverviewPage.tsx>)：顶部新增楼层选择器（数据源 `listEnterpriseFloors`，默认选中 `is_default` 楼层），Q1 占位 `FloorPlanHeatmap` 替换为 `RiskDistributionStage`；点击分区多边形会联动层级树（`selectedKeys` 受控）与拓扑图高亮。
- 修改 [RiskManagementTab.tsx](<C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/pages/Enterprise/RiskManagementTab.tsx>)：操作区新增「四色分布图工作台」入口；同时 zone 创建/更新透传 `floor_plan_polygon`，确保旧表单绘制的 v2 多边形真正落库。
- 修改 [EnterpriseDetailPage.tsx](<C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/pages/Enterprise/EnterpriseDetailPage.tsx>)：`RiskManagementTab` 的 `floorPlanUrl` 优先取默认楼层接口（`enterprise-floors` 中 `is_default` 的 `floor_plan_url`），无默认楼层/接口未就绪时回退 `enterprise.floor_plan_url`。
- 修改 [RiskZoneForm.tsx](<C:/Users/55061/Documents/数字化预案自动生成 2/frontend/src/components/enterprise/RiskZoneForm.tsx>)：`floor_plan_polygon` 改为 v2 结构（`version: 2` + `color_source` + `color` + `polygons[]`），确认时写入 `crypto.randomUUID()` 与分区名标签，打开弹窗时兼容回读 v2/旧结构顶点。

### 验证结果

- `cd frontend && npx tsc -b`：通过（0 错误）
- `npx vitest run src/store/riskMappingWorkbenchStore.test.ts`：19 passed
- `git diff --check`：无空白/冲突标记问题

### 自审

- 与计划差异：计划样例中楼层选择器用 `overview.floors` 作为选项源，但后端 `/overview` 实际只返回单楼层，若照搬会导致选择器永远只有一个选项，故改用 `listEnterpriseFloors` 提供全部楼层；另按需求中的「文字/标签」补充渲染了 `canvas_texts`。
- 联动方向：分布图点击分区 → 层级树选中该分区 + 拓扑/多边形高亮；树点击对象节点仍会联动到所属分区（保持原有行为）。
- 范围控制：未修改 TASKS.md、计划文件、后端文件；工作区中任务 9 的未提交改动（WorkbenchCanvas、Store 等 5 个文件）均未纳入本次提交，仍保留在工作区，由任务 9 自行提交。