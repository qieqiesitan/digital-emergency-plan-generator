任务 2 已完成并通过自审。以下是汇报：

## 执行报告

**计划：** `docs/superpowers/plans/2026-08-04-risk-mapping-workbench.md` — 任务 2（后端服务纯函数与 Schema）
**分支：** codex/protego-integration
**提交：** `e424fa4` — `feat(risk-mapping): add workbench schemas and geometry service`（未执行 git finish/推送）

### 修改文件（仅限指定 3 个）

- 创建 [risk_mapping_service.py](C:/Users/55061/Documents/数字化预案自动生成 2/backend/app/services/risk_mapping_service.py)：`LEVEL_ORDER`/`LEVEL_COLORS` 常量、`normalize_polygon`（旧版 points → v2 结构）、`validate_polygon_v2`、`effective_color`（手动色优先）、`max_risk_level`、`ensure_default_floor`、`cascade_counts`，严格按计划步骤 2.1 实现。
- 修改 [risk_management.py](C:/Users/55061/Documents/数字化预案自动生成 2/backend/app/schemas/risk_management.py)：新增楼层（FloorCreate/Update/Response）、多边形（RiskPolygonPoint 带 0-100 范围校验、RiskPolygon、RiskZoneFloorPlanPolygon、RiskCanvasText）、扩展分区/对象 Schema（新增 floor_id、floor_name、max_risk_level、effective_color，RiskObjectCreate 增加"风险点必须绑定分区和坐标"校验）、批量保存（BatchSaveRequest/Response）与工作台/总览（WorkbenchResponse、OverviewResponse）Schema，按步骤 2.2 实现。
- 创建 [test_risk_mapping_service.py](C:/Users/55061/Documents/数字化预案自动生成 2/backend/tests/test_risk_mapping_service.py)：3 个纯函数测试，按步骤 2.3 实现。

### 测试结果

```
cd backend && .venv\Scripts\python.exe -m pytest tests/test_risk_mapping_service.py tests/test_risk_mapping_migration.py -v
15 passed in 0.47s
```

新服务测试 3/3 通过，任务 1 迁移测试 12/12 通过，无回归。

### 自审

- Schema/服务模块导入冒烟通过，`app.routers.risk_management`、`app.routers.enterprises` 导入正常。
- 行为级验证：`RiskZoneFloorPlanPolygon` 拒绝越界坐标、`RiskObjectCreate` 拒绝无分区/坐标的风险点、`effective_color` 手动色与自动色逻辑、顶点数校验均符合预期。
- `git diff --check` 无空白/冲突标记错误；提交仅含上述 3 个文件（199 行新增、7 行删除）。
- 过程中修复一处计划代码落地问题：`RiskObjectCreate` 按计划原文为多行类体，我最初压缩成单行导致 `IndentationError`，已改为标准多行类体并重跑全部测试通过。

### 备注（需知悉）

- 按约束未修改 TASKS.md（其快照更新留给主控/任务 11）；修改前按铁律执行了 `git save`，保存点 `04f3ba9` 顺带收纳了会话前已存在的 TASKS.md 改动与 review 文本文件，内容无丢失，与仓库既有的 savepoint 惯例一致。
- 已知中间态回归（TASKS.md 已登记）：在任务 3/4 完成前，旧 `POST /zones` 尚未传 `floor_id` 会因模型 NOT NULL 约束失败，属计划内后续任务处理范围，本任务未改动路由器。