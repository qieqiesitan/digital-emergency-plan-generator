# 预案生成系统接入 PROTEGO 商城方案

## 摘要

将「数字化预案自动生成系统 2」的 AI 预案生成能力以 **后端 API 网关** 模式接入 PROTEGO 商城。客户在商城下单并支付后，系统自动将订单和采集的企业资料推送到预案生成系统，触发 AI 自动生成预案全文，完成后回调通知商城，客户在订单详情页下载 DOCX 文件。

**原则**: 最小侵入、复用现有架构、两边独立部署、API 解耦。

---

## 架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                        PROTEGO 商城                               │
│                                                                   │
│  下单 → 支付 → 资料收集 → [推送到预案系统] → 接收回调 → 下载交付  │
│                              │                         ▲          │
│                    ReportGatewayService              回调API       │
│                         (HMAC签名)                  (HMAC验签)     │
└─────────────────────────────┼─────────────────────────┼──────────┘
                              │   ywt-net (Docker)      │
                              ▼                         │
┌──────────────────────────────────────────────────────────────────┐
│                     数字化预案自动生成系统                         │
│                                                                   │
│  /api/external/plans ─→ 自动建企业 → 建预案项目 → AI生成 → 导出   │
│                              │                                    │
│                     /api/external/callback (HMAC)                 │
└──────────────────────────────────────────────────────────────────┘
```

---

## 接口设计

### 预案生成系统新增对外 API（3 个端点）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/external/plans` | POST | 创建预案并触发AI生成 |
| `/api/external/plans/{taskId}/status` | GET | 查询生成进度 |
| `/api/external/plans/{taskId}/files/{fileId}` | GET | 下载生成的文件 |

**认证**: 所有 `/api/external/*` 端点使用 HMAC-SHA256 签名验证。共享密钥通过环境变量注入，两边一致。

**POST /api/external/plans 请求体**:
```json
{
  "external_order_id": "PROTEGO-order-123",
  "external_user_id": "user-456",
  "plan_type": "comprehensive",
  "enterprise": {
    "name": "西安喜来登大酒店",
    "industry": "酒店",
    "contact_name": "张三",
    "contact_phone": "13800138000"
  },
  "documents": [
    {"name": "企业营业执照.pdf", "url": "https://protego-oss/...", "type": "license"},
    {"name": "风险评估报告.docx", "url": "https://protego-oss/...", "type": "risk_report"}
  ],
  "callback_url": "http://protego-server:8081/api/v1/callbacks/report-status"
}
```

**响应**:
```json
{
  "task_id": "task-uuid-789",
  "status": "accepted",
  "estimated_minutes": 15
}
```

### PROTEGO 商城新增回调端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/callbacks/report-status` | POST | 接收预案生成系统的完成通知 |

**请求体**:
```json
{
  "task_id": "task-uuid-789",
  "external_order_id": "PROTEGO-order-123",
  "status": "completed",
  "files": [
    {"name": "综合应急预案.docx", "url": "http://emergency-plan-backend:8000/api/external/plans/task-uuid-789/files/file-abc", "size": 245760},
    {"name": "风险评估报告.docx", "url": "http://...", "size": 123456}
  ]
}
```

---

## 实现要点

### A. 预案生成系统侧（Python FastAPI）
1. 新增 `app/routers/external.py` 路由器，挂载 `/api/external` 前缀
2. 新增 `app/middleware/hmac_auth.py` — 对 `/api/external/*` 做 HMAC 签名验证
3. `POST /api/external/plans` 流程：
   - 验签 → 按 `external_user_id` 查找或自动创建用户 → 按企业名查找或自动创建企业 → 创建 PlanProject → 后台异步触发全章节 AI 生成 → 生成完调用 DOCX 导出 → 回调 PROTEGO
4. 复用已有的 `_gen_bg.py` 后台生成任务机制
5. 新增 `app/services/external_service.py` — 封装回调 HTTP 调用（HMAC 签名 + 重试 3 次）
6. 新增 `app/services/external_file_store.py` — 下载外部文件 URL 到本地 uploads 目录

### B. PROTEGO 商城侧（Spring Boot）
1. **重写 `ReportGatewayService`**：将当前存根改为真实 HTTP 调用
   - `pushToExternal(order)` — 组装请求体 → HMAC 签名 → POST 到预案系统
   - `queryStatus(taskId)` — GET 查询生成进度
2. **新增 `CallbackController`** + `ReportCallbackService`：
   - 验签 → 更新 `report_requests` 表状态 → 下载文件到 OSS/MinIO → 更新 order 状态为 `report_ready`
3. **新增 `HmacUtils.java`** — HMAC-SHA256 签名/验签工具类
4. **新增配置项** `application.yml`：
   ```yaml
   report-system:
     base-url: http://emergency-plan-backend:8000
     shared-secret: ${REPORT_HMAC_SECRET}
     callback-host: http://protego-server:8081
   ```
5. **OrderService 扩展**：支付确认成功后，自动调用 `ReportGatewayService.pushToExternal(order)` 触发预案生成
6. **数据库表**：已有 `report_requests` 和 `report_api_logs` 两张表，字段基本够用，只需补充 `external_task_id` 和进度字段

### C. 前端改动（最小化）
1. 订单详情页 `OrderDetail.vue`：在订单状态流中新增「预案生成中」状态卡片，显示进度百分比（轮询 `/api/reports/by-order/{orderId}`）
2. 预案生成完成后显示下载按钮，点击下载 DOCX 文件

---

## 数据映射

| PROTEGO 字段 | 预案系统字段 | 转换逻辑 |
|-------------|------------|---------|
| `Order.id` | `external_order_id` | 透传，用于回调关联 |
| `Order.userId` | `external_user_id` | 透传，预案系统自动查找/创建用户 |
| `Order.enterpriseName` | `Enterprise.name` | 按名称去重，不存在则自动创建 |
| `Order.projectDesc` | 注入 AI 提示词上下文 | 作为项目背景信息 |
| `Product.planType` 映射 | `plan_type` | 需要一张配置映射表（见下文） |
| 资料收集中上传的文件 | `documents[]` | 下载到预案系统本地作为 AI 上下文 |
| `Order.contactName` / `contactPhone` | 企业联系人 | 自动填充到企业信息 |

### 商品类型 → 预案类型映射

在 PROTEGO `products` 表新增 `plan_type` 字段（VARCHAR），下单时带入 Order → 推送时映射。示例：

| 商品名 | plan_type |
|--------|-----------|
| 综合应急预案编制 | `comprehensive` |
| 专项应急预案编制 | `special` |
| 现场处置方案编制 | `on_site` |
| 全套预案编制 | `all` |

---

## 部署方案

两边系统已共享 `ywt-net` Docker 网络，通过容器名互访：

```yaml
# PROTEGO docker-compose.yml 新增环境变量
protego-server:
  environment:
    REPORT_SYSTEM_BASE_URL: http://emergency-plan-backend:8000
    REPORT_HMAC_SECRET: ${REPORT_HMAC_SECRET}

# 预案系统 docker-compose.yml 新增环境变量
backend:
  environment:
    EXTERNAL_API_HMAC_SECRET: ${REPORT_HMAC_SECRET}
    PROTEGO_CALLBACK_URL: http://protego-server:8081
```

两边用同一个 `REPORT_HMAC_SECRET`（通过 `.env` 或密钥管理注入）。

---

## 关键边界

- **不合并用户体系**：两边用户独立，通过 `external_user_id` 映射关联
- **不嵌入前端编辑器**：商城用户不接触预案编辑界面，只需看到生成进度和下载
- **AI API Key 不传递**：预案系统的 AI 配置独立管理（商城系统不感知用哪个模型）
- **文件中转**：预案系统生成的文件通过 API URL 暴露，PROTEGO 下载后存到自己的 MinIO/OSS

---

## 测试方案

1. **单元测试**：HMAC 签名/验签工具类、数据映射逻辑
2. **集成测试**：模拟完整流程 — 创建订单 → 推送预案系统 → 等待生成 → 接收回调 → 验证文件可下载
3. **异常测试**：预案系统不可达（超时/5xx）、签名验证失败、回调重复通知幂等
4. **冒烟测试**：在 Docker 环境中端到端走一遍完整流程

---

## 假设

- 两边系统部署在同一 Docker 网络内（`ywt-net` 已存在，确认可用）
- 预案生成系统的 AI API Key 已由运营人员提前配置好
- 企业信息不完整时预案系统用 AI 补全（已有提示词模板支持）
- 不需要实时流式展示 AI 生成内容给商城用户（仅显示进度）
- `products` 表新增字段在已有 Flyway 迁移机制下安全执行
