# 法规条文精准匹配 —— 优化方案 V2.0

> 版本: V2.0 | 日期: 2026-07-17 | 状态: 待实施（含 V1.0 代码审查发现 + 增强方案）

---

## 一、问题诊断 + V1.0 Bug 清单

### 1.1 根本症结
当前系统的条文检索链路是一条「法规级粗放管道」：
section_key -> 匹配法规ID -> 读取法规全文 -> 返回全部条文

不做条文级别的独立索引、独立评分、独立召回。噪音比约 95%。

### 1.2 V1.0 实现中引入的致命缺陷

| Bug ID | 严重度 | 文件:行 | 症状 |
|--------|--------|---------|------|
| B1 | 致命 | __init__.py:9 | threading.Lock 阻塞事件循环，并发请求服务卡死 |
| B2 | 致命 | graph.py:112 | query_by_topic() 缩进错误，非匹配节点触发 UnboundLocalError |
| B3 | 致命 | vector_store.py:99 | ChromaDB where 语法无效，向量搜索静默返回空 |
| B4 | 致命 | sync.py:279 | log_event() 错误缩进在 except 块内 |
| B5 | 高危 | retriever.py:186 | fallback 路径跳过 topic 过滤，噪音比回到 95% |
| B6 | 高危 | context_builder.py:42 | 永久缓存无失效机制 |

### 1.3 精度瓶颈

| 检索层 | 当前做法 | 精度问题 |
|--------|---------|---------|
| plan_type -> 法规 | index.yaml 静态ID列表 | 精确但静态 |
| section -> topic | section_topics.yaml 正则 | 依赖AI标注质量 |
| topic -> 条文 | 无：返回整部法规所有条文 | 核心缺口：噪音比~95% |
| vector 语义补充 | 仅兜底(>=30条)，返回法规级节点 | 最精确信号被浪费 |
| 精确关键词匹配 | 不存在 | 无法用条文号直接命中 |

### 1.4 量化评估

典型场景（comprehensive 预案 x sec_响应处置）：
- 匹配法规: 6部 x ~60条 = 360条注入prompt
- 实际相关: 12-18条
- 噪音比: ~95%

---

## 二、目标架构：四层检索金字塔

```
                    +------------------+
                    |  L3: LLM Rerank  |  <- NEW V2.0: Top-30 -> 精排5-8条
                    +------------------+
                    | L2: 混合召回+打分  |
                    |  图谱topic匹配    |
                    |  +向量语义搜索    |
                    |  +scorer多维打分  |
                    +------------------+
                    | L1: BM25 关键词   |  <- NEW V2.0: 条文号/法规简称精确匹配
                    +------------------+
                    | L0: 图谱精确匹配   |
                    |  plan_type -> 法规 |
                    +------------------+
```

### 2.1 核心设计原则

1. **条文是一等实体**：独立ID、独立图谱节点、独立向量
2. **四级检索逐层收窄**：BM25精确命中 -> 混合召回宽进 -> scorer打分 -> LLM精排严出
3. **最终注入5-8条**，保证LLM注意力聚焦
4. **保持向后兼容**
5. **异步安全**：asyncio.Lock替代threading.Lock
6. **缓存可失效**：入库时主动失效 + 5分钟TTL兜底

### 2.2 目标数据流

```
法规入库 (sync.py)
  |
  +-> 图谱写入（法规节点 + article子节点 + topics边 + refs边）
  |
  +-> 条文级写入
        +-> texts/{reg_id}.md          (保持不变)
        +-> 向量索引: id = art_{reg_id}_{article_number}
        +-> BM25倒排索引 (NEW)
        +-> 缓存失效通知 (NEW)

预案生成 (_build_section_prompt)
  |
  +-> RegulationContextBuilder.get_chapter_context()
        +-> retriever.retrieve_articles()
              |
              +-> Step 0: BM25精确匹配 (NEW)
              +-> Step 1: 图谱条款文召回 + fallback topic过滤
              +-> Step 2: 向量语义召回（增强查询，不设最小条数限制）
              +-> Step 3: 合并去重 + 多维打分 -> Top-30
              +-> Step 4: LLM二次精排 (NEW) -> 5-8条
              +-> Step 5: 按法规分组 -> 注入prompt
```

### 2.3 文件变更清单

```
新增:
  regulations/bm25_index.py       -- BM25倒排索引 (NEW)
  regulations/llm_reranker.py     -- LLM二次精排 (NEW)

修改:
  regulations/__init__.py         -- asyncio.Lock替代threading.Lock
  regulations/graph.py            -- 修复query_by_topic缩进 + infer_article_topics
  regulations/vector_store.py     -- 修复ChromaDB filter语法
  regulations/retriever.py        -- 集成BM25 + 增强fallback topic过滤 + 改进语义查询
  regulations/context_builder.py  -- 缓存失效机制 + 接入LLM rerank + 删除旧方法
  regulations/sync.py             -- 修复log_event缩进 + 缓存失效 + BM25重建
  routers/generation.py           -- get_retriever()调用处加await
  services/chat_dispatch.py       -- 法规tool的get_graph()调用处加await
```

---

## 三、Phase 0: 修复V1.0致命Bug（0.5天，前置条件）

### 3.1 B1: threading.Lock -> asyncio.Lock

文件: regulations/__init__.py
影响: 并发请求时服务卡死（"完全无法使用"的根因）

```python
import asyncio

_retriever = None
_lock = asyncio.Lock()

async def get_retriever():
    global _retriever
    if _retriever is not None:
        return _retriever
    async with _lock:
        if _retriever is not None:
            return _retriever
        graph = RegulationGraph()
        graph.load()          # 纯文件IO，不涉及竞态
        vector_store = None
        try:
            vector_store = RegulationVectorStore()
        except ImportError:
            pass
        _retriever = RegulationRetriever(graph, vector_store)
    return _retriever
```

所有调用get_retriever()的地方需加await：
- chat_dispatch.py: _get_regulation_stats, _list_regulations, _search_regulations, _search_regulation_articles
- context_builder.py: build_for_plan, get_chapter_context
- injector.py: inject_regulations
- generation.py: _build_section_prompt (间接触发)

### 3.2 B2: query_by_topic() 缩进修复

文件: regulations/graph.py:105-124

```python
def query_by_topic(self, topic, limit=10):
    results = []
    for nid, data in self._g.nodes(data=True):
        topics = data.get("topics") or []
        if topic in topics and data.get("status") != "abolished":
            node = dict(data)
            node["id"] = nid
            node.setdefault("label", node.get("code") or node.get("id", ""))
            node.setdefault("full_name", node.get("label", ""))
            node.setdefault("code", "")
            node.setdefault("topics", [])
            results.append(node)         # <- 移到if内部
            if len(results) >= limit:    # <- 移到if内部
                break
    return results
```

### 3.3 B3: ChromaDB filter 修复

文件: regulations/vector_store.py:99

```python
# 修复前: where = {"regulation_id": {"": filter_ids}}
# 修复后:
where = {"regulation_id": {"$in": filter_ids}} if filter_ids else None
```

### 3.4 B4: log_event 缩进修复

文件: regulations/sync.py:260-290

log_event()从auto-classify的except块内移到平级，末尾加缓存失效：

```python
    # 5. 写入article子节点
    try:
        for art in parsed.get("articles", []):
            graph.add_article_node(regulation_id, art)
    except Exception as e:
        logger.warning("Article node creation failed: %s", e)

    # 6. 自动归类
    try:
        mgr = ArticleIndexManager()
        cl = mgr.auto_classify(regulation_id, ...)
        if cl: mgr.update_index(regulation_id, cl)
    except Exception as e:
        logger.debug("Auto-classify skipped: %s", e)

    # 7. 历史日志（正确位置）
    log_event(regulation_id, "created", operator, {...})

    # 8. 缓存失效 (NEW)
    from app.regulations.context_builder import RegulationContextBuilder
    RegulationContextBuilder.invalidate_all()
```

### 3.5 B5: fallback路径增加topic过滤

文件: regulations/retriever.py:_graph_article_recall

```python
if not arts:
    for art in self._load_articles(reg):
        art_text = art.get("text", "")
        art_num = art.get("number", "")
        # V2.0: 在条文文本中搜索section_topics关键词
        if section_topics:
            matched = any(st in art_text or st in art_num for st in section_topics)
            if not matched:
                continue  # 跳过不相关条文
        # ... 构造 ArticleCandidate
```

### 3.6 B6: 缓存失效机制

文件: regulations/context_builder.py

```python
import time

class RegulationContextBuilder:
    _cache: dict = {}
    _cache_ttl: float = 300.0  # 5分钟

    @classmethod
    def invalidate_all(cls):
        cls._cache.clear()

    @classmethod
    def invalidate(cls, plan_type: str):
        keys = [k for k in cls._cache if k[0] == plan_type]
        for k in keys:
            del cls._cache[k]

    def build_for_plan(self, plan_type, enterprise_data):
        cache_key = (plan_type, enterprise_data.get("id", ""))
        if cache_key in self._cache:
            if time.time() - self._cache[cache_key]["_ts"] < self._cache_ttl:
                return self._cache[cache_key]["data"]
        result = ...  # retrieval
        self._cache[cache_key] = {"data": result, "_ts": time.time()}
        return result
```

---

## 四、Phase 1: BM25精确关键词匹配（NEW, 0.5天）

### 4.1 新增文件: regulations/bm25_index.py

职责：对条文全文建立BM25倒排索引，作为检索管路第0级（L1），高分命中直接进入最终输出。

设计要点：
- 中文2-gram分词 + 条文号模式「第X条」特殊保留
- 法规编号模式「GB/T 29639-2020」作为整体token
- 索引持久化到data/bm25_index.json
- 入库时与向量索引同步重建

BM25ArticleIndex类：
- index_articles(articles, reg_id, reg_name, reg_code) - 批量入库
- search(query, top_k=10) -> [{id, score, meta, text}] - BM25检索
- _tokenize(text) - 中文2-gram + 条文号/编号模式 + 英文词
- persist() / load() - 持久化/恢复

### 4.2 集成到RegulationRetriever

```python
class RegulationRetriever:
    def __init__(self, graph, vector_store, scorer=None, bm25_index=None):
        self.bm25_index = bm25_index

    def _bm25_article_recall(self, query_text, top_k=5):
        if not self.bm25_index:
            return []
        return [ArticleCandidate(id=r["id"], ..., is_exact_match=r["score"]>5.0)
                for r in self.bm25_index.search(query_text, top_k)]
```

---

## 五、Phase 2: 条文级topic标注增强（0.5天）

### 5.1 条文topic推断

文件: regulations/graph.py 新增 infer_article_topics()

在条文文本中搜索TopicValidator.VALID_TOPICS关键词：
- 出现 >= 2次 -> 纳入该topic
- 出现1次且与法规级topic重叠 -> 纳入

在fallback路径中用此方法推断topic后再参与section_topics过滤。

---

## 六、Phase 3: 增强语义查询构造（0.25天）

改造 _build_semantic_queries，利用企业数据构造多条查询：

1. 主查询: plan_type中文名 + section标题
2. 行业查询: 行业名称 + section标题 + 规范要求
3. 风险源查询: 前3个风险源名称 + section标题 + 法规条款
4. 危化品查询: 前3个危化品名称 + 安全管理 + section标题
5. 企业名称查询: 企业名称 + section标题

向量搜索对每条查询取top_k=25，并集去重。

---

## 七、Phase 4: LLM二次精排（NEW, 0.5天）

新增文件: regulations/llm_reranker.py

流程：
scorer打分Top-30 -> 一次LLM call (temperature=0, max_tokens=500) -> 输出5-8条article_id
失败时静默回退到scorer原始排序

LLMReranker类：
- rerank(candidates, section_title, section_text, plan_type) -> list[str]

设计考量：
- 复用用户已有AI配置，不增加第三方成本
- 每章仅1次rerank call，30条batch传入
- temperature=0确保排序稳定

---

## 八、Phase 5: 跨法规引用完善 + 清理（0.25天）

1. sync.py入库时接入CrossReferenceDetector（代码已有，未接入）
2. 删除context_builder.py旧_format_context方法（重复定义）
3. 清理graph.py重复import re

---

## 九、实施顺序与工时汇总（3天）

| Phase | 内容 | 估时 | 优先级 |
|-------|------|------|--------|
| Phase 0 | 修复致命Bug B1-B6 | 0.5天 | P0 |
| Phase 1 | BM25精确匹配索引+接入 | 0.5天 | P1 |
| Phase 2 | 条文级topic标注 | 0.5天 | P1 |
| Phase 3 | 增强语义查询构造 | 0.25天 | P2 |
| Phase 4 | LLM二次精排 | 0.5天 | P2 |
| Phase 5 | 引用完善+清理 | 0.25天 | P3 |
| Phase 6 | 迁移脚本+回归测试 | 0.5天 | P3 |

---

## 十、关键设计决策

**Q: 为什么BM25而非ElasticSearch？**
A: ~100部法规，轻量内存BM25足够，零外部依赖，部署复杂度不变。

**Q: 为什么加LLM Rerank？**
A: scorer的topic匹配依赖人工标注，总有漏标/错标。LLM能理解语义关联。每章仅1次调用，成本可控。

**Q: 为什么选asyncio.Lock？**
A: 全局单例必须保证一次初始化。graph.load()读JSON纯IO，不影响并发。锁仅在首次初始化时短暂持有。

---

## 十一、验收标准

1. B1-B6全部修复：并发不卡死、query_by_topic正确、向量搜索生效、日志正确写入
2. BM25精确命中：「安全生产法第二十一条」直接返回该条文
3. 噪音比95% -> <50%：360条 -> 5-8条
4. 向量语义搜索始终启用（无>=30限制）
5. 新法规入库后5分钟内同预案类型可见
6. LLM rerank失败时静默回退，不影响生成流程
7. 所有旧API返回格式不变