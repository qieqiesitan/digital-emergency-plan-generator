# 数字化应急预案自动生成系统 — 前端开发计划

> **版本**：1.0 | **日期**：2026-06-05 | **关联**：技术方案 v1.0、10 份 PRD、功能清单 v1.0

---

## 一、技术基础

### 1.1 技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | React | 18.x | SPA 主框架 |
| 语言 | TypeScript | 5.x | 全量类型覆盖 |
| 构建工具 | Vite | 5.x | 开发/生产构建 |
| UI 组件库 | Ant Design | 5.x | 企业级组件，按需导入 |
| 图标库 | @ant-design/icons | 5.x | 与 Ant Design 配套 |
| 富文本编辑器 | TipTap | 2.x | ProseMirror，Markdown/HTML 互转 |
| HTTP 客户端 | Axios | 1.x | 拦截器、Token 刷新 |
| 路由 | React Router | 6.x | 嵌套路由 + 路由守卫 |
| 状态管理 | React Context + useReducer | — | 全局状态（认证、当前企业） |
| 服务端状态 | TanStack Query | 5.x | 缓存、分页、乐观更新 |
| Markdown | markdown-it | 14.x | 导出预览渲染 |
| HTML 净化 | DOMPurify | 3.x | XSS 防护 |
| 测试框架 | Vitest | 1.x | 单元测试 + 组件测试 |
| 组件测试 | @testing-library/react | 16.x | React 组件测试 |
| E2E | Playwright | 1.x | 浏览器端到端 |

### 1.2 项目初始化

`ash
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install antd @ant-design/icons react-router-dom axios
npm install @tiptap/react @tiptap/starter-kit @tiptap/extension-table @tiptap/extension-table-row @tiptap/extension-table-cell @tiptap/extension-table-header @tiptap/extension-underline @tiptap/extension-placeholder @tiptap/extension-text-align
npm install @tanstack/react-query markdown-it dompurify dayjs
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom @playwright/test
`

### 1.3 Vite 代理配置

`	ypescript
// vite.config.ts
import { defineConfig } from ''vite'';
import react from ''@vitejs/plugin-react'';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: { ''/api'': { target: ''http://localhost:8000'', changeOrigin: true } },
  },
  resolve: { alias: { ''@'': ''/src'' } },
});
`

## 二、目录结构

`
frontend/
├── public/
│   └── favicon.svg
├── src/
│   ├── main.tsx                        # ReactDOM.createRoot
│   ├── App.tsx                         # QueryClientProvider + RouterProvider
│   ├── routes/
│   │   ├── index.tsx                   # createBrowserRouter
│   │   └── ProtectedRoute.tsx          # 路由守卫
│   ├── layouts/
│   │   ├── AuthLayout.tsx              # 认证页布局（居中卡片）
│   │   └── MainLayout.tsx             # 主布局（侧边栏+顶栏+Outlet）
│   ├── contexts/
│   │   ├── AuthContext.tsx             # 认证上下文
│   │   └── EnterpriseContext.tsx       # 企业上下文
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useCurrentEnterprise.ts
│   │   ├── useAutoSave.ts             # 自动保存（3s debounce PUT）
│   │   ├── useSSE.ts                  # SSE 流式读取
│   │   ├── useDebounce.ts
│   │   └── useConfirmDelete.ts
│   ├── services/
│   │   ├── api.ts                      # Axios 实例+拦截器
│   │   ├── authService.ts
│   │   ├── userService.ts
│   │   ├── enterpriseService.ts
│   │   ├── riskSourceService.ts
│   │   ├── emergencyResourceService.ts
│   │   ├── planService.ts
│   │   ├── sectionService.ts
│   │   ├── generationService.ts        # AI 生成（SSE）
│   │   ├── templateService.ts
│   │   ├── versionService.ts
│   │   ├── exportService.ts
│   │   ├── dashboardService.ts
│   │   └── aiConfigService.ts
│   ├── types/
│   │   ├── auth.ts                     # User, LoginRequest, TokenResponse
│   │   ├── enterprise.ts              # Enterprise, OrgGroup, SurroundingInfo
│   │   ├── riskSource.ts              # RiskSource（含 risk_level 枚举)
│   │   ├── emergencyResource.ts       # EmergencyResource（含 is_external）
│   │   ├── plan.ts                    # PlanProject, PlanSection, PlanStatus
│   │   ├── template.ts                # PlanTemplate, SectionTemplate
│   │   ├── generation.ts             # SSEEvent
│   │   ├── version.ts                # PlanVersion, VersionDiff
│   │   ├── export.ts                 # ExportTask
│   │   ├── dashboard.ts              # DashboardData
│   │   ├── aiConfig.ts               # AIConfig
│   │   └── common.ts                 # PaginatedResponse, ApiResponse
│   ├── components/
│   │   ├── common/
│   │   │   ├── PageHeader.tsx
│   │   │   ├── ConfirmDeleteModal.tsx
│   │   │   ├── LoadingSpinner.tsx
│   │   │   └── EmptyState.tsx
│   │   ├── enterprise/
│   │   │   ├── EnterpriseSwitcher.tsx
│   │   │   ├── OrgStructureEditor.tsx    # Modal
│   │   │   ├── RiskSourceForm.tsx        # Modal
│   │   │   ├── EmergencyResourceForm.tsx # Modal
│   │   │   ├── SurroundingInfoForm.tsx   # Modal
│   │   │   └── RiskLevelTag.tsx
│   │   ├── plan/
│   │   │   ├── SectionTree.tsx           # 递归渲染
│   │   │   ├── SectionTreeNode.tsx
│   │   │   ├── RichTextEditor.tsx        # TipTap 封装
│   │   │   ├── AIGenerateButton.tsx      # 状态机
│   │   │   ├── PlanStatusTag.tsx
│   │   │   ├── PlanTypeTag.tsx
│   │   │   └── ExportProgressModal.tsx
│   │   └── version/
│   │       ├── VersionCompareModal.tsx
│   │       └── VersionRollbackModal.tsx
│   ├── pages/
│   │   ├── Login/LoginPage.tsx
│   │   ├── Register/RegisterPage.tsx
│   │   ├── Dashboard/DashboardPage.tsx
│   │   ├── Enterprise/
│   │   │   ├── EnterpriseListPage.tsx
│   │   │   ├── EnterpriseCreatePage.tsx
│   │   │   ├── EnterpriseEditPage.tsx
│   │   │   └── EnterpriseDetailPage.tsx
│   │   ├── Plan/
│   │   │   ├── PlanListPage.tsx
│   │   │   ├── PlanCreatePage.tsx         # 步骤向导
│   │   │   ├── PlanEditorPage.tsx         # 核心：左树+右编辑
│   │   │   ├── VersionListPage.tsx
│   │   │   └── ExportPreviewPage.tsx
│   │   └── Settings/
│   │       ├── ProfilePage.tsx
│   │       └── AIConfigPage.tsx
│   ├── utils/
│   │   ├── constants.ts               # 预置数据常量
│   │   ├── riskMatrix.ts              # 风险等级矩阵
│   │   ├── formatters.ts             # 格式化工具
│   │   └── validators.ts             # 密码/手机/邮箱校验
│   └── styles/
│       ├── global.css
│       └── print.css
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
`


## 三、组件架构

### 3.1 组件层级树

`
App
├── QueryClientProvider
│   └── RouterProvider
│       ├── AuthLayout
│       │   ├── LoginPage
│       │   └── RegisterPage
│       └── ProtectedRoute
│           └── AuthProvider
│               └── EnterpriseProvider
│                   └── MainLayout
│                       ├── Sider（侧边栏）
│                       │   ├── Logo
│                       │   ├── Menu
│                       │   └── SettingsMenu
│                       ├── Header（顶栏）
│                       │   ├── EnterpriseSwitcher
│                       │   └── UserAvatar
│                       └── Content（Outlet）
│                           ├── DashboardPage
│                           ├── EnterpriseListPage
│                           ├── EnterpriseCreatePage / EditPage
│                           ├── EnterpriseDetailPage（5 Tabs）
│                           ├── PlanListPage
│                           ├── PlanCreatePage（4 Steps）
│                           ├── PlanEditorPage
│                           │   ├── SectionTree
│                           │   └── EditorPanel → RichTextEditor
│                           ├── VersionListPage
│                           ├── ExportPreviewPage
│                           ├── ProfilePage
│                           └── AIConfigPage
`

### 3.2 组件职责矩阵

| 组件 | 职责 | 自身状态 | 关键 Props |
|------|------|----------|-----------|
| AuthProvider | 用户登录状态、Token 自动刷新 | user, isAuthenticated, isLoading | — |
| EnterpriseProvider | 当前企业、企业列表、切换 | currentEnterpriseId, enterprises | — |
| ProtectedRoute | 未登录→/login | — | children |
| MainLayout | 侧边栏+顶栏+内容区 | collapsed | — |
| EnterpriseSwitcher | 下拉切换当前企业 | — | — |
| SectionTree | 递归渲染章节树，选中/展开 | selectedKey, expandedKeys | sections, onSelect |
| RichTextEditor | TipTap 封装，Markdown↔HTML | — | content, onChange, readOnly |
| AIGenerateButton | AI 生成状态机 | idle/loading/done/error | planId, sectionKey, onContent |
| RiskLevelTag | 风险等级彩色标签 | — | level: 重大/较大/一般/低 |
| PlanStatusTag | 预案状态标签 | — | status: draft/completed |


## 四、状态管理

### 4.1 AuthContext

`	ypescript
interface AuthState {
  user: User | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}
interface AuthContextValue extends AuthState {
  login: (email: string, password: string) => Promise<void>;
  register: (data: RegisterRequest) => Promise<void>;
  logout: () => void;
  updateProfile: (name: string) => Promise<void>;
  changePassword: (oldPwd: string, newPwd: string) => Promise<void>;
}
`
Token 存储：localStorage（access_token + refresh_token）。自动刷新由 api.ts 拦截器处理。

### 4.2 EnterpriseContext

`	ypescript
interface EnterpriseState {
  currentEnterpriseId: string | null;
  enterprises: Enterprise[];
  isLoading: boolean;
}
interface EnterpriseContextValue extends EnterpriseState {
  setCurrentEnterprise: (id: string) => void;
  refreshEnterprises: () => Promise<void>;
}
`
currentEnterpriseId 持久化到 localStorage，页面刷新后恢复。

### 4.3 服务端状态（TanStack Query）

所有 API 数据（列表、详情）通过 React Query 管理，享受自动缓存、后台刷新、分页。

`	ypescript
// 企业列表（含分页）
export function useEnterprises(params: EnterpriseQueryParams) {
  return useQuery({
    queryKey: [''enterprises'', params],
    queryFn: () => enterpriseService.list(params),
    placeholderData: keepPreviousData,
  });
}

// 风险源删除（乐观更新）
export function useDeleteRiskSource(enterpriseId: string) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => riskSourceService.delete(enterpriseId, id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [''riskSources'', enterpriseId] });
      queryClient.invalidateQueries({ queryKey: [''enterprise'', enterpriseId] });
    },
  });
}
`

### 4.4 状态流向图

`
localStorage (access_token, refresh_token, enterpriseId)
       │
  ┌────┴─────────────┐
  │   AuthContext     │  user / isAuthenticated
  └────┬─────────────┘
       │
  ┌────┴─────────────┐
  │ EnterpriseContext │  currentEnterpriseId
  └────┬─────────────┘
       │
  ┌────┴──────────────────────┐
  │      TanStack Query        │  服务端状态缓存
  └────┬──────────────────────┘
       │
  ┌────┴──────┐
  │ Axios api │  Token 注入 + 401 刷新
  └───────────┘
`


## 五、路由设计

### 5.1 路由表

| 路由 | 页面组件 | 认证 |
|------|----------|------|
| /login | LoginPage | 否 |
| /register | RegisterPage | 否 |
| /dashboard | DashboardPage | 是 |
| /enterprises | EnterpriseListPage | 是 |
| /enterprises/new | EnterpriseCreatePage | 是 |
| /enterprises/:id | EnterpriseDetailPage | 是 |
| /enterprises/:id/edit | EnterpriseEditPage | 是 |
| /plans | PlanListPage | 是 |
| /plans/new | PlanCreatePage | 是 |
| /plans/:id/edit | PlanEditorPage | 是 |
| /plans/:id/versions | VersionListPage | 是 |
| /plans/:id/preview | ExportPreviewPage | 是 |
| /settings/profile | ProfilePage | 是 |
| /settings/ai-config | AIConfigPage | 是 |

实现方式：React Router v6 的 createBrowserRouter，AuthLayout 和 MainLayout 为布局路由，ProtectedRoute 包裹所有需认证路由。

### 5.2 侧边栏菜单结构

- 🏠 工作台 → /dashboard
- 🏭 企业管理 → /enterprises
- 📋 预案列表 → /plans
- ────────────
- ⚙ 设置
  - 个人资料 → /settings/profile
  - AI 配置 → /settings/ai-config

### 5.3 路由守卫

ProtectedRoute 组件：从 AuthContext 读取 isAuthenticated 和 isLoading。
- isLoading=true → 显示 LoadingSpinner
- isAuthenticated=false → Navigate to /login
- 正常 → 渲染 children


## 六、API 服务层

### 6.1 Axios 实例（services/api.ts）

核心机制：请求拦截器注入 access_token，响应拦截器处理 401 自动刷新。

`	ypescript
const api = axios.create({
  baseURL: '/api/v1',
  timeout: 30000,
  headers: { 'Content-Type': 'application/json' },
});

// 请求拦截器：自动注入 Token
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) config.headers.Authorization = 'Bearer ' + token;
  return config;
});

// 响应拦截器：401 自动刷新
let isRefreshing = false;
let failedQueue: Array<{resolve: Function; reject: Function}> = [];

api.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config;
    if (error.response?.status === 401 && !originalRequest._retry) {
      if (isRefreshing) {
        return new Promise((resolve, reject) => {
          failedQueue.push({ resolve, reject });
        }).then((token) => {
          originalRequest.headers.Authorization = 'Bearer ' + token;
          return api(originalRequest);
        });
      }

      originalRequest._retry = true;
      isRefreshing = true;

      try {
        const refreshToken = localStorage.getItem('refresh_token');
        const { data } = await axios.post('/api/v1/auth/refresh', { refresh_token: refreshToken });
        const newToken = data.data.access_token;
        localStorage.setItem('access_token', newToken);
        localStorage.setItem('refresh_token', data.data.refresh_token);

        failedQueue.forEach(({ resolve }) => resolve(newToken));
        failedQueue = [];

        originalRequest.headers.Authorization = 'Bearer ' + newToken;
        return api(originalRequest);
      } catch (refreshError) {
        failedQueue.forEach(({ reject }) => reject(refreshError));
        failedQueue = [];
        localStorage.removeItem('access_token');
        localStorage.removeItem('refresh_token');
        window.location.href = '/login';
        return Promise.reject(refreshError);
      } finally {
        isRefreshing = false;
      }
    }
    return Promise.reject(error);
  }
);
`

关键点：isRefreshing + failedQueue 机制防止多个 401 同时触发多次刷新。

### 6.2 Service 文件清单

| 文件 | 函数 | API 路由 |
|------|------|----------|
| authService | register, login, refresh, logout | POST /auth/* |
| userService | getProfile, updateProfile, changePassword | GET/PUT /users/me |
| enterpriseService | list, create, get, update, delete, getOrgStructure, updateOrgStructure, getSurrounding, updateSurrounding | /enterprises |
| riskSourceService | list, create, get, update, delete | /enterprises/:id/risk-sources |
| emergencyResourceService | list, create, get, update, delete | /enterprises/:id/resources |
| planService | list, create, get, update, delete, duplicate | /plans |
| sectionService | list, get, update | /plans/:id/sections |
| generationService | generateSection(SSE), generateBatch(SSE), stopGeneration | /plans/:id/generate |
| templateService | list, get | /templates |
| versionService | list, get, create, compare, rollback | /plans/:id/versions |
| exportService | getPreview, exportDocx, validate, getTaskStatus, download | /plans/:id/export |
| dashboardService | getDashboard | /dashboard |
| aiConfigService | get, update, delete, test | /settings/ai-config |


## 七、页面开发清单与规格

### 7.1 页面优先级与依赖

| 序号 | 页面 | 路由 | 优先级 | 依赖组件 | 依赖 Context |
|------|------|------|--------|----------|-------------|
| 1 | LoginPage | /login | P0 | — | Auth |
| 2 | RegisterPage | /register | P0 | — | Auth |
| 3 | DashboardPage | /dashboard | P0 | EnterpriseSwitcher | Auth + Enterprise |
| 4 | EnterpriseListPage | /enterprises | P0 | PageHeader, ConfirmDeleteModal | Auth + Enterprise |
| 5 | EnterpriseCreatePage | /enterprises/new | P0 | PageHeader | Auth |
| 6 | EnterpriseEditPage | /enterprises/:id/edit | P0 | PageHeader | Auth |
| 7 | EnterpriseDetailPage | /enterprises/:id | P0 | OrgStructureEditor, RiskSourceForm, EmergencyResourceForm, SurroundingInfoForm, RiskLevelTag | Auth |
| 8 | PlanListPage | /plans | P0 | EnterpriseSwitcher, PlanStatusTag, PlanTypeTag | Auth + Enterprise |
| 9 | PlanCreatePage | /plans/new | P0 | Steps | Auth + Enterprise |
| 10 | PlanEditorPage | /plans/:id/edit | P0 | SectionTree, RichTextEditor, AIGenerateButton | Auth + Enterprise |
| 11 | ProfilePage | /settings/profile | P0 | — | Auth |
| 12 | AIConfigPage | /settings/ai-config | P0 | — | Auth |
| 13 | ExportPreviewPage | /plans/:id/preview | P1 | ExportPreviewFrame | Auth |
| 14 | VersionListPage | /plans/:id/versions | P1 | VersionCompareModal, VersionRollbackModal | Auth |


### 7.2 页面详细规格

#### LoginPage（/login）

- **布局**：AuthLayout 左右两栏，左侧系统名称+简介，右侧登录卡片
- **字段**：邮箱(Input+格式校验)、密码(Input.Password+显示切换)、记住我(Checkbox，仅记邮箱)
- **交互**：提交→loading→成功跳转/dashboard，失败Alert错误；底部链接到注册
- **状态**：初始/校验失败/提交中/提交失败/已登录自动重定向

#### RegisterPage（/register）

- **布局**：同 LoginPage
- **字段**：姓名(1-100字符)、邮箱、密码(内置规则实时提示：>=8位+字母数字)、确认密码(一致性校验)
- **交互**：注册成功→自动登录→跳转/dashboard；底部链接到登录
- **状态**：初始/校验失败/提交中/提交失败

#### DashboardPage（/dashboard）

- **布局**：标准内容区
- **组件**：4张Statistic统计卡片(企业数/预案总数/已完成/风险源数)+3个快捷新建卡片+最近编辑List(最多5条)
- **数据源**：GET /api/v1/dashboard
- **状态**：加载中Skeleton/空数据EmptyState+引导/正常显示
- **交互**：统计卡片可点击跳转列表，最近编辑条目可点击进入编辑器，快捷新建进入向导

#### EnterpriseListPage（/enterprises）

- **组件**：PageHeader+新建按钮、搜索框(Input.Search)、行业Select筛选、Table
- **Table列**：企业名称(可点击)、行业、员工数、风险源数、预案数、更新时间、操作(编辑/删除)
- **分页**：page_size=20，默认updated_at倒序
- **交互**：删除弹出ConfirmDeleteModal，确认后刷新

#### EnterpriseCreatePage / EditPage

- **CreatePage 表单**：企业名称*(必填)、地址(TextArea)、行业(Select预置+自定义输入)、经营范围(TextArea)、员工人数(InputNumber)、建筑概况(TextArea)
- **EditPage**：同表单但预填数据
- **交互**：保存→POST/PUT→跳转详情页；取消→返回列表
- **状态**：提交中loading/校验失败/提交失败Alert

#### EnterpriseDetailPage — 5个Tab

**Tab-1 基本信息**：Descriptions组件展示+编辑按钮跳转EditPage

**Tab-2 组织架构**：Collapse按小组折叠，每组成员Table；编辑按钮→OrgStructureEditor Modal(增删小组+成员，保存PUT /org-structure)

**Tab-3 风险源**：筛选栏(类别多选+等级Select)+新增按钮→RiskSourceForm Modal；Table按risk_level倒序，操作列编辑/删除；Modal字段(类别Select/名称/位置/描述/可能性Radio/严重性Radio/管控措施TextArea)

**Tab-4 应急资源**：筛选(类别+内外RadioGroup+搜索)+新增→EmergencyResourceForm Modal；Table区分内外部列；Modal中外部分开关控制后续字段

**Tab-5 周边环境**：三个子区(周边单位Card列表/敏感目标Card列表/交通状况Text)+编辑→SurroundingInfoForm Modal(动态增删行表单)

每个Tab右上角Badge显示条目数量


#### PlanListPage（/plans）

- **组件**：PageHeader+新建按钮、EnterpriseSwitcher、Tabs(全部/综合/专项/现场)、Radio(全部/草稿/已完成)、搜索框
- **列表**：Ant Design List，每条显示标题+PlanTypeTag+事故类型(专项/现场)+PlanStatusTag+章节进度条(xx/yy)+更新时间+操作(编辑/删除/复制P1)
- **交互**：点击条目→/plans/:id/edit

#### PlanCreatePage（/plans/new）

- **步骤向导**（Ant Design Steps）：
  - Step1：三个大卡片选择预案类型(综合/专项/现场)，未选中禁用下一步
  - Step2：仅专项/现场显示——Select事故类型(来自当前企业风险源类别去重)，支持手动输入
  - Step3：预案标题(Input，默认值：企业名-预案类型名)+信息摘要确认
  - Step4：确认创建按钮→POST /plans→跳转编辑器

#### PlanEditorPage（/plans/:id/edit）——核心页面

- **布局**：左栏280px可折叠 + 右栏弹性
- **左栏(SectionTree)**：可编辑预案标题+PlanStatusTag+递归章节树(层级缩进20px/级)；每个节点状态图标:已填绿色勾/未填灰色圈/必填未填红色Badge/ai_generatable显示机器人图标；点击节点切换编辑目标；当前选中高亮
- **右栏(EditorPanel)**：顶部工具栏(章节标题+字数统计+AIGenerateButton+全屏按钮P1)；RichTextEditor(TipTap工具栏:加粗/斜体/H1-H3/列表/缩进/表格/撤销重做，占位符文案)；底部状态栏(保存状态指示器)；AI生成时编辑器readOnly+流式追加
- **自动保存(useAutoSave)**：内容变更3秒debounce→PUT /sections/:key；状态指示:保存中灰色/已保存绿色/未保存黄色
- **AI生成交互**：点击按钮→loading→SSE流式→done闪烁1秒→idle；失败Alert可重试；未配AI时弹出引导弹窗→跳转/settings/ai-config

#### ProfilePage（/settings/profile）

- **两个卡片**：基本信息(首字头像+可编辑姓名+只读邮箱+只读注册时间)；修改密码(原密码+新密码+确认+提交按钮→成功后强制重新登录)

#### AIConfigPage（/settings/ai-config）

- **配置表单**：提供商Select(切换时更新默认base_url/model_name并清空Key)、API Key(遮罩+显示切换)、模型名称、自定义地址、高级选项Collapse(temperature Slider/max_tokens InputNumber/top_p Slider)
- **按钮**：测试连接(不保存)+保存配置
- **状态卡片**：未测试灰/成功绿/失败红


#### ExportPreviewPage（/plans/:id/preview）（P1）

- **布局**：全屏无侧边栏，顶部工具栏(返回+下载docx+打印)，内容为白底A4容器渲染HTML
- **交互**：加载GET /export/preview→渲染HTML；下载docx→POST /export/docx→同步下载或异步弹出进度Modal(轮询task状态)

#### VersionListPage（/plans/:id/versions）（P1）

- **组件**：PageHeader+新建版本Modal+Table(Checkbox最多选2个+版本号+创建方式Tag+说明+时间+详情/回滚)
- **对比弹窗**：双栏并排+按章节diff高亮(修改黄/新增绿/删除红)
- **回滚弹窗**：二次确认→POST rollback→刷新编辑器

---

## 八、关键共享组件

### 8.1 SectionTree

Props: sections(PlanSection[]), selectedKey, onSelect, templateStructure(含ai_generatable/required)
渲染: 递归遍历，level缩进=20px*level
图标: hasContent=绿勾，!hasContent+required=红Badge，!hasContent=灰圈，ai_generatable=机器人图标

### 8.2 RichTextEditor

Props: content(Markdown), onChange(Markdown回调), readOnly, placeholder
内部: TipTap useEditor, extensions=[StarterKit,Underline,TextAlign,Placeholder,Table]
初始化: markdownToHtml(content)，输出: htmlToMarkdown(editor.getHTML())

### 8.3 AIGenerateButton

状态机: idle[点击]→loading[SSE done]→done[1s]→idle; loading[SSE error]→error[重试]→loading
Props: planId, sectionKey, onContentChunk(增量), onComplete(全文), onError

### 8.4 EnterpriseSwitcher

位于MainLayout Header，Select组件，options来自EnterpriseContext.enterprises

### 8.5 RiskLevelTag

Props: level(重大/较大/一般/低)，颜色: 重大red/较大orange/一般gold/低blue

---

## 九、开发阶段与顺序

### Week 1-2: 基础设施 + 认证 + 企业CRUD

| 天 | 任务 | 产出 |
|----|------|------|
| D1-2 | 项目脚手架,Vite+React+TS+AntD | 目录结构+配置 |
| D2-3 | types/ 全部类型定义 | 15个类型文件 |
| D3-4 | services/api.ts 拦截器 | Token注入+刷新队列 |
| D4-5 | AuthContext+AuthLayout+Login+Register | 认证闭环 |
| D6 | MainLayout(侧边栏+顶栏+Outlet) | 主框架 |
| D7 | EnterpriseContext+EnterpriseSwitcher | 企业上下文 |
| D8-10 | EnterpriseList+Create+Edit+Detail(Tab1) | 企业CRUD |

### Week 3: 企业数据管理

| 天 | 任务 | 产出 |
|----|------|------|
| D11-15 | EnterpriseDetail Tabs 2-5 | OrgStructureEditor,RiskSourceForm,EmergencyResourceForm,SurroundingInfoForm |

### Week 4-5: 预案编辑核心

| 天 | 任务 | 产出 |
|----|------|------|
| D16-17 | PlanListPage+PlanCreatePage | 预案列表+新建向导 |
| D18-19 | SectionTree组件 | 递归章节树 |
| D20-21 | RichTextEditor组件 | TipTap封装 |
| D22-24 | PlanEditorPage+useAutoSave | 编辑器主框架+自动保存 |

### Week 6: AI生成 + 仪表盘 + 设置

| 天 | 任务 | 产出 |
|----|------|------|
| D25-27 | useSSE+generationService+AIGenerateButton | SSE流式+生成按钮 |
| D28 | PlanEditorPage集成AI生成 | 流式追加到编辑器 |
| D29 | AIConfigPage | AI配置+测试连接 |
| D30 | DashboardPage | 仪表盘 |
| D31 | ProfilePage | 个人资料+改密码 |

### Week 7: 导出 + 版本（P1）

| 天 | 任务 | 产出 |
|----|------|------|
| D32-34 | ExportPreviewPage+ExportProgressModal | 导出预览+下载 |
| D35-37 | VersionListPage+Compare+Rollback | 版本管理 |

### Week 8: 打磨与测试

| 天 | 任务 | 产出 |
|----|------|------|
| D38-39 | 全流程E2E(Playwright) | E2E脚本 |
| D40 | 组件单元测试(Vitest) | 测试文件 |
| D41-44 | UI状态完善+Bug修复 | Loading/Empty/Error全覆盖 |
| D45 | 生产构建验证 | vite build |


## 十、状态处理规范

每个页面/组件必须覆盖以下四种状态：

| 状态 | UI 表现 | 触发条件 |
|------|---------|----------|
| Loading | Ant Design Skeleton / Spin | 首次数据加载中 |
| Empty | Empty 组件 + 引导文案/按钮 | 数据为空（新用户） |
| Error | Alert 错误信息 + 重试按钮 | API 请求失败 |
| Unauthorized | 自动跳转 /login | Token 过期且刷新失败 |

全局 Error 处理：API 调用失败统一用 message.error() 提示（由 api.ts 拦截器处理非 401 错误）。

---

## 十一、测试策略

### 单元测试（Vitest）

- riskMatrix.ts：可能性x严重性矩阵所有9种组合
- validators.ts：密码规则、邮箱格式、手机号格式
- formatters.ts：日期格式化、字符截断
- useAutoSave Hook：3秒debounce、连续变更仅触发一次

### 组件测试（Testing Library）

每个共享组件至少1个测试：
- SectionTree：渲染层级、选中高亮、状态图标
- RichTextEditor：初始内容加载、onChange回调
- AIGenerateButton：状态机流转、按钮禁用
- RiskLevelTag：四种颜色正确渲染

### E2E 测试（Playwright）

核心流程：
1. 注册→登录→仪表盘→创建企业→录入风险源→创建综合预案→AI生成→编辑→导出docx
2. Token 刷新流程：超时→自动刷新→继续操作
3. 企业切换：预案列表跟随变化
4. 权限隔离：用户A看不到用户B的企业

---

## 十二、风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| TipTap 与 Ant Design 样式冲突 | 编辑器UI异常 | CSS变量隔离，TipTap使用独立样式命名空间 |
| SSE 在代理下被缓冲 | 生成内容延迟显示 | Nginx proxy_buffering off；Vite proxy 确认无缓冲 |
| Token刷新并发冲突 | 多个401同时刷新 | api.ts 实现 isRefreshing + failedQueue |
| Ant Design 5 打包体积大 | 首屏慢 | 按需导入（手动import具体组件路径） |
| Markdown↔HTML 转换失真 | 导出格式异常 | 单元测试覆盖列表嵌套、表格、加粗斜体组合 |

---

## 十三、交付检查清单

- [ ] npm run dev 正常启动，端口 5173
- [ ] npm run build 无 TypeScript 错误
- [ ] npm run lint 通过（ESLint + Prettier 配置）
- [ ] 所有 TypeScript 类型无 any 逃逸
- [ ] 14 个页面全部路由可访问且渲染正确
- [ ] 登录→注册完整流程通过
- [ ] Token 过期自动刷新无感知
- [ ] 企业 CRUD 完整流程（创建→查看→编辑→删除）
- [ ] 风险源 CRUD + 等级自动计算 + 红/橙/黄/蓝标签
- [ ] 应急资源 CRUD + 内部/外部切换
- [ ] 组织架构编辑器（增删小组+成员）
- [ ] 预案编辑器：章节树选择→富文本编辑→3秒自动保存→保存状态指示
- [ ] AI 生成：SSE 流式输出到编辑器→生成中只读→完成后可编辑
- [ ] 仪表盘：统计数字与实际数据一致
- [ ] AI 配置：测试连接成功/失败反馈正确
- [ ] 个人资料：修改姓名+密码后强制重新登录
- [ ] 导出预览：HTML 正确渲染（P1）
- [ ] 导出 .docx：文件可下载（P1）
- [ ] 版本列表/对比/回滚（P1）
- [ ] Loading / Empty / Error 状态全部覆盖
- [ ] Chrome / Edge 兼容性验证通过
