import pathlib

lines = []
def a(s=""): lines.append(s)

a("# PRD-13：法规知识图谱与智能合规引擎")
a("")
a("> 版本：v1.0 | 日期：2026-07-07 | 状态：待评审")
a("")
a("---")
a("")
a("## 1. 产品概述")
a("")
a("### 1.1 问题陈述")
a("")
a("当前系统在生成风险评估报告、应急资源调查报告、综合应急预案、专项应急预案、现场处置方案时，所有编制依据完全依赖大模型内置知识。大模型训练数据存在截止日期，导致引用已废止法规、遗漏新发布法规、条文引用不准确等问题。")
a("")
a("### 1.2 解决方案")
a("")
a("GraphRAG（知识图谱 + 向量检索 + Prompt 注入）：构建安全生产法规知识库，AI 基于实时检索到的法规原文撰写编制依据。")
a("")
a("### 1.3 三大核心原则")
a("")
a("| 原则 | 说明 |")
a("|---|---|")
a("| 傻瓜化维护 | 粘贴全文 → AI 自动解析 → 点确认入库 |")
a("| 松耦合 | 独立 Python package，与业务代码仅一行调用耦合 |")
a("| 静默降级 | 法规模块异常 → 自动回退原有逻辑 |")
a("")

out = "\n".join(lines)
pathlib.Path(r"C:\Users\55061\Documents\数字化预案自动生成 2\prd\PRD-13.md").write_text(out, encoding="utf-8")
print("section 1 done")
